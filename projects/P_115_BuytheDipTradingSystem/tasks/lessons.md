# P_115 Lessons

Working-state file. Read at every INIT STEP 3. Append durable trade/process learnings here.

---

## Log

- 2026-06-09 — File created (was missing under tasks\).

- 2026-08-17 — Investigation discipline: before declaring any pipeline component "broken" or "never worked," check the archive/zip files sitting alongside the live folder (e.g. signals\processed\*.zip), the P_400 paper/live order-note folders for matching order_ids, and past chat history via conversation_search -- not just a raw directory listing. Wrongly concluded the STEP2 SIGNAL_V2 emitter had never fired; 16+ of 44 flagged signals were sitting in an already-noticed but unopened archive zip, and past sessions (7/28, 7/30) showed the emitter working. Also used a stale/wrong tracker Excel path (C:\...\P_115_BuytheDipTradingSystem\data\P_115_TrackerDashboard_V3.xlsx) instead of the canonical one (D:\OneDrive\Documents\AJZStrategiesLLC\P_115_TrackerAudit\P_115_118_TrackerDashboard_V2.xlsx), causing a false "tracker abandoned since Feb 2026" conclusion. Never declare a system-wide failure from a single incomplete scan or an unverified file path.
- 2026-08-17 — Field fabrication: never state a value for a required signal-emission field (e.g. signal_horizon) as established/normal practice unless it traces to an actual source (schema default, documented architecture-doc example, config constant, or Tony's explicit instruction). A prior session (7/28/26) wrote horizon=10 with zero derivation shown -- no such default exists anywhere in the codebase or docs; the only documented reference value is "3-5 days" in the architecture doc's own cli.py example. If a required field has no traceable source, say so plainly and either use the one documented example or ask -- never silently assert a number as fact.
- 2026-08-18 — Chart authority scope: on a P_115-style chart, the chart's own scoring label (LogEntry/Verdict, e.g. ASYM) is the sole governing signal for that determination -- full stop, no resolution needed against other badges on the same chart. Wrongly flagged NTRA's unrelated breakout-confirmation badges (Setup/Confirm/PriorHigh, a separate overlay) as being "in conflict" with the ASYM verdict and asked Tony to arbitrate. Those badges belong to a different subsystem and don't bear on the P_115-style verdict at all. Chart + P_115 label is king; don't manufacture a conflict between it and an unrelated indicator just because both are visible in the same screenshot.
- 2026-08-18 — signal_horizon correction: the architecture doc's "3-5 days" is stale/superseded. Per 8/17 session (CPAY real-trade duration ~11 trading days), Tony agreed to "10-15 trading days" as the standing value going forward. Used "3-5 days" again same day before catching it -- the 8/17 agreement wasn't yet in this file, only in chat history, so nothing carried it forward. Use "10-15 trading days" for signal_horizon unless Tony specifies otherwise for a given ticker.
- 2026-08-21 -- SignalSource determination: SignalSource is set by the message's BATCH HEADER (e.g. "P_118 STEP 1", "P_115 STEP 1"), never by which chart indicator or study name appears in a screenshot. A P_118-headed batch keeps SignalSource=P_118 for every ticker in it, including trailing "STEPS 1-2 [TICKER EP:x]" entries that follow with no new header of their own -- even when the chart shown for that ticker is running the P_115_BuyTheDipChart indicator. P_118 candidates are REQUIRED to run through the P_115 recheck engine per V111, so seeing the P_115 chart/indicator on a P_118 ticker is expected, not a signal the ticker is actually P_115-sourced. This exact mistake happened twice in one session on 2026-08-20 (CDNA, V wrongly logged SignalSource=P_115, corrected in-session after Tony caught it) and the in-session fix was never written anywhere durable -- so it repeated verbatim in a brand new session on 2026-08-21 (INCY, MA, V, same wrong reasoning). Root cause: a same-session correction is not a fix until it is written to this file or the skill doc; fixing it in the moment and moving on lets it recur indefinitely. When a ticker's batch-header scope is genuinely ambiguous, ask -- do not infer SignalSource from the chart.

- 2026-08-24 -- PatternType on a P_118 BUY must be READ FROM THE CHART IMAGE directly, never asked for, even when the only chart shown is the P_115 recheck chart (expected per V111) with no explicit pattern-name text on it. Initially held the MA/V tracker rows and asked Tony for the Eddie Z pattern type instead of visually reading the actual candle/price structure in the uploaded chart images -- Tony pushed back ("What do You mean I pasted both charts"). Re-viewed both images directly: MA showed a ~6-8 week decline/base (mid-June to late-July) + breakout through 528/540 resistance + ~1-week tight handle at 540-548 + second breakout to new highs = Cup & Handle. V showed a ~5-week tight horizontal consolidation (~355-386, ~8% depth) breaking to a new closing high today = Flat Base. This is the second occurrence of this exact mistake -- a 6/15/26 session summary already recorded Tony pushing back on Claude asking for PatternType instead of reading it directly, and that fix never made it into this file. When a chart image is present on a P_118 BUY/ASYM, look at the actual price structure before concluding a pattern can't be determined -- absence of a text label naming the pattern is not absence of the pattern.

- 2026-08-24 -- signal_source in the SIGNAL_V2 JSON packet (via cli.py --source flag) must always be P_115, even on a P_118-headed batch -- the 8/21/26 lesson above only covers the 27-column TRACKER's SignalSource column (batch-header-driven, P_118 correct there). Passed --source P_118 on MA's emission by analogy to that tracker rule and it wrote signal_source="P_118" straight into the packet, caught immediately on the mandatory read-back and corrected via targeted replace before V's emission. These are two different fields with opposite rules: tracker SignalSource = scan/batch provenance (P_115/P_116/P_117/P_118/P_910/P_920); packet signal_source = scoring-engine identity, always P_115, per architecture doc 8.2 step 3 ("Do not substitute P_116/P_118/P_910/P_920 here"). Always read the emitted JSON back and check signal_source specifically before trusting a P_118/P_910/P_920-sourced emission.

- 2026-09-04 -- Windows-MCP relay: a single stuck tools/call is not the same as a dead relay. This session, one PowerShell call hung ~4 min (client-log id=32, cancelled), and the initial read was "relay down, tell Tony to restart" -- but the very next call (id=33) returned in 0.6s, and everything after it worked normally the rest of the session. Correct first move on a stuck call is one fresh retry before recommending a restart; jumping straight to "relay down" overstates a single-call hang. Separately, earlier in the same client log, the windows-mcp server process itself cold-started and immediately shut down repeatedly (13:20:34/37/41, then again 13:33:13/17/22) with no user action and no error line explaining it -- a genuine spontaneous crash-restart cycle, root cause unknown from the log alone. Also: when reading a pasted MCP client log, don't guess at what caused a gap or a restart cycle -- wrongly assumed Tony had manually restarted Claude Desktop between the two cycles; the log showed no such event and Tony corrected it. State only what the log actually shows. This is infra-level (Claude Desktop / windows-mcp), not P_115-specific -- applies to any project session using windows-mcp. See WO-P000-E21.001 (Cowork pilot) for a related infra-reliability angle.
