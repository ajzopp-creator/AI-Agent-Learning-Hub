# P_300 Task Queue Archive

**File:** `tasks/todo_archive.md`
**Companion to:** `tasks/todo.md` (live file)
**Retention standard:** WO-P000-E8.001
**First archive pass:** 2026-07-22

Dated session-log entries removed from the top of the live todo.md to
keep it under its size cap. Nothing deleted -- full text preserved
exactly as it appeared live. Newest-first, same as the live file.
Archived range: 2026-07-07 through 2026-07-17 (7th session that day).

---

**>>> 2026-07-17 (7th) WO-P300-E5.001 FILED (PENDING, new epic) -- import-linter architectural boundary enforcement:** Filed during the CNK/GURE/LCID/NNDM/TWST batch's long "post" wait, following a KB article discussion (`KnowledgeBase/2026-07-13_10 Python Libraries...md`, marked reviewed-no-match for strategy relevance, disposition notes the two real gaps found: scalene + import-linter). E5 is the next open epic number (E1-E4 already used; E4 is tonight's eval-loop/cache epic, this is a separate concern). Stub only -- explicitly not planned or built. Scope note written into the WO: P_300 only (not Hub-wide, other projects lack the same formal layer split), no CI exists here so this becomes a PEH check not an automatic gate, and the first real audit run might not come back clean (enforced by eye so far, never exhaustively). Next step whenever picked up: define layer contracts as prose first, get explicit sign-off, only then write config -- no build yet.

---
**>>> 2026-07-17 (6th) E4.004 REDESIGNED (v1.1) -- min-new-date partition replaces same-day guardrail, 2 real bugs found+fixed:** Following the (4th) entry's real-world finding (M-100), redesigned per the correctness argument: existing pid X is safe to reuse from cache iff `X.anchor_date <= min(new anchor_dates)` -- no new pid can be earlier than X, so X's corpus is provably unchanged, for ANY date mix, not just same-day. `domain/eval_incremental.py` v1.1 (155 lines) -- `_partition_unaffected()` replaces the all-or-nothing guardrail; `IncrementalGuardrailError` narrowed to a true internal-invariant check (pre_batch missing an expected pid), no longer fires on real data. `application/incremental_post_batch.py` v1.1 -- docstring-only. `tests/test_eval_incremental.py` v1.1 (239 lines) -- swapped the obsolete backdated-insert test for a mixed-dates correctness test (5/10 existing pids safely reused, not same-day) + a genuine internal-invariant test.

**M-101 (2nd real bug, found during this build's own PEH):** mixed-dates test initially failed on RESULT ORDER, not scores -- `sorted(reused + rescored)` doesn't reproduce `run_walk_forward()`'s tie-break order on same-date pids (stable-sort ties depend on insertion order; concatenating two separately-built lists doesn't preserve the reference's natural order). Fixed by ordering via the exact same `sorted(all_metadata.keys(), key=anchor_date)` computation the reference uses, not a sort over the assembled list. No new test needed -- the existing mixed-dates test's byte-identity check already caught it.

PEH (p140) after the fix: both suites clean -- `test_eval_scoring.py` 5/5 (unchanged, regression gate), `test_eval_incremental.py` 4/4 (same-day happy path, mixed-dates correctness, internal-invariant guardrail, application-layer fallback). M-100 + M-101 in lessons.md. WO-P300-E4.004.md updated with RESOLVED note.

**Still open:** v1.1 has not had a real BulkAddPattern run -- today's real batch ran under v1.0's logic before this redesign. Honest caveat carried forward: today's specific batch (`min_new_date` near `MINE_MIN_ANCHOR_DATE`) would still rescore almost everything even under v1.1 -- real speedup depends on a batch with a more recent `min_new_date`, not guaranteed every run.

---
**>>> 2026-07-17 (5th) PROMOTED -- live catalog now 8969 patterns / 323 symbols:** Tony ran `ingest-mined --promote` for real (`C:\Users\Trader\.conda\envs\p140\python.exe python\cli.py ...` -- first attempt used bare `python` + wrong cwd-relative path, hit system Python 3.14 + `cli.py` not found; corrected to the full p140 path + `python\cli.py`). checkout/checkin delta confirmed clean: symbols 317->323 (+6), source_files 559->567 (+8, matches the 8 mined files), pattern_instances 8175->8969 (+794, matches inserted count), pattern_bars +15880 (794 x 20-bar window, exact), forward_labels +3970 (794 x 5 horizons, exact). Backup auto-saved: `071726catalog.db.bak`.

Independently verified (not taken on console word, M-054): live `071726catalog.db` now 51,904,512 bytes -- byte-identical to `staging_ingest_mined.db`'s size, confirming the atomic move landed the real staged content. `071726catalog.db.bak` is 47,341,568 bytes -- byte-identical to this session's morning catalog-summary baseline, confirming the pre-promote snapshot is intact and rollback-safe.

**BulkAddPattern process fully closed out for this batch:** mine-patterns -> ingest-mined (staging, M-099 cache-miss took the full ~35min pre-fix + guardrail-triggered full post per E4.004's real-world finding) -> archive (8/8) -> promote (this entry). Live catalog current for daily evals. E4.004 redesign proposal (min_new_date partition -- reuse cache for any existing pid dated <= the earliest new pid's anchor_date, not just same-day) discussed, not yet planned or built -- Tony's go-ahead pending.

---
**>>> 2026-07-17 (4th) REAL BULKADDPATTERN BATCH COMPLETE -- M-079 GATE CLEAN, M-100 FILED (E4.004's premise disproven for this pipeline):** First real production run of the full BulkAddPattern process against 8 files (ALSN/CGBD/CNSWF/CRH/LECO/LPX/STZ/SW). mine-patterns: 799 candidates (403 uptrend/396 breakdown). ingest-mined: 794 inserted / 0 skipped-duplicate / 5 audit-failed (all backfill-boundary, MINE_MIN_ANCHOR_DATE gate working as designed). Staging 8175 -> 8969 patterns. Archive: 8/8 to E:\...\Jul26BULKPattern.zip.

**M-079 gate result (independently verified, not taken on console word):** BUY precision 68.09% -> 68.49% (+0.39pts), PASS accuracy 58.77% -> 58.92% (+0.16pts), both held/improved -- new-pattern bucket deltas (BUY+371/WATCH+175/PASS+248=794) cross-checked against the console's "794 inserted" via a completely independent code path (report-writer counts vs. insert counter), exact match. Staging DB file size (49.5MB vs live 47.3MB) confirms the write physically happened. Clean to promote at Tony's discretion -- not yet promoted, staying manual per standing rule.

**E4.004 real-world finding -- premise did not hold:** the incremental-post guardrail fired on pattern #1 (new pid anchor_date 2021-08-18, nowhere near today) and correctly fell back to a full ~35-min re-score (10:26:36-11:01:48). Root cause: `mine-patterns` finds candidates at their REAL historical chart dates, not "today" -- E4.004's WHY premise ("today's approved picks have today's anchor_date") was wrong for this pipeline from the start, and likely wrong for manual AddPattern.bat too (chart date != entry date). Zero wrong data resulted -- the guardrail is exactly why -- but E4.004 currently provides ZERO real speedup for BulkAddPattern. Documented in full in WO-P300-E4.004.md's new REAL-WORLD FINDING section; M-100 added to lessons.md. **Not resolved -- Tony's call next session:** leave as inert safety net, revert to E4.003-only (cached-pre, no incremental-post attempt), or redesign around what actually varies between runs.

---
**>>> 2026-07-17 (3rd) M-099 FIXED -- cache fingerprint replaces mtime, PEH-VERIFIED:** Real bug caught live mid-batch (see the (2nd) entry below and the still-running BulkAddPattern batch above it): E4.003's pre-batch cache was keyed on `<stem>_<mtime_ns>_<tag>`, and `P_300_RunBulkAddPattern.ps1`'s daily dated-rollover copy (`071526catalog.db` -> `071726catalog.db`) gives a byte-identical file both a new filename AND a new mtime -- guaranteed cache MISS on the first run of every single day, defeating the exact optimization E4.003 built. Tony's call: bug fix to existing code, not a new WO (E4.004 was scoped to "post," this is "pre" and predates E4.004 entirely).

`infrastructure/eval_io.py` v1.5 -- `_cache_key()` now fingerprints catalog CONTENT (`pattern_instances` row count + `MAX(pattern_instance_id)` + file size) instead of mtime; `_stable_stem()` strips the `<mmddyy>` rollover prefix too. Docstring/changelog compressed to land at 299/300 lines (file was already at 298 before this fix). `tests/test_eval_scoring.py` v1.1 -- cache fixture rebuilt as a real minimal SQLite db (the fingerprint runs a real COUNT/MAX query, a text-file mtime fixture no longer works); new `_test_rollover_copy_still_hits()` proves the exact case this fix targets.

PEH (p140): both suites re-run clean -- `test_eval_scoring.py` 5/5 (parallel==serial, n_patterns==10, cache round-trip, cache miss on real content change, rollover copy hits), `test_eval_incremental.py` (E4.004, untouched by this fix) 3/3 unchanged as a regression gate. M-099 added to lessons.md.

---
**>>> 2026-07-17 (2nd) WO-P300-E4.004 BUILT + PEH-VERIFIED (OWNER_DONE):** Incremental "post" M-079 scoring -- only newly-inserted pids get scored on a BulkAddPattern run; unaffected pids reuse the cached pre-batch verbatim, guarded by an anchor-date assertion that falls back to a full re-score if the temporal-exclusion assumption is ever violated. Design changed from the 2026-07-16 draft plan mid-planning: found `domain/eval_scoring.py` and `application/ingest_mined_pipeline.py` both sitting at exactly 300 lines (zero headroom) before writing any code, so redesigned to touch neither file's cap -- two new files (`domain/eval_incremental.py` 140 lines, `application/incremental_post_batch.py` 71 lines) instead of direct edits, `ingest_mined_pipeline.py`'s edit compressed (docstring/changelog trimmed ~14 lines) to land at exactly 300 rather than an accepted overage. `assemble_incremental_post_batch()` split into a `_validated_pre_results()` helper mid-build to stay under the 50-line function limit.

PEH (p140, MCP PowerShell wedged on the python.exe call per M-030, handed off to Tony): `tests/test_eval_scoring.py` (E4.003, unmodified) re-run FIRST as a regression gate -- ALL 4 CHECKS PASSED, confirms this session's edits didn't break the already-CLOSED E4.003 WO. `tests/test_eval_incremental.py` (E4.004, new, 174 lines) -- ALL 3 CHECKS PASSED: incremental batch byte-identical to a full re-score (10 existing + 3 new synthetic patterns), guardrail correctly raises on a backdated insert, application-layer fallback returns a correct full re-score without the exception escaping (WARNING logged as designed).

WO-P300-E4.004 moved to OWNER_DONE (not CLOSED) per WO_COMPLETION_GATE.md's Independent Review Requirement. **Acceptance criteria still open, flagged explicitly in the WO:** the real-catalog timing claim (post-batch near-instant vs. the prior ~30.8 min) is only synthetic-fixture-proven so far -- needs an actual BulkAddPattern run against the live 8175-pattern catalog to confirm for real, separate deliberate next step.

---
**>>> 2026-07-17 WO-P300-E4.003 INDEPENDENT REVIEW COMPLETE -- CLOSED:** Separate session (not the 2026-07-16 build session) re-verified all 4 Acceptance Criteria against real code/output, per WO_COMPLETION_GATE.md's Independent Review Requirement -- did not trust the prior session's closure note (M-054). Confirmed: all 5 as-built files' line counts exact-match the FILE PLAN table (eval_io.py=298, eval_scoring.py=300, ingest_mined_pipeline.py=300, config.py=543, test_eval_scoring.py=162); config.py grep confirms EVAL_PARALLEL_ENABLED=True/EVAL_PARALLEL_WORKERS=None actually defined; tests/test_eval_scoring.py re-run for real via PEH handoff (MCP PowerShell wedged on the python.exe call per M-030, handed off to Tony) -- ALL CHECKS PASSED, exit 0 (parallel==serial byte-identical n=10, cache round-trip hit + mtime-change miss both correct); real production cache artifact confirmed on disk (models/eval_cache/071526catalog_1784125024276327100_default.json, 10.1MB) as independent physical evidence the cited 8175-pattern production run actually happened. No discrepancy found. WO-P300-E4.003.md updated: Completion Gate's last checkbox checked, Status OWNER_DONE -> CLOSED, INDEPENDENT REVIEW section appended.

---
**>>> 2026-07-16 WO-P300-E4.003 BUILT + PEH-VERIFIED (OWNER_DONE), WO-P300-E4.004 FILED:** Built both fixes from WO-P300-E4.003's plan -- infrastructure/eval_io.py v1.4 (pre-batch cache, keyed on catalog path + mtime_ns + override tag), domain/eval_scoring.py v1.2 (optional ProcessPoolExecutor parallel path in run_walk_forward(), per-worker globals via _init_worker), application/ingest_mined_pipeline.py v1.3 (wired cache check + parallel flag through run_ingest_mined()), config.py v1.17 (EVAL_PARALLEL_ENABLED/EVAL_PARALLEL_WORKERS), tests/test_eval_scoring.py v1.0 new (permanent regression test -- parallel-vs-serial byte-identity + cache round-trip/miss).

PEH run (p140, 2026-07-16 23:xx): Step 1 (synthetic regression gate) ALL CHECKS PASSED. Step 2 (real live catalog, 071526catalog.db, 8175 patterns): load_full_catalog 2.8s, run_walk_forward(parallel=True) 1848.6s (~30.8 min) for one full pass, cache write 0.1s -- cache entry now on disk for this exact catalog snapshot, so the "pre" half is free on every future run until the next promote. Parallel speedup was modest (~15% vs serial estimate) despite 24 logical cores available -- likely per-worker pickling overhead + score_one's O(corpus_size) dict-rebuild per pattern, which doesn't parallelize away. Not a blocker; WO's actual goal (stop the promote-time hang, kill the wasted duplicate "pre" pass) is met.

WO-P300-E4.003 moved to OWNER_DONE (not CLOSED) per WO_COMPLETION_GATE.md's Independent Review Requirement -- Completion Gate checklist filled in the WO file; a separate session/subagent needs to re-verify Acceptance Criteria before CLOSED.

**Bigger lever found during closure discussion, filed as WO-P300-E4.004 (PENDING, not started):** walk-forward's corpus for any pattern is only patterns with a STRICTLY EARLIER anchor_date. Every daily BulkAddPattern batch (0-15 new picks) has today's date -- later than every existing catalog pattern -- so none of the existing 8175+ patterns' scores can ever change by adding them. "Post" currently re-scores the whole staging catalog anyway (the actual ~30 min Tony sits through on every batch, per his question this session about whether M-079 is necessary every time -- yes, it's the promotion gate, but it shouldn't cost this much). E4.004 proposes scoring only the new pids against the cached-pre results for everything else, with an explicit anchor-date guardrail assertion. Plan-gate applies (3-file change) -- needs its own approved plan next session before any build.

---
**>>> 2026-07-15 (5th) BulkAddPattern PROMOTED -- catalog now 071526catalog.db, 8175 patterns / 317 symbols, Tony's explicit call to skip the M-079 walk-forward comparison and trust catalog-summary alone (WO-P300-E4.003 runtime bug forced this):** Ran `ingest-mined --promote` against `staging_ingest_mined.db`. checkout/checkin delta confirmed clean: symbols 307->317 (+10), source_files 548->559 (+11, matches the 11 mined files), pattern_instances 6039->8175 (+2136), pattern_bars 120780->163500 (+42720), forward_labels 30195->40875 (+10680). Backup auto-saved: `071526catalog.db.bak`. Post-promote `catalog-summary`: 0 hollow records, OVERALL HEALTHY. Forward-label win rates essentially flat vs. pre-promote (5d 50.8%->51.3%, 7d 51.4%->51.8%, 10d 51.8%->52.0%, 15d 51.9%->51.9%, 20d 51.8%->51.3%) -- no visible red flag in the aggregate view, though this is not the same as the regression-specific pre/post walk-forward comparison that was skipped when `ingest-mined` was killed.

BulkAddPattern process fully closed out for this batch: mine-patterns -> ingest-mined (staging insert, M-079 killed) -> archive (11/11 manual) -> promote (this entry). Live catalog is current for daily evals.

---
**>>> 2026-07-15 (4th) ingest-mined KILLED at operator request (WO-P300-E4.003 bug in action), 11-file archive completed manually:** `ingest-mined` was terminated (PID 591284, Stop-Process) after 115+ minutes stuck in the M-079 walk-forward eval with no completion in sight -- the exact defect WO-P300-E4.003 documents. Safe to kill: `staging_ingest_mined.db` had not changed since 10:17:04 (9 seconds after start), confirming the real insert into staging was already complete and nothing was lost by killing the read-only eval scoring. No pre/post walk-forward comparison report was produced this run.

Since `P_300_RunBulkAddPattern.ps1` never reached its own Step 3 (archive) before the kill, the 11 files still sitting in `data/bulk/mine/` (AMX, ATKR, CPSS, EME, FCX, FLR, GWH, IVPAF, RITM, SHO, WY) were archived manually via the same tested `archive-mined` CLI command used earlier today -- 11/11 archived to `E:\AI-Agent-Learning-Hub_BackupFiles\Jul26BULKPattern.zip`, 0 failures. `data/bulk/mine/` confirmed empty.

**Open decision, Tony's call:** `staging_ingest_mined.db` (models/) has the new mined patterns inserted but has NOT been reviewed via the M-079 walk-forward comparison (skipped, per above) and has NOT been promoted to live. Promoting without that report means trusting `catalog-summary`'s aggregate output alone this one time, not the regression-specific pre/post comparison. Staging DB left in place, untouched, ready for `--promote` whenever Tony decides.

---
**>>> 2026-07-15 (3rd) WO-P300-E4.003 FILED -- M-079 walk-forward eval performance:** Root-caused the "hung" `ingest-mined` run above: `staging_ingest_mined.db` stopped changing 9 seconds after start (real insert already committed) while the process kept burning CPU for 40+ minutes on `domain/eval_scoring.py`'s pre+post walk-forward comparison -- first time that eval has run against the full 6039-pattern catalog. Not a bug: `run_walk_forward()` has no cap, no cache, no parallelization. Two real, additive fixes identified: (1) the "pre" (live catalog) batch is recomputed from scratch every run even though it can't have changed since the last promote -- cache it, keyed on catalog path + mtime; (2) `score_one()` has no shared state across pattern_ids -- embarrassingly parallel, not yet parallelized. WO-P300-E4.003 filed (PENDING, not started) so it surfaces at future INITs alongside E4.001/E4.002.

---
**>>> 2026-07-15 (2nd) REAL ARCHIVE OF data/bulk/mine/ COMPLETE -- 40/40, 0 FAILURES:** Ran `archive-mined` for real against all 40 files in `data/bulk/mine/` (the same corpus already mined + ingested + promoted for real on 2026-07-14 -- "was in the batch" = "safe to archive", per that session's own note). Every file appended to `E:\AI-Agent-Learning-Hub_BackupFiles\Jul26BULKPattern.zip` (13.5 MB, 40 entries, no per-entry rename per the file's design) and deleted from `data/bulk/mine/` only after zip verification. `data/bulk/mine/` confirmed empty afterward. This was Tony's explicit pre-session cleanup request, run standalone via the CLI (not through `P_300_RunBulkAddPattern.ps1`, since mine-patterns/ingest-mined for this corpus were already done manually on 2026-07-14).

---
**>>> 2026-07-15 BULKADDPATTERN ARCHIVE STEP PEH-VERIFIED (file #1 + #3 of 4):** PEH run against `utilities/archive_mined_file.py` v1.0 + `cli.py`'s `archive-mined` wiring v1.14, scratch-data only (fresh `tempfile.mkdtemp()` tree, `DATA_BULK_MINE`/`MINE_ARCHIVE_DIR` monkeypatched -- real `data/bulk/mine/` and the real E:\ archive were never touched). 10/10 checks PASS: happy path (no-rename, date-first `<MMMYY>BULKPattern.zip` naming confirmed), refuses a file outside `DATA_BULK_MINE`, source survives a forced zip-write failure, and `cli.py archive-mined --xlsx PATH` routes correctly through `main(argv)` to the same `run_archive()`.

**Flagged, not a bug:** re-mining a file with an identical name (e.g. same symbol + date window re-exported) hits the existing entry-exists-skip path -- the zip keeps the FIRST version's bytes but the SECOND file's source still gets deleted, silently dropping its content. Confirmed present here; it's the same shape as `archive_scanner_file.py`'s already-shipped duplicate handling, not a new defect introduced by this file. No fix made -- Tony's call whether mined-file naming makes a same-name re-mine collision reachable in practice. No `tests/test_archive_mined_file.py` created (Regression Test Governance: clean PEH run, no real bug fix, same posture as the file #5 audit-agent PEH).

**Still owed before any real run of `P_300_RunBulkAddPattern.ps1`:** none for files #1/#3 -- both now real-PEH-verified. `config.py` v1.16 (file #2, additive constants only) and `P_300_RunBulkAddPattern.ps1` (file #4) were confirmed via read-through (dated-catalog-rollover mirrors `P_300_AddPattern.bat`; per-file exit-code handling on `mine-patterns`/`ingest-mined`/`archive-mined` loop all correct) -- not executed for real, since `mine-patterns` and `ingest-mined` already carry their own PEH from 2026-07-13/14 and a live run would touch the real catalog. All 4 files now cleared for a real run at Tony's discretion.

---
**>>> 2026-07-14 (7th, SESSION CLOSE) "BULKADDPATTERN" PROCESS BUILT -- NOT YET PEH-VERIFIED:** 4 files delivered, formalizing the automated equivalent of manual AddPattern (mine-patterns -> ingest-mined -> archive), scaling single-file to full-batch the same way AddPattern.bat/RunAllPatterns.ps1 are two sizes of the manual process:
1. `python/utilities/archive_mined_file.py` NEW -- mirrors archive_scanner_file.py's append-verify-delete safety pattern; 2 deliberate differences (Tony's call): no per-entry rename, date-first zip naming (`<MMMYY>BULKPattern.zip` vs Scanner Loop's basename-first convention).
2. `config.py` v1.16 -- added `MINE_ARCHIVE_DIR` (same E:\ drive as Scanner Loop, separate named constant) + `MINE_ARCHIVE_SUFFIX`. Resolves the "no auto-archive in v1" deferral noted in v1.13's DATA_BULK_MINE comment.
3. `cli.py` v1.14 -- new `archive-mined --xlsx PATH` subcommand, mirrors `archive-pattern`'s shape exactly.
4. `P_300_RunBulkAddPattern.ps1` NEW -- full orchestrator: dated catalog rollover (ported from AddPattern.bat) -> catalog-summary baseline + pause-to-confirm -> mine-patterns -> ingest-mined -> loop archive-mined over every file left in data/bulk/mine/. `--promote` deliberately NOT included -- stays a separate, manual, deliberate step same as every other catalog write in this project.

**Naming resolved:** "BulkAddPattern" -- Tony's own framing, confirmed correct: this automates the SAME judgment chain as manual AddPattern (spot a real pattern -> trust it matches -> sanity-check -> commit), just applied exhaustively across full history instead of one live setup at a time. Audit gate (mine_audit.py) + M-079 eval are the automated equivalents of a human's own recall-check and sanity-check.

**Folder confusion resolved mid-session:** `data/bulk/nightly_scan` is NOT part of this process -- that's Scanner Loop's (WO-P300-E3.001) own folder, a separate report-only pipeline (last-bar-only STRICT re-detection, watchlist, never writes to the catalog). Confirmed via conversation_search against the 2026-07-12 session where Tony's "why not call it BulkAddPattern" question about Scanner Loop was answered (Scanner Loop deliberately never writes to the catalog, unlike this new process). Tonight's actual IntelliScan 5-symbol workflow uses `RunAllDailyEvals.ps1` (Pipeline B, unchanged) for evaluation, and Scanner Loop separately if/when Tony wants the nightly_scan STRICT-redetection watchlist -- neither touched by tonight's BulkAddPattern build.

**NOT DONE -- explicitly flagged, do not skip:** zero PEH verification on any of the 4 files. `archive-mined` DELETES source files after a successful zip write -- this must be PEH-tested against scratch data (mirroring file #6/#7's PEH approach from earlier tonight) BEFORE any real run of `P_300_RunBulkAddPattern.ps1`. First task next session.

**Session ending here per Tony's explicit instruction.** Summary of tonight overall: WO-P300-E3.002 fully closed (build + PEH + first real production run, 508->6039 patterns promoted); M-092/M-093/M-095 real bugs found and fixed; WO-P300-E4.001 (cli.py refactor) and WO-P300-E4.002 (get_latest_catalog_path typed sibling) filed, PENDING; "BulkAddPattern" process built, PEH still owed.

**>>> 2026-07-14 (6th) REAL FIRST PRODUCTION PROMOTE COMPLETE, INDEPENDENTLY VERIFIED:** Re-ran `--promote` after M-095 fix -- PROMOTED successfully. pattern_instances 508 -> 6039 (+5531), pattern_bars +110620, forward_labels +27655, source_files +40. Claude independently verified (not taken on the tool's own printed word, M-054): real `071026catalog.db` size 3.2MB -> 33.4MB (~12x, consistent with pattern count growth); `071026catalog.db.bak` confirmed byte-identical to the pre-promote state (3,346,432 bytes, matching this morning's manual backup at `models/archive/databases/pre_mine_batch_071026catalog_20260714.db` -- two independent copies of pre-batch state now exist). File identity through the atomic move traced via mtime back to the actual staged build, not a fresh/different artifact.

**WO-P300-E3.002 is now FULLY complete** -- build, PEH, AND real first production run (mine-patterns -> CSV review, document-only per density decision -> ingest-mined -> promote) all done. The only remaining open items from this WO are the two follow-ups filed as their own WOs: WO-P300-E4.001 (cli.py command-registry refactor) and WO-P300-E4.002 (get_latest_catalog_path() typed sibling) -- both PENDING, not started, will surface at future INITs.

**Live catalog composition note for next session:** 6039 patterns is a big jump from 508 -- M-046 (lambda tuning, blocked on sufficient h20-complete ledger rows) and M-033's 70:30 uptrend:pullback ratio maintenance both now have a much larger real population to work against. Worth revisiting both at next session's INIT.

**>>> 2026-07-14 (5th) REAL FIRST PROMOTE ATTEMPT CRASHED, ROOT-CAUSED + FIXED (M-095), WO-P300-E4.002 FILED:** Real batch (5,531 inserted / 53 correctly audit-rejected, all consistent with expectations -- see prior entries) reached `--promote`, crashed `'str' object has no attribute 'exists'`. Real live catalog.db confirmed untouched (mtime unchanged, crash happened before any write). Root cause: `get_latest_catalog()` (str by design, M-089 convention: callers must wrap in Path()) unwrapped at 3 call sites found via full-codebase grep -- `catalog_merge_pipeline.py`'s `build_staging_merge()` + `promote_staging_to_live()` (the one that crashed), and this session's own `ingest_mined_pipeline.py` `run_ingest_mined()` (latent, hadn't crashed yet only because shutil.copy2/load_full_catalog tolerate str). All 3 fixed (`Path(get_latest_catalog())`), versions bumped (catalog_merge_pipeline.py v1.1, ingest_mined_pipeline.py v1.2), new regression test `tests/test_get_latest_catalog_path_safety.py`.

**WO-P300-E4.002 filed (PENDING):** typed sibling `get_latest_catalog_path() -> Path` in db_utils.py -- systemic fix so this convention stops needing to be manually remembered at every call site. Tony's explicit call: file as a WO (not just a lessons.md note) so it surfaces at every future INIT until closed. Not started.

**RE-PROMOTE READY:** same staging DB (`models/staging_ingest_mined.db`, still valid, untouched by the crash) -- ready to re-run `--promote` with the fix in place.

**>>> 2026-07-14 (4th) WO-P300-E3.002 CLOSED OUT -- FILE #7 CLI-WIRING PEH PASS, ALL 9/9 FILES REAL-PEH-VERIFIED:** `cli.py`'s ingest-mined argparse wiring PEH-verified via Claude Code -- 4/4 checks PASS on first run (flag routing, real build-mode run through `main(argv)` against real ADBE data, `--promote` against a missing staging DB correctly returning 1, a REAL `--promote` staging->scratch-live-copy exercising `catalog_checkout`/`catalog_checkin`/`atomic_move` through the CLI entry point). Real live `catalog.db` mtime independently confirmed unchanged by Claude after the fact (`windows-mcp:FileSystem mode=info`: last modified 2026-07-10 09:00:43, untouched all session) -- not just taken on the script's own word (M-054).

WO-P300-E3.002.md updated: Status header -> OWNER_DONE (all 9 files), FINAL CLOSURE section added with the full files #6-9 build/PEH table, Completion Gate checklist added (all items satisfied except the N/A DRAFT-file row). Real first production run (real mine-patterns -> Tony's own CSV review -> real ingest-mined -> real --promote against the actual live catalog) remains a separate, deliberate next step -- same posture as WO-P300-E3.001/E2.001's build-complete-not-yet-real-run pattern.

WO-P300-E4.001 (cli.py command-registry refactor, drafted this session) remains PENDING, not started -- separate go-ahead required before any build begins.

**>>> 2026-07-14 (3rd) WO-P300-E3.002 FILES #7-9 BUILT, 9/9 FILE PLAN COMPLETE:** `cli.py` v1.13 (821 lines total, +ingest-mined subcommand mirroring merge-research-catalog's default-build/--promote shape exactly) -- flagged as well past the 300-line hard limit, same established exception as WO-P300-E3.001 (cli.py has been an aggregation-only exception since 637 lines). `P_300_MinePatterns.bat` + `P_300_IngestMined.bat` NEW, both goto-label launchers mirroring `P_300_MergeResearchCatalog.bat`'s template exactly.

**cli.py refactor flagged, deferred per Tony's call:** flat 15-subcommand dispatch has no natural split point left. Proposed command-registry pattern (python/cli/main.py + one file per category: pipeline_a/pipeline_b/bulk/utility, each exporting register(subparsers)) logged to Backlog below -- terminal syntax unchanged, not started, scoped as its own item after this WO closes.

**NOT YET DONE, WO stays OWNER_DONE not CLOSED:** cli.py's ingest-mined argparse wiring itself (--csv/--input-dir/--staging-db/--live-db/--promote parsing + routing) has NOT been PEH-verified -- only the underlying run_ingest_mined() function has (files #6 PEH, 2 real bugs found/fixed, M-092/M-093). Neither .bat file has been run for real. No real production run of Phase 1 or Phase 2 has happened -- everything to date is scratch/PEH data against real files.

**>>> 2026-07-14 (2nd) WO-P300-E3.002 FILE #6 (`ingest_mined_pipeline.py`) BUILT + PEH-VERIFIED, 2 REAL BUGS FOUND + FIXED:** Phase 2 orchestrator built (282 lines) -- approved CSV -> per-symbol audit gate -> staging insert via catalog_merge_io.py (reused unchanged) -> M-079 staging walk-forward eval. Density decision (document-only, prior entry) wired through: no BOIL/ASTS/MSTR/ARDX/CRK filtering anywhere in this file.

**Real PEH against real ADBE data (one real file from data/bulk/mine/, scratch-copied, original untouched throughout):** 3 approved candidates ingested, 0 skipped-duplicate, 0 audit-failed on the clean run; staging pattern_instances = live_before (508) + 3; both M-079 reports written; re-ingest-into-same-connection correctly recognized all 3 as already present; tampered-horizon negative test correctly caught by the audit gate. Real live catalog.db and source xlsx mtimes confirmed unchanged throughout.

**M-092 (domain/mine_audit.py v1.1):** `_MOVE_PCT_EPSILON` widened 1e-9 -> 1e-6 -- wasn't sized for mine_report_writer.py's :.6f CSV round-trip, so every real approved row failed audit on a move_pct "mismatch" invisible at 6 decimals. CSV precision itself untouched (deliberate operator-facing precision). `tests/test_mine_audit.py` NEW (file's first real bug fix, Regression Test Governance) -- 2/2 PASS.

**M-093 (application/ingest_mined_pipeline.py v1.1, in-scope for file #6):** `_build_window_and_labels()` now rejects a window whose earliest bar predates `MINE_MIN_ANCHOR_DATE` -- the backfill boundary only gated the anchor bar itself, not the 20-bar window measured backward from it; a legitimately-eligible anchor's window could still reach into backfilled 0.0-placeholder bars and fail `PatternBarRecord` validation. `tests/test_ingest_mined_pipeline.py` NEW -- 2/2 PASS. Neither mine_audit.py, pattern_miner.py, catalog_merge_io.py, mine_report_writer.py, nor catalog_merge_pipeline.py touched during either fix.

File #6 of 9 done. Next: file #7 (`cli.py` +ingest-mined subcommand).

**>>> 2026-07-14 DENSITY DECISION MADE -- DOCUMENT-ONLY:** Tony's call on the BOIL/ASTS/MSTR/ARDX/CRK density finding (2026-07-13 volume-question entry below): document-only, no pre-filter, no flag column. M-079's staging walk-forward eval is the real gate (same one that caught E2.003) -- trusting it to catch a real problem rather than pre-filtering ahead of it. Recorded in WO-P300-E3.002.md. Known accepted risk: correlated-row volume from these 5 symbols may need revisiting later if it affects lambda tuning (M-046). Phase 2 build (`ingest-mined`, files #6-9) starting next.

**>>> 2026-07-13 (9th) VOLUME QUESTION RESOLVED, DEFINITIVE:** Ran the audit agent against the full 40-symbol corpus with a real live-catalog collision check (read-only SELECT, 18/5584 = 0.3% real overlap -- not the volume driver) plus a matched-window confound check on the density finding. BOIL/ASTS/MSTR/ARDX/CRK's tight avg_gap (2.44-3.77 vs. 5.0-51.3 for everything else) held explicitly explained the confound (those 5 are the only 753-bar files vs. ~2513 for the rest) by truncating all 35 long files to the same 753-bar window and re-running mine_bars() + a full self-audit (80/80 clean). Crowded five stayed ranked 1-5 of 40; gap to the nearest competitor (INTC, 5.02) WIDENED rather than closed. Verdict, verbatim: "REAL, ISOLATED DENSITY... Not a window-length artifact." Two script-local bugs found and fixed en route (M-089: `get_latest_catalog()` returns str not Path; M-090: live schema column is `symbols.ticker` not `symbols.symbol` -- confirms `P300_System_Architecture_V1.1.md`'s schema section is stale). lessons.md M-084 formally resolved (was Open since 2026-07-12); WO-P300-E3.002 doc updated with the finding. Design implication for Phase 2 (density pre-filter vs. flag vs. document-only) is not yet decided -- Tony's call, next.

**>>> 2026-07-13 (8th) WO-P300-E3.002 AUDIT AGENT BUILT + PEH-VERIFIED 10/10, FILE #5 DONE:** `domain/mine_audit.py` v1.0 (145 lines) -- per Tony's explicit direction, `audit_symbol()` does not re-derive a second copy of the eligibility/outcome math; it re-runs `pattern_miner.py`'s already-proven `mine_bars()` against freshly re-read bars and diffs an approved CSV row against that real recompute. Covers all of the WO's named checks (move-clears-threshold, anchor-is-launch, eligibility windows, post-backfill boundary) via one diff; catalog collision is a separate explicit check against a caller-supplied `existing_keys` set -- domain stays I/O-free, Phase 2's orchestrator (not built) owns the real catalog read.

**PEH 10/10 PASS against real CAT data (Claude Code, first run, no fix needed):** 130 real candidates mined from CAT's real 2512-bar grid. Clean self-audit 100% pass, tampered-horizon negative test caught + correctly named without false-positiving the other 129 rows, fabricated catalog-collision negative test caught + correctly named, source XLSX mtime confirmed unchanged (domain no-I/O contract held). No bug found -- per Regression Test Governance a permanent `tests/test_mine_audit.py` is triggered by a real bug fix, not a clean run; none created yet, PEH script (ad-hoc, `Agentic-Hub-Governance\verify\`) covers both negative-test shapes if a future touch needs a template. WO-P300-E3.002 status header updated: OWNER_DONE (Phase 1 only) -> OWNER_DONE (Phase 1 + audit agent). Closure note appended to the WO doc.

**Still open:** Phase 2 (`ingest-mined`, files #6-9), the 5584-candidate volume question, BAC standalone trace.

**>>> 2026-07-13 (7th) WO-P300-E3.002 PHASE 1 BUILT + PEH-VERIFIED 9/9, OWNER_DONE:** `infrastructure/mine_report_writer.py` v1.0 (164 lines -- MineCandidateRow dataclass, markdown report writer, candidates CSV writer with `keep` defaulted YES, `read_approved_candidates_csv()` for Phase 2) + `application/mine_patterns_pipeline.py` v1.0 (128 lines -- pure orchestration, `MineRunResult` field names matched exactly against `cli.py` v1.12's already-shipped `_cmd_mine_patterns` handler, which was written ahead of these files per the WO's delivery order). `config.py`'s MINE section (file #1 of 9) was already complete from an earlier session -- no edit needed.

**Real PEH run against the actual 40-file `data/bulk/mine/` corpus, 9/9 PASS:** live `071026catalog.db` and `bulk_research.db` mtime byte-identical before/after (Phase 1's core requirement -- 100% read-only, confirmed against real data not a scratch copy), all 40 files scanned with 0 parse failures, 5584 candidates found (2946 uptrend / 2638 breakdown), report + CSV both written correctly to a scratch dir, CSV row count and content verified against the in-memory result, all rows default `keep=YES`, `read_approved_candidates_csv()` round-trips cleanly. Output written to `verify/_scratch_mine_reports/` only -- not production `MINE_REPORTS_DIR`; the real first production run (`python cli.py mine-patterns`, writing to the actual report location) is a separate, deliberate next step, not done as part of this PEH.

**Volume finding, flagged for next session's audit-agent planning, not a defect:** 5584 candidates / 40 symbols = ~140/symbol average. This is the exact concern the WO's own text names as the reason `domain/mine_audit.py` exists -- "Tony does NOT audit rows manually; the comparison against his own historical picks IS the audit" and per-row CSV approval becomes optional "once the audit agent is validated." The audit agent is NOT built yet. Until it is, approving a CSV this size on keep=YES defaults means trusting ~5584 rows with no automated check behind it -- worth deciding on explicitly next session (audit agent design, or a pre-filter/threshold before the CSV is even generated), not something to skip past when Phase 2 planning starts.

**Explicitly deferred to a NEW session per Tony's direction:** audit agent (`domain/mine_audit.py`, WO file #5) and all of Phase 2 (`ingest-mined`, files #6-9 -- approved-CSV read, audit gate, staging insert via `catalog_merge_io.py` reuse, M-079 staging walk-forward eval, promote). Not started, not planned in detail this session -- Phase 1 is the deliberate stopping point.

**>>> 2026-07-13 (6th) WO-P300-E3.002 VALIDATION GATE CLEARED -- Tony accepts 77/84 + 4 named edge cases, Phase 1 build starting:** Confirmed with Tony: the M-085/086/087 validation chain (parts 5-16, this session) is sufficient to clear WO-P300-E3.002's validation gate. 77/84 hard hits (91.7%), 3 confirmed OUTCOME-INVALID (consistent every round), BLK named as a genuine v2.0 architecture scope boundary (M-087), NVDA/TECK understood as OUTCOME-INVALID-shaped, BAC separately flagged and unresolved but immaterial to the gate decision. Zero anchors left unexplained. Explicit clarification given same session: today's 91.7% is a RECALL statistic (does the miner rediscover Tony's own known winners), not an expectancy/win-rate number -- it cannot be compared to Tony's ~62% manual realized win rate or the catalog's own ~60-61% baseline forward-label win rate. Real expectancy impact of the mined patterns remains an open empirical question for Phase 2's required M-079 staging walk-forward comparison (same gate that caught E2.003's 68.8%->63.6% regression) -- not something Phase 1 validation work can answer.

Proceeding to Phase 1 build (files 3-4 of the WO's 9-file plan: `infrastructure/mine_report_writer.py`, `application/mine_patterns_pipeline.py`) -- read-only, no DB write, per the WO's explicit scope. Audit agent (`domain/mine_audit.py`) and Phase 2 (`ingest-mined`, `catalog_merge_io.py` reuse, M-079 staging eval, promote) explicitly deferred to a NEW session per Tony's direction -- not started this session.

**>>> 2026-07-13 (5th, FINAL) WO-P300-E3.002 VALIDATION COMPLETE -- ALL 84 GROUND-TRUTH ANCHORS INDIVIDUALLY EXPLAINED, ZERO UNEXPLAINED (M-087):** Part 16 traced BLK/NVDA/TECK -- the 3 anchors part 15 left unexplained after M-086's date-attribution fix -- directly against eligibility (`_is_eligible`) and outcome (`_qualifies_for_class`) at their real crossover bars. Two distinct findings, both real, neither a `pattern_miner.py` bug:

- **BLK:** the qualifying breakdown move completes in the bar BEFORE eligibility opens (still uptrend regime at that point) -- by the time the crossover confirms, the move's already played out, nothing left to clear 15% forward from an eligible bar. Genuine v2.0 architecture scope boundary: outcome is always measured strictly forward from an already-crossed-over bar, so a move that LEADS its own confirming crossover by more than ~1-2 bars is structurally unreachable by design, not by defect.
- **NVDA 2026-05-05 + TECK 2026-05-20:** no qualifying move anywhere near their real crossover, either direction, any horizon. Closer to OUTCOME-INVALID (AME/PCAR/SHW) than to a real gap -- the earlier apparent "match" was the same M-086 mechanism, just without a clean later crossover to reattribute to.

M-087 added to lessons.md with full detail.

**FINAL ACCOUNTING -- all 84 anchors resolved, nothing left unexplained:**
| Bucket | Count | Status |
|---|---|---|
| HITS | 77 (91.7%) | Real, crossover-consistent, `mine_bars()`-confirmed |
| OUTCOME-INVALID | 3 | AME/PCAR/SHW -- consistent every round, all session |
| M-087 boundary case | 1 | BLK -- named v2.0 architecture limitation |
| M-087 invalid-shaped | 2 | NVDA/TECK -- likely not real 15% moves under this measurement |
| Separately flagged, undiagnosed | 1 | BAC -- marginal/hairline crossover + move, different shape, not folded into any bucket above |
| **TOTAL** | **84** | **0 unexplained** |

**Session-wide arc summary (parts 5-16, all one continuous validation effort):** started at 45.5% (v1.5), the v1.x outcome-only architecture topped out at 86.4% with a confirmed density regression (M-084), triggering the v2.0 crossover-gated redesign. v2.0/v2.1 real validation: 71.4% (60/84, M-085's raw number) -> widening the VALIDATION SCRIPT's `resolve_pick` (not the miner) through 3 real bug-fixes -- 56.0% (part 11 regression) -> 60.7% (part 12, still regressed) -> 81.0% (part 13, correct two-tier fix) -> 91.7% (part 15, M-086 date-attribution confirmed) -> **all 84 individually accounted for (part 16, M-087)**. `pattern_miner.py` v2.1 itself has not been shown to have a single confirmed defect anywhere in this entire chain -- every real bug found and fixed this session was in the validation tooling, not the production miner.

**WO-P300-E3.002 status decision -- Tony's call, not made in this session:** validation is now as complete as it can get without a new architecture change (BLK's boundary case would require deliberately extending eligibility to look backward before a crossover -- a real design decision, not a default). 77/84 hard hits + 4 individually-understood edge cases + 3 consistent invalids is a fully-accounted-for, defensible number against the WO's own already-once-revised acceptance criteria ("zero misses = bug" -- confirmed unreachable by pure price-outcome mining, 2026-07-12 entry). Whether this clears the bar for Phase 1 pipeline build (report writer, `mine_patterns_pipeline.py`) is Tony's decision next session -- WO-P300-E3.002 remains PENDING as of this entry, no auto-clear taken.

**>>> 2026-07-13 (4th) WO-P300-E3.002 M-086 CONFIRMING LOOKUP -- TRUE RATE 77/84 (91.7%), 3 GENUINELY OPEN ANCHORS REMAIN:** Part 15 checked whether `mine_bars()` already found each of M-086's 12 mismatched picks as a real candidate at its later, correct crossover date (0-12 trading bars forward of the ground-truth date). **9/12 CONFIRMED** -- CRK, DE, DVN, HAL, NVDA 2026-03-30, TDC 2026-02-04, TDC 2026-03-30, TDC 2026-04-29 (the whipsaw case -- resolved cleanly despite part 14's "no clean single flip" flag), TECK 2026-05-04. These 9 are settled: never a `pattern_miner.py` gap, purely `resolve_pick`'s validation-script anchoring the ground-truth date before the real MT crossover occurred (M-086's mechanism, confirmed end-to-end). **True reachable-hit rate: 68 (part 13) + 9 = 77/84 (91.7%).**

**3/12 NOT FOUND, genuinely open -- BLK 2026-02-23, NVDA 2026-05-05, TECK 2026-05-20:** no `mine_bars()` candidate at all within the +0..+12 bar window, even in the correct direction. M-086's "anchor precedes real crossover" explanation does NOT fully cover these -- even the correctly-dated window produced nothing. Different question from M-086, and different from BAC (still separately flagged, marginal/hairline case). These 3 need a crossover-window eligibility trace (same shape as the earlier BURL/GOOGL/GS/HAL/INTC/NVDA v2.1 misses) before anyone can say whether they're genuine `pattern_miner.py` gaps or a third, distinct ground-truth artifact. Not diagnosed yet -- next natural step if this WO continues.

**Session arc, for the record:** 60/84 (M-085 baseline) -> 47/84 (part 11, loop-order bug) -> 51/84 (part 12, wrong direction model) -> 68/84 (part 13, correct two-tier fix) -> 77/84 (part 15, M-086 date-attribution confirmed for 9/12). Every drop below baseline was a real, found, fixed bug in the VALIDATION script -- `pattern_miner.py` v2.1 itself was never touched after v2.1 shipped and has not been shown to have a single confirmed defect this entire session. 3 anchors (BLK, NVDA 2026-05-05, TECK 2026-05-20) remain the only unexplained gap in an 84-anchor ground truth.

**WO-P300-E3.002 still stays PENDING, Phase 1 pipeline build still not cleared** -- 3/84 unexplained is close to a defensible acceptance threshold but is Tony's call, not an auto-clear; the WO's own acceptance criteria ("zero misses = bug") was already revised once this WO (2026-07-12 entry) as unreachable by pure price-outcome mining -- whether 3 residual, individually-traced anchors clears the (still not renumbered) bar is an open decision, not yet made.

**>>> 2026-07-13 (3rd) WO-P300-E3.002 SETTLED -- 12 MISMATCHED MISSES ARE A GROUND-TRUTH ARTIFACT, NOT A pattern_miner.py GAP (M-086):** Part 14 diagnostic (bar-by-bar `mtdiff` trace on all 12 direction-mismatched real misses from part 13) traced the exact mechanism. Every one of the 12 anchors on a bar still in the OPPOSITE `mtdiff` regime -- the resolved direction's real MT crossover happens 2-9 trading days LATER (BLK +5, CRK +3, DE +7, DVN +9, HAL +8, NVDA x2 +3, TDC x2 +4/+6, TECK x2 +2/+3). Root cause: part 13's tier-2 fallback checked "does a qualifying move exist nearby, in either direction" without requiring that direction's OWN crossover to have already occurred at the anchor -- structurally guaranteed to borrow a later, unrelated trend's outcome and misattribute it backward. **This is NOT a `pattern_miner.py` defect** -- v2.1's `mine_bars()` never makes this mistake, since `_is_eligible()` already hard-gates direction-matches-current-crossover; the bug was specific to the validation script's tier-2, which had no equivalent gate. M-086 added to lessons.md, generalized rule: an outcome-only resolution heuristic must also verify the resolved class's own state-defining condition held at the anchor point, not just that SOME qualifying outcome exists nearby.

**PENDING CONFIRMATION, not yet run:** whether these 12 are simply mis-dated relative to `pattern_miner.py`'s own crossover convention (Tony's manual pick a few days ahead of formal MT confirmation) -- if so, `mine_bars()` likely already found each one at its real, later crossover date, and the true reachable-hit rate is higher than 68/84 (part 13's number). Simple lookup against each real crossover date's already-computed candidate list, no new `resolve_pick` logic needed. Offered to Tony, not yet actioned as of this entry.

**BAC 2026-01-29 stays separately flagged** -- different shape entirely (hairline mtdiff=+0.0010, marginal 15.69% move over a 108-day extended horizon), not part of the 12-pick mechanism above. Standalone case, not evidence toward or against M-086's generalization.

**WO-P300-E3.002 still stays PENDING, Phase 1 pipeline build still not cleared** -- this is real progress on the ground-truth validation methodology, not a green light. v2.1 production code (`pattern_miner.py`) is looking increasingly solid through this whole exercise -- every real defect found this session (M-083, M-084 resolved, the part-11/12 search-order bugs, M-086) has been in surrounding tooling (the miner's own v1.x-v2.1 history, or the validation scripts), not in the currently-shipped v2.1 itself.

**>>> 2026-07-13 (2nd) WO-P300-E3.002 WIDENED resolve_pick, PARTS 11-13 -- REAL VALIDATION NUMBER NOW 81.0% (68/84), TWO OPEN QUESTIONS FLAGGED, SCRIPT ITERATION STOPPED DELIBERATELY (SESSION CONTINUES):** Continuation of M-085 (below): widened `resolve_pick` (the VALIDATION script's matcher, not `pattern_miner.py` itself, which was untouched all three parts) through three real iterations, each catching a genuine bug in the prior one -- not blind parameter tuning.

**Part 11 (first widened attempt):** promoted the M-085 exhaustive spot-check logic into `resolve_pick`, but nested offset-outer/direction-inner. Shipped with a real syntax bug first (non-raw docstring containing `C:\Users\...` parsed `\U` as a unicode escape -- caught immediately on Tony's first run, fixed by making the docstring `r"""`; logged as a recurring PEH-script shape, same family as M-032). Real run: **HITS 47/84 (56.0%), WORSE than the 60/84 pre-widening baseline.** 33/34 real misses also flagged DIRECTION_MISMATCH -- root cause found: offset-outer meant a NEARER wrong-direction match could preempt a FARTHER crossover-consistent one, purely from loop order.

**Part 12:** "fixed" by forcing one global direction (`dir0`, the target bar's own inferred direction) across the whole window before trying the opposite -- but this was itself wrong: the ORIGINAL narrow `resolve_pick` (parts 5-9) used each PROBE's OWN locally-inferred direction at offsets +/-1, not a fixed `dir0`. Forcing `dir0` onto every offset tested the wrong class at bars near their own crossover transition. Real run: **HITS 51/84 (60.7%)** -- improved, still below baseline.

**Part 13 (current):** two-tier design, no global direction forced. Tier 1 = each probe's own locally-inferred direction, full +/-2 window, nearest-offset-first (strict superset of the narrow original's real behavior). Tier 2 (fallback only, if tier 1 finds nothing anywhere) = each probe's opposite class, marked non-crossover-consistent. Real run: **HITS 68/84 (81.0%), REAL MISSES 13** (down from 34->30->13 across the three parts) -- first result to land in the ~78-80/84 range M-085 projected.

**Two open questions in the remaining 13, deliberately NOT script bugs, iteration stopped here:**
1. **12 mismatched misses** -- these only qualify under the direction OPPOSITE the bar's own crossover context, and `pattern_miner.py`'s own `mine_bars()` independently did NOT produce them as candidates (tier-2 fallback correctly rejecting them, not failing). Real question: are these 12 ground-truth picks simply unreachable under the current v2.0/v2.1 crossover-gated eligibility model, or is `mtdiff`-sign direction inference noisy/wrong near a transition bar for some of them? Design call, not a bug fix -- explicitly Tony's decision next session, per this session's established pattern of not letting Claude guess architecture calls.
2. **BAC 2026-01-29** -- the one real miss NOT direction-mismatched (direction agrees, still no candidate match) -- same exception every round since M-085. Different in kind from the 12 above; worth a standalone trace (off-by-one in the candidate-crossover comparison, `mine_bars()` range issue, etc.) before folding it into the direction-inference question. Do not generalize from n=1.

**Why iteration stopped at part 13, not a part 14:** the first two rounds' misses were each dominated by one traceable script bug (loop order, then wrong direction model) -- both real, both fixed. Part 13's remaining 13 are structurally different: tier 1 is now a clean superset of the narrow original with no obvious defect left, and both remaining categories look like real findings about `pattern_miner.py`'s design, not `resolve_pick` implementation gaps. Further tuning risks chasing noise instead of surfacing the real question.

**NEXT SESSION:** (1) Tony decides on the 12-pick direction-inference/reachability question -- does NOT resolve itself from more search-radius tuning. (2) standalone BAC trace. (3) WO-P300-E3.002 still stays PENDING, Phase 1 pipeline build still not cleared -- this session's real number (68/84, 81.0%) is progress over M-085's raw 60/84 but the two open questions above are unresolved, same discipline as every prior round.

**>>> 2026-07-13 WO-P300-E3.002 PATTERN MINER v2.0/v2.1 -- CROSSOVER-GATED ARCHITECTURE, REAL VALIDATION 71.4% (60/84), M-085 FOUND AT SESSION CLOSE (SESSION CLOSED, NOT DONE):** Continuation of 2026-07-12's session (outcome-only mining topped out at 86.4%/v1.6 with a suspected density regression on volatile names -- see that entry). Tony diagnosed the real architectural gap directly from an uploaded IntelliScan `.isc` (`P_300_Potential_Crossover_v12.isc`): mining pure outcomes was never going to reproduce a process that starts from a sparse, crossover-gated population (his scan keys on `Cross(PMAMedium,0,AMAMedium,0)`, TripleCrossCriteria=True), not "screen every bar."

**v2.0 built (crossover-gated eligibility, same-class re-arm removed):** `config.py` v1.15 -- `MINE_XOVER_MAX_BARS=20` + `MINE_IGNITION_MAX_BARS=3`, both MEASURED (not guessed) from a Step-1 diagnostic: for all 66 (then-current) real picks, bars-since-most-recent-same-direction-MT-crossover distribution came back min=0/p25=1/median=4/p75=7/max=39, with 50% within 3 bars, 86% within 11 (matching Tony's own scan `DaysInTrend=11`), 95% within 20. `pattern_miner.py` v2.0 -- new `_bars_since_crossover()` (mtdiff sign-run walk-back); `_is_eligible()` now gates on that plus direction match; `MinedCandidate` gained `bars_since_crossover` + `entry_tier` (IGNITION/CONTINUATION). v1.6's same-class re-arm REMOVED entirely (confirmed via real-data gap analysis, 2026-07-12, to have reintroduced M-083 on volatile names -- gap==1 at 90-98% on ASTS/BOIL/MSTR/ARDX). Cross-class interruption (v1.5) kept unchanged. Ground-truth matching logic also changed (Tony's correction): a hit is now "shares the pick's own crossover event," not bar-proximity -- the old +/-2-bar test doesn't fit an architecture where the cursor typically anchors at bar 0-2 of a trend while a real pick's median position is bar 4. `tests/test_pattern_miner.py` rewritten for v2.0 -- 14/14 PASS.

**Two real ground-truth-extraction bugs found and fixed mid-session, unrelated to pattern_miner.py itself:** (1) a GOOGL anchor-date mismatch (Tony's live chart example, 4/4->4/17, wasn't in the archived ground truth -- confirmed it was never run through AddPattern, a genuine gap not a bug) led to discovering the extraction regex required an 8-digit archive-date prefix, silently excluding any plain `Pattern_<start>_<end>_<SYMBOL>.xlsx` file -- affected 19 symbols (10 with zero picks under the old regex: AMD/BAC/CAT/CVX/GS/META/NKE/SPY/WMT/XOM; 9 undercounted). (2) the OUTCOME-INVALID/no-match resolution logic (`resolve_pick`) needed to try the opposite direction and idx+/-1 before giving up -- an earlier round found 25/27 of a 30-pick "invalid" bucket actually qualified nearby. Both fixed; corrected ground truth is now a PERMANENT versioned file, `tests/mine_ground_truth.py` v1.0 -- **84 anchors / 40 symbols**, self-verified on write (extraction count == re-import count). Supersedes three ad-hoc PEH-script extractions this session.

**v2.1 built (fresh-same-class-crossover window truncation):** first real validation of v2.0 against the corrected 84-anchor ground truth found 6 real misses (BURL, GOOGL 2026-03-18, GS, HAL, INTC, NVDA), traced 6/6 to the SAME mechanism: an earlier same-class match's capped (20-bar) jump lands INSIDE or PAST the very next crossover's own eligibility window, so those bars are never evaluated at all (eligibility tiering itself was correct -- the cursor simply never revisited them; GS was the extreme case, a 104-day-horizon match's 20-bar jump still overshooting the entire next window). Fix: `_find_fresh_crossover()` -- a bar that is ITSELF a fresh same-direction crossover (bars_since_crossover==0) inside the capped window truncates it, same mechanism as v1.5's cross-class interruption but checking for a structurally rare new ignition instead of outcome requalification (the distinction that keeps this from reintroducing v1.6's mistake). `pattern_miner.py` v2.1, 298 lines. `tests/test_pattern_miner.py` -- 18/18 PASS, including an end-to-end fixture reproducing the actual bug shape. Re-validated: **all 6 prior misses resolved, 0 new misses, 0 regressions.**

**Real validation result, fully confirmed (after catching and fixing a bug in Claude Code's own validation script -- a `resolve_pick()` regression that silently merged the OUTCOME-INVALID and NO-CROSSOVER buckets; caught because the two 24-item buckets were symbol-for-symbol identical across rounds, flagged and fixed before the number was trusted): **HITS 60/84 (71.4%), OUTCOME-INVALID 24, REAL MISSES 0, NO-CROSSOVER 0, FILE ERRORS 0.**

**M-085 found at session close, Tony's explicit ask for a spot-check before closing:** the 24-pick OUTCOME-INVALID bucket had been carried across multiple rounds as "genuine ground-truth noise" on the strength of `resolve_pick`'s 4-combination search (original direction + opposite direction at the pick bar, original direction at idx-1/idx+1). An exhaustive spot-check (both directions, idx-2..idx+2, 10 checks per pick) on one pick per distinct symbol among the 24 found **only 3/17 genuinely invalid (AME/PCAR/SHW -- consistent with every prior round's triage)**; **14/17 actually qualify** somewhere the narrower search never looked. If that ratio holds across the full 24-pick bucket, true HITS is likely **~78-80/84 (93%+), not 60/84**. Additional finding: several (AMZN, GOOGL, CIEN, SPY) qualify under a DIFFERENT class than their recorded inferred direction -- mtdiff-sign direction inference at the exact pick bar may be noisy/transitional for some picks, not yet root-caused. M-085 added to lessons.md.

**NEXT SESSION:** (1) widen `resolve_pick` to the full idx-2..idx+2/both-directions search (or replace it with the spot-check's exact logic) and re-run the complete 84-anchor validation -- expect ~78-80/84. (2) Investigate the direction-mismatch pattern (AMZN/GOOGL/CIEN/SPY) as a possibly separate, real issue in how a pick's direction gets inferred, not just a search-radius gap. (3) Only after both are resolved: WO-P300-E3.002 moves to Phase 1 pipeline build (report writer, mine_patterns_pipeline.py) -- NOT cleared yet. PEH staged at `Agentic-Hub-Governance\verify\run_this.py`/`run_this_context.txt` holds the exhaustive spot-check script (part 10) as the last real state; will need replacing with the widened-`resolve_pick` full sweep next session.

**>>> 2026-07-12 WO-P300-E3.002 PATTERN MINER v1.5/v1.6 -- REAL GROUND-TRUTH VALIDATION: 45.5% -> 86.4%, ONE OPEN CONCERN CARRIED TO NEXT SESSION (SESSION CLOSED, NOT DONE):** INIT this session found two stale facts before any code was touched (M-017/M-054 discipline): the WO's own status line read "PENDING -- awaiting Tony's 20-symbol export" but all 34+6 grid files were already staged in `data\bulk\mine\`; and the ground-truth anchor count cited in the WO doc (~85/20 symbols) and todo.md (57/34) both disagreed with what's actually on disk. Real ground truth extracted directly from archived AddPattern source filenames (`data\processed\2026-0[567].zip` + `History Grid 0526.zip`, start-date token = anchor) via PowerShell zip-entry enumeration: **66 unique (symbol, anchor_date) pairs across 30 symbols** (10 of the 40 staged symbols -- AMD/BAC/CAT/CVX/GS/META/NKE/SPY/WMT/XOM -- have zero archived picks in these zips, so no ground truth exists for them yet).

**Unrelated cleanup, same session, before the miner work:** `.claude\skills\python-project-architecture\SKILL.md` compressed 11.4KB -> 8.6KB (~24%) per Tony's request -- prose sections (Layer Rules, Regression Test Governance, Common Mistakes) converted to tables, no rule/path/condition removed. Regression Test Governance itself (added earlier the same day, pre-INIT) preserved intact.

**v1.5 built (M-082 recurrence + cross-class interruption):** v1.4's cursor jump reused raw `horizon_days` as the jump distance -- M-082's cap (jump capped at `_MAX_STANDARD_HORIZON`=20 regardless of search length) had been silently lost when the dedup-window formula it originally patched was fully replaced by v1.4's cursor-scan rewrite. Reapplied at the correct step: `jump = min(horizon_days, _MAX_STANDARD_HORIZON)`. Added cross-class interruption: `_find_interruption()` checks the OPPOSITE class at standard horizons only inside the capped window; a real reversal there ends the window early instead of running its full length. `tests/test_pattern_miner.py` NEW -- first real application of Regression Test Governance -- 7 tests (5 BEHAVIOR, 2 SOURCE) backfilling M-081/M-082 + the new interruption logic. 7/7 PASS (synthetic fixtures).

**Round 1 real validation (v1.5): 30/66 hits (45.5%), 36 misses.** Triaged by Claude Code: 5 confirmed invalid ground truth (AME/BLK/DE/PCAR/SHW -- never clear 15% under either class even with unbounded extended search), 31 real misses -- 100% traced to the M-083 family (a later, independently-qualifying same-class anchor swallowed whole by an earlier match's capped consumption window), 0 unexplained.

**v1.6 built (same-class re-arm, the direct generalization):** `_scan_class()` now calls `_find_interruption()` TWICE per match -- opposite_class (v1.5, unchanged) AND want_class (new) -- and truncates the window at whichever comes first. A later bar independently re-qualifying for the SAME class at a standard horizon is a fresh ignition, not more of the current match's own consumption. `_find_interruption()` itself needed no change (already class-agnostic). `test_pattern_miner.py`'s wiring test rewritten for the two-call structure; still 7/7 PASS.

**Round 2 real validation (v1.6): 57/66 hits (86.4%), 9 misses -- v1.6 fixed 25 of the 31 real Round-1 misses.** Triaged: 3 confirmed invalid ground truth (AME/PCAR/SHW, consistent every round), 6 real residual misses (BLK/DVN/GOOGL/ICE/NVDA/TDC) -- all traced to an extended-search match whose own capped window still happens to reach a later, separately-qualifying bar (smaller residual of the same M-083 family).

**OPEN CONCERN, NOT RESOLVED -- carried to next session:** the v1.6 run also reported 16,146 total extra candidates across 30 symbols (mean 37.4/100 bars). Claude Code's per-symbol density table showed a clean correlation with independently-known stock volatility (ASTS/BOIL/MSTR/ARDX/CRK -- the volatile short 3-yr grids, plus NVDA -- at the top; PCAR/ITW/AME/ICE/LMT/SHW -- boring industrials -- at the bottom) and argued this supports "real moves, not a bug." Claude pushed back: that correlation is consistent with BOTH "genuinely volatile stock" and "reintroduced per-bar re-triggering," so it can't discriminate between them on its own. Computed the number that can -- average bar-gap between same-class candidates (`eligible_bars / candidate_count`): **ASTS uptrend ~1.11 bars, BOIL breakdown ~1.15 bars, MSTR ~1.49, ARDX ~1.57** vs. **PCAR ~33.7 bars, LMT ~60.8 bars**. A ~1.1-bar average gap means a new "launch" on essentially every consecutive trading day -- no real stock produces that as distinct human-recognizable events. Working theory: same-class re-arm, correct on moderate-volatility names, has reintroduced a bounded (20-day-capped) version of the exact per-bar-qualification problem (M-083) v1.4 was built to eliminate, specifically on the highest-volatility names where nearly every bar is within reach of *some* standard-horizon qualifying move. M-084 added to lessons.md: the volatility-correlation argument alone can't rule this out, since both hypotheses predict it -- the gap distribution is what discriminates, not the aggregate density.

**NEXT SESSION:** get the raw ASTS (and ideally BOIL/MSTR/ARDX) same-class candidate anchor-date list, IN ORDER, from Claude Code, and check whether consecutive gaps are literally back-to-back calendar days (confirms the bug -- fix scoped narrowly, not a blind parameter tune) or something less alarming than the average implies (real density -- revisit the WO's own "~15 sec/symbol approve/reject" workflow assumption with Tony instead, since 500+ candidates/symbol breaks it regardless of correctness). PEH staged at `Agentic-Hub-Governance\verify\run_this.py` + `run_this_context.txt` for this specific diagnostic (raw candidate dump + real gap histogram, not just an average) -- overwrites the Round-1/2 full-validation script; that script's ground-truth list (66 anchors, embedded literally in the file) is the durable artifact if the sweep needs re-running later. v1.6 is NOT cleared for Phase 1 pipeline (report writer / mine_patterns_pipeline.py) build until this is resolved -- WO-P300-E3.002 stays PENDING.

**>>> 2026-07-12 WO-P300-E3.002 OUTCOME-FIRST PATTERN MINER -- PLAN APPROVED (M-078), PHASE 1 BUILD STARTING:** Tony approved the validation-first plan same session. 9-file plan in the WO doc. Validation ground truth: 57 unique manual-pick anchors across 34 symbols that already have 10-yr grids (21 loose in data\bulk\, 13 extractable from E:\10_Pattern_BulkCreate.zip -- copies only, originals untouched); Tony additionally exports 6 small-caps (MSTR, ASTS, TDC, CRK, ARDX, BOIL) to cover the cap-size gap. Build order: config -> miner -> report writer -> phase 1 pipeline -> PEH validation vs ground truth -> iterate -> audit agent -> phase 2. Mid-session note: a brief MCP host stall hit both filesystem tools simultaneously right after this entry was first attempted -- confirmed via M-057 discipline (checked the actual file before assuming failure) that the write never landed; re-issued clean after tools recovered.

**>>> 2026-07-12 WO-P300-E3.001 SCANNER FUNNEL LOOP -- BUILT + PEH-VERIFIED 7/7, STATUS OWNER_DONE (real-corpus run still owed):** Drafted, decided, built, and verified in one session per the 2026-07-10 close-out's NEXT SESSION CANDIDATE note. **Data-source decision:** Tony confirmed he can bulk-export IntelliScan's native crossover screen (whole universe, one action) in the SAME `<years>[I]_Pattern_<SYMBOL>.xlsx` / `BulkBarRaw` format already used for the research corpus -- meaning zero new parsing or detection code needed; `bulk_grid_reader.py` and `bulk_pattern_detector.py` (WO-P300-E2.001) are reused completely unchanged. This WO re-applies the full 9-condition STRICT test to narrow VP's cruder native crossover call down to the same bar the research catalog uses, point-in-time only (last bar per file, not a historical sweep). Report-only by design -- no DB write anywhere in this WO's code, keeping it out of WO-P300-E2.003's rejected-merge territory; winners still enter the live catalog via the unmodified, operator-run AddPattern pipeline (funnel model). **Archiving decision (Tony corrected the initial no-archiving draft):** every processed file, hit or not, archives to a monthly zip on the external backup drive (`E:\AI-Agent-Learning-Hub_BackupFiles\10_Pattern_BulkCreate<MMMYY>.zip`), reusing `archive_pattern_file.py`'s proven append-verify-then-delete pattern. **5 files delivered** (config.py v1.12 +SCANNER LOOP constants, utilities/archive_scanner_file.py NEW 172 ln, infrastructure/scanner_report_writer.py NEW 91 ln, application/scanner_loop.py NEW 126 ln, cli.py v1.11 +scanner-loop/+--reports-dir, P_300_ScannerLoop.bat NEW). **PEH 7/7 PASS (Claude Code):** real read-only parse of `data/bulk/10_Pattern_CIEN.xlsx` (scratch copy, source untouched) independently reproduced the documented CIEN STRICT hit from the 2026-07-10 CIEN head-to-head entry below; full end-to-end `run_scanner_loop()` run confirmed archive + report + delete all worked correctly against the scratch copy. **Two real gaps caught during verification, fixed at the source:** (1) `run_scanner_loop()` had no override for its report-write location -- would have written into production `outputs/reports/scanner/` during the scratch test; fixed by adding a `reports_dir` parameter (v1.1) instead of just patching the test -- M-080 added, same family as M-075. (2) cosmetic `SyntaxWarning` from an unescaped `E:\` in a config.py docstring, fixed. `data/bulk/nightly_scan/` created and empty, ready for the first real export. **NOT YET DONE:** the actual first real nightly IntelliScan-crossover export + `P_300_ScannerLoop.bat` run against real multi-symbol data -- separate, deliberate next step per this project's standard build-then-real-run pattern.

**PEH iteration 3 finding (2026-07-10):** check 5's dedup test asserted whole-catalog (ticker, anchor_date) uniqueness -- wrong invariant. Confirmed via add_pattern_pipeline.py that Pipeline A has never enforced that (only source_files.filename is UNIQUE, EC-023); live catalog already has 4 legitimate PATTERN_IDENT-vs-PATTERN_IDENT pairs from historical re-ingests with a different export window (BOIL 2026-01-16 ids 449/487, PLTR 2025-10-23 ids 17/36, RRC 2026-03-30 ids 353/405, WFG 2026-01-08 ids 26/437) -- all pre-date this WO, out of scope. Test rewritten to check what decision 4 actually needs: no BULK_SCAN row collides with ANY existing pattern, not whole-catalog uniqueness. All 495 bulk inserts already confirmed collision-free against this narrower, correct check.

**PEH VERIFICATION: 7/7 PASS, exit 0 (2026-07-10).** Two real bugs found and fixed across 3 iterations: (1) catalog_reader.py called a helper by a name I hadn't actually verified (_in_clause_placeholders vs the real _build_in_clause) -- typed from memory instead of checking, fixed. (2) My own PEH test asserted the wrong invariant (whole-catalog ticker+anchor_date uniqueness, which Pipeline A has never enforced) -- Tony's diagnostic request surfaced 4 real pre-existing PATTERN_IDENT-vs-PATTERN_IDENT duplicate pairs (BOIL/PLTR/RRC/WFG, all historical re-ingests with a different export window, confirmed via add_pattern_pipeline.py that only source_files.filename is UNIQUE), test corrected to check the actual decision-4 requirement instead. Final run: live catalog.db mtime byte-identical before/after, all 495 STRICT bulk patterns processed (0 unaccounted for), every merged row's bar_offset/forward_labels-count/triple_cross-zero-fill structurally correct, zero BULK_SCAN collisions against the existing 508 patterns, neural_index<-neural_x_max mapping exact for all 3 sampled patterns. Code verified correct against real data via scratch copy -- real live catalog.db still untouched.

**>>> 2026-07-10 (11th) SESSION CLOSE-OUT:** staging_merge_catalog.db deleted (Tony ran it, absence verified via FileSystem info -- path not found). Confirmed beforehand it was never a get_latest_catalog() hazard: the digit-prefix filter in db_utils.py excludes it by design. Architecture confirmed as deliberate two-DB split: live catalog (508 PATTERN_IDENT, trades) + research catalog (3111 instances, analysis only), merge bridge (WO-P300-E2.003 code) verified and parked -- BULK_SCAN enum + merge pipeline remain in place, promotion never run, one command away if ever revisited. NEXT SESSION CANDIDATE: new WO for the scanner loop -- nightly STRICT detection across licensed symbols -> watchlist report for Tony's eyeball review -> winners enter live catalog via normal AddPattern. Supported by CIEN head-to-head (10th entry above): equal precision (80%/80%), detector's edge is breadth (caught Aug-2025 +52.5% CIEN move manual missed), manual's edge is continuation entries invisible to the detector (condition 1 failed on all 5 of Tony's picks). NOT started -- needs a WO draft and Tony's scope decisions first.

**>>> 2026-07-10 (10th) CIEN HEAD-TO-HEAD: manual vs STRICT detector (Tony's direct challenge -- "why is my eyeballing better than your bulk analysis"):** Replicated the exact bulk detector (conditions read from bulk_pattern_detector.py + pattern_detector.py this session, constants verified: trend window 3, swing 60/3, spacing 5) in sandbox against Tony's uploaded 10yr CIEN export + his 5 manual Pattern files. Ground truth: 21 episodes of >=15% rise and 14 of >=15% fall within 20 fwd trading days over the 5 predictive years. RESULTS: precision is EQUAL -- manual 4/5 picks into >=15% risers (80%), 0 into >=15% losers; STRICT 4/5 decided (80%), 0 into losers. Coverage is opposite: Tony's 4 winners all rode ONE episode (Jan-May 2026 +52%) = 1/21 distinct episodes; STRICT caught 4/21 across 5 years including Aug-2025 +52.5% that manual missed entirely; 17/21 caught by NEITHER. Condition autopsy on Tony's picks: condition 1 (MT crossover 1 bar ago) failed on ALL FIVE -- his style buys established trends mid-run (continuation), the detector only fires at ignition. STRICT-as-veto would have blocked all 4 of his winners. His one loser (2026-06-05, -14.2%) passed only 3/9 with all four trend conditions failing -- detector-as-dashboard would have flagged it. STRICT fired on CIEN TODAY (2026-07-10, no fwd data yet). Strategic read: detector = breadth scanner (watchlist generator across ~1500 symbols), manual = precision entry gate; merge-into-analog-pool remains contraindicated per the 63.6% staging eval.

**>>> 2026-07-10 (9th) WO-P300-E2.003 BUILD COMPLETE (7/7 files), NOT YET PEH-VERIFIED -- FIRST WO THAT CAN WRITE TO LIVE catalog.db:** All 7 files shipped: schemas.py v2.3->v2.4 (+BULK_SCAN to DataOriginType), catalog_reader.py v1.0->v1.1 (origin_type->origin_types tuple, IN-clause query; 2 downstream call sites mechanically renamed -- daily_evaluate_pipeline.py + loo_replay.py stay PATTERN_IDENT-only, no live-behavior change), eval_io.py v1.2->v1.3 (catalog_path override + BULK_SCAN in load filter), catalog_merge_io.py v1.0 NEW (per-pattern merge: normalize via domain/normalization.py duck-typed against BulkBarRaw, reindex bar_offset to live -19..0 convention, neural_index<-neural_x_max mapping verified against both ingest manifests -- same VP column NeuralXMax under different field names, NOT a data gap -- triple_cross zero-filled+documented since WO-P300-E2.001 Phase 0 already established price-level-vs-diff mismatch and no safe conversion exists, exact symbol+anchor_date dedup), catalog_merge_pipeline.py v1.0 NEW (stages a copy of the live catalog, merges STRICT-tier bulk patterns into the COPY only, runs walk-forward eval against both untouched-live and staged-post for real comparison; promote_staging_to_live() is a separate function never auto-called), cli.py v1.8->v1.9 (+merge-research-catalog, build mode default + --promote as a distinct explicit action), P_300_MergeResearchCatalog.bat NEW. Every file's mtime confirmed via windows-mcp:FileSystem mode=info immediately after writing.

**Process note -- Tony pushed back hard on the deferral being framed as drift:** quoted WO-P300-E2.001's own 2026-07-08 approved-decisions text back to him ("Any merge or analog-pool use is a separate future WO gated on Phase 5 validation") to show the isolation was original scope, not introduced this session. Made all 5 open decisions myself (STRICT-only, staging+eval-as-result not mid-build gate, add BULK_SCAN, exact dedup, register bulk_scan_v1) instead of re-asking, per his explicit "you are the expert" precedent from earlier in the session.

**Two real gaps caught and corrected DURING the build, not before:** (1) neural_index was nearly zero-filled alongside triple_cross on a wrong assumption -- Tony asked to be refreshed on whether it was genuinely unavailable, which prompted checking both ingest_manifest.json files directly: neural_index (live) and neural_x_max (bulk) map to the SAME VP export column (NeuralXMax, column 15) under different field names, confirmed via manifest cross-reference, not assumption. Only triple_cross_short/medium/long remain zero-filled (3 fields, not 4) -- that one's a genuine, previously-documented (WO-P300-E2.001 Phase 0) format mismatch. (2) volume_zscore was also wrongly flagged as needing zero-fill -- it's computed by domain/normalization.py from the window's own volume, not sourced from a raw VP column, so it works identically on bulk data with no gap at all.

**NOT YET DONE:** PEH verification. Staged run_this.py + run_this_context.txt (M-070 two-file contract) with 7 checks including the critical one: real live catalog.db mtime proven byte-identical before/after the full build (build_staging_merge only ever writes to a staging copy). M-057 timing risk flagged in the context file -- walk-forward eval runs twice (pre + post) at roughly triple the pattern count of the last timed run, likely exceeds windows-mcp:PowerShell's ~4-min ceiling; Claude Code Desktop recommended if unsure. Real staging build (P_300_MergeResearchCatalog.bat) and promotion (--promote) are both separate, later, deliberate steps -- WO-P300-E2.003 status remains PENDING.

**>>> 2026-07-10 (8th) WO-P300-E2.003 DECISIONS + FILE PLAN LOCKED, BUILD STARTING:** Tony pushed back on the 5-question framing -- pointed out (correctly) that the drift concern was about process, and I confirmed via WO-P300-E2.001's own approved-decisions text that the merge deferral was original 2026-07-08 scope, not something introduced today. Made all 5 decisions myself instead of re-asking: STRICT-tier only, staging-copy+eval reviewed as a result not a mid-build gate, add BULK_SCAN to DataOriginType, exact symbol+anchor_date dedup, register bulk_scan_v1 as its own feature_sets row. 7-file plan (schemas.py, catalog_reader.py, eval_io.py, catalog_merge_io.py NEW, catalog_merge_pipeline.py NEW, cli.py, P_300_MergeResearchCatalog.bat) written to WO-P300-E2.003.md before any file touched (M-078 discipline). Build starting next.

**>>> 2026-07-10 (7th) WO-P300-E2.003 DRAFTED (merge research catalog into live catalog.db) -- PENDING, 5 open decisions:** Tony asked what a merge+go-live takes; investigated the real code before answering rather than assuming a simple copy. Three real gaps confirmed by reading the actual schemas: (1) research catalog's pattern_bars has no normalized _pct columns -- similarity.py's DTW only reads those; (2) bar_offset needs reindexing to the live 0=anchor/negative=setup convention (anchor framing itself is already correct -- bulk_hit_writer.py already slices the window ending at the detection day, this was an overstatement corrected in the WO text); (3) DataOriginType is a hard-typed enum in schemas.py (PATTERN_IDENT/EVAL_SET only) and catalog_reader.py hard-filters on it -- merged BULK_SCAN rows would be silently invisible to Pipeline B without a schema+query change. Tony chose physical merge over federated read. WO-P300-E2.003 drafted with 5 decisions needed before any file plan: tier scope (STRICT-only recommended), recalibration gate (staging-copy walk-forward re-eval before real promotion, mirrors CE_GATE_ENABLED governance), DataOriginType handling, symbol/pattern collision dedup, feature_set_id reconciliation. Does not proceed to file planning until Tony resolves these.

**REAL-DB RUN CONFIRMED: 5/5 PEH PASS (2026-07-10).** Console output (125 updated/1 unchanged, 100 sector_stats rows) independently verified against the actual DB, not trusted at face value: every symbol's sector value matches sector_map.csv exactly (126 symbols checked, not just non-NULL), sector_stats has exactly 100 rows with every below_min_n flag independently recomputed against the live threshold, the pre-promote backup exists, and the report file has exactly 100 data rows. Manually read the report content too -- 10 real GICS sectors + Index/Diversified (SPY) kept separate, LOW-N flags render correctly on every sub-threshold cell, std_return_pct correctly shows -- at n=1 rather than a fabricated number. **WO-P300-E2.002 status: PENDING -> OWNER_DONE.** Closure block appended to the WO doc with the full Acceptance Criteria checklist. Not CLOSED -- normal pattern for this project (OWNER_DONE until Tony formally closes).

**PEH VERIFICATION: 4/4 PASS, exit 0 (2026-07-10).** First run hit a false failure on check 1c (127 vs 126) -- traced to the TEST's own assertion confusing sector_map.csv's total line count (127, incl. header) with its data row count (126 = 125 STOCK + 1 ETF, matching the WO's own scope figures exactly) -- not a loader bug. Fixed the assertion, Tony re-ran clean: all imports clean, SectorMapRow validates real-data shapes (empty source_note, ETF sector=None) and rejects both Decision 2/5 violations, loader parses the real 126-row CSV correctly, and the full end-to-end run against a scratch copy (15555 rows analyzed -> 100 sector_stats cells) matched an independent SQL recount exactly for the top-2 cells plus a clean idempotency re-run. Code is verified correct. **Still not run against the real bulk_research.db** -- that's `P_300_Phase5Analysis.bat` (or `cli.py phase5-analysis` directly), a separate deliberate operator-run step, not yet done.

**>>> 2026-07-10 (6th) WO-P300-E2.002 BUILD COMPLETE (11/11 files), NOT YET PEH-VERIFIED:** Full approved file plan shipped: config.py v1.10->v1.11 (+SECTOR_MIN_N_THRESHOLD=30 etc.), schemas_bulk.py v1.1->v1.2 (SectorMapRow removed -- confirmed unimported via Select-String first), schemas_sector_analysis.py v1.0 then v1.1 NEW (SectorMapRow moved+extended, SectorStatsRecord), domain/sector_stats_calc.py v1.0 NEW, infrastructure/sector_map_loader.py v1.0 NEW, infrastructure/sector_backfill_io.py v1.0 NEW, infrastructure/sector_stats_io.py v1.0 NEW, infrastructure/sector_report_writer.py v1.0 NEW, application/phase5_analysis.py v1.0 NEW, cli.py v1.7->v1.8 (+phase5-analysis subcommand), P_300_Phase5Analysis.bat NEW. Every file's mtime confirmed via windows-mcp:FileSystem mode=info immediately after writing (M-051/M-073 discipline).

**Real-data bug caught mid-build (schemas_sector_analysis.py v1.0->v1.1):** source_note was originally `min_length=1` (required) based on an assumed shape; checking the REAL sector_map.csv before building the loader (file #5) found ~87% of rows have an empty source_note (only populated for GICS mismatches/unusual securities) -- would have rejected nearly every real row on first load. Fixed before file #5 was written on top of the wrong assumption. Also confirmed the CSV encodes ETF sector as the literal string "N/A", not blank -- sector_map_loader.py translates this to sector=None before constructing SectorMapRow.

**Decision 6 resolved same-day:** SECTOR_MIN_N_THRESHOLD -- WO's Acceptance Criteria referenced "the agreed minimum-N threshold" but no number existed anywhere in the WO doc, todo.md, or lessons.md. Claude recommended flat N=30 per sector x tier x horizon cell (standard normal-approximation floor; NOT the ~384 whole-catalog CI figure from WO-P300-E2.001, which would over-flag at this grain). Tony confirmed. Logged as WO-P300-E2.002 Decision 6.

**M-078 logged:** the file plan itself was approved in the prior session but the session ended ("Let's start a new session") before it was written to todo.md -- next session's INIT correctly found no plan on disk and produced a false "you don't have a plan" correction until Tony pasted the original transcript. Plan is now written to disk retroactively (see prior entry in this file) before any file was touched, closing the gap for future sessions.

**NOT YET DONE:** PEH verification. Staged `run_this.py` + `run_this_context.txt` to `Agentic-Hub-Governance\verify\` (two-file contract, M-070) -- runs an import/schema smoke test plus a full end-to-end pipeline run against a SCRATCH COPY of bulk_research.db (never the live file), including independent SQL re-derivation of win_rate/mean_return_pct for 2 sectors per the WO's own Acceptance Criteria, plus a backfill idempotency check. Real bulk_research.db has NOT been backfilled and sector_stats does NOT yet exist in the live file -- that only happens when Tony runs the PEH script (confirms code correctness first) and then, separately and deliberately, `P_300_Phase5Analysis.bat` against the real DB. WO-P300-E2.002 status remains PENDING until PEH passes and the real run is confirmed.

**>>> 2026-07-10 (5th) WO-P300-E2.002 FILE PLAN APPROVED (M-078 -- plan discussed+approved last session but never written to disk; corrected this session):** Delivery order config -> schemas -> domain -> infrastructure -> application -> cli -> .bat. 11 touches (8 new, 3 edits). Not yet started -- file #1 (config.py) next.

| # | File | Action | Est. lines |
|---|------|--------|------------|
| 1 | python\config.py | edit | +6 |
| 2 | python\schemas_bulk.py | edit (remove SectorMapRow, moving it out) | -8 (285->~277) |
| 3 | python\schemas_sector_analysis.py | new -- SectorMapRow (moved + extended with asset_class, source_note) + SectorStatsRecord | ~100 |
| 4 | python\domain\sector_stats_calc.py | new -- pure logic: n/win-rate/mean/std per sector x tier x horizon, ETF bucket handled separately, min-N flagging. No I/O. | ~90 |
| 5 | python\infrastructure\sector_map_loader.py | new -- reads+validates sector_map.csv; fails loudly if any catalog symbol is missing from it | ~70 |
| 6 | python\infrastructure\sector_backfill_io.py | new -- one-time UPDATE symbols SET sector=... for STOCK rows, idempotent | ~80 |
| 7 | python\infrastructure\sector_stats_io.py | new -- sector_stats table DDL (additive) + query + Lock+Temp-DB+Atomic Move write (reuses atomic_move from verify_ingestion.py) | ~130 |
| 8 | python\infrastructure\sector_report_writer.py | new -- formats sector_stats rows into a markdown report, writes to outputs\reports\ | ~70 |
| 9 | python\application\phase5_analysis.py | new -- orchestrates steps 5-8 | ~120 |
| 10 | python\cli.py | edit -- add --phase5-analysis | +18 |
| 11 | P_300_Phase5Analysis.bat | new -- operator launcher, goto-label pattern | ~50 |

sector_stats design: single current snapshot (DELETE+INSERT each run, computed_at timestamp), not an ever-growing history table -- re-runnable, DB backups cover historical snapshots. research_catalog_io.py already at 468 lines (pre-existing M-031 debt, not touched further). SectorMapRow already existed in schemas_bulk.py (symbol/sector only, never imported anywhere) -- safe to move+extend rather than patch in place.

**>>> 2026-07-10 (4th) M-059 CODE FIX SHIPPED -- ledger_db.py, 187->241 lines:** insert_fired_signal() no longer bare-inserts. Added UNIQUE index on (ticker, signal_date, signal_class, chosen_horizon) in _init_schema() + ON CONFLICT DO NOTHING in the insert, returning the existing ledger_id on conflict (logged at INFO) instead of writing a duplicate row. Pre-flight confirmed 0 duplicate groups across the live 230-row buy_ledger.db before adding the index -- no migration needed. Functional smoke test (isolated tempfile.mkdtemp() DB, never touched production): exact duplicate absorbed into the same ledger_id with row count flat; different chosen_horizon or different signal_class correctly still insert as new rows. PASS on first run. M-059 now closed at the code level, not just the 2026-06-29 one-time data cleanup -- future batch re-runs can no longer duplicate the ledger. Also checked M-046 (lambda tuning) readiness while here: h20_return_pct filled = 2/230 rows (up from 0 on 06-29, oldest signal just crossed +20 trading days) -- nowhere near sufficient, still blocked, expect slow one-row-at-a-time growth as the ledger matures.

**>>> 2026-07-10 (3rd) WO-P300-E2.001 FINANCIAL + SERVICES + TECHNOLOGY (45 files), DB-VERIFIED, 126 files total now:** 3 more sectors licensed by Tony. 15/sector judged sufficient (prior sectors landed ~350-400 RELAXED each, past the ~384 n needed for +/-5% margin at 95% CI). Financial (15): JPM/BAC/WFC/C/GS/MS/SCHW/BLK/AXP/MET/PGR/TRV/ICE/CME/SPGI. Services (15, revised): EXPD/CHRW not in VantagePoint, substituted GWW/PWR -- ADP/PAYX/CTAS/ROL/RSG/CPRT/VRSK/MCO/FDS/GWW/PWR/ROP/IT/EFX/BR. Technology (15): MSFT/GOOGL/META/NVDA/AVGO/ORCL/CRM/ADBE/CSCO/IBM/INTC/AMD/TXN/QCOM/NOW. Pre-run QA caught 4 problems before running (checked data\bulk\ directly, not just Tony's "ALL EXPORTED" claim): CME missing, AMD missing, ICE was a bad 32.5KB partial export, CTAS misnamed "History Grid (CTAS).xlsx" -- all 4 fixed and reverified before go-ahead. Run: all 45 parsed clean, 0 errors, 19 expected hollow-skips (insufficient forward data, correctly not written). Console: STRICT=495/RELAXED=2616 (3111 total), 126 symbols/source_files. DB-verified via PEH, PASS on first try: exact match to console, pattern_bars=62220 (3111x20), forward_labels=15555 (3111x5), 0 hollow, per-file sum for the 45 new files = 1090 = 3111-2021 exactly. WO-P300-E2.001 updated with full detail. Catalog now covers 7 full sectors + IntelliScan mixed batch + Energy (14/15 -- WMB confirmed NOT AVAILABLE in VantagePoint, not a pending re-export, Energy sector considered complete at 14), 126 files total. Still OWNER_DONE, not CLOSED.

**>>> 2026-07-10 (2nd) WO-P300-E2.001 ENERGY SECTOR (14/15) + INTELLISCAN 7-FILE RUN -- both DB-VERIFIED, 81 files total now:** Two bulk-extract runs today. (1) IntelliScan overnight-run 7-file mixed batch (BURL/GNL/TECK/UI/UNFI/WYNN/XPO, sector=None): STRICT=263/RELAXED=1382 (1645 total), DB-verified exact match, 67 symbols/source_files, 0 hollow. (2) Energy sector, 14 of 15 exported (WMB not found/not exported; HESM exported in place of HES, confirmed intentional by Tony -- different security): COP/CVX/DVN/EOG/FANG/HAL/HESM/KMI/MPC/OKE/PSX/SLB/VLO/XOM. Checkpoint correctly skipped the 7 already-done files. One hollow-skip logged (insufficient forward data, anchor too close to data edge) -- expected, not written to catalog. Console: STRICT=317/RELAXED=1704 (2021 total), 81 symbols/source_files. DB-verified via PEH (Tony ran directly): exact match to console, pattern_bars=40420 (2021x20), forward_labels=10105 (2021x5), 0 hollow, per-symbol sum for the 14 new files = 376 = 2021-1645 exactly. WO-P300-E2.001 updated with full detail. Process note: Claude's first PEH staging attempt this session used create_file (sandbox-local, never reached the real machine) -- re-staged via windows-mcp:FileSystem before either check ran for real; M-073 recurrence logged in lessons.md. Catalog now covers 4 full sectors + IntelliScan mixed batch + Energy (14/15), 81 files total. Still OWNER_DONE, not CLOSED.

**>>> 2026-07-09 (7th) WO-P300-E2.001 CONSUMER NON-CYCLICAL SECTOR -- 15 symbols, DB-VERIFIED (cross-checked via Claude Code), 60 files total now:** `P_300_BulkExtract.bat` run in two passes: first 11 symbols (CLX/COST/GIS/KHC/KMB/KR/MDLZ/MO/PM/STZ/WMT), then a makeup pass for 4 initially-missed symbols (CL/KO/PEP/PG). Checkpoint correctly handled both partial runs. All 15 parsed clean across both passes, 0 errors. Console (final): STRICT=230/RELAXED=1246 (1476 total). DB-verified via full per-symbol/tier breakdown (60 symbols) -- confirmed independently via Claude Code as a second tool session, exact match: 1476 instances, 29520 bars, 7380 labels, 0 hollow. WO-P300-E2.001 updated. Catalog now covers 4 full sectors (Basic Materials, Capital Goods, Consumer Cyclical, Consumer Non-Cyclical) + the original 3-symbol test set, 60 files total. Session closed -- fresh DB backup taken.

**>>> 2026-07-09 (6th) WO-P300-E2.001 CONSUMER CYCLICAL SECTOR -- 15 symbols, DB-VERIFIED, 45 files total now:** `P_300_BulkExtract.bat` run against 15 new Consumer Cyclical symbols (AMZN/BKNG/DHI/F/GM/HD/LEN/LOW/MAR/MCD/NKE/RCL/SBUX/TJX/YUM) -- checkpoint correctly skipped the 30 already-complete files. All 15 parsed clean, 0 errors. Console: STRICT=168/RELAXED=953 (1121 total). DB-verified via full per-symbol/tier breakdown (45 symbols) -- summed programmatically, exactly 1121, matches console precisely. 22420 bars, 5605 labels, 0 hollow. WO-P300-E2.001 updated. Catalog now covers 3 full sectors (Basic Materials, Capital Goods, Consumer Cyclical) + the original 3-symbol test set, 45 files total. DB backup taken before this run (models/research/backups/bulk_research_20260709.db, pre-Consumer-Cyclical snapshot).

**>>> 2026-07-09 (5th) WO-P300-E2.001 CAPITAL GOODS SECTOR -- 15 symbols, DB-VERIFIED, 30 files total now:** `P_300_BulkExtract.bat` run against 15 new Capital Goods symbols (AME/BA/CAT/ETN/FAST/GD/IR/ITW/LMT/NOC/PCAR/PH/PNR/WM/XYL) -- checkpoint correctly skipped the 15 already-complete files. All 15 parsed clean, 0 errors. Console: STRICT=104/RELAXED=621 (725 total). DB-verified via full per-symbol/tier breakdown (30 rows) -- summed by hand, exactly 725 instances / 104 STRICT / 621 RELAXED, 14500 bars, 3625 labels, 0 hollow, matches console precisely. One self-correction during verification: flagged the log's last checkout (724) as a possible 1-off bug before checking -- resolved as a misread on my part (checkout lines are pre-write snapshots, not post-write; 724 was correct pre-final-write state, real total was 725 all along). WO-P300-E2.001 updated. Catalog now covers 2 full sectors (Basic Materials + Capital Goods) + the original 3-symbol test set, 30 files total.

**>>> 2026-07-09 (4th) WO-P300-E2.001 M-077 FIX SHIPPED + LIN RE-RUN -- all 15 Basic Materials + test symbols now in catalog:** `bulk_grid_reader.py` v1.1 -> v1.2, ROC% blank-string ('') now maps to 0.0 (pre-backfill zero convention), same family as M-072's infinity fix. Verified in two PEH steps: (1) parse-only, no writes -- LIN parsed clean, bar_count=1929 not 2512 (explained: LIN/Linde plc only listed since 2018-10-31 merger, correctly shorter real history, not a bug); (2) real `P_300_BulkExtract.bat` re-run -- checkpoint correctly processed only LIN, 2 STRICT/19 RELAXED, console totals STRICT=56/RELAXED=299 across all 15. DB-verified (3rd PEH pass): pattern_instances=355 (56/299), pattern_bars=7100, forward_labels=1775, symbols=15, source_files=15, hollow_count=0 -- exact console match. One correction: my own PEH context file predicted pattern_bars=6940, a plain arithmetic slip (355x20=7100, not 6940) -- caught because the operator pasted the real output instead of accepting the prediction; the actual DB number was correct the whole time. WO-P300-E2.001 updated with full detail. Still OWNER_DONE -- Basic Materials sector now fully populated (15 files) as a scale proof point; eventual full scope is ~250-300 files across multiple sectors.

**>>> 2026-07-09 (3rd) WO-P300-E2.001 SECOND REAL RUN -- 12 Basic Materials symbols, 11/12 clean, LIN failed (real code gap, not bad data):** `P_300_BulkExtract.bat` re-run against 12 new symbols (LIN/APD/DOW/SHW/ALB/FCX/SCCO/NEM/NUE/STLD/CF/MOS) + the 3 already-complete files (checkpoint correctly skipped AAPL/DE/SPY). 11/12 succeeded; LIN failed cleanly ("Cannot coerce '' to float at row 1853 col 13", roc_pct column) -- traced to the pre-2021-07-14 VP predictive-column blank window (same root cause family as M-072's '∞' fix, different blank spelling this coercer doesn't yet handle). Catalog untouched for LIN; not marked complete in checkpoint, will auto-retry once fixed. DB-verified via PEH (same run_this.py, context updated): pattern_instances=334 (54 STRICT/280 RELAXED), pattern_bars=6680, forward_labels=1670, symbols=14, source_files=14, hollow_count=0 -- exact match to console, internally self-consistent. Fix proposed (extend `_coerce_cell`'s float branch to map `''` -> 0.0 alongside the existing `'∞'`/`'inf'` handling) but NOT yet built -- operator held off until all 14 successful symbols were independently confirmed. WO-P300-E2.001 updated with full detail.

**>>> 2026-07-09 (2nd) WO-P300-E2.001 FIRST REAL PRODUCTION RUN -- DB-VERIFIED:** `P_300_BulkExtract.bat` run against the 3 real files in `data/bulk/` (AAPL/DE/SPY, 10-yr/2,512 bars each). Console: 3/3 completed, 0 errors, 5 STRICT / 73 RELAXED. Verified directly against `bulk_research.db` via PEH, not just the console log (M-054) -- caught and fixed a real bug in the verification script itself first (queried `s.symbol`, actual column is `ticker`), then confirmed: AAPL 1/23, DE 3/25, SPY 1/25 (78 total), pattern_bars=1560 (78x20), forward_labels=390 (78x5), symbols=3, source_files=3, hollow_count=0 -- internally consistent and matches console exactly. Per-symbol counts run lower than the WO's original Phase-0 `.isc`-decode estimate (SPY 5/39 etc.) -- expected, that estimate predates the real detector build; flagged in the WO for the record, not a failure. WO-P300-E2.001 closure block updated with full detail. Still OWNER_DONE, not CLOSED -- 3 files is the first real batch, not the eventual ~12-15 symbols/sector (~250-300 files) target scope.

**>>> 2026-07-09 WO-P300-E2.001 STATUS HEADER CORRECTED (PENDING -> OWNER_DONE):** WO file's own header still read PENDING despite its 2026-07-08 closure note documenting the build-complete/17-17-PEH-PASS result below it -- a governance-ledger inconsistency, no code/build change. Corrected to OWNER_DONE, closure block appended matching the E1.004 format (build summary, M-074/M-075 recap, held-not-CLOSED pending real-corpus run). Session otherwise closed with no further action -- only remaining item is the operator populating `data/bulk/` for the first real `P_300_BulkExtract.bat` run (Backlog, next session).

**>>> 2026-07-08 BULK PATTERN EXTRACTION -- WO-P300-E2.001 BUILD COMPLETE (all planned files shipped, PEH-verified end-to-end against a real export):** Continuation of the same-day earlier build (schema + domain + infrastructure layers). This stretch: `infrastructure/research_catalog_io.py` NEW (bootstrap DDL + row I/O + Lock+Temp-DB+Atomic Move promote, 16/16 PEH PASS against a synthetic round-trip + hollow-rejection negative test), `domain/bulk_labeler.py` NEW (forward-label math for BulkBarRaw, separate from labeler.py per the same rationale as bulk_pattern_detector.py), `application/bulk_hit_writer.py` NEW (single-detection persistence, split out of bulk_extract_pipeline.py v1.0 which landed at 309 lines -- over the 300 hard limit), `application/bulk_extract_pipeline.py` v1.2 (orchestration-only after the split; checkpoint_path parameter added), `cli.py` v1.7 (+bulk-extract subcommand), `P_300_BulkExtract.bat` NEW (operator launcher, mirrors P_300_Preflight.bat's simple pattern).

**Real end-to-end PEH run (Tony's request) against `data/processed/5_Pattern_SPY.xlsx` (5-yr SPY, real file, not synthetic) found and fixed two real bugs, both logged:**
- **M-074:** `bulk_hit_writer.py` v1.0 hardcoded `expected_delta["symbols"]=0` / `["source_files"]=0` -- wrong on every first-hit-per-symbol/file (the common case for a scanning pipeline), causing `verify_and_promote_research` to reject every single hit. Fixed by checking row existence before insert and setting the delta from that. v1.1.
- **M-075:** `run_bulk_extraction()` had no `checkpoint_path` override -- always wrote the LIVE `bulk_extract_checkpoint.json` even when `master_db`/`temp_db` were scratch paths, so the (pre-M-074-fix) test run's 0-detection failure got recorded as "file completed" in the real checkpoint, silently skipping the file on every subsequent test run with zero log output. Fixed by adding a required-effectively `checkpoint_path` parameter, threaded through. v1.2. Confirmed no live-checkpoint cleanup was needed -- the file doesn't exist on disk (no real bulk-extract run has ever happened).

**Final PEH result: 17/17 real PASS**, including a resume-run check (second `run_bulk_extraction()` call against the same scratch paths adds zero new detections, pattern_instances count unchanged). 26 real pattern_instances landed against real SPY data (1 STRICT, 25 RELAXED), zero hollow, SPY symbol row confirmed. All scratch artifacts cleaned up after the run; real `data/processed/5_Pattern_SPY.xlsx` untouched.

**Still not yet run against the operator's real intended corpus** -- `data/bulk/` and `data/reference/sector_map.csv` don't exist as real project locations yet; this whole build has been verified via scratch/PEH paths only. First real `P_300_BulkExtract.bat` run (once operator populates `data/bulk/`) is the next natural test, separate session. M-070 (PEH two-file contract), M-071 (PEH sys.path), M-072 (infinity value), M-073 (create_file silent-fail -- verify writes with a direct read), M-074, M-075 all logged to lessons.md this session.

**>>> 2026-07-07 Chaikin batch M-069 fix CONFIRMED live -- P_300_RunChaikinBatch.ps1 standalone re-run, 13/15 succeeded:** Verified against vault directly (M-054), not the console claim -- spot-checked ACHR (Chaikin section present, Neutral rating, financials/earnings/technicals/experts sub-sections all populated correctly) and BITX (correctly untouched, no Chaikin section -- ETF, Power Gauge doesn't rate it). Full result: ACHR/BAM/BLK/CRM/DD/FUTU/GSL/JD/MS/NEXA/PLCE/SBSW/SEDG appended (13); BITX/CRPT failed cleanly with no partial writes (both ETFs, Power Gauge page error not a login wall). M-069 fix holds in production. Next live DailyEval run should be watched to confirm P_300_RunAllDailyEvals.ps1's inline fix (not the standalone script) also fires correctly end-to-end.
**>>> 2026-07-07 Chaikin batch trigger fixed (M-069) -- was silently skipping every run:** DailyEval batch (19 symbols, 8 BUY + 7 WATCH) correctly evaluated and vault-logged, but the Chaikin Power Gauge post-eval step printed "No BUY/WATCH symbols today -- skipping Chaikin batch" despite 15 actionable signals. Root cause: `P_300_RunAllDailyEvals.ps1` filtered vault notes by today's run date, but notes are filed under anchor_date (last close, one session behind). Fixed by parsing BUY/WATCH tickers directly from the run's own log instead of the vault filename. Not yet re-run live to confirm the Chaikin batch actually fires next time -- next DailyEval run is the real test.

**Also this session -- lessons.md corrupted and recovered:** a broken PowerShell edit truncated tasks/lessons.md to 3 bytes. Recovered via `git checkout` to the last commit (6a20937, 2026-06-30), then M-067/M-068 (existed pre-corruption, recovered from past-chat search) and M-069 + the M-032 addendum (both from this session) were manually restored via a single full-file write. Verified post-write: M-066 through M-069 all present, file size matches. No content lost. Root cause of the corruption itself was an unguarded `-1` string index in a PowerShell insert script -- avoid scripted mid-file inserts on this file going forward; prefer full-file read-and-rewrite for lessons.md specifically given its size and how costly a bad index is here.
**>>> 2026-07-07 P_300_AddPattern.bat DAY-ROLLOVER COPY BUG FOUND AND FIXED (M-032 recurrence):** Tony asked why the catalog was still named `062326catalog.db` on a new day instead of rolling to `070726catalog.db`. Traced: the 2026-06-23 rebuild's day-rollover copy block set `NEWCATALOG` and referenced `%NEWCATALOG%` inside the same parenthesized `if/else (...)` block -- percent-expansion-at-parse-time bug, silently evaluated empty on every run since the rebuild, "copying to" printed blank, `copy /y` target was an empty filename and failed with no errorlevel check. No data damage -- ingest still wrote correctly to the sole existing catalog -- but the daily-snapshot naming convention never actually fired. Fixed via the goto-label pattern (restructured `if/else` into `if ... goto :label` + top-level statements, no delayed expansion needed) -- M-032 already prescribed this pattern; the 06-23 rebuild just didn't follow it correctly. M-032 lessons.md entry updated with the recurrence + generalized rule (don't set-and-use a `%VAR%` inside the same `(...)` block, period -- not just the `!VAR!` case). Not yet re-run against a live day-boundary to confirm the fix in practice (next natural test: next session that crosses midnight with an AddPattern run).
**>>> 2026-07-07 (2nd) ADDPATTERN RETRY BATCH -- 4/5 ingested, 1 rejected (correcting operator claim of "all 5 processed"):** Retry of the 5 files rejected in the 07-07 (1st) batch, re-exported with wider setup windows / corrected date ranges. Verified against catalog + log directly (M-054) -- console said all 5 processed, log shows one ERROR. Catalog: 482->486 patterns / symbols steady at 297 / 9720 pattern_bars / 2430 forward_labels, 062326catalog.db, 0 hollow, OVERALL HEALTHY throughout. CRM (id=483), DD (id=484), MGA (id=485), BITO 2026-04-02 (id=486) all ingested clean and archived. 1 rejection:
- `Pattern_20260116_20260205_BITO.xlsx` -- `Sheet 'BITO' not found in workbook. Available sheets: ['BOIL']`. Same failure family as the QMX/NDAQ and GIB/CIGI mislabeled-sheet cases -- VP export tagged BITO in the filename but the actual sheet inside is BOIL. File remains in `data\historical_patterns\`, will block every future AddPattern run until re-exported with the correct sheet.
**>>> 2026-07-07 ADDPATTERN BATCH -- 35/40 ingested, 5 rejected (correcting operator claim of "all 40 processed"):** Verified against catalog directly (M-054) -- log claimed all 40 processed but contains 5 `[ERROR] add-pattern failed -- catalog untouched` tracebacks. Catalog: 447->482 patterns / 281->297 symbols / 9640 pattern_bars / 2410 forward_labels, 062326catalog.db, 0 hollow, OVERALL HEALTHY throughout. 5 rejections, all files still sitting in `data\historical_patterns\`:
- `Pattern_20260116_2026021_BITO.xlsx` -- filename regex mismatch, date segment truncated (`2026021` not `202602XX`) -- needs rename/re-export with correct 8-digit date.
- `Pattern_20260403_20260423_BITO.xlsx` -- `launch_date 2026-04-03 not found in bars (range 2026-01-07 to 2026-07-06)` -- second BITO file, different failure mode (stale/wrong-range export).
- `Pattern_20260127_20260220_CRM.xlsx` -- insufficient setup history, need 19 bars before launch_idx=15, have 15.
- `Pattern_20260130_20260212_DD.xlsx` -- insufficient setup history, need 19 bars before launch_idx=16, have 16.
- `Pattern_20260202_20260213_MGA.xlsx` -- insufficient setup history, need 19 bars before launch_idx=17, have 17.

**Open follow-up:** all 5 files block nothing else (per-file rejection, catalog untouched on each) but will keep failing identically until re-exported with a wider setup window (CRM/DD/MGA, off by 2-4 bars each) or corrected filename/date range (BITO x2).

**File:** `tasks/todo.md`
**Status:** Active roadmap
**Last Updated:** 2026-07-13 (WO-P300-E3.002 pattern_miner.py v2.0/v2.1 -- crossover-gated architecture replacing outcome-only mining; real validation 60/84=71.4%, 0 real misses, 0 regressions; M-085 found at session-close spot-check -- OUTCOME-INVALID bucket mostly a search-radius artifact, true rate likely ~78-80/84; NOT cleared for Phase 1 pipeline build yet.) | 2026-07-12 (WO-P300-E3.001 Scanner Funnel Loop built + PEH-verified 7/7, status OWNER_DONE -- 5 files, data-source + archiving decisions resolved same session, M-080 added. Real-corpus nightly run still owed as a separate step.) | 2026-07-09 (WO-P300-E2.001 status header corrected PENDING -> OWNER_DONE, governance-only) | 2026-07-08 (WO-P300-E2.001 Bulk Pattern Extraction BUILD COMPLETE -- research_catalog_io.py, bulk_labeler.py, bulk_hit_writer.py, bulk_extract_pipeline.py v1.2, cli.py v1.7 +bulk-extract, P_300_BulkExtract.bat. 17/17 real PEH PASS against 5_Pattern_SPY.xlsx. M-074 + M-075 added -- see Current State.) | 2026-07-04 (WO-P300-E1.004 OWNER_DONE -- signal_emitter.py v2.3 + daily_evaluate_pipeline.py v1.21: [OK]/[REJECTED] print visibility fix for --clean's logging.disable(logging.WARNING), plus mean_ret *100 scaling fix in the SIGNAL_V2 rationale. PEH-verified 3/3 PASS. M-068 added. Pending: next live batch confirms the *100 fix in a real packet.) | 2026-07-04 (AddPattern batch: 10/12 ingested clean, 2 rejected -- both `Pattern_*_QMX.xlsx` files fail with sheet-name mismatch, workbook contains sheet 'NDAQ' not 'QMX' (same failure shape as the 2026-06-30 GIB/CIGI case). Catalog untouched on both per design. Catalog: 436->446 patterns / 277->281 symbols / 8920 bars / 2230 forward labels, 0 hollow, HEALTHY. Both QMX files still in `data\historical_patterns\`, will block future batches until operator re-exports or renames -- see Current State.) | 2026-07-03 (Ledger recalibration re-run verified as a same-day no-op, no regression: `ledger-fill` + `ledger-calibration` re-run at 10:00 produced calibration numbers byte-identical to the pre-run baseline (h5 conf=0.60 n=109, h10 conf=0.37 n=4, h15 conf=0.74 n=2, overall 0.59). Given the M-060/061/062/063/064 history, did not trust the clean log -- queried `buy_ledger.db` directly per M-054. Confirmed the fill DID write real values (e.g. KMI ledger_id=47 h15=0.0069), but those values were already persisted by an earlier same-day run (COHR filled_date=20260702, most others already 20260703 before this run) -- no new trading day had elapsed, so the re-run was a harmless COALESCE-safe rewrite of already-correct data, not a silent-discard bug. No code changes. See Current State.) | 2026-06-29 (Ledger dedup 175->142 rows + ledger-fill's first real output ever -- 4 stacked bugs (M-060/061/062/063/064) found and fixed in sequence; h5=115/h7=97/h10=58/h15=2/h20=0 real fills, verified via before/after counts. See Current State.) | 2026-06-28 (AddPattern + DailyEval batches run live under config v1.8 (BUY_MIN_Z_SCORE=1.0): catalog 331->362 patterns / 231->245 symbols; 9 BUY/12 WATCH/1 PASS across 22 live symbols, 0 errors. M-058 added -- see Current State.) | 2026-06-28 (Stage 6 eval loop SHIPPED -- 5 files, walk-forward run on 331-pattern catalog 062326catalog.db at z>0.0 vs z>1.0; BUY_MIN_Z_SCORE re-tightened 0.0->1.0 in config.py v1.8, closing M-034 re-eval trigger. M-057 added.) | 2026-06-17 (M-051 REAL fix shipped: report_writer.py v1.8 + daily_evaluate_pipeline.py v1.20. CORRECTS the entry below dated 2026-06-12 -- that closure was never verified against the file; report_writer.py was still v1.7 with the bug intact through today. See M-054.) | 2026-06-16 (WO-P300-E1.001 IntelliScan stop integration SHIPPED: intelliscan_reader.py v1.0 NEW + signal_schemas.py v2.1 + signal_emitter.py v2.1 + daily_evaluate_pipeline.py v1.18. Smoke PASS 12 symbols. WO-P115-E2.001 OPEN -- same pattern for P_115.) | 2026-06-12 (print_signal_report_clean() hardcoded status strings fixed via Claude Code -- M-051 closure. P_800 Hub interface (signal_emitter -> write_to_vault SIGNAL_V2) confirmed working: 2026-06-12_COF.md written to vault automatically on BUY/WATCH. WO-P300-E1.001 BACKLOG created (resistance lookup to replace VP predicted high as target formula). system-doc-initializer SKILL compressed v3.0 + M-051 added globally (Protocol D Loop F). CLAUDE.md updated: WO table corrected, schemas_signal_packet.py flagged vestigial, last-updated bumped.) | 2026-06-11 (ATR operator runtime check DONE: CGBD BUY h=5 -- signal class unchanged, guideline stop/target + atm_at_signal present and non-zero, pipeline clean exit. 061026catalog.db detected at INIT -- count unverified due to PowerShell MCP timeout; health check deferred to next session.) | 2026-06-11 (Enhancement 2 gate-on prerequisites COMPLETE: report_writer smoke PASS (all 3 scenarios) + NFR-1 determinism replay PASS (CGBD BUY h=5, all 5 horizons, signal_class/chosen_horizon/per-horizon stats identical across 2 runs, certainty_equivalent included). ledger_record.py M-019 bug fixed (Unicode arrow -> ASCII). Junk replay ledger entries (ids 41-42) deleted; fired_signals back to 40 rows. One remaining gate-on prerequisite: lambda tuning against ledger.) | 2026-06-09 (INIT reconciliation: live catalog 060826catalog.db = N=186 / 155 symbols / 0 hollow / OVERALL HEALTHY, mtime 2026-06-08 10:43:43. Corrects three stale tracking claims -- see reconciliation entry below. | Decision-flag surfacing shipped: SIP v3.0->v3.1 + SKILL aligned; M-048 added.) | 2026-06-09 (Enhancement 2 shipped: Certainty-Equivalent BUY gate. CARA exponential utility (Kochenderger Ch. 6) scores top-K analog forward returns -> risk-adjusted CE return per horizon; gates BUY when ce >= CE_MIN_THRESHOLD AND CE_GATE_ENABLED. Shipped OFF (observe-only, NARRATOR_ENABLED precedent) -- live signals byte-identical until lambda tuned + flag flipped. 6 files: config v1.7, schemas_pipeline_b v1.3, domain/utility.py v1.0 NEW, aggregator v1.1, signal_classifier v1.1, report_writer v1.7. M-046 + M-047 added. utility.py smoke PASS verified; gate-ON path + e2e determinism replay still OWED before flip.) | 2026-06-08 (Enhancement 1 shipped: P_300 -> P_400 SIGNAL_V2 signal packet via P_800 Hub interface. signal_emitter v2.0 + daily_evaluate_pipeline v1.15 + architecture v2.7 Enhancement Log. COHR live BUY validated -> packet written. M-045 added.) | 2026-06-03 (INIT reconciliation: catalog 060326catalog.db = N=156 / 136 symbols / 0 hollow / OVERALL HEALTHY; +17 patterns / +15 symbols across 3 untracked sessions (060126/060226/060326). 3 enhancements logged to Backlog: batch ingester [plan approved, build deferred], P_400 order-file integration [flagged NEXT], BUY-precision + social-sentiment layer.)
**Maintained By:** Anthony Zoppi + Claude (architect)

---



---

**Second archive pass: 2026-07-23.** Archived range: 2026-07-18 through
2026-07-19 (10th session that day). Live todo.md had grown back over
its ~100KB cap; this pass moved everything older than 2026-07-20 out
of the top dated-session-log portion. Full text preserved exactly as
it appeared live, nothing deleted. Done via `windows-mcp:FileSystem`
full-file rewrite (the `filesystem` MCP server's `edit_file` tool was
down for the entire session this pass was done in).

---

**>>> 2026-07-19 (10th, Sonnet) WO-P300-E4.006 filed -- lever #3 design decided, not built:** "replace single source of truth" -- both open questions from the lever #3 discussion answered: (1) top-K cache replaces `run_incremental_post_batch`'s current incremental path outright, not a third option alongside it (justified: the existing incremental path has never once fired on a real batch -- both real batches hit ~0% reuse and fell through to full rescore); (2) storage is a new table inside catalog.db itself, not a separate cache file -- single source of truth, no second fingerprint/invalidation scheme needed.

Filed as its own WO (E4.006, not folded into E4.005 which is now functionally closed end-to-end) with the real economics stated plainly: at today's scale the top-K path is ~21 min, not under the 15-min target by itself -- the real justification is 1700-symbol scale, where a full rescore becomes ~17-18 HOURS and this lever is the difference between a batch being a per-batch operation at all or not.

Four design questions genuinely left open (schema shape, K=20 vs headroom, migration mechanics against a live catalog, whether CATALOG_TABLES needs the new table added -- which ties directly into WO-P300-E5.002's fold-in-progress, flagged so the two aren't planned in isolation from each other). Sequencing keeps the Finding-3 tie census and the cache's initial population as ONE combined ~63-min run rather than two, since both need the same full O(N^2) pass.

**Not planned in file-level detail, not built.** Plan-gate still applies -- exact line counts and function signatures are next session's work, after the four open items above get resolved.

---
**>>> 2026-07-19 (9th, Sonnet) PROMOTE COMPLETE -- live catalog is now 10,761 patterns / 328 symbols. Filed WO-P300-E5.002 for the real fix:** Tony ran the verify-then-promote script. VERIFY PASS (exact deltas, zero hollow records), promote ran. Independently re-checked before logging (M-054): pattern_instances +1792, pattern_bars +35840, forward_labels +8960 -- all three exact matches, not approximate. Live catalog file 49.5MB->59.3MB; backup at 071726catalog.db.bak is 49.5MB, matches the pre-promote size exactly. Post-promote content was re-verified via a fresh SQL query INSIDE the script itself (10,761 patterns confirmed on disk after the move) -- stronger evidence than a file-size check alone, and I don't just trust the script's own printout regardless (checked the .bak/live file sizes on disk myself too, matched).

**Filed WO-P300-E5.002** for the real fix to the gap Tony's question surfaced: fold `promote_staging_to_live()` through the same `verify_and_promote()` the single-pattern path gets, instead of the current informational-only checkout/checkin. Full WO written with the actual scope questions still open (does MergeResult/IngestMinedResult need a new summary field to know its own expected_delta, or does a shared lower-level helper get extracted) -- not decided, not built, filed so it surfaces at every future INIT rather than living only in this chat.

**Live catalog state:** 10,761 patterns / 328 symbols (was 8,969/323). BUY precision 69.04%, PASS accuracy 60.13%. Preflight status JSON (P_300_preflight_status.json) is stale again as of this promote -- flagging for next INIT's Step 5b, re-run P_300_Preflight.bat before trusting it.

**This closes out today's WO-P300-E4.005 thread end to end:** JIT (Phase 2a) -> real speedup measurement (Phase 2b, ~11.5x not the estimated 50-200x) -> lever #2 re-eval (Phase 2c, parked, config.py fixed to match) -> M-079 gate (both metrics improved) -> promote (done). Lever #3 (top-K state cache) is the only remaining throughput lever; WO-P300-E4.001/E4.002 (cli.py refactor, typed sibling) and the newly-filed E5.002 are the open threads for next session.

---
**>>> 2026-07-19 (8th, Sonnet) Real architecture gap found (Tony's question), closed for THIS promote, filed as a backlog item for real:** Tony asked why the bulk-merge promote path and the single-pattern AddPattern path don't share the same verification logic. Checked, not assumed: they genuinely don't. `verify_temp_db()` (infrastructure/verify_ingestion.py, used by every single-pattern write via `verify_and_promote()`) enforces a per-table expected-delta check AND a hollow-record scan (patterns missing bars/labels -- the EC-027/EC-057 tripwire). `promote_staging_to_live()` (application/catalog_merge_pipeline.py) does neither -- just checkout -> atomic_move -> checkin, informational logging only, no enforcement. Nothing in the code or changelog history suggests this was deliberate; looks like catalog_merge_pipeline.py (built 2026-07-10 for WO-P300-E2.003) reused only `atomic_move` from verify_ingestion.py and reimplemented a lighter pre/post snapshot instead of computing a real expected_delta and calling the existing `verify_and_promote()` wholesale -- the M-082 pattern (duplicate logic diverging across rewrites), just at the write-protocol level instead of the DTW-math level. **Real fix (folding bulk promote through verify_and_promote(), computing expected_delta from the merge/ingest summary) is 2+ files and needs its own plan -- not built now, filing as a backlog item next session, not silently dropped.**

**Closed the gap for THIS promote specifically, not just talked about it:** built a verify-then-promote script that computes a REAL expected_delta from actual config constants (1792 new patterns x BULK_WINDOW_LENGTH=20 x FORWARD_HORIZONS=5 -> pattern_instances+1792/pattern_bars+35840/forward_labels+8960, not guessed), calls the real `verify_temp_db()` (M-082) against staging_ingest_mined.db, and only calls the real `promote_staging_to_live()` if that passes exactly -- refuses and leaves live untouched otherwise. Both files written via windows-mcp:FileSystem, re-read after writing (120 lines, clean EOF -- M-051/M-054). Handed off, expected well under a minute (pure SQL + file rename, not a rescore).

**This is the actual live-catalog promote -- if it completes, staging_ingest_mined.db becomes the new live catalog (10761 patterns, 323->? symbols, replacing 071726catalog.db which gets preserved as .bak).**

---
**>>> 2026-07-19 (7th, Sonnet) M-079 WALK-FORWARD GATE COMPLETE -- PASS, promote recommended, one bug found and fixed:** Tony ran the staged gate script (~63.3 min). Verified independently before logging (M-054): bucket sums exact (8969/10761/1792, no arithmetic drift). Timing cross-check is the interesting part -- 63.3 min actual vs 63.1 min predicted for a SERIAL full rescore at N=10761 (0.3% off) -- confirms EVAL_PARALLEL_ENABLED=False was actually honored by the real production call path, independent of what the log said.

**Gate result: BOTH metrics improved.** BUY precision 68.49%->69.04% (+0.55pts), PASS accuracy 58.92%->60.13% (+1.20pts). Cross-session sanity check: today's PRE numbers match the 2026-07-17 entry's POST numbers exactly (expected -- same cached batch, live catalog unchanged since) -- pipeline chaining correctly, not drifting.

**Found a real bug the pasted summary didn't surface:** the reuse-fraction-skip log message hardcoded "full parallel re-score" regardless of the actual EVAL_PARALLEL_ENABLED value -- harmless until yesterday's Phase 2c flip, then actively misleading (said parallel, ran serial -- the timing check above is what caught it, not the log text). Fixed: application/incremental_post_batch.py v1.2->v1.3, now reports the real flag value. Single hardcoded string, one file, no plan-gate needed -- version-bumped and changelogged, re-read after writing to confirm (M-051).

**Promote recommended, NOT actioned.** Both gate metrics improved, same bar every prior real batch was held to -- but promoting to live is a separate, explicit, consequential call (`promote_staging_to_live()`, changes Pipeline B's live signal path) that needs Tony's go-ahead, not something to run on "finish it"-level authorization. All results logged to WO-P300-E4.005.md (new M-079 GATE section + status header), both files re-read after writing to confirm (M-051).

**Waiting on Tony: promote staging_ingest_mined.db to live, or hold?**

---
**>>> 2026-07-19 (6th, Sonnet) config.py v1.20 -- EVAL_PARALLEL_ENABLED flipped True->False, AND M-079 walk-forward gate staged for the current batch:** "finish it" -- two pieces:

**1) Real, evidenced production fix:** flipped EVAL_PARALLEL_ENABLED False, directly off Phase 2c's findings (parallel proven slower than serial at every tested config post-JIT). Single-constant change, version-bumped (v1.19->v1.20) with full changelog + inline comment citing the WO. Safe by construction: parallel/serial are proven byte-identical (test_eval_scoring.py + every Phase 2a/2b/2c check), so this cannot change any BUY/WATCH/PASS output, only wall-clock. Re-read after writing to confirm (M-051): `EVAL_PARALLEL_ENABLED: bool = False` confirmed on disk at line 306. This also means the M-079 gate below will run serial by default -- the faster real option per Phase 2c, not a manual override.

**2) M-079 walk-forward promote-gate comparison staged, not yet run.** This is the actual outstanding gate for staging_ingest_mined.db (10761 patterns, 1792 new) vs live 071726catalog.db (8969) -- interrupted back on 2026-07-18 by the silent exit-1 crash during its "post" step, never completed since. Built run_this.py (187 lines) reusing REAL production functions end to end, not re-derived logic (M-082): `application.ingest_mined_pipeline._run_pre_batch()` for the live-catalog pre-batch, `application.incremental_post_batch.run_incremental_post_batch()` for staging's post-batch, `infrastructure.eval_io.write_walk_forward_report()` for both reports. BUY precision / PASS accuracy read directly from each result's real `.correctness` field (not re-implemented) -- same metric definitions as the 2026-07-17 real-batch entry (BUY precision 68.09%->68.49%, PASS accuracy 58.77%->58.92%), so this run's numbers will be directly comparable.

Found and cited a real efficiency win before running: an eval-cache entry already exists for the exact live catalog (`models/eval_cache/catalog_8969_8969_51904512_default.json`) from the earlier session's crashed ingest-mined run -- its "pre" step completed and cached before the "post" step died. So this run should skip the ~44-min live-catalog rescore and go straight to the ~60-65 min staging rescore -- script asserts pre_batch.n_patterns==8969 and warns (not silently patches) if that's wrong.

Both files written via windows-mcp:FileSystem, re-read after writing (head/tail spot-checked, 187 lines clean EOF -- M-051/M-054). Handed off to Tony. Expected runtime ~60-70 min -- the real, necessary cost of the actual promote gate, not something to shortcut.

**Next:** report back with the real BUY precision / PASS accuracy numbers -- promote decision follows from those, Tony's call.

---
**>>> 2026-07-19 (5th, Sonnet) WO-P300-E4.005 PHASE 2c COMPLETE -- PASS, lever #2 confirmed non-viable, no crossover found:** Tony ran the staged 3-experiment script via Claude Code, pasted full output back (~600s runtime). Verified independently before logging (M-054): serial rate 221.4ms/task matches the corpus_size band's expected cost exactly (sanity-checks the whole design). Re-derived overhead structure from raw numbers, not just the reported ratios:

**A (worker sweep, batch=50):** no worker count beat serial; best was 2 workers at 1.18x slower. Overhead isn't simple fixed-cost-per-worker -- it GROWS with worker count (7.5s@2 -> 14.0s@4 -> 26.6s@8) then plateaus ~30.8s at 16/24, consistent with contention once enough workers spawn concurrently (candidate: numba cache file contention or Windows process-spawn contention -- not root-caused further).

**B (pool reuse):** batch 2 was 2.12x faster than batch 1 on the same warm pool (19.51s vs 41.29s) -- one-time worker-startup cost confirmed real, matches A's finding.

**C (batch-size sweep, real default 24 workers):** no crossover in 50-400 (ratios 3.10x/2.14x/1.51x/1.01x). Flagged an anomaly Tony's summary didn't surface: the ABSOLUTE overhead behind that trend is flat ~22-24s through size=200, then collapses to ~1.1s at size=400 -- doesn't fit a clean fixed- or per-task-overhead model (naive extrapolation predicted ~1.23x at 400, actual was ~1.01x). Logged as an unreproduced single-run anomaly, not a confirmed trend -- would need a repeat at 300-500 to trust it.

**Bottom line logged to the WO (status header, candidate-lever #2 entry, and a new acceptance-criteria checkbox, all updated): lever #2 as scoped does not beat serial at any tested configuration -- parked. Lever #3 (top-K state cache) is now the only lever with a clear path to the <15-min target.** All edits re-read after writing to confirm they landed (M-051).

**Next (Tony's call):** M-079 walk-forward promote-gate comparison (required regardless, still outstanding), or lever #3 design/build (now the sole remaining throughput lever). Nothing time-sensitive.

---
**>>> 2026-07-19 (4th, Sonnet) WO-P300-E4.005 PHASE 2c STAGED -- lever #2 re-eval, three experiments, not yet run:** "build the re eval" -- built run_this.py (271 lines) with three timing-only experiments against the real staging catalog, all drawing from a corpus_size-banded pool (2500-4000) so per-task cost is roughly constant, isolating the variable each experiment actually tests:

**A) Worker-count sweep** (batch=50, same size as Phase 2b's inverted result) across [2,4,8,16,default] -- does ANY worker count beat serial at this task size, or is the loss structural.
**B) Pool-reuse test** -- two consecutive 100-task batches through the SAME live pool (not recreated) -- separates one-time worker-startup cost (numba cache load + module re-import per worker, Windows spawn not fork) from recurring per-task dispatch/pickle overhead. If batch 2 is meaningfully faster than batch 1, it's a one-time cost that amortizes on real multi-hundred-pattern batches.
**C) Batch-size sweep** (50/100/200/400) at config.py's REAL default worker count (EVAL_PARALLEL_WORKERS=None) -- looking for a crossover size where parallel actually wins.

Verified signatures against the real domain/eval_scoring.py before writing (`_init_worker(all_metadata, historical_windows, all_labels, threshold_overrides)`, `_score_one_worker(pattern_id)`, `EVAL_PARALLEL_WORKERS: int | None = None` in config.py) -- no guessed signatures this time.

Overwrote the previous run_this.py (Phase 2b's now-complete, already-logged profiling script; Phase 1's own run_this_profiling.py remains untouched on disk as the original reference). Both files written via windows-mcp:FileSystem, re-read after writing to confirm content landed (head/tail spot-checked, 271 lines, clean EOF, no truncation -- M-051/M-054).

**Not yet run.** Explicitly no pass/fail threshold on the timing numbers -- script reports whatever's real, including a null result ("parallel doesn't help at any tested configuration"). Handed off to Tony per peh-handoff. Expected runtime ~10-15 min.

---
**>>> 2026-07-19 (3rd, Sonnet) WO-P300-E4.005 PHASE 2b COMPLETE -- PASS, real speedup ~11.5x (NOT the 50-200x estimate), one new open finding:** Tony ran the staged script via Claude Code, pasted full output back. Verified independently before logging (M-054 -- not taken on the pasted delta table alone): self-check PASS (20 pids) + parallel-vs-serial byte-identical (50 pids, True) both hold -- JIT kernel proven to change nothing but speed, again. Re-derived the speedup myself from the raw CSV rather than trusting the pasted summary: mean_total_ms 4066.82->355.07 (11.45x), median 4004.66->354.69 (11.29x); asymptotic per-pair rate from the 6 largest-corpus_size non-outlier rows = 0.0654ms/pair vs pre-JIT 0.7836ms/pair = 11.97x -- consistent. **Real uplift is ~12x, not the WO's 50-200x estimate** -- logged as a correction, not a win overstated.

**New finding: lever #2 (parallel) inverted post-JIT, not just "still unreliable" like Phase 1's flag.** Phase 1 measured parallel at a positive-but-flagged 2.08x; Phase 2b measured parallel 5.57x SLOWER than serial (32.12s vs 5.77s on the same 50-pid slice) -- each task got so cheap that ProcessPoolExecutor's fixed per-task overhead now exceeds the task itself. Byte-identity held regardless (correctness unaffected). Lever #2 as scoped needs re-evaluation before further investment -- may not be viable at this granularity at all.

**Three CSV outliers flagged (system noise, not correctness -- self-check already independently proves correctness):** pids 9819/9945/1284, all slower than trend, none faster. Not investigated further.

**<15-min target status, re-derived with the REAL rate (not the estimate):** current full-rescore architecture (O(N^2/2) pairs) at N=10,738 -> ~62.9 min serial even post-JIT (~12x faster than Phase 1's ~12.5hr pre-JIT projection, but still far over 15 min for a full rescore). This doesn't change the WO's own 2026-07-18 architectural conclusion (target applies to the per-batch O(B*N) path via the top-K state cache, lever #3, not yet built; full rescore becomes a scheduled event) -- it just replaces the estimate-based numbers behind that conclusion with measured ones. Lever #1 (JIT) confirmed necessary but, at its real magnitude, clearly not sufficient alone.

All of the above written into WO-P300-E4.005.md under a new PHASE 2b RESULTS section + acceptance-criteria checkbox + status-line update; both files re-read after writing to confirm the edits landed (M-051).

**Next (Tony's call):** M-079 walk-forward promote-gate comparison (required regardless of the above), or lever #3 (top-K state cache) design/build, or the newly-surfaced lever #2 re-evaluation. Nothing time-sensitive.

---
**>>> 2026-07-19 (2nd, Sonnet) WO-P300-E4.005 PHASE 2b STAGED -- post-JIT speed-measurement script handed off, not yet run:** Tony's "go" resumed the session. Built run_this.py as Phase 1's run_this_profiling.py (2026-07-18, unchanged logic -- same LIVE_CATALOG/STAGING_CATALOG, same deterministic _select_stratified_subset(), so it reproduces the SAME 150-pid subset Phase 1 measured, making this a true before/after comparison) plus one addition: _warm_up_jit(), a throwaway dtw_distance() call before the timed loop so numba's cache-load/compile doesn't pollute the first sampled pattern's dtw_s. Verified the real dtw_distance(seq_a, seq_b) signature against domain/similarity.py before writing the warm-up call (first draft guessed a 3-arg signature that doesn't exist -- caught before writing to disk, not after).

Overwrote the previous run_this.py (the silent exit-1 diagnostic) -- safe per the WO's own 2026-07-18 note: that diagnostic was killed by the harness's max_runtime at i=6083/10738 and Tony explicitly decided NOT to resume it separately, since the M-079 walk-forward comparison (still ahead regardless) exercises the same untested tail as a natural byproduct. Not resurrecting a live, still-wanted script.

Both files written via windows-mcp:FileSystem (not PowerShell, M-103) and independently read back after writing (M-051/M-054 -- not trusting the write call's own byte count): run_this.py head+tail re-read, matches source exactly, 316 lines, clean EOF; run_this_context.txt confirmed on disk, 3,833 bytes. Neither file executed yet -- handed off to Tony per peh-handoff (PowerShell MCP wedges on any python.exe subprocess call, M-030).

**Not yet run.** Next: Tony runs it via Claude Code Desktop, pastes output back -- compare against Phase 1's recorded baseline (mean_total_ms=4066.82, dtw_proper=99.3%, ~0.784ms/pair serial). PASS proves the JIT kernel didn't change any output, only speed; the component-timing delta is the actual Phase 2b deliverable. M-079 walk-forward promote-gate comparison still follows after this.

---
**>>> 2026-07-19 (1st) SESSION OPENED, NO CODE TOUCHED -- INIT + catalog reconciliation only:** Ran full INIT. Work-order ledger reviewed live (not from memory): WO-P300-E4.003 CLOSED, E4.004 OWNER_DONE (4th criterion still open), E4.005 OWNER_DONE on Phase 2a (DTW kernel JIT'd via numba, byte-identity 39/39 confirmed against pre-JIT baseline across corpus_size 220-8,733) -- post-JIT speed measurement and the M-079 walk-forward BUY-precision/PASS-accuracy promote-gate comparison both still outstanding, neither run yet. E4.001/E4.002 still PENDING, not started. E5.001 stub still PENDING. Silent exit-1 diagnostic (staged at verify\run_this.py) still not run.

**Catalog reconciliation flagged:** `P_300_preflight_status.json` was stale (generated 2026-07-17 08:54, pre-dating that day's promote) -- still showed `071526catalog.db`/8,175/317. Live catalog on disk confirmed via directory listing to be `071726catalog.db` (49.5MB), matching the 2026-07-17 promote entry already in this log (8,969 patterns/323 symbols). Catalog wins per M-017; preflight JSON needs a re-run of `P_300_Preflight.bat` next session for a fresh read.

LM Studio was not running (no lm-studio/lms process found) -- Pipeline B/domain work unaffected (deterministic, no narrator dependency), but flagging since Step 5c would otherwise HALT.

Tony's call: nothing time-sensitive, stopping here. Next task whenever resumed: WO-P300-E4.005 post-JIT speed measurement, then the M-079 walk-forward promote-gate comparison.

---
**>>> 2026-07-18 (5th session, Fable 5) WO-P300-E4.005 PHASE 2a COMPLETE -- JIT build shipped, byte-identity PASS 39/39:** 6-file build (baseline capture -> similarity.py JIT + requirements.txt + tests/test_similarity.py -> regression check), delivered in that order so the pre-JIT baseline was real. domain/similarity.py's dtw_distance() grid now runs through a private @njit(cache=True, fastmath=False) kernel -- same algorithm, float64 arrays instead of lists; public signature/contract/per_feature_distances/composite_distance/rank_by_distance all unchanged. test_similarity.py (new, permanent, 7 tests) reuses similarity.py's own smoke-harness fixtures plus a pinned-reference DTW check. Real-catalog regression: 39 candidates, corpus_size 220-8,733 (near-full range), 0 mismatches, byte-identical to the pre-JIT baseline. Environment detour along the way: numba 0.61.2 (originally planned) doesn't import against this env's numpy at all -- upgraded to 0.64.0 (confirmed minimal version adding NumPy 2.4 support), which surfaced a pip conflict with pandas-ta's hard pin on 0.61.2 -- verified via Hub-wide grep that pandas-ta is unused anywhere (declared nowhere, imported nowhere), so harmless; requirements.txt now says numba==0.64.0 (the real installed version). New lesson M-104 filed (pip conflict warnings reflect declared pins, not usage -- grep before reacting). Full record in the WO under PHASE 2a RESULTS. NOT done: post-JIT speed measurement (Phase 1's ~0.784ms/pair pre-JIT number hasn't been re-measured against the new code, so the 50-200x estimate is still unconfirmed) and the M-079 walk-forward BUY-precision/PASS-accuracy comparison, which is the actual live-path promote gate -- neither has run yet.

---
**>>> 2026-07-18 (4th session, Fable 5) WO-P300-E4.005 PHASE 1 PROFILING COMPLETE (PASS):** run_this_profiling.py ran clean (self-check PASS, 150-pid stratified subset, corpus_size 19-10,656). Full results in the WO under PHASE 1 RESULTS; headline: DTW is 99.3% of per-pattern cost (confirms JIT as lever #1); true serial rate measured at ~0.784ms/pair -- 12-14x higher than the E4.003-derived figure the WO originally cited, resolving Finding 4 in the exit-1 diagnostic's direction; full rescore projects to ~12.5 hours serial (not ~3), cross-validating last session's pace note; JIT confirmed load-bearing (not optional) for the <15-min target -- post-JIT estimate 168-670s for a 2,000-pattern batch at N=10,738 vs ~78 min pre-JIT; parallel measured 2.08x on 50 pids (not E4.003's 15%) but flagged as unreliable -- that slice was the low-corpus-size tercile by construction, needs a corpus-size-representative re-test before trusting a number for lever #2; tie census 0/150 at the top-20 boundary (sample, not full census). Full CSV + all component numbers at Agentic-Hub-Governance\verify\profiling_out.txt. Next: build plan (plan-gate, file counts, Tony approval) before any production code -- numba install approval and the M-079 live-path gate both apply per the WO's Finding 1 flags.

---
**>>> 2026-07-18 (3rd session, Fable 5) WO-P300-E4.005 APPROACH REVIEW COMPLETE -- levers re-ranked, scale re-scoped, all Tony-approved, WO amended in place:** Code-verified review (similarity.py, eval_scoring.py, eval_incremental.py, eval_io.py, incremental_post_batch.py read in full). Findings now in the WO: (1) JIT-the-DTW-kernel promoted to lever #1 -- interpreted cell ops ~95% of pair cost (estimate), 50-200x candidate, loop-faithful njit fastmath-OFF keeps byte-identity, numba = NEW p140 dependency needing install approval, similarity.py is live-path-shared so M-079 gate applies before any promote; (2) pairwise (pid_a,pid_b) cache demoted -- ~3x today holds (each earlier-dated pair computed exactly once per full rescore, N^2/2 total) but O(N^2) storage is a dead end; replaced by top-K state cache (O(N*K) storage, O(B*N) per batch -- the lever that survives 1700-symbol scale); (3) tie-order fragility -- rank_by_distance has no explicit tiebreak, ties resolve by dict construction order, so top-K byte-identity is fragile against any refactor; census in Phase 1(e) before formalizing (distance, pid); (4) unexplained 3-12x per-pair discrepancy between E4.003 (~55-64us/pair) and the live exit-1 diagnostic (214-762us/pair floor from its own fsync'd log) -- Phase 1(f) joins the diagnostic's 10,738 timing samples against pid->corpus_size. Scale honesty recorded: full rescore caps at ~25-45k patterns even fully optimized (estimates, assumptions stated in WO); <15-min target re-scoped to the per-batch O(B*N) path, full rebuild becomes a scheduled event at scale -- new acceptance criterion added. Phase 1 spec amended: items (e)/(f), 150-pid corpus-size-stratified subset, instrumented-vs-uninstrumented model_dump_json self-check, stdlib tooling (perf_counter primary, cProfile on a 20-pid slice only; scalene rejected -- new dependency, no added value for a function-boundary split). Profiling script to stage at Agentic-Hub-Governance\verify\run_this_profiling.py -- sibling filename, one-off peh-handoff deviation because run_this.py is occupied by the live diagnostic (Tony letting it run until tonight; suspect zone 101-200 already cleared clean, so the original exit-1 was environmental, not a poison pattern). Script staging next; plan-gate after profiling numbers.

---
**>>> 2026-07-18 (2nd) WO-P300-E4.005 FILED (PENDING) -- <15-min post-batch throughput target, profile-first:** Tony set the operational goal: BulkAddPattern batch to eval-ready in under 15 minutes, statistically correct (no sampling, no approximation, byte-identity standard inherited from E4.003/E4.004), recurring workflow at 1700-symbol licensed scale. Real arithmetic in the WO: target needs ~84ms/pattern vs measured 1035ms serial / ~226ms parallel -- ~12x gap that no single existing lever closes (E4.003 parallel measured at only ~15% gain on 24 cores; E4.004 incremental useless when min_new_date sits at the backfill floor, which both real batches did; pairwise-DTW-cache estimate ~3x on this batch's real shape).

WO structure: Phase 1 profiling is the ONLY approved work -- component-level split of where the 1035ms actually goes (DTW proper / corpus dict-rebuild / ranking / parallel overhead), real staging catalog, bounded 100-200 pattern subset. Candidate levers ranked in the WO (pairwise cache + parallel-scaling fix primary, numba JIT multiplier, pre-filter parked as statistically risky, sampling rejected outright). Plan-gate after profiling before any build. E4.004's open 4th acceptance criterion folds into this WO's measured result.

**Execution plan (Tony's call):** approach review + Phase 1 design runs on Claude Fable 5 max effort (usage boost window, ~1 day left); build phases return to Sonnet high effort once approach is settled. Kickoff prompt staged at `docs\prompts\E4005_fable_kickoff_prompt.txt` -- includes the challenge-the-approach mandate, the 1700-symbol-scale honesty check on whether 15 min is even sustainable, and a warning that the Fable session's profiling PEH script would overwrite the still-possibly-pending exit-1 diagnostic at `verify\run_this.py` (ask Tony first).

---
**>>> 2026-07-18 SILENT EXIT-1 DIAGNOSTIC STAGED -- real forensics from a Grok terminal log, verified not assumed:** Tony supplied a detailed failure analysis (attributed to Grok) for the carried-forward silent exit-1 item. Verified the load-bearing claims against real files before acting on them (M-054): `C:\Users\Trader\.grok\sessions\...\terminal\call-10257548-0efd-46f7-b56f-b32ec15830d4-81.log` exists and its content matches the pasted summary verbatim -- serial rescore of 10738 must-rescore patterns (staging_ingest_mined.db 10761 vs live 071726catalog.db 8969, new_pattern_ids=1792, min_new_date=2021-08-13, safe=23), log ends right after "100/10738 scored", no traceback, no 200-line. `staging_ingest_mined.db` confirmed still on disk (unpromoted), modified/accessed timestamps line up with the log exactly.

Both source DBs still being on disk means the exact same must-rescore run is reproducible for real, not just theorized. Staged a diagnostic re-run (`Agentic-Hub-Governance\verify\run_this.py`) that adds what the original lacked: per-pattern heartbeat fsync'd to a progress-log file (survives a hard/external kill, which a bare try/except would not catch) and a real try/except with full traceback around `score_one()`. Processes the suspected zone (original indices 101-200) first, then the remainder -- full run ~3hrs at the original ~1035ms/pattern, expected and not itself a failure. Handed off to Tony; NOT yet run.

---
**>>> 2026-07-18 WO-P300-E4.004 v1.2 BUILT + PEH-VERIFIED -- "worth it" reuse-fraction threshold:** Same session as the independent review above. Implemented Tony's proposal: `config.INCREMENTAL_MIN_REUSE_FRACTION` (new constant, 0.5, config.py v1.19 -- named constant, not hardcoded, per Tony's explicit call) checked in `application/incremental_post_batch.py` v1.2 before attempting the incremental path; below it, skips straight to a full `run_walk_forward(parallel=True)` instead of building a second rescore path. `domain/eval_incremental.py` v1.2 gained `compute_reuse_fraction()` (cheap pre-check, reuses `_partition_unaffected()` rather than re-deriving it, M-082).

`tests/test_eval_incremental.py` v1.2 grew from 4 to 6 checks: fraction-correctness against the known mixed-dates fixture (5/10=0.5), and a genuine skip-path proof (poisons the domain-layer call so it fails loudly if invoked when it shouldn't be, not just a coincidental-match check). File was initially written at 313 lines (over the 300 cap) -- trimmed docstrings/comments to 291, not accepted as an overage.

PEH re-run (independent-review session, not a separate reviewer for this specific increment -- noted explicitly in the WO): `test_eval_scoring.py` 5/5 PASSED (unaffected), `test_eval_incremental.py` 6/6 PASSED. Both exit 0. Does NOT resolve E4.004's still-open 4th Acceptance Criterion (real wall-clock speedup on a live batch) -- changes which path a low-reuse batch takes, not whether either path is actually faster in production. WO stays OWNER_DONE.

---
**>>> 2026-07-18 WO-P300-E4.004 INDEPENDENT REVIEW COMPLETE -- STAYS OWNER_DONE (not CLOSED):** Separate session (not the 2026-07-17 build session) re-verified per WO_COMPLETION_GATE.md's Independent Review Requirement, did not trust the build session's closure note (M-054). File/code checks: all 4 v1.1 files' line counts exact-match disk (155/79/300/239); confirmed `tests/test_eval_incremental.py` actually lives at project-root `tests\`, not `python\tests\` as the WO's Affects line implied -- corrected in the WO. Code re-read confirms the min-new-date partition, guardrail, and application-fallback wiring all present as described. Noted (not a blocker): `domain/eval_incremental.py` imports `PatternMetadata` from `infrastructure.catalog_reader` -- a domain-importing-infrastructure pattern, but confirmed pre-existing and identical in the already-CLOSED E4.003's `eval_scoring.py`, not a new violation.

Real independent PEH re-run via peh-handoff (not taken on the build session's word): `test_eval_scoring.py` 5/5 PASSED, `test_eval_incremental.py` 4/4 PASSED, both exit 0, no discrepancy found. 3 of 4 Acceptance Criteria independently confirmed. **4th criterion (real-world wall-clock speedup on a live BulkAddPattern batch) is still genuinely unmet** -- v1.1 has not had a real production run yet, and this can't be confirmed by code review or synthetic tests. Per governance, WO stays OWNER_DONE, does not move to CLOSED. Closes on the next real batch that either confirms the speedup or gets an explicit Acceptance Criteria amendment from Tony.

Next up (Tony's call): the two carried-forward items above (E4.004 "worth it" threshold cutoff, silent exit-1 trace).

---
**>>> 2026-07-18 CARRIED FORWARD FROM PRIOR SESSION (not previously logged) -- two E4.004-adjacent open items, neither built:** Recap pasted by Tony to level-set this session; prior session's offer to log was never confirmed before close (no M-102 entry exists in lessons.md, confirming nothing hit disk).

1. **E4.004 "worth it" threshold proposal:** Add a threshold in the application-layer orchestrator (`run_incremental_post_batch`) -- if the safe-reuse fraction is below a cutoff (e.g. <50%), skip the incremental path entirely and call `run_walk_forward(parallel=True)` directly instead of building a parallel rescore path from scratch. High reuse fraction -> incremental still wins; low reuse fraction (both real batches so far) -> falls through to the fast, known-good E4.003 path. Not planned or built. Needs a cutoff value decision + plan-gate review (touches E4.004's application layer).

2. **Silent exit 1, untraced:** A run died silently at exit 1 somewhere around pattern ~101-200, no traceback, not one of Tony's two intentional early-exits (neither printed). Possible unhandled exception on a specific pattern with output swallowed -- possibly the same mined batch that already surfaced a data-quality edge case (referenced by Tony as "M-102" in the recap, but no such lessons.md entry exists yet -- needs confirming/filing for real, not assumed). Proposed fix: try/except-with-traceback wrapper to identify the failing pattern_instance_id.

Both explicitly deferred -- Tony called it after a long night, picking up fresh next session.
