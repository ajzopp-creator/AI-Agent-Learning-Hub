---
ticker: DAL
lifecycle_status: CLOSED
close_date: "2026-08-20"  # confirmed closed via live Schwab pull; exact close date unknown, Tony confirmed 2026-08-20 no longer held
source_label: P_116
vehicle: "OPTION_CALL 260918C00100000"
qty: 4
---

# DAL - external position (not P_400-managed)

Subscription-service position (P_116), surfaced by `audit-book` (WO-P400-E4.00x)
with no corresponding P_400 book record. Separate from any fresh DAL stock
signal that may pass through P_400 -- filename suffix `_EXT` avoids any
collision. Tagged per WO-P400-E6.003 so `audit-book` reports it as
known-external instead of untracked noise, and so `has_duplicate()` sees
it for scale-in/replace/skip checks. Never went through P_400
evaluate/spec/council -- no lifecycle fields beyond the minimum populated
by design.
