---
ticker: QQQ
lifecycle_status: FILLED
source_label: P_300
vehicle: "STOCK"
qty: 5.0185
---

# QQQ - external position (not P_400-managed)

Subscription-service position (P_300), surfaced by `audit-book` (WO-P400-E4.00x)
with no corresponding P_400 book record. Tagged per WO-P400-E6.003 so
`audit-book` reports it as known-external instead of untracked noise, and
so `has_duplicate()` sees it for scale-in/replace/skip checks. Never went
through P_400 evaluate/spec/council -- no lifecycle fields beyond the
minimum populated by design.
