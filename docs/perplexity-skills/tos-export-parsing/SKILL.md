---
name: tos-export-parsing
description: Use for parsing ThinkOrSwim export files, extracting trading data fields, and converting TOS output into structured formats for analysis or downstream workflows.
---

# TOS Export Parsing

Use this skill when working with ThinkOrSwim export files such as XML, CSV, or text-based platform outputs.

Extract relevant fields, normalize inconsistent structures, identify key indicators or values, and convert the data into structured outputs suitable for Python analysis, database loading, or project workflows.

Prefer clean parsing, explicit field mapping, and reliable handling of repeated export patterns.