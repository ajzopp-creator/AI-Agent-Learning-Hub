# WO-P800-E6.001 — P_805 Consensus Dashboard (P_115 Candidate Surface)

**Status:** IN_PROGRESS -- build complete 2026-09-08, first live-verified run + Independent Review still pending
**Owner:** P_800
**Affects:** P_800 (dashboard/vault build — owns 100% of this); P_805 (data
source, no code change expected — reads existing live `ranked.csv`); P_115
(tracker consumer, SignalSource=P_117 bucket)
**Depends On:** none

---

## WHY

Tony's stated ultimate goal for P_805: surface its ranked-consensus tickers
where he can act on them for P_115 processing. No dashboard or P_115
consumption path exists today — greenfield.

## DECISIONS (2026-09-06, made in a P_805 session)

1. **Candidate list, not an auto-trigger.** P_805 output sits at the same
   tier as P_117's subscription-service inputs (Big Trends/SNT). P_115's
   mandatory chart-pattern check and "analysis first, Tony decides after"
   rule for outside signals stays intact — the dashboard surfaces
   candidates for Tony to glance at and manually run through P_115's
   diagnostic. Nothing here should silently create a BUY signal.
2. **Tracker placement: fold into P_117, no new tier.** SignalSource stays
   `P_117` ("Outside Recommendations") for anything that graduates from
   this dashboard into the 27-column tracker — Tony doesn't need
   newsletter-consensus visually separated from Big Trends/SNT right now.
   Reversible later if he wants to isolate email-sourced signals (e.g. to
   pause just those).
3. **Noted, not in scope:** Tony's longer-term aspiration is full
   automation — P_115's diagnostic eventually auto-gating on its own
   criteria against these candidates, still with no skipped checks, just
   machine-executed instead of manual. Not building toward this now, but
   the dashboard's data shape shouldn't foreclose it later.
4. **No manual input, anywhere.** Corrects an earlier misreading in this
   same conversation: Tony's action stays exactly what it already is —
   pulling a candidate symbol up on a TOS chart to eyeball the pattern.
   That is not data entry for this dashboard. The "BUY/ASYM passed"
   columns are a JOIN against records that already exist, not anything
   Tony types into this tool.
5. **Verdict source: the Tracker, not the raw signal JSON.** P_115 emits
   a signal JSON (input to P_400) carrying the native `BUY | ASYM | PASS`
   vocabulary directly — no P_400 council-language mapping needed, so
   the vocabulary-mismatch concern raised earlier in this conversation
   doesn't apply. But that JSON is temporal and gets archived once P_400
   consumes it (`trading_journal\TradeOrderManagement\signals\`) — not a
   practical thing to query historically. The Tracker Dashboard spreadsheet
   is where the same verdict is available "anytime" per Tony, so it's the
   actual join target for MTD/YTD counting, not the archived JSON.
6. **PASS excluded; BUY and ASYM both count; paper and live both count**
   — no distinction needed between paper/live for this dashboard's purpose.

## DASHBOARD SHAPE (corrected, 2026-09-06)

| Email Source | Today's Symbols | MTD Candidates | YTD Candidates | MTD BUY/ASYM | YTD BUY/ASYM |
|---|---|---|---|---|---|

- **Today's Symbols / MTD Candidates / YTD Candidates:** pure P_805
  history, no new data needed — count `ranked.csv` rows by `sources`
  (grouped back to sender/email) across the relevant date range.
- **MTD BUY/ASYM / YTD BUY/ASYM:** join each candidate symbol against the
  Tracker Dashboard spreadsheet (`F:\OneDrive\...\P_115_118_TrackerDashboard_V2.xlsx`
  per `p020-project-context`, flagged there as possibly `D:\` instead —
  confirm drive letter live, don't trust either literal) where the verdict
  column reads BUY or ASYM, count distinct matches per source per period.

## DATA SOURCE (already live, no P_805 change needed for baseline scope)

`P_805\data\daily\YYYY-MM-DD_ranked.csv` — one row per consensus ticker
(≥2 distinct sources in the lookback window):
`ticker, source_count, sector_count, sources, direction, first_seen, last_seen`.
Produced daily by the 9:15 AM scheduled pipeline (Phase 4).

## OPEN -- resolved 2026-09-08 (P_800-context session); remaining before CLOSED

All five original design questions resolved this session:
- Surface: new `## P_805 Consensus` section in `Dashboard.md` (marker-delimited),
  not a `.base` -- Bases cannot cross-source join.
- Write path: new dedicated `dashboard_section_writer.py`, not `write_to_vault()`
  -- this is a computed summary table, not a per-record frontmatter note.
- Cadence: batch, run after the 9:15 AM P_805 pipeline (not live/on-demand).
- Drive letter: resolved live -- real path is
  `D:\OneDrive\Documents\AJZStrategiesLLC\P_115_TrackerAudit\P_115_118_TrackerDashboard_V2.xlsx`,
  sheet `Tracker Log`. `config.py` resolves it via `os.environ["OneDrive"]`
  (matches p115_backfill's existing convention) rather than hardcoding a drive.
- Join logic: Symbol match + Tracker `Date` within
  `[first_seen, first_seen + 15 trading days]` (matches P_115's own default
  signal horizon) + `Step1Verdict` in `{BUY, ASYM}`.

Still open before CLOSED:
- First live-verified run via PEH handoff (not run inline through
  Windows-MCP -- known bridge-hang risk) to confirm real row counts.
- Independent Review (separate session, did not write this build).
- Confirm the 15-trading-day join window against Tony's actual usage pattern
  once real MTD/YTD numbers are visible -- currently a reasoned default, not
  Tony-confirmed.

## Occurrence Log

**2026-09-06** — WO opened in a P_805 session (design-decision
conversation with Tony; data source and two placement decisions made).
Filing the requirement only — no live P_800/vault work done this
session, per boundary rule (P_800 owns all vault/display work; a
P_805-context session redirects cross-project builds through a WO
rather than touching vault templates/schemas directly).

**2026-09-06 (later, separate P_805 session)** — Found this WO already
on disk while about to draft the same thing from scratch; updated in
place rather than duplicating. Tony corrected the "Average Buy
recommendations" framing twice: first clarifying it means P_115's own
BUY/ASYM verdict (not price performance, no new market-data feed
needed), then correcting an assistant misread that had proposed a
manual Tony-logs-it input field — there is no manual input anywhere,
the dashboard joins P_805 candidates against verdicts that already
exist. Also resolved the P_115-vocabulary-vs-P_400-vocabulary concern
raised earlier this session: P_115 emits its own signal JSON in native
BUY/ASYM/PASS language before P_400's council ever reworded it, but that
JSON archives after use — the Tracker spreadsheet is the practical
query target instead, per Tony. Dashboard shape and Decisions section
updated above; two new OPEN items added (drive-letter conflict, join
logic).

---

**2026-09-08 (P_800-context session)** -- All five OPEN design questions
resolved live (see OPEN section above). Built full domain/infrastructure/
application layer set under
`projects\P_800_Automation_Note_Taking\python\p805_consensus\` (13 files,
config through cli.py + .bat + MCP-safe wrapper), plus one-time marker
insertion into `Dashboard.md` (backed up first as
`Dashboard.md.bak_2026-09-08`). Live-verified during this session: Tracker
workbook real path and columns (Date/Symbol/Step1Verdict, 2,529 rows), and
`ranked.csv` schema against an actual 2026-09-08 file. Not yet run
end-to-end -- first execution deferred to a PEH-handoff session per the
Hub's no-inline-Python-via-Windows-MCP rule. Status moved PENDING ->
IN_PROGRESS.

---
**Created:** 2026-09-06 (Tony's request, P_805 session)
**Author:** Claude
