# WO-P300-E5.006 -- Sector/Regime Context in Pattern Matching: Measure Before Building

**Status:** PENDING (research finding recorded; measurement pass NOT run, nothing built)
**Owner:** P_300
**Affects (if it ever gets built -- NOT authorized by this WO):** would touch
  `python/domain/similarity.py` (the DTW feature set), `python/schemas.py`
  (a new feature version -- `baseline_5bar_v1` may NOT be silently changed,
  per architecture doc Section 9.1), a sector column on the LIVE catalog's
  `symbols` table (see CORRECTION below -- the RESEARCH catalog already has
  one), and a full `topk_cache` rebuild across all 16,801 patterns. That last
  item is the exact O(N^2) cost WO-P300-E4.006 and WO-P300-E5.004 exist to
  prevent -- it is not a free schema addition.
  **The measurement pass proposed below touches NONE of these.**
**Depends On:** None. Related to WO-P300-E5.005 only in that it borrows that
  WO's own ~5pp materiality threshold as the decision bar.
**Acks:** N/A

---

## CORRECTION 2026-07-28 -- THIS WO WAS FILED ON A FALSE PREMISE

As originally filed (2026-07-27) this WO asserted "no sector mapping exists;
the `symbols` table is `(symbol_id, ticker)` only" and proposed building a
sector-state measurement from scratch. **That was wrong.** A grep run during
an unrelated investigation surfaced `infrastructure/sector_backfill_io.py`
and `infrastructure/sector_stats_io.py`, both dated 2026-07-10, both from
**WO-P300-E2.002**. Reading them shows the following already exists in
`models/research/bulk_research.db`:

- A `sector` column on `symbols`, backfilled from `config/sector_map.csv`
  with zero-tolerance full-coverage verification (`verify_full_coverage`
  raises BEFORE any file is copied if a catalog ticker has no map entry).
  ETF rows are explicitly set to `sector=NULL` rather than left unset.
- A **`sector_stats` table**: `sector_label`, `detection_tier`,
  `horizon_days`, `n`, `win_rate`, `mean_return_pct`, `std_return_pct`,
  `below_min_n`, `computed_at`.

That last item is decisive. **`sector_stats` IS win rate stratified by
sector and horizon, with an explicit small-n flag** -- materially the
measurement this WO proposed building. The original filing proposed
constructing from scratch something that may already exist, possibly
already populated.

This is M-082 in the exact form lessons.md warns about: logic re-derived
independently instead of re-running proven logic. It is also an M-100
failure -- the premise ("no sector mapping exists") was asserted from a
schema reading of the LIVE catalog and generalized to the project without
checking the tree. Premises get the same evidence bar as acceptance
criteria.

**Consequence for scope:** the coverage concern raised below (CNSWF /
CDPYF / UEC not mapping cleanly to the 11 SPDRs) may already be settled --
`verify_full_coverage`'s zero-tolerance rule means that if those tickers
exist in `bulk_research.db` at all, a mapping decision was already made and
documented for each. Do not re-litigate it until `sector_map.csv` has been
read.

**Nothing below this line has been re-validated against the above.** Treat
the WHY section as a literature review that still stands on its own, and
the build proposal as SUPERSEDED pending the E2.002 review now added as
step 1 of NEXT STEPS.

---

**Created:** 2026-07-27, Tony's direct question during a pre-export duplicate
  check: he has 10 years of SPDR sector-ETF history available in VP and asked
  whether sector state at the time of a historical pattern should be part of
  selecting which catalog patterns match a live candidate -- or whether that
  is overkill.

---

## WHY -- the question, and what the literature actually supports

Tony's hypothesis: when Pipeline B ranks catalog patterns by DTW distance to
a live candidate, it currently ignores what the candidate's SECTOR was doing.
Two patterns with identical bar geometry -- one formed while its sector was
leading, one while its sector was being sold -- are treated as equally good
analogs. Should sector state condition the match?

**Real support exists, but at the wrong horizon.** Moskowitz & Grinblatt
(1999), "Do Industries Explain Momentum?" (J. Finance 54:4, 1249-1290) is the
canonical result: industry momentum accounts for much of the individual-stock
momentum anomaly, and individual momentum is significantly less profitable
once industry momentum is controlled for. That is the strongest published
version of this hypothesis. **The catch: their formation and holding periods
are 6-12 months. P_300's forward labels are 5/7/10/15/20 TRADING DAYS.**
Nothing in that paper claims sector state predicts a 5-day move. The horizon
gap is roughly 10-30x and is not a detail -- it is the main reason this is
filed as "measure first" rather than "build it."

**Closer to usable:** Cooper, Gutierrez & Hameed (2004), "Market States and
Momentum" (J. Finance 59:3, 1345-1365) -- mean monthly momentum profit was
+0.93% following up-markets vs. -0.37% following down-markets. Real evidence
that CONTEXT conditions outcomes. But that is MARKET state, not sector state
-- and P_010 already computes exactly that (`spy_posture`, `qqq_posture`,
`avg_posture`, `risk_mode`). The Hub's existing architectural answer is that
regime context lands at P_400 sizing, NOT inside P_300's deterministic
matcher. This WO does not propose overturning that; it proposes testing
whether the data justifies revisiting it.

**Supporting reference:** Cakici et al., "Momentum and industry growth"
(Rev. Fin. Economics, 2006) -- individual stock momentum varies almost
monotonically with industry growth quintile. Closest published analog to the
exact stratification test proposed below.

**Counterweight, taken seriously:** Bailey, Borwein, Lopez de Prado & Zhu,
"The Probability of Backtest Overfitting" -- the probability of selecting an
overfit strategy grows rapidly with the number of trials. A sector-state
feature carries at least four free parameters (which ETF, what lookback,
how many buckets, relative-to-SPY or absolute). Adding four forks to the
matcher without a pre-registered test is precisely the failure mode that
paper describes.

## THE REAL COST, specific to this codebase (not generic caution)

1. **It degrades the thing that currently works.** Conditioning the corpus on
   sector regime means the top-20 becomes "closest match WITHIN the same
   sector regime" instead of "closest match overall." That trades pattern-
   shape fidelity for context fidelity. Which carries more signal is an
   empirical question, not a design preference -- and it is answerable with
   the eval machinery already built (Stage 9 sweep + ablation + LOO
   utilities).
2. **No sector mapping exists.** The `symbols` table is `(symbol_id, ticker)`
   only. A sector map, a SPDR bar ingest path, and a new feature version
   would all be new construction.
3. **Coverage gaps are real, not hypothetical.** From the live `data/live/`
   roster checked this same session: CNSWF (Constellation Software, Canadian
   OTC) and CDPYF do not map cleanly to the 11 SPDR sectors; UEC's honest
   sector proxy is URA, which is not a SPDR fund at all. Any mapping would
   need a documented fallback for these, not a silent default.

## PROPOSED MEASUREMENT PASS (read-only, zero schema change)

**Do the cheapest strictly-easier version of the question first.** Every
catalog pattern is already scored BUY/WATCH/PASS with a known forward outcome
by the existing M-079 walk-forward machinery. Tag each pattern's `anchor_date`
with the contemporaneous P_010 `avg_posture` (data Tony already computes
daily), bucket it, and check whether BUY win rate stratifies across buckets.

- Read-only against the live catalog + P_010 history. No writes, no
  migrations, no new ingest, no `topk_cache` touch.
- **Decision bar, pre-registered here BEFORE looking at any result** (this is
  the point of filing it): if BUY win rate spread across posture buckets is
  under ~5pp, market regime carries no usable signal at P_300's horizons,
  and sector state -- a noisier, more granular version of the same idea --
  almost certainly carries less. Question answered, no build, WO closes.
  5pp borrowed deliberately from WO-P300-E5.005's own threshold, itself
  anchored to the one real regression this project has measured
  (WO-P300-E2.003, a 5.2pp BUY precision drop).
- Only if the market-level test clears that bar does the sector-level
  version get a plan-gate conversation. Even then: research finding brought
  to Tony for a decision BEFORE any code, same as WO-P300-E5.004 Part A.

## EXPLICITLY NOT DECIDED / NOT AUTHORIZED

- Nothing in this WO authorizes touching `similarity.py`, the feature set,
  the schema, or the matcher. The measurement pass is the entire scope.
- Whether P_010 posture history is even retrievable at the granularity
  needed (per-anchor-date, back to 2021-08-10, the catalog's earliest
  anchor) has NOT been checked. First real step is confirming that data
  exists before designing the test -- premises get the same evidence bar as
  acceptance criteria (M-100).
- Whether the right context variable is P_010 `avg_posture`, sector-relative
  strength, or something else entirely. The proposal starts with
  `avg_posture` only because it is free -- not because it is known to be best.

## NEXT STEPS (none started)

1. **READ WO-P300-E2.002 AND QUERY `sector_stats` FIRST.** Nothing else in
   this list is valid until that is done. Specifically: (a) read E2.002 in
   full, (b) read `config/sector_map.csv` for actual coverage of the live
   roster, (c) run `query_sector_stats()` read-only against
   `models/research/bulk_research.db` to see whether the table is populated
   and what run produced it. If `sector_stats` already answers the
   stratification question, this WO closes as a duplicate of E2.002 and the
   only deliverable is a pointer to the existing numbers.
2. Only if step 1 shows a real gap: confirm P_010 posture history is
   retrievable per-date back to 2021-08-10. Tony has 10yr VP history grids
   for SPY/QQQ/VXX and other ETFs, so reconstruction is feasible -- but
   reconstructed-from-VP posture is NOT the same artifact as P_010's actual
   contemporaneous record, and which one the test uses must be stated
   explicitly, not left implicit. Note `avg_posture` is exactly
   `(spy_posture + qqq_posture) / 2` -- verified against the 2026-07-27
   RiskConfig.json (-4.429016 + -10.533875) / 2 = -7.481445, matching the
   file's stored value exactly. VXX is NOT an input, so its reverse-split
   history is not a concern for this test.
3. Write the stratification query as a read-only PEH script. Pre-register the
   bucket definition and the 5pp bar in the script's own context file BEFORE
   running it, so the result cannot be reinterpreted after the fact.
4. Report the real numbers to Tony either way -- including, and especially,
   a null result. A measured "no signal here" is a successful outcome of this
   WO, not a failed one.
5. Full file-level plan (line estimates, plan-gate, M-003) before any code
   beyond the read-only measurement script.

---

Filed 2026-07-27 at Tony's direct instruction ("YES") so this surfaces at
every future INIT rather than being lost to chat history -- M-015/M-017
discipline: live docs, not thread memory, are authoritative. Research and
literature review done this session; measurement pass NOT run; nothing built.
