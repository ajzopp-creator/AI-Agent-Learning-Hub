# WO-P020-E1.007 -- Schwab Token Pre-Flight Check + Fail-Fast Alert (BACKLOG-3)

**Status:** IN_PROGRESS
**Note:** Code complete + verified against the real failure case. Awaiting one happy-path confirmation after next Schwab reauth before marking COMPLETE.
**Owner:** Claude
**Affects:** P_020
**Depends On:** --

---

## WHY

2026-07-25: Scheduled weekly update (P_020_Weekly_Update_Scheduled.bat, run
via Cowork) failed on an expired Schwab refresh token. Token file was last
written 2026-07-18 -- exactly the 7-day refresh-token lifetime -- and no
run happened in between to refresh it. Pipeline burned through Step 1
(balance pull, failed) and Step 2 (Schwab trade pull, failed with
authlib.OAuthError: invalid_grant) before halting. Failure was buried in
audit_logs\P_020_Weekly_YYYYMMDD.log -- nothing surfaced it proactively.

This is exactly BACKLOG-3 (P_020_Future_Enhancements.md): "Current batch
silently fails when Schwab token has expired. Add a pre-flight check that
probes the token and prompts for re-auth if expired, before running the
full pipeline."

## FIX

1. `python\api\P_020_Schwab_Token_Manager.py` -- `__main__` block exits
   non-zero (sys.exit(1)) when `test_connection()` fails, instead of
   silently returning. Existing `test_connection()` logic unchanged --
   it already does a real API probe (get_account_numbers()), just needed
   to fail loud when run as a script.

2. `P_020_Weekly_Update_Scheduled.bat` + `P_020_Weekly_Update.bat` -- new
   Step 0 runs the token manager as a pre-flight probe BEFORE Step 1
   (balance) and Step 2 (trade pull). On failure: distinct exit code 2
   (vs. 1 for other step failures), distinct STATUS_FILE message
   "TOKEN_EXPIRED" (vs. generic "FAILED Step N"), and a clear re-auth
   instruction pointing at P_020_Schwab_Auth.bat. Pipeline halts before
   burning either pull attempt.

3. `tests\test_p020_token_preflight.py` -- regression test locking in the
   exit-code contract (fail -> sys.exit(1), success -> no exit) via
   monkeypatched test_connection(), per Regression Test Governance.

## COWORK ALERT (manual step, outside this WO's code)

Code changes make the failure fast and distinctly signaled
(exit code 2, STATUS_FILE = "TOKEN_EXPIRED ..."), but Cowork's scheduled
task is not configurable via MCP from this session. Tony needs to add an
instruction to the Cowork weekly task prompt: check
C:\Users\Trader\Desktop\P_020_Weekly_Status.txt after the run; if it reads
TOKEN_EXPIRED, message Tony directly (don't just log it) with the re-auth
step. Draft instruction text provided in chat.

## VERIFICATION

- [x] py_compile on P_020_Schwab_Token_Manager.py -- clean, exit 0
- [x] tests\test_p020_token_preflight.py -- 2 passed
- [x] Live test 2026-07-25 10:33 (real expired token, not simulated):
      ran P_020_Weekly_Update_Scheduled.bat -- Step 0 caught it,
      STATUS_FILE = "TOKEN_EXPIRED 2026-07-25 10:33", log shows Step 0
      only, Steps 1-5 never ran
- [ ] Happy-path test (valid token, confirm Steps 1-5 proceed unchanged)
      -- blocked on Tony re-running P_020_Schwab_Auth.bat; do this on next
      reauth

---

**Created:** 2026-07-25

---

## PART 2 -- P_400 VAULT ATTRIBUTION, SHADOW MODE (added 2026-07-25)

Scope grew during the session: Tony directed that P_020 stop relying on
the Tracker Dashboard as its only system-attribution source, since all
trade projects began routing through P_400 on 2026-07-24. Built the
read side and the measurement instrument; the cutover itself is gated
on upstream work (see BLOCKED ON below).

### WHY

Tracker coverage is structurally thin for the systems Tony actually
traded this week. Of 2,103 tracker rows, P_115/P_118 account for 86%;
P_116 has 19 rows, SNT 8, and DAY is not a category at all. All three
trades hand-classified on 2026-07-25 (MS -> P_116, XLK -> P_116,
EBAY -> SNT) fell through to TOS_Import for exactly that reason.

### FILES

| File | Action | Lines |
|---|---|---|
| `python\database\config.py` | modified | 113 |
| `python\database\vault_schemas.py` | NEW | 124 |
| `python\database\domain\system_resolver.py` | NEW | 141 |
| `python\database\infrastructure\vault_system_reader.py` | NEW | 177 |
| `python\database\application\system_attribution.py` | NEW | 73 |
| `python\database\application\ingest_pipeline.py` | modified | 275 |
| `tests\test_p020_system_resolver.py` | NEW | 197 |

Two mid-build plan corrections, both from bad line estimates:
- schemas.py additions were estimated +40, actual +101 -> would have hit
  330 lines, over the 300 hard limit. Extracted to `vault_schemas.py`.
- ingest_pipeline.py hit 323 lines for the same reason. Attribution
  orchestration extracted to `application/system_attribution.py`.

### DESIGN DECISIONS (Tony signed off 2026-07-25)

1. **Shadow mode.** `VAULT_SHADOW_MODE = True`. The vault result is
   computed and tallied but never written to `trades.system`. Tracker
   stays authoritative. Flipping to False requires a new work order.
2. **p115_linked / p300_linked are never used for attribution.** 189 of
   191 records carry `p300_linked: true` -- a schema default, not real
   attribution. Using them would produce confident wrong matches.
   Only `why_code` and `signal_source` are read.
3. **DROPPED records excluded.** Verified case: MS has a vault record
   dated 2026-07-02, BLOCKED/DROPPED, while the real MS fill was
   2026-07-23. Symbol-only matching would attribute a taken trade to a
   passed-on signal.
4. **Forward-only date window, 0..+7 days.** A signal cannot post-date
   its own fill, so the Tracker's symmetric +/-3 day walk is wrong here.
   Window is configurable; shadow data will calibrate it.
5. **Path via P_800's VAULT_FOLDER_MAP.** `from obsidian_writers.config
   import VAULT_ROOT, VAULT_FOLDER_MAP` -- verified importable in p140
   with no sys.path work. Survives the pending WO-P800-E3.003 rename.

### VERIFICATION

- [x] py_compile clean on all seven files
- [x] `tests\test_p020_system_resolver.py` -- 11 tests pass
- [x] Live reader probe against the real vault:
      191 records / 181 skipped on DROPPED / 10 covered / 0 attributed.
      MS covered=False (DROPPED filter working), MRCY covered=True
      (2026-07-23 SUBMITTED, inside window).
- [ ] Shadow tally observed on a real weekly ingest run (next Saturday)

### BLOCKED ON (upstream, not P_020)

Attribution will read 0% until P_400 populates a usable source field.
Filed separately -- P_400 needs a `signal_source` populated at evaluate
time from packet provenance, plus a lifecycle status that distinguishes
a real fill from an evaluated-and-dropped signal.

Note the design change from the original plan: Tony correctly rejected
"populate why_code from the locked WHY vocabulary." The system half of
that vocabulary (BTD/OIL/EXT/EZB/VPT/SNT/DAY) is structural provenance
P_400 already knows and should emit automatically; only the situational
half (ASYM/IFFY/LEARN/CROWDED/FOMO/REVENGE) is genuinely manual. The
reader checks `why_code` first then `signal_source`, so it works either
way once one of them is populated.

### IMMEDIATE VALUE BEFORE ATTRIBUTION LANDS

The `covered` index measures what fraction of P_020 fills have any
P_400 record inside the window -- i.e. whether everything is really
routing through P_400 since the 2026-07-24 switch, or whether trades
are still bypassing it. That number is readable now.
