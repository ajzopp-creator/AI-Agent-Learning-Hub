# WO-P020-E1.015 -- Extend Transaction Ingestion to IRA Account (9885)

**Status:** OWNER_DONE
**Owner:** P_020
**Affects:** P_020 (schwab_trade_pull.py, ingest_pipeline.py, cli.py import command,
db_writer.py, paper_import.py's ThinkLog join logic)
**Depends On:** none (hard blocker) -- soft-related to NEXT-1 (WHY/SIG vocabulary lock,
P_020_Future_Enhancements.md), not blocking since the ThinkLog parser is open-vocabulary
**Related:** WO-P020-E1.009 (balance pull), WO-P020-E1.011 (positions pull) -- both already
generalized to IRA9885 via `account_map`. BACKLOG-8 (Live Account ThinkLog Tagging,
P_020_Future_Enhancements.md) plans the same tag-join extension for AJZ6348 -- build the
shared join function once, use it for both if BACKLOG-8 lands first or alongside this.

---

## WHY

Balance and positions pulls (`cli.py balance --account IRA`, `cli.py positions --account IRA`)
already work for 9885 via the shared `account_map` lookup. Transaction ingestion did not --
`schwab_trade_pull.py` / `ingest_pipeline.py` / `cli.py import` only targeted `AJZ6348`.

Tony's call (2026-08-20): the IRA account holds investments and swing trades worth tracking.
Extend ingestion so 9885 transactions land in `trades`/`exits` like AJZ's do.

## DECISIONS (2026-08-20)

1. **System/tag source:** IRA bypasses `system_resolver.py` (vault/tracker/default priority)
   entirely -- same as paper. Tags come from ThinkLog: `INV` for generic investment/swing
   trades with no specific signal system, or an actual existing project code (e.g. `P_115`,
   `P_300`) if the trade genuinely traces to one. Parser is open-vocabulary already, so `INV`
   needs no code change to accept -- add it to the WHY vocabulary table in
   `docs\P_020_MASTER_SYSTEM_DOCUMENTATION_v1_0.md` Section 9.5 for documentation consistency.
2. **Date scope:** 1/1/2026 forward -- same rule as AJZ, not full available history.
3. **Tag flow:** Follow the same rules as paper -- Symbol+Date join (±3-day window, LIFO
   concat), `reason`/`signal_strength` populated from ThinkLog, not left NULL.

## SCOPE -- BUILT

- `schwab_trade_pull.py` (`python\api\P_020_Schwab_Trade_Pull.py`): accepts `--account IRA`
  (default `AJZ`, valid options `AJZ`/`IRA`/`BOTH` -- confirmed live 2026-09-02, argparse
  block + error message both present).
- `import_command.py`: `find_latest_pull()` maps `IRA` -> `inherited_roth` pull folder;
  `resolve_account_id()` maps `IRA`/`INHERITED_ROTH` -> `IRA9885` -- confirmed live
  2026-09-02, both mapping dicts read directly from source.
- **Tag join:** built into the same ThinkLog-join pipeline paper uses (Symbol+Date, ±3-day
  window, LIFO concat) -- exercised live 2026-08-30 for the manual IRA backfill (see
  Occurrence Log). `system_resolver.py`'s vault/tracker path is NOT used for IRA -- see next
  item.
- `system_attribution.py` (`python\database\application\`): `IRA_ACCOUNT_ID = "IRA9885"`
  constant + explicit `if account_id == IRA_ACCOUNT_ID:` bypass gate, confirmed live at
  line 118, 2026-09-02 (docstring: "IRA9885 bypasses the vault/Tracker/default layer
  entirely (WO-P020-E1.015 Decision 1) -- same as paper.").
- `db_writer.py`: writes with `account_id='IRA9885'`. Dedup key unchanged
  (`schwab_transaction_id`), account-agnostic -- same mechanism already proven on AJZ/PAPER.
- Date filter: 1/1/2026 forward confirmed -- earliest live IRA9885 `open_date` in the DB is
  2026-01-05 (COPX), no pre-2026 IRA rows present.
- No change to AJZ ingestion path, dedup logic, or `v_trade_summary` schema this WO.

---

## ACCEPTANCE CRITERIA -- STATUS

| Criterion | Status | Evidence |
|---|---|---|
| `cli.py import --account IRA` pulls, filters, consolidates, writes IRA txns (1/1/26 fwd), touches no AJZ6348 row | MET | Code path confirmed live 2026-09-02 (see SCOPE); non-IRA row count (620) unchanged by IRA presence, spot-checked same session |
| Trades carry `account_id='IRA9885'`; `reason`/`signal_strength` populated from ThinkLog, not NULL | MET | Live DB query 2026-09-02: 9/9 IRA9885 trades have `system=reason='P_117'`, `signal_strength='B'`; `NULL` count = 0 |
| `system_resolver.py` vault/tracker priority confirmed NOT invoked for IRA rows | MET | `system_attribution.py` line 118 bypass gate read directly, 2026-09-02 |
| Dedup via `schwab_transaction_id` confirmed safe to re-run | MET (by design) | Same dedup key/mechanism as AJZ/PAPER paths, no IRA-specific branch -- inherits existing proven behavior, not re-tested standalone this WO |
| Orphaned sells flagged in audit log, never dropped silently | MET (by design) | `_resolve_orphans_against_db()` in `import_command.py` is account-agnostic, takes `account_id` as a parameter -- same path IRA uses |
| `v_trade_summary` returns IRA rows correctly filtered by `account_id` | MET | Live query 2026-09-02: `SELECT COUNT(*) FROM v_trade_summary WHERE account_id='IRA9885'` = 9, matches `trades` table count |
| Compile-clean under p140 warnings-as-errors | MET | `python -W error::SyntaxWarning -m py_compile` on all 4 touched files, exit=0 across the board, 2026-09-02 |
| Independent review confirms the fix in a separate session before CLOSED | OPEN | This session (2026-09-02) rewrote the WO and re-verified against live code/DB, but it is the implementing/correcting session -- cannot self-certify independent review per WO_COMPLETION_GATE.md and Standing Rule (system-doc-initializer SKILL.md) |

All acceptance criteria are MET except the review gate itself, which structurally requires a
separate session.

---

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[x] Any new or changed shared-resource location reflected in P_000_SYSTEM_DOCUMENTATION.md / CLAUDE.md / skill files -- N/A, no shared-resource location changed this session
[x] CALLER PROPAGATION -- N/A, no function signatures changed this session (documentation-only correction against already-live code)
[x] IMPERATIVE SWEEP -- N/A, no rule/Must/Must-Not changed
[x] Downstream projects in Affects: notified -- Affects: is P_020-internal files only, no other project touched
[x] No sys.path side-channels introduced (ref WO-P000-E2.003) -- N/A, no code changes this session
[x] If schema/signal contract changed: version bumped, consuming projects notified -- N/A, no schema/contract change this session
[x] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\ -- none exist (confirmed via directory listing, 2026-09-02)
[x] One ledger entry per WO confirmed

---

## Occurrence Log

**2026-08-20** -- WO opened. Decisions locked same session (system/tag source = ThinkLog
`INV`-or-real-project-code, bypass `system_resolver.py`; date scope 1/1/26 forward;
Symbol+Date join same as paper). File set to PENDING, build not started.

**2026-08-22** -- Major closeout session for six unrelated WOs (E1.002/E1.007/E1.009/E1.011/
E1.014/E1.016) surfaced a dry-run bug: `--dry-run` only skipped `last_run.json`, DB writes
still committed regardless. Caught live -- **9 IRA9885 trades wrote to the DB without
approval** during that session's testing (trade_ids 3132-3140), tagged generic
`system='INV'`, `reason='INV'`, `signal_strength=NULL`. Dry-run bug fixed same session
(4 regression tests added). Side effect discovered: most of this WO's SCOPE was already
built as part of that fix -- `schwab_trade_pull.py` already accepted `--account IRA/BOTH`,
`import_command.py` already resolved IRA -> `IRA9885` + `inherited_roth` folder,
`system_attribution.py` already bypassed vault/Tracker for IRA9885 per Decision 1. Gap
identified: the 9 live IRA rows had `signal_strength=NULL`, failing this WO's own
Acceptance Criteria.

**2026-08-30** -- Tony's call: retag all 9 IRA9885 trades as `P_117` (external
recommendation) with real per-symbol descriptions (CNBC pundit, Schwab analysis, personal
bias, unknown, external post -- sourced from Tony's own notes) instead of leaving them
generic `INV`. Manual ThinkLog-format CSV built
(`data\tos_exports\live\P_020_IRA_ThinkLog_Manual_Backfill_2026-08-30.csv`), dry-run preview
showed all 9 matching cleanly, committed via `thinklog --account IRA9885 --commit`. All 9
verified `system=reason='P_117'`, `signal_strength='B'` same session. Gap from 8/22 closed.
WO file itself flagged as still needing a rewrite to match reality -- not done that session.

**2026-09-02** -- WO file rewritten this session to match actual live state. Every
Acceptance Criteria item independently re-verified against live code and live DB in this
session (not carried forward from memory of prior sessions) -- see Acceptance Criteria table
above for each item's evidence. Status PENDING -> OWNER_DONE. Independent Review still
required in a separate session before CLOSED, per WO_COMPLETION_GATE.md and this skill's
Standing Rule -- the session that corrects/verifies a WO's own state cannot certify its own
independent review.

---

**Created:** 2026-08-20 (Tony's request, P_020 session)
**Updated:** 2026-09-02 -- rewritten to reflect actual built state (see Occurrence Log); all
Acceptance Criteria live-reverified same session; Status PENDING -> OWNER_DONE
**Author:** Claude
