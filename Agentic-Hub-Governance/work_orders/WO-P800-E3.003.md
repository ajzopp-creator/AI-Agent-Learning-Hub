# WO-P800-E3.003 -- Rename TradeManagement -> TradeOrderManagement (Vault Folder Taxonomy)

**Status:** CLOSED (2026-07-25) -- P_800, P_400, and P_300 sides all done. All four Acks landed (P_115/P_020 N/A, P_400 done, P_300 done). See P_300-Side Implementation below for the last piece.
**Owner:** P_800
**Affects:** P_115, P_300, P_400, P_020
**Depends On:** none
**Acks:**
  P_115: N/A -- no code references old path
  P_300: done (2026-07-25) -- daily_evaluate_pipeline.py + test fixture fixed, PEH-verified live (see P_300-Side Implementation below)
  P_400: done (2026-07-25) -- config.py BOOK_DIR/PAPER_BOOK_DIR fixed, regression test flipped, PEH-verified live (see P_400-Side Implementation below)
  P_020: N/A -- already resolves path dynamically via P_800 config, no change needed
**Related:** WO-P400-E2.019 (paper/real routing -- uses current TradeManagement/P400
  path for now, not blocked on this WO)

---

## WHY

Tony's call: `TradeManagement` (no "Order") is the wrong name for the vault
namespace holding P115/P300/P400/P020 lifecycle notes. It should be
`TradeOrderManagement`, consistent with the P_400 project name and the
existing `TradeOrderManagement\signals\` folder.

Note this reverses the direction WO-P400-E2.012 (2026-06-17) concluded --
that WO found P_400's own config.py pointed at the wrong root
(`TradeOrderManagement\P400`, a dead folder) and corrected it to match the
already-live `TradeManagement\P400` used by P_800's VAULT_FOLDER_MAP. This
WO is not reopening that defect -- E2.012's diagnosis was correct given the
naming at the time. This WO changes the naming itself, hub-wide, by
deliberate decision.

## CURRENT STATE

`trading_journal\TradeManagement\` contains:
  _archive\, P020\, P115\, P300\, P400\
`trading_journal\TradeOrderManagement\` contains:
  signals\ (raw SIGNAL_V2/P400SIG JSON packets -- correctly named today)
  P400\ (pre-cutover archive only, per E2.012 disposition -- not live)

Live write path today: obsidian_writers/config.py `VAULT_FOLDER_MAP` maps
P115/P300/P400/P020 all to `TradeManagement/<project>`.

## SCOPE (proposed, not yet applied -- needs Tony sign-off on approach
before any file moves)

1. Physically move `TradeManagement\{P115,P300,P400,P020,_archive}` into
   `TradeOrderManagement\` (folder rename/merge -- `TradeOrderManagement`
   already exists with `signals\` and the old `P400\` pre-cutover archive
   in it, so this is a merge, not a clean rename).
2. `obsidian_writers/config.py` -- update all four `VAULT_FOLDER_MAP`
   entries (P115, P300, P400, P020) from `TradeManagement/...` to
   `TradeOrderManagement/...`.
3. P_400's own `config.py` -- `BOOK_DIR` currently hardcodes
   `TradeManagement/P400` independently of P_800's map (not derived from
   it). Needs the same update, plus `PAPER_BOOK_DIR` (see WO-P400-E2.019).
4. Audit P_115, P_300, P_020 project code for any hardcoded
   `TradeManagement` path references outside the shared config (same class
   of bug E2.012 found -- projects sometimes carry their own copy of a path
   instead of reading P_800's map).
5. Obsidian-side: check `Bases\*.base` files (e.g. `P020_Performance.base`)
   and any dataview queries for hardcoded `TradeManagement` folder
   references that would break silently on move.
6. Decide disposition of `TradeOrderManagement\P400\_pre_cutover_archive\`
   (pre-existing folder at the merge target) -- collision check before
   move.

## OUT OF SCOPE

- Any change to `TradeOrderManagement\signals\` -- already correctly named,
  not touched by this WO.
- WO-P400-E2.019's paper-book routing fix -- proceeds now against the
  current `TradeManagement/P400` root; will follow this WO's rename when it
  lands, not block on it.

## ACCEPTANCE CRITERIA

- All four VAULT_FOLDER_MAP entries point at `TradeOrderManagement/...`.
- No live files left behind in old `TradeManagement\` paths (verified
  empty or explicitly archived).
- A live evaluate() + record write for a real symbol lands in the new path
  and is read back correctly by book_loader.py (P_400 heat/exposure gate
  regression check, same class of verification as E2.012).
- Obsidian vault opens with no broken links/Bases queries referencing the
  old path.
- P_115, P_300, P_020 each confirm (Ack) their own code has no orphaned
  hardcoded `TradeManagement` references left over.

---

**Created:** 2026-07-21 (P_400 session, Tony's request)
**Logged by:** Claude

## P_800-Side Implementation -- 2026-07-25

Files changed (archived before edit):
- `obsidian_writers\config.py` -- archived to `config_backup_2026-07-25_WO-P800-E3.003.py` first.
  All 5 VAULT_FOLDER_MAP entries (P115, P300, P400, P400_PAPER, P020) updated from
  `TradeManagement/...` to `TradeOrderManagement/...`. Verified by reading back
  lines 64-70 post-edit.

Vault folder merge (robocopy /E /MOVE, source removed after successful copy):
- `TradeManagement\P115` (1483 + audit\2) -> `TradeOrderManagement\P115` -- exit 1 (OK)
- `TradeManagement\P300` (404) -> `TradeOrderManagement\P300` -- exit 1 (OK)
- `TradeManagement\P400` (191 + paper\1) -> `TradeOrderManagement\P400` -- exit 3
  (OK; merged into existing `_pre_cutover_archive\` (1) and `_pre_cutover_archive\paper\`
  (2) with zero filename collisions, confirmed by pre-move collision check)
- `TradeManagement\P020` (202) -> `TradeOrderManagement\P020` -- exit 1 (OK)
- `TradeManagement\_archive\P020_pre_tradeid_fix` (190) -> `TradeOrderManagement\_archive\P020_pre_tradeid_fix` -- exit 1 (OK)

Verification: post-move recursive file count under old `TradeManagement\` = 0.
Post-move totals under `TradeOrderManagement\` reconcile exactly against pre-move
counts (2473 files moved, 0 lost). Old `TradeManagement\` left as empty shell
(not deleted -- robocopy removed now-empty subfolders automatically; root + `_archive\`
remain).

Bases (.base files, all six) grepped pre-move for hardcoded `TradeManagement` --
zero hits, resolve dynamically, no changes needed or made.

Historical `signal_source_link` values inside already-written vault notes and
`TradeOrderManagement\signals\{processed,rejected}\*.json` were left untouched
(point-in-time record of path at write time, not a live pointer).
`trading_journal\.obsidian\workspace.json` (Obsidian's own open-tabs cache) also
left untouched -- cosmetic, self-heals on next vault interaction.

Not touched (outside P_800 boundary -- see Still Open):
P_400's `python\config.py`, P_400's `python\tests\test_p400_known_bugs.py`,
P_300's `python\application\daily_evaluate_pipeline.py`.

## Still Open

1. ~~**P_400** -- config.py BOOK_DIR/PAPER_BOOK_DIR~~ -- DONE 2026-07-25, see
   P_400-Side Implementation below.
2. ~~**P_400** -- test_p400_known_bugs.py assertion flip~~ -- DONE 2026-07-25, see
   P_400-Side Implementation below.
3. ~~**P_300** -- daily_evaluate_pipeline.py line 437 hardcoded path~~ -- DONE
   2026-07-25, see P_300-Side Implementation below.
4. ~~**P_300** -- test_signal_emitter_dry_run.py fixture~~ -- DONE 2026-07-25,
   see P_300-Side Implementation below (turned out to be more than a fixture
   string -- real stale-API bug found and fixed too).
5. **P_800** (lower priority, deferred) -- own test files still reference old path:
   `python\tests\smoke_test_pbf_2026_01_21.py`, `python\tests\test_p115_write.py`,
   `python\tests\test_signal_v2_e2e.py` (gate test -- review test logic before editing,
   not a blind string swap). Not fixed this session -- out of the scope Tony approved.
6. **P_800** (lower priority, deferred) -- `shared_resources\chaikin_enrichment\`
   test fixtures (`test_note_reader.py`, `test_run_chaikin_batch.py`) reference old
   path in fixture data. Package's own config.py already resolves dynamically, so this
   is test-fixture hygiene only, not a functional risk.
7. **P_000** -- Section 7.1 of P_800_SYSTEM_DOCUMENTATION.md still shows the old
   `TradeManagement\` folder tree; needs the same correction pass as WO-P000-E9.001.
   Not fixed this session -- doc edits weren't in the approved scope.

## P_400-Side Implementation -- 2026-07-25

Files changed (archived before edit, same pattern as P_800-side):
- `python\config.py` -- archived to `config_backup_2026-07-25_WO-P800-E3.003.py`.
  `BOOK_DIR` and `PAPER_BOOK_DIR` (lines 24-25) updated from `TradeManagement/P400`
  to `TradeOrderManagement/P400`; comments updated to cite this WO.
- `python\tests\test_p400_known_bugs.py` -- archived to
  `test_p400_known_bugs_backup_2026-07-25_WO-P800-E3.003.py`. Assertion in
  `test_e2012_book_dir_not_dead_folder` flipped: now requires
  `"TradeOrderManagement" in parts and "TradeManagement" not in parts`.

Verification: PEH run via `Agentic-Hub-Governance\verify\run_this.py` (Tony's
terminal, p140). Confirmed `BOOK_DIR` = `trading_journal\TradeOrderManagement\P400`
with 190 `.md` records on disk, `PAPER_BOOK_DIR` exists at the `\paper` subfolder.
Full `test_p400_known_bugs.py` suite: 8/8 passed, including the flipped
`e2012_book_dir_not_dead_folder`. No regressions in the other 7 known-bug guards
(E2.007 x2, E2.012 field remap, E2.013, E2.014, E2.018, E3.005).

## LOOP -- CLOSED 2026-07-25

All three sides (P_800, P_400, P_300) done, all four Acks landed. Condition for
CLOSED stated at the top of this section is now met.

## P_300-Side Implementation -- 2026-07-25

Files changed (backed up before edit, same pattern as P_800/P_400 sides):
- `application/daily_evaluate_pipeline.py` -- backed up to
  `daily_evaluate_pipeline_backup_2026-07-25_WO-P800-E3.003.py`. Line 437
  `signal_source_link` f-string: `TradeManagement/P300/` ->
  `TradeOrderManagement/P300/`. Single string swap, no logic change.
- `test_signal_emitter_dry_run.py` -- backed up to
  `test_signal_emitter_dry_run_backup_2026-07-25_WO-P800-E3.003.py`.

**Real bug found during PEH verification, unrelated to the path rename:**
the test still called `emit_signal_packet()` with the old v1.0 signature
(`vault_root=...`, no `chosen_horizon`) -- stale since the emitter moved to
v2.0/SIGNAL_V2 back in June, so the test had been silently broken (or
silently not run) since then. Fixed in the same pass: call updated to the
real v2.0 signature (`chosen_horizon=10`), `write_to_vault` monkeypatched
instead of a live `vault_root` write, validated against `SignalV2` from
`shared_resources.python_utils.signal_schemas` instead of the old local
`SignalPacket`/`schemas_signal_packet`. New assertion added on
`signal_source_link` itself (`== "trading_journal/TradeOrderManagement/P300/2026-06-07_COHR.md"`)
-- the old test never actually checked that field's value, only used it as
fixture input.

**PEH verification, real, 2026-07-25:** 4/4 steps PASS. Syntax + import
clean. Exact-string check on the live pipeline file: 0 old-path hits, 1
new-path hit. Full `test_signal_emitter_dry_run()` re-run for real after
the v2.0-signature fix -- all 7 internal steps PASS including the new
`signal_source_link` field assertion, full JSON packet dumped and
inspected. No live vault or catalog write (TemporaryDirectory + monkeypatch
throughout).