# WO-P000-E19.001 -- Protocol F4: Git Session-End Reminder (system-doc-initializer)

**Status:** CLOSED -- Independent Review completed 2026-09-01 (separate session, see block below).
**Owner:** P_000
**Affects:** All Hub projects (system-doc-initializer runs at every session's INIT, Hub-wide)
**Depends On:** None
**Acks:**

---

## WHY

While answering a question about WO-P000-E2.001's pending downstream Acks,
Tony asked whether the recurring "N days of uncommitted work" gap
(2026-08-04 gap check, recurring again by the 2026-08-07 Independent
Review) traces to incomplete INIT processes. Investigation confirmed yes:

`GIT_WORKFLOW.md` (WO-P000-E2.001's own deliverable) prescribes a
status -> stage -> commit -> push routine at every session's end. Checked
the three places a session-close reminder would actually fire:
- `P_000_SYSTEM_DOCUMENTATION.md` -- zero mentions of git/commit/push.
- `WO_COMPLETION_GATE.md` -- zero mentions.
- system-doc-initializer Protocol F (F1 Lessons, F2 State, F3 Status-Claim)
  -- none of the three touches git.

Same failure shape P_115's SIP v3.6 already named for `lessons.md`: a
written instruction is not the same as an executed step. The workflow doc
existed; nothing ever invoked it.

## FIX

Added **Protocol F4 -- Git Session-End Reminder** to system-doc-initializer
SKILL.md, alongside F1/F2/F3. Fires every session at close, unconditionally
(not gated on F1/F2 findings, since nearly every session touches files).
Since Claude can't run git via windows-mcp (credential-helper conflict,
same as every other git touchpoint in this Hub), the protocol's job is
narrow: surface a mandatory close-out line telling Tony to run his own
`GIT_WORKFLOW.md` session-end steps. No new mechanism invented -- reuses
the existing doc, just wires an actual trigger to it.

Logged as EC-008 in system-doc-initializer's own Error Corrections Log.

## COMPRESSION PASS (same edit, per standing instruction)

Tightened EC-006's lead sentence (removed quoted-dialogue verbosity,
kept root cause + fix intact) and confirmed no operational step, HALT
condition, path, or functional content was touched elsewhere in the file.
Net: +1,103 chars (new F4 protocol + EC-008 entry + footer note) against
-~180 chars removed from EC-006 -- a real net addition, since the ask was
a new protocol, not a pure compression pass. Full-file compression audit
(all Steps 0-5, Edge Cases, Standing Rule) found no other redundant
passages safe to cut without losing functional content -- prior compression
passes (v3.0, 2026-06-12; table-tightening, 2026-07-13) had already done
that work.

## ACCEPTANCE CRITERIA

- [x] Protocol F4 added, same structure/format as F1-F3 (Trigger + rule +
      EC reference).
- [x] EC-008 entry added above EC-006 (newest-first order preserved).
- [x] Footer "Last Updated" chain updated, no prior history removed.
- [x] No operational step, HALT condition, path, or functional content cut
      anywhere in the file (verified: full post-edit read, diffed against
      backup).
- [x] Live skill reflects v-next -- Tony confirmed re-upload via
      Customize -> Skills, 2026-08-29 (same session). Cannot be verified
      from inside this session either way -- skill content loads once at
      conversation start, so a same-session upload never shows here
      regardless of success (same limitation as WO-P000-E12.001's
      OUTSTANDING note). A fresh session's INIT loading F4/EC-008 is the
      actual confirmation; flagged for Independent Review to check.

---

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths -- `.claude\skills\system-doc-initializer\SKILL.md`
[x] Any new or changed shared-resource location reflected -- N/A, no new
    location, existing skill file edited in place
[x] CALLER PROPAGATION -- N/A, no function/API signature changed; this is a
    procedural protocol addition
[x] IMPERATIVE SWEEP -- the new rule IS the imperative text (Protocol F4
    itself), not a changelog-only note
[x] Downstream projects in Affects notified -- Affects is Hub-wide (every
    project's INIT runs this skill); this WO + the skill's own EC-008 entry
    are the notification, same passive-ledger pattern as every other
    Hub-wide WO
[x] No sys.path side-channels introduced -- N/A, no code
[x] No schema/signal contract changed -- N/A
[x] DRAFT files for this WO deleted -- none created
[x] One ledger entry per WO confirmed -- this file only
[x] Independent Review -- COMPLETED 2026-09-01. See INDEPENDENT REVIEW block below.

---

## INDEPENDENT REVIEW -- 2026-09-01 (separate session from 2026-08-29 build)

This WO's own Acceptance Criteria named the exact test: "A fresh session's
INIT loading F4/EC-008 is the actual confirmation." This session is that
fresh session -- its INIT read system-doc-initializer\SKILL.md live at
session start (2026-09-01) and Protocol F4 (Git Session-End Reminder) and
EC-008 were both present and applied (F4's close-out line was surfaced in
this session's own INIT response). Backup file SKILL.md.backup_2026-08-29_181659
confirmed present on disk. No operational content found missing against
F1-F3 structure.

Verdict: PASS. Closing.

---

Backup taken before edit: `SKILL.md.backup_2026-08-29_181659`.

*Filed and built same session, 2026-08-29, per Tony's direct instruction
("do it compress and make more efficient when changing skill").*
