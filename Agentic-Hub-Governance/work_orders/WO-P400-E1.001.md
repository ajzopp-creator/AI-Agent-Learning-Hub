# WO-P400-E1.001 -- P_400 v2.0 Signal Reader
**Status:** CLOSED
**Owner:** P_400
**Affects:** (consumes P_800 shared contract)
**Deliverable:** P_400 reader that ingests SIGNAL_V2 JSON packets from trading_journal\TradeOrderManagement\signals\*_v2.0.json, importing the canonical model via "from shared_resources.python_utils.signal_schemas import SignalV2" (NOT from P_800 internals). Validates each packet against SignalV2; rejects malformed packets (no repair). Feeds the three-gate sizing process (P_400/P_000). Dual-read aware: also tolerates legacy *_signal.json (P400SIG) during the 2-week compat window, drops legacy handling at cutover.
**Verified:** 2026-06-09 (p140) -- reader built; test_read_signals.py 4/4; cli.py reads 18 live v2 packets, 0 rejected, 1 legacy tolerated; SignalV2 imported from shared_resources. Re-verified 2026-06-11 after WO-P800-E2.003 field rename: 4/4 pass, 34 valid, 0 unexpected rejects, 1 legacy tolerated
**Depends On:** WO-P800-E2.002 (must reach OWNER_DONE first -- supplies the shared import) [SATISFIED 2026-06-09: WO-P800-E2.002 OWNER_DONE + Verified, 5/5 pass; unblocked by P_400 session]
**Acks:** N/A (no downstream consumers of P_400 reader)
WHY:
The signal packet is a cross-project contract: producers (P_115/P_300) -> router
(P_800) -> consumer (P_400). The reader must import SignalV2 from the neutral
shared_resources location so P_400 never reaches into P_800's package internals.
That shared location is created by WO-P800-E2.002, so this reader is correctly
parked behind it -- building first would bake in the bad P_800-internal import.

SCOPE (P_400 session owns the build; this record tracks the dependency only):
  1. Reader module under P_400 that scans TradeOrderManagement\signals\ for
     *_v2.0.json, parses + validates each against SignalV2.
  2. Import contract: from shared_resources.python_utils.signal_schemas import SignalV2
  3. Malformed packets rejected (log + skip), per locked design decision #3.
  4. Stock asset_class: position_size sentinel handled by three-gate sizing
     downstream (P_400/P_000), not trusted from the packet.
  5. Compat window: tolerate legacy *_signal.json until cutover, then drop.

VERIFY (at P_400 build time):
  - Reader parses the existing _v2.0.json packets in signals\ (19 present at
    2026-06-08) without error.
  - Malformed packet is rejected, not repaired.
  - SignalV2 import resolves from shared_resources, not from obsidian_writers.

LOOP:
  Build waits on WO-P800-E2.002 -> OWNER_DONE. When P_400 adopts the shared
  import and the reader passes verify, set this WO Verified -> OWNER_DONE, then
  set Ack=done on WO-P800-E2.002 (and E2.001) to close the chain.

BLOCKED BY:
  WO-P800-E2.002 (shared signal-packet contract) -- not yet OWNER_DONE.

Created 2026-06-08 (P_800 session) by Claude, cross-project WO owner.
Tracking record only -- the P_400 reader is built in a P_400 session, not here.
Canonical copy: 04-Shared-Resources\work_orders\WO-P400-E1.001.md