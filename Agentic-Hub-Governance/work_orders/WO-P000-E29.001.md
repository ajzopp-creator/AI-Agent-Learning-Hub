# WO-P000-E29.001 — Wire Boot-File Awareness into system-doc-initializer (and Revisit Questions 1, 3, 4)

**Status:** IN_PROGRESS -- Step 0.6 live in the app since 2026-09-11. First real FRESH hit (P_115, 2026-09-14) exposed a gap (EC-010), fixed same day as Step 0.6 item 3b. Scope extended 2026-09-25 (Tony-approved): startup script now names each boot session and imports it into Desktop's Code tab (see 2026-09-25 section). Remaining before OWNER_DONE: one clean morning run of the updated startup script, plus a live INIT confirming amended Step 0.6/3b on a FRESH hit. Independent Review (separate session) after that.
**Owner:** P_000
**Affects:** system-doc-initializer SKILL.md (hub-wide), and by extension every project that runs INIT via a live Desktop/claude.ai session: P_115, P_300, P_400, P_800, P_010, P_020, P_025, P_820
**Depends On:** WO-P000-E28.001 (CLOSED 2026-09-11 -- built the boot files this WO makes system-doc-initializer aware of)
**Blocker:** none

---

## Background

WO-P000-E28.001 built and piloted a working morning startup: the WO scanner, then scoped headless Claude Code per daily project, each writing a `P_<ID>_Boot.md`. That pilot succeeded and was verified independently. But it only built the *producer* side. The *consumer* side was named as an explicit, deliberate gap in E28.001 rather than folded in:

> "system-doc-initializer SKILL.md has not been touched. The boot files this WO produces are not yet consumed by anything -- if Tony opens Claude Desktop and starts a live P_115 session right now, system-doc-initializer runs its old Steps 0-5 with no awareness that a fresh P115_Boot.md already exists."

Surfaced concretely on 2026-09-10 when Tony noticed the boot-file-based workflow wasn't producing the usual suggested-chat-name heading. That specific symptom got a narrow, immediate fix in E28.001 (the orchestrator script now writes the heading into the boot file itself, deterministically). This WO is the real fix underneath: making the live-session skill actually aware boot files exist, rather than patching each individual symptom of it not knowing.

E28.001 also left three of its original five open questions unresolved -- folded into this WO's scope rather than left orphaned:

1. Of system-doc-initializer's current steps -- Step 0 runtime check, Step 0.5 project ID, Step 1 system doc load, Step 1b daily gate check, Step 2 four-section load, Step 3 chat name, Step 4 apply params, Step 5 Protocols A-F -- which are mechanical/scriptable (now proven out by the boot-file pilot) and which genuinely require a live session's judgment?
3. Does per-project SIP-style INIT need to run every single session, or only when something's actually changed since that project's last INIT -- a staleness gate?
4. Should the Section 2 four-section system-doc load (1.5 Definitions, 3.4 AI Behavior Rules, 6 Error Corrections, 11.4 Parameter Registry) be trimmed now that WO status is offloaded to the scanner?

(Question numbering kept consistent with E28.001 -- Question 2 and 5 were answered there and aren't repeated here.)

## What This WO Would Need to Decide, Once Prioritized

- **Detection:** how does a live session know a boot file is fresh vs. stale? Likely an mtime check against session start, same pattern the orchestrator itself already uses to verify boot files -- but needs a concrete rule (how fresh is "fresh enough" for a live session to trust it).
- **Behavior on hit:** if a fresh boot file exists, does system-doc-initializer skip straight to displaying it (fastest), or still run a lighter verification pass before trusting it (safer, slower)?
- **Behavior on miss:** falls back to today's Steps 0-5 unchanged -- this WO shouldn't remove that path, only add a faster one in front of it.
- **Questions 1, 3, 4 above** feed directly into how much of Steps 0-5 survive this rewrite at all.

None of this is decided yet -- this WO is a placeholder for the scope, not the scope itself.

## Scoping (2026-09-11)

Answering the Detection/Behavior questions and folded-in Questions 1/3/4, based on today's P_115 boot file (`P115_Boot.md`, correct heading confirmed on disk) and E28.001's own gap note.

**Detection -- fresh vs. stale.** A live session can't reuse the orchestrator's `$runStart` mtime check verbatim (no equivalent clock at INIT time in a live chat). Proposed rule: a boot file is fresh if (a) its mtime is from the current calendar day, AND (b) no WO file for that project has a LastWriteTime newer than the boot file's. (b) catches the case where a separate session closed or updated a WO after the boot ran, which (a) alone would miss.

**Behavior on hit.** If fresh by both checks: skip straight to displaying the boot file's content (WO table, flags, next step) plus the Suggested chat name heading it already contains -- fastest path, the whole point of this WO. Still run Step 0 (live env/runtime check) every session regardless -- that's about THIS session's own MCP connection, not something a headless run minutes earlier can attest to (EC-009 precedent: client switches happen mid-session with no signal).

**Behavior on miss.** Unchanged -- falls back to today's Steps 0-5 in full. This WO adds a faster path in front of that, doesn't remove it.

**Q1 -- mechanical vs. live-judgment.** Boot-file-coverable: Step 0.5 (project ID -- the matched boot filename effectively confirms it), Step 1b (daily gate check -- already close to the scanner's job), Step 3 (chat name -- already done, confirmed working on disk today). Stays live: Step 0 (this session's own env check, not transferable), Step 1/2 (system doc's 4 sections -- Definitions, AI Behavior Rules, Error Corrections, Parameter Registry -- none of which a project boot file covers, see Q4), Step 4 (apply params -- ongoing behavior, not a one-time step), Step 5 (Protocols A-F -- ongoing behavior for the whole session, not an init-time check).

**Q3 -- run every session or staleness-gated?** Gate it: on a fresh-boot hit, skip the full re-derivation and go straight to Step 0 + display. Only fall through to full Steps 1-2 if the boot is missing/stale, or if the session's actual task needs system-doc detail the boot file doesn't carry (e.g., touching Python architecture rules, parameter registry values) -- lazy-load rather than always front-loading all 4 sections.

**Q4 -- trim the 4-section load?** No -- these were never WO-status content, so scanner offload doesn't touch them. They're the live-judgment layer (definitions, MUST/MUST NOT rules, error corrections, exact parameter values) that no boot file or scanner run produces. One real trim candidate flagged, not decided: Section 6 (Error Corrections Log) is now 11 entries and growing -- could filter to "recent + still-relevant" instead of "all" at load time. Separate scope, not deciding here.

**Not yet decided:** exact SKILL.md wording/structure for the new boot-file-check step this implies, and where it slots relative to existing Step 0/0.5. Needs the actual revision pass.

## Build (2026-09-11)

`system-doc-initializer` SKILL.md revised on disk: `C:\Users\Trader\AI-Agent-Learning-Hub\.claude\skills\system-doc-initializer\SKILL.md` -- new Step 0.6 (Boot-File Check) inserted between Step 0.5 and Step 1, footer "Last Updated" line updated. Backup at `.claude\skills\system-doc-initializer\backups\SKILL.md.backup_2026-09-11_093200`. Disk edit only -- per Protocol E, this does NOT update the live skill Claude Desktop actually runs; Tony must re-upload via Customize -> Skills -> system-doc-initializer -> edit/replace before it takes effect.

Not done yet: a live INIT dry run against a project with a genuine fresh boot file (P_115 or P_300 both qualify as of this morning) to confirm Step 0.6 actually fires and behaves as scoped, before OWNER_DONE. Independent Review (separate session) still required after that, per standard WO lifecycle.
## Live Evidence + Amendment (2026-09-14)

Recorded 2026-09-25 from the live skill's own EC-010 entry -- this WO was not updated at the time. Tony re-uploaded the revised SKILL.md via Customize -> Skills (Step 0.6 is present in the skill Desktop runs). A live P_115 session on 2026-09-14 hit a FRESH boot file, so Step 0.6 fired for real -- that counts as the first live dry run, and it failed in one respect: the FRESH path skipped the project's session header/summary (balance, risk mode, trading mode, strategies active) and its tasks\lessons.md read, and a known 8/18 lesson recurred on a live signal call. Fixed the same day as Step 0.6 item 3b (FRESH hit still requires the lessons read plus a live-read header/summary). The amended 3b behavior has NOT yet been confirmed on a FRESH hit.

## Desktop Session Import (2026-09-25)

Problem (Tony, 2026-09-25): even with boot files, every morning still meant opening a new chat per project and asking it to read the boot file. Goal: open Desktop and find each project's session already there, under the standard chat name.

Findings, tested live 2026-09-25 (P_000 session):
- Claude.ai chats cannot be created or named from a script (no public API). Claude Code sessions can be imported into Desktop's Code tab with the deep link claude://resume?session=<session_id>; the session_id is already in each headless run's JSON stdout log.
- Test 1: imported today's 05:32 P115 boot run -> landed in the Code tab with full history, resumable, but titled "General coding session" / "Untitled".
- Test 2: claude -p "reply PONG" --name "P_115 TEST Friday, September 25, 2026" (Claude Code 2.1.282) -> transcript carried customTitle; imported via claude://resume -> Desktop showed the name. Confirmed by screenshot.
- The "Unable to move the cache: Access is denied" lines printed when the link fires are cosmetic (a second Desktop process handing the link to the running one).
- The 05:32 P115 boot run skipped tasks\lessons.md "to save budget" at $0.31 of its $0.50 cap -- the cap did not bind; it was the model's choice. Fix is in the prompt, not the budget.

Built (Tony approved 2026-09-25), P_000_StartUp_ClaudeDesktop.ps1, 186 -> 220 lines, parse errors 0, ASCII-only, backup at utils\P_000_StartUp_ClaudeDesktop.ps1.backup_20260925_062330. Diff vs backup: 3 lines replaced, 34 added, nothing else touched.
1. Standard chat name (P_115 Friday, September 25, 2026 05:32 ET) built before launch, passed via --name, and reused for the boot file heading so the two always match.
2. Boot prompt now requires reading the project's tasks\lessons.md and listing still-relevant lessons.
3. session_id captured from each run's JSON log.
4. New Import-BootSessions function: after Desktop launches, waits for its process, then fires claude://resume once per OK run (failed runs are not imported) and reports each import in the summary.

Not yet verified: a full morning run of the updated script end to end.

P_010 hookup (Tony approved 2026-09-25): new on-demand scheduled task P_000_Morning_Boot (no trigger, RunLevel Limited, interactive, 45-min limit) runs the startup script unelevated. P_010_daily_posture.bat gained STEP 5, after STEP 4, which fires that task via schtasks /run unless today's verify\startup_logs_YYYYMMDD_* folder already exists (once per day; midday MCP-wrapper re-runs skip), non-blocking and non-fatal, and fires even when MORNING_RUN_FAILED.flag is present (the boot files surface that failure). Routed through a separate task because P_010_Daily_Posture_930AM runs elevated (RunLevel Highest, logon trigger) -- launching Desktop from it would open Desktop as admin, and an admin/non-admin claude://resume handoff is untested. Bat filename unchanged (Task Scheduler references it). Direction agreed same day: work in pre-built Code-tab sessions for a few mornings, log every Chat-side gap, then file one WO for the full Code migration scoped from real gaps.

## Next Steps

1. DONE 2026-09-11 -- prioritized; Status moved PENDING -> IN_PROGRESS; scoping section above.
2. Revise system-doc-initializer SKILL.md (version bump) per the scoping decisions above, with a live INIT dry run before calling it OWNER_DONE -- same rollout discipline as prior skill revisions (P_300 SIP v3.3 precedent, E21.001/E28.001 pilot-then-verify precedent). DONE -- revision landed 2026-09-11 (see Build), amended 2026-09-14 (see Live Evidence + Amendment).
3. Run the updated P_000_StartUp_ClaudeDesktop.ps1 on a normal morning; confirm each OK project appears in Desktop's Code tab under its standard name and each boot file lists lessons.
4. Live INIT on a FRESH-boot project confirming Step 0.6 item 3b (lessons read + live header/summary). Then OWNER_DONE with Completion Gate block, then Independent Review in a separate session.

---

*Created 2026-09-10, P_000 session. Filed at Tony's explicit instruction -- "create WO now, I'll prioritize it later" -- rather than left as a named-but-unfiled gap in WO-P000-E28.001. Drafting: Claude. Review: Tony.*
