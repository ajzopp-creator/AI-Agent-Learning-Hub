# WO-P400-E6.001 -- Obsidian Performance Data: Dead Bases Paths + Order Lifecycle Never Closes the Loop

**Status:** IN_PROGRESS -- dead-path Bases fix applied 2026-08-11 (see FIX APPLIED below). Scope item 2 code build complete 2026-09-07 (see BUILD COMPLETE below, 13 files across P_020/P_400/shared_resources/P_800, all PEH-validated). Remaining Gate 1 (real migration run) passed live 2026-09-08 -- orders table + 4 indexes created against production P_020_trades.db, 23 columns verified. Gate 2 (real end-to-end reconcile) run live 2026-09-08 against the paper/TOS statement path -- SLS correctly matched and correctly declined to promote (documented MKT-fill parser gap, not a bug), SPY (options) surfaced and fixed a real 100x contract-multiplier bug in order_reconciler.py and db_order_writer.py, ref EC-010, live-verified twice (realized_pnl $3.00, exit_price $3.13). Live-API path (reconcile --account AJZ) not yet run -- see Remaining Gates below. Still open: Gate 2's live-API leg, Independent Review, permanent-tests decision.
**Owner:** P_400
**Affects:** P_020 (Bases/P020_Performance.base shares the same dead-path bug; recommended fix extends P_020's SQLite-as-source-of-truth pattern to P_400 orders), P_800 (owns Bases/ and the vault rename that caused this -- WO-P800-E3.003)
**Depends On:** none

---

## WHY

Tony asked (2026-08-11, P_020 session) for a plan to get real performance data
out of Obsidian for both paper and live Orders, since P_400 is the
authoritative Trade Order Management layer. Investigation surfaced two
separate problems, one small and immediate, one structural.

## FOUND LIVE -- 2026-08-11

**1. Both performance Bases are silently broken.** `Bases/P400_Trades.base`
and `Bases/P020_Performance.base` both filter on
`file.folder contains "TradeManagement/..."`. That folder was renamed to
`TradeOrderManagement/...` hub-wide on 2026-07-25 (WO-P800-E3.003).
`TradeManagement` no longer exists on disk (confirmed via
`obsidian_list_files_in_dir` -- 404). Both Bases have been returning zero
rows since the rename, 17 days, unnoticed -- neither P_400's nor P_020's
skill file logged this as a bug because the rename WO's own completion
never checked Bases/ as a caller of the old path.

`P400_Trades.base` additionally sorted on a `date` field that does not
exist in the P400 note schema (`signal_date`/`run_date`/`entry_date`/
`close_date` do; `date` does not) -- a second, independent defect in the
same file.

**2. P_400's order notes never close the loop.** Sampled records across
`TradeOrderManagement/P400/`, `/paper/`, `/P115/`: `lifecycle_status`
stops at `SUBMITTED` or `DROPPED` permanently. `entry_date`, `close_date`,
`realized_pnl` are null in every sampled record, including ones from
June. Nothing in P_400 ever writes back once an order fills or closes --
there is no reconciliation step. P_020 has the equivalent closed loop
(SQLite -> vault export with real `realized_pnl`/`realized_R`/`outcome`)
but only for the live account's Schwab-sourced trades; it has no
visibility into P_400's council verdicts, why/sig tags, or paper-account
orders.

Net effect: the performance data Tony asked for does not exist yet for
P_400-originated orders, live or paper, regardless of the Bases bug --
fixing the Bases path alone would just render an accurate table of nulls.

## FIX APPLIED -- 2026-08-11 (Scope item 1)

- `Bases/P400_Trades.base`: folder filter `TradeManagement/P400` ->
  `TradeOrderManagement/P400`; sort field `date` -> `run_date` (the only
  existing schema field that expresses "when P_400 acted on this").
- `Bases/P020_Performance.base`: folder filter `TradeManagement/P020` ->
  `TradeOrderManagement/P020`. Column/sort fields already matched schema
  (`close_date`, `symbol`, `account_id`, `system`, `realized_pnl`,
  `realized_R`, `outcome`, `sig_code`) -- path was the only defect.
- Bug rows added to `p400-project-context` and `p020-project-context`
  skill files (Bugs table + changelog each) -- IN THE APP skill update
  still required (Customize -> Skills), not just the disk file.

Verification: `TradeManagement` confirmed absent via vault listing (404).
`TradeOrderManagement/P400`, `/paper`, `/P020` confirmed present with
files via the same tool. Field names cross-checked against 3 sampled
P400 notes (2 non-paper, 1 paper) and 1 P020 note. Not yet confirmed
inside the Obsidian app itself (Bases render client-side; no way to
trigger that from here) -- ask Tony to open both Bases and confirm rows
now appear next time he's in the vault.

## SCOPE -- item 2, NOT STARTED

Recommended direction (not yet approved for build): extend P_020's
already-locked "SQLite is single source of truth" architecture to P_400
orders instead of leaving Obsidian as P_400's only, dead-ended record.

- P_400 orders land in SQLite at submit time (new `orders` table, or a
  `pending` status row in `trades`).
- A reconciliation step -- reusing P_020's existing Schwab pull, not a
  new integration -- matches submitted orders against actual fills/exits
  and writes real `entry_date`/`close_date`/`realized_pnl` back.
- Obsidian notes become a generated view again (same pattern P_020's
  vault export already uses), not a hand-maintained or dead-ended
  primary record.
- Keeps ownership clean: SQLite stays P_020's, P_400 becomes a writer
  into it, Obsidian stays P_800's presentation layer -- no project
  editing across a boundary it doesn't own.

**DESIGN REFINEMENT -- 2026-08-11 (same session, Tony).** Matching entry
to exit doesn't have to lean on `exit_allocator.py`'s FIFO/symbol/date
heuristic as the primary path. Confirmed in the D_020_2026-08-11 TOS
account statement export (`Account Order History filtered by SPY`
section): closing/replacing orders carry an explicit back-reference to
the order they belong to -- `TRG BY #5375753645` (triggered-by) and
`RE #5376488736` (re-issued-from) in the Spread column, pointing at
another order's Order ID. Bracket/OCO order groups are therefore
explicitly linked by Schwab/TOS, not just inferred. This makes
reconciliation a lookup by linked ID for anything placed as a bracket
(entry + attached stop/target), not a guess.

**Two matching tiers, not one:**
1. **Primary -- linked order ID.** If the closing order references the
   entry order_id (directly or via a short reference chain), match on
   that. Deterministic, no heuristic involved.
2. **Fallback -- P_020's existing FIFO/symbol/date matcher.** Covers the
   case a bracket doesn't cover: a naked entry closed later by a
   standalone order with no attached stop/target, so no link exists to
   follow. Not a gap -- `exit_allocator.py` already handles exactly this
   shape of match today. Tier 2 stays as backstop, not replaced.

**Not yet confirmed:** whether Schwab's live orders API exposes this
same parent/child linkage programmatically (bracket/OCO order structures
usually do -- e.g. child order strategies -- but this hasn't been checked
against an actual API response yet, only seen in the TOS CSV export's
Account Order History section, which today's parsers don't read at all --
only the Cash Balance section is parsed). Confirming this against a live
`fetch` call is a prerequisite before Tier 1 can be built for the live
account.

**DESIGN REFINEMENT 2 -- 2026-08-11 (same session, Tony).** Paper and
live cannot share one data-acquisition path, structurally -- not a style
choice. Live has the Schwab API. Paper runs on TOS paperMoney, which has
no API at all; the only way data leaves it is a CSV export, permanently.
So Tier 1 for paper can only ever come from parsing the Account Order
History section of the TOS export (unparsed today -- only Cash Balance
is read); Tier 1 for live may come from a live API call once confirmed,
or fall back to the same CSV-section parse if the API doesn't expose it.

This isn't a new problem to solve -- P_020 already has this exact shape
solved: `paper_import.py` (CSV-based) and `schwab_trade_pull.py`
(API-based) are two separate ingestion adapters already feeding one
shared SQLite schema. Reconciliation should follow the same pattern: two
adapters (one per data source), both normalizing into a common shape
before the Tier 1/Tier 2 matching logic runs once, shared. "One
reconciliation path or two" (below) is answered at the acquisition
layer -- always two -- not at the matching layer, which stays one.

Not scoped yet: which table P_400 writes to, how council/why/sig
metadata (fields SQLite's `trades` table doesn't currently have) gets
carried through, the Tier-1 live-API confirmation, and building the
Account-Order-History-section parser neither ingestion path has today.
Needs a design session with Tony before any code.

**DESIGN DECISION -- 2026-08-21 (Tony).** `council_verdict`/`why_code`/
`sig_code` get new columns directly on SQLite's `trades` table -- not a
separate table, not Obsidian-only. Resolves NEXT SESSION PREP item 2.
Item 1 (Tier-1 live-API parent/child linkage confirmation) still open,
diagnostic staged same session (verify/run_this_P400_E6001_orderlinkage_
20260821_083259.py).

**COORDINATION NOTE -- 2026-09-06 (WO-P000-E22.001/E26.001).** The Hub
adopted an Attribution Integrity Standard after this WO was drafted:
every trade/order record carries a Signal Source ID and a confidence tier
(`CONFIRMED`/`INFERRED`/`UNRESOLVED`). When the BUILD PLAN's
`migration_add_orders_table.py` actually gets built, its `why_code`/
`sig_code`/`source_project` columns should be designed against that
vocabulary directly (e.g. `why_code` as the Signal Source ID, a new
`confidence_tier` column alongside it) rather than as a separate scheme
that needs reconciling with WO-P000-E24.001's `trades.confidence_tier`
later. No change to this WO's build plan or sequencing -- just a design
constraint for whenever Scope item 2 actually starts. See WO-P000-E26.001
for the smaller, already-actionable piece (adding `confidence_tier` to
the existing live vault write, independent of this WO's larger build).

**STEP-BY-STEP AUDIT -- 2026-08-11 (same session, Tony's request: "check
what we do with paper vs API for every step").** Read the actual code
(`ingest_pipeline.py`, `schwab_mapper.py`, `exit_allocator.py`,
`paper_import.py`, `paper_csv_reader.py`, `exit_builder.py`) rather than
assuming. Corrects the "matching logic stays shared" claim in Design
Refinement 2 above -- it does not, today:

| Step | Live | Paper |
|---|---|---|
| Data acquisition | Schwab API JSON pull (`ingest_pipeline.py`) | Manual TOS CSV export -> `P_020_TOS_Parser_v2.py` (nominally retired per PROJECT_PLAN_v3, but still the only active path for paper since paperMoney has no API) -> intermediate `_OPTIONS_IMPORT.csv`/`_STOCKS_IMPORT.csv` -> `paper_csv_reader.py`. Reader does NOT consume the raw Account Statement CSV directly. |
| Multi-fill grouping | Real `orderId`, grouped in `schwab_mapper._aggregate_by_order()` | `REF #` text parsed from the CSV `DESCRIPTION` field, inside the old TOS parser -- same concept, string-matched, not a queryable ID |
| Entry/exit classification | `schwab_mapper._parse_transaction()` from txn type | Old TOS parser, from BOT/SOLD in `DESCRIPTION` |
| **Entry-to-exit matching** | `exit_allocator.py` -- FIFO, qty-aware, built by WO-P020-E1.001 specifically to fix exits being greedily consumed by the wrong sibling entry | `exit_builder.py` does NO matching -- it only formats exit slots the old TOS parser already matched using the simpler pre-E1.001 "grab up to 2 exits" logic. **Same bug class E1.001 fixed for live is still live in paper, unfixed, independent of this WO.** Also caps 2 exits (paper) vs 3 (live, `MAX_EXIT_SLOTS`). |
| Order-ID linkage (TRG BY#/RE#) for Tier 1 | Not read today. `orderId` already flows through for same-order grouping; whether the payload also carries cross-order parent/child linkage is unconfirmed | Not read today -- only source is the Account Order History CSV section, which the active paper pipeline never touches (see Data acquisition row) |
| Dedup | `schwab_transaction_id` | `(account_id, symbol, date, entry_price, source)` tuple |

**Consequence for this WO's scope:** building Tier-1 order-linkage for
paper is not a small addition to `paper_csv_reader.py` -- it requires
either extending the old TOS parser (already marked retired once) or a
new reader built against the raw Account Statement CSV's Order History
section from scratch, bypassing the old parser entirely. Separately: the
parent-vs-nominal-retirement status of `P_020_TOS_Parser_v2.py` should
get a straight answer before more is built on top of it either way.

**SPLIT OFF -- 2026-08-11 (same session).** This audit surfaced a real,
active P_020 bug unrelated to P_400/Obsidian: `P_020_AccountParser.bat`
was pointed at a dead parser version (`v2.3.py`) with a double-counted-
exit bug, instead of the already-correct `v2.4.py`. Not P_400's to own --
fixed and logged separately under **WO-P020-E1.013** (COMPLETE, same
session), not folded in here. Noted here only for traceability: this is
where it was found.

## BACKLOG NOTES -- record_commands.py gaps found 2026-08-21

Found live while working an expired paper order (V, scale-in attempt,
DAY limit expired unfilled 16:00:00). Both are P_400-only, unrelated to
the SQLite reconciliation build above, but same neighborhood (order
lifecycle accuracy) so logged here rather than a new WO.

1. **No disposition for "attempted, expired unfilled."** Only `submit`
   (filled/working) and `decline` (chose not to execute) exist. Used
   `decline` as a workaround (Tony's call, 2026-08-21) -- writes
   `drop_reason: MANUAL_DECLINE` for an order that was genuinely
   attempted, not skipped. Note appended to the vault record by hand
   to correct the record; the CLI itself still has the gap.
2. **`record --decline` has no `--paper` flag.** `cmd_record_decline()`
   ignores it entirely (`cmd_record_submit()` has it, decline doesn't).
   Wrote from a stale `trade_mode_value: REAL` in eval_cache regardless
   of account -- `source: P400` came out wrong for a paper attempt,
   corrected by hand to `P400_PAPER` after the fact.
3. **`target_2` is a real vault frontmatter field but never gets
   populated.** `write_p400_record()` has no `target_2` parameter --
   found live 2026-08-24 recording a manual MA options override that
   had a real T2 (780) with nowhere structured to put it; folded into
   `option_override_justification` free text instead as the only
   available field. Same gap affects any stock/option record with a
   genuine second target.

Fix for all three, if built: `cmd_record_decline()` gains `paper: bool`
(mirrors submit) plus a third disposition, e.g. `--expired`, that
writes `drop_reason: EXPIRED_UNFILLED` instead of `MANUAL_DECLINE`.
`write_p400_record()` gains a `target_2: Optional[float] = None`
parameter, threaded through same as `target_1`. Small -- combined
`record_commands.py`/`record_writer.py` change stays well under 50
lines. Not scoped further than that; low priority, no live money
involved when this happens
since nothing filled.
## NEXT SESSION PREP -- added 2026-08-11

Design session for Scope item 2 needs these confirmed first:

1. RESOLVED 2026-08-21 -- confirmed live via diagnostic (29 orders,
   last 60 days): `childOrderStrategies` is a real, populated key.
   `orderStrategyType` breakdown: TRIGGER 12, OCO 7, SINGLE 10. Linkage
   is a nested tree, not a flat sibling-ID field like TOS's TRG BY#/RE#
   -- parent orderId -> childOrderStrategies[] -> each child has its own
   orderId and can nest again (observed: TRIGGER -> OCO -> two SINGLEs,
   e.g. GE 260918C410 entry -> OCO bracket -> filled target + canceled
   stop). Confirms Tier-1 matching can walk this tree directly; no need
   to parse the TOS CSV Account Order History section for the live
   account. Paper still has no equivalent -- CSV parse remains the only
   path there (see STEP-BY-STEP AUDIT table above).
2. RESOLVED 2026-08-21 -- council/why/sig metadata gets new columns
   on `trades` (Tony's decision, see DESIGN DECISION above).

## ACCEPTANCE CRITERIA -- Scope item 1 (met)

- `TradeManagement` string does not appear in either Base file.
- Every field referenced in both Base files exists in the schema of the
  notes it queries.

## BUILD PLAN -- 2026-08-21 (Tony approved direction: "do it now")

Both NEXT SESSION PREP items resolved same session -> Scope item 2's
first acceptance criterion (Tony approved a direction) is MET. Direction:
recommended architecture as written above (SQLite source of truth,
two-tier matching, two ingestion adapters). Not a same-session build --
18 files across 3 projects. This section is the file plan; actual code
still needs its own per-file go-ahead (Protocol A) when build starts,
likely a dedicated P_020/P_400/P_800 session.

**Real finding while sizing this:** `P_020/infrastructure/db_writer.py`
is already at 238 lines / 5 public functions (soft-limit already hit).
New order-write functions do NOT extend it -- new file
`db_order_writer.py` instead. One reason to change: trade-writing vs.
order-writing are different lifecycles.

### P_020 (schema + reconciliation owner)
| File | Est. Lines | Purpose |
|---|---|---|
| `infrastructure/migration_add_orders_table.py` | ~110 | New `orders` table (order_id, account_id, symbol, side, qty, planned entry/stop/target, submitted_ts, schwab_order_id, status, `council_verdict`/`why_code`/`sig_code`, source_project) + indexes. Idempotent, modeled on `migration_add_tag_columns.py`. |
| `schemas.py` (extend) | +~35 (245->280) | New `Order` Pydantic model |
| `domain/order_linkage.py` | ~85 | Pure logic: recursively walk `childOrderStrategies`, return grouped OrderChain (root + descendants). No I/O. |
| `infrastructure/schwab_order_puller.py` | ~90 | `get_orders_for_account()` wrapper. I/O only, no tree logic. |
| `infrastructure/paper_order_history_parser.py` | ~190 | NEW parser for the raw TOS Account Statement CSV's Order History section (unparsed by anything today) -> same OrderChain shape via TRG BY#/RE#. The big lift flagged in the 2026-08-11 audit. |
| `domain/order_reconciler.py` | ~150 | Tier-1 (OrderChain from either source) + Tier-2 fallback (imports existing `exit_allocator.py`, not rewritten). Produces entry_date/close_date/realized_pnl. |
| `infrastructure/db_order_writer.py` | ~100 | `insert_order()` / `update_order_status()` / `promote_order_to_trade()`. New file -- see finding above. |
| `application/reconcile_command.py` | ~120 | Orchestration: pull (live API or paper CSV) -> linkage -> reconciler -> db_order_writer. New CLI subcommand. |
| `cli.py` (extend) | +~15 | Wire `reconcile --account AJZ [--paper]` subcommand |
| `tests/test_order_linkage.py` | ~70 | |
| `tests/test_order_reconciler.py` | ~90 | |
| `tests/test_paper_order_history_parser.py` | ~90 | |

### Shared (`shared_resources/python_utils/`)
| File | Est. Lines | Purpose |
|---|---|---|
| `p020_order_writer.py` | ~90 | Thin contract P_400 calls to insert a `pending` order row into P_020's SQLite at submit time. P_400 never reaches into P_020's internals directly -- same generalization pattern as `schwab_client.py` (WO-P400-E4.001). |

### P_400 (writer at submit time)
| File | Est. Lines | Purpose |
|---|---|---|
| `infrastructure/order_submit_writer.py` | ~70 | Builds the Order payload (council_verdict, why_code, sig_code, planned entry/stop/target) and calls the shared writer |
| `application/record_commands.py` (extend) | +~25 | Wire the writer call into `cmd_record_submit()` |
| `tests/test_order_submit_writer.py` | ~50 | |

### P_800 (presentation -- generated view)
| File | Est. Lines | Purpose |
|---|---|---|
| `obsidian_writers/application/regenerate_p400_notes.py` | ~110 | After P_020's reconcile runs: read reconciled rows, re-write vault note lifecycle_status/entry_date/close_date/realized_pnl via existing `write_to_vault()` overwrite path |
| P_800 test extension | ~60 | |

**Total: 18 files, ~1,650 new/changed lines across P_020/P_400/P_800/shared.**

**Sequencing (build order, not necessarily one session each):** P_020
migration + schemas first (nothing else works without the table) ->
domain/order_linkage.py + schwab_order_puller.py (live path, already
API-confirmed this session) -> reconcile_command.py wired to live only
-> shared p020_order_writer.py + P_400 submit-time writer (live orders
start flowing end-to-end) -> P_800 regenerate_p400_notes.py (vault
becomes a real generated view for live orders) -> paper_order_history_
parser.py (the largest single lift, no existing code to build from) ->
paper wired into reconcile_command.py last.

**Trigger -- DECIDED 2026-08-21 (Tony).** No new scheduling mechanism.
`reconcile` becomes a new step inside the existing
`P_020_Weekly_Update.bat` (currently 5 steps: token check -> balance ->
Schwab trade pull -> import -> analyze -> dashboard regen). New Step
[4/6] "Reconciling P_400 orders..." runs `reconcile_command.py`, slotted
after Step 3 (Import trades -- P_020's own trades need to be current
first, since Tier-2 fallback matching reads that table) and before the
old Step 4 (Analyze, renumbers to 5/6). New Step [6/6] runs P_800's
`regenerate_p400_notes.py` last, after reconciled data exists, so the
vault note rewrite always has real data to write. Same weekly cadence
P_020 already uses for closing the loop on live-account trades (see WHY
section above) -- P_400 orders get folded into the same rhythm, not a
separate one. `reconcile_command.py` still gets a standalone CLI
subcommand too, for on-demand manual re-runs outside the weekly job.
## ACCEPTANCE CRITERIA -- Scope item 2 (not yet met, blocks CLOSED)

- MET 2026-08-21 -- Tony approved the recommended direction (Add
  them to SQLite and go / do it now). See BUILD PLAN above.
- A P_400 order that fills gets its real entry/exit data reflected in
  both the vault note and `P400_Trades.base` without hand editing.
- Independent review (separate session) per WO_COMPLETION_GATE.

---

**Created:** 2026-08-11 (P_020 session, Tony's request)
**Author:** Claude


## BUILD COMPLETE -- 2026-09-07 (Scope item 2 code build)

All production code from the BUILD PLAN above is written and PEH-
validated (every file's `.done` marker shows PASS, run via Claude Code
per the peh-handoff protocol -- windows-mcp:PowerShell stalled 4+ min on
every compile-subprocess call this session, relay confirmed healthy each
time via a fast ping, so every file after the first two handoffs was
handed straight to Claude Code rather than retried). **Not yet run
against live production data** -- see remaining gates below.

### Files built (13 production files, 4 project areas)

**P_020:**
- `infrastructure/migration_add_orders_table.py` -- new `orders` table.
  Never executed against the real DB (`migrate()` not run) -- validated
  only via `:memory:` SQLite.
- `schemas.py` (extend) -- new `Order` model.
- `domain/order_linkage.py` -- `childOrderStrategies` tree walker.
- `domain/order_reconciler.py` -- Tier 1 (chain linkage) + Tier 2
  (`exit_allocator.py` fallback, unmodified).
- `infrastructure/schwab_order_puller.py` -- live orders pull wrapper.
- `infrastructure/paper_order_history_parser.py` -- TOS Account Order
  History section parser (the largest single lift, per the original
  sizing).
- `infrastructure/db_order_writer.py` -- `insert_order`/
  `update_order_status`/`promote_order_to_trade`.
- `infrastructure/db_reader.py` (extend) -- `get_open_real_orders`/
  `get_open_paper_orders`.
- `application/reconcile_command.py` -- live + paper orchestration,
  sharing `_index_pulled_orders()`/`_reconcile_one()` unchanged between
  both paths (Tony's explicit requirement).
- `cli.py` (extend) -- `reconcile --account/--start/--end` and
  `reconcile --paper --statement` subcommand.

**Shared (`shared_resources/python_utils/`):**
- `p020_order_writer.py` -- P_400-to-P_020 order-submit bridge. Fixed
  live during this build: a `sys.modules` collision (P_400's own
  `infrastructure`/`domain` packages shadowing P_020's when both are
  loaded in the same process) -- stash/restore added, re-verified
  through the recreated collision.

**P_400:**
- `config.py` (+1 line) -- new `ACCOUNT_ID = "AJZ6348"` constant
  (didn't exist anywhere in P_400 before this build).
- `infrastructure/order_submit_writer.py` -- new. `side` is hardcoded
  `"long"`: confirmed via `spread_sizer.py`/`build_spread_spec.py` that
  every P_400 structure today is a debit position (stock buys, long
  options, debit spreads) -- not a guess, there is no short/credit
  mechanism in P_400 to derive it from.
- `application/record_commands.py` (extend) -- `cmd_record_submit()`
  now also writes into P_020's `orders` table via the shared bridge,
  independent of the existing vault write (one failing doesn't block
  the other).

**P_800 (`P_800_Automation_Note_Taking/python/p400_regenerate/`, new
sub-project mirroring the existing `p115_backfill/` precedent in the
same folder -- namespaced imports, not the single-file
`regenerate_p400_notes.py` originally sized):**
- `infrastructure/vault_note_scanner.py` -- finds a P400/P400_PAPER
  vault note by `order_id`.
- `infrastructure/p020_reader.py` -- reads P_020's closed,
  not-yet-synced P_400 orders.
- `domain/note_merger.py` -- merges reconciled outcome into the note's
  existing field dict, stripping P_800-injected bookkeeping keys before
  re-submission.
- `application/regenerate_command.py` -- orchestrates the sync,
  idempotent via the note's own `lifecycle_status`.

### Deviations from the original BUILD PLAN

- **No permanent test files were created.** The plan's
  `tests/test_order_linkage.py`, `test_order_reconciler.py`,
  `test_paper_order_history_parser.py` (P_020),
  `test_order_submit_writer.py` (P_400), and the P_800 test extension
  were not written as permanent pytest-style files. All validation this
  session went through one-off PEH scripts
  (`Agentic-Hub-Governance\verify\run_this_*.py`), which are ephemeral
  by design (staged, run once by Claude Code, `.done`-marked) -- there
  is currently no regression suite living in the actual project
  `tests\` folders for any of this build. Worth a decision: backfill
  permanent tests from the PEH scripts, or accept PEH coverage as
  sufficient and formally drop those 4 line items from BUILD PLAN.
- **`regenerate_p400_notes.py` became a 4-file namespaced sub-project**
  (`p400_regenerate\`) instead of one ~110-line file, matching the
  existing `p115_backfill\` architecture already in the same P_800
  project folder rather than the BUILD PLAN's single-file estimate.

### Real findings during the build (beyond what BUILD PLAN anticipated)

- `LinkedOrder` (`order_linkage.py`) needed extending twice past its
  original file-3 scope: once for quantity/asset_type/avg_fill_price/
  timestamps (needed by the reconciler), once more for `put_call`
  (needed by `db_order_writer.py`'s `promote_order_to_trade()` to call
  `schwab_mapper.py`'s existing `_map_asset_type()` correctly).
- `orders` table gained 3 columns beyond the original design
  (`entry_fill_price`, `asset_type`, `put_call`) for the same reason --
  `Trade.entry_price`/`Trade.asset_type` need real values, not just the
  planned ones, to promote a closed order correctly.
- TOS Account Order History timestamps (`7/24/26 10:31:28`) aren't ISO
  format -- `order_reconciler.py`'s `_to_date()` expects ISO and was
  silently returning `None` for every paper order until
  `paper_order_history_parser.py` normalized the format at the source,
  keeping `order_linkage.py`/`order_reconciler.py` unchanged for both
  paper and live, per Tony's explicit requirement.

### Remaining gates before this scope item can close (1 of 4 done, item 2 partially done -- see below)

1. ~~Run `migration_add_orders_table.py` for real against
   `P_020_trades.db`~~ -- **DONE 2026-09-08.** Live run via Claude Code
   created `orders` table + 4 indexes (idx_orders_symbol,
   idx_orders_account_id, idx_orders_status,
   idx_orders_schwab_order_id); 23 columns verified via `PRAGMA
   table_info`. DB grew 311,296 -> 335,872 bytes (11:17:54 AM). Backup
   at `P_020_trades.db.backup_2026-09-08` confirmed unchanged after the
   run. One pass failed clean before this one -- `SyntaxError:
   unicodeescape` from a non-raw docstring in the staged PEH script
   containing a Windows path; caught before the DB was touched, fixed
   by making the docstring a raw string, re-run passed.
2. A real end-to-end run: one live order through `reconcile --account
   AJZ`, one paper order through `reconcile --paper --statement <real
   export>`, confirming actual Schwab/TOS data reconciles correctly
   (every test this session used synthetic or mocked data). **Paper leg
   DONE 2026-09-08** -- two real cases run against the actual TOS
   statement export (not synthetic/mocked):
   - SLS (paper, long 100 @ $13.00): Tier 1 chain linkage correctly
     matched the real FILLED stop-loss exit (entry_date/entry_fill_price
     both correct), but `promote_order_to_trade()` correctly declined to
     write a Trade -- `realized_pnl` came back `None` because the exit
     was a MKT order and the v1 TOS parser's documented limitation
     leaves `avg_fill_price` null for MKT fills. Correct refusal to
     fabricate, not a bug.
   - SPY (options, IRON CONDOR primary leg, short 1 call @ 3.16):
     surfaced a real bug -- `order_reconciler.py` (Tier 1
     `_compute_pnl()` and the Tier 2 inline calc) and
     `db_order_writer.py`'s `_build_exit()` both dropped the options
     100x contract multiplier -- `realized_pnl` came back $0.03 (should
     be $3.00), back-solved `exit_price` came back $0.16 (should be
     $3.13). Fixed: `order_reconciler.py` now routes through
     `exit_builder.compute_exit_pnl()` instead of reimplementing the
     math inline (passing `put_call.lower()`, not `asset_type`, which
     carries Schwab's raw EQUITY/OPTION string); `db_order_writer.py`
     multiplies qty by 100 for OPTION rows before dividing realized_pnl
     back out. Live-verified twice after the fix: `realized_pnl` $3.00,
     `exit_price` $3.13, both correct. Ref **EC-010**
     (P_000_SYSTEM_DOCUMENTATION.md Section 6). Test rows cleaned up
     after each run -- `orders` table confirmed empty (0 rows) as of
     2026-09-08.
   **Still open:** the live-API leg (`reconcile --account AJZ`) has not
   been run against a real live-account order -- both cases above went
   through the paper/TOS statement path only.
3. Independent review per WO_COMPLETION_GATE (unchanged from the
   existing Scope item 2 acceptance criteria below).
4. Decide on the permanent-tests question above.
