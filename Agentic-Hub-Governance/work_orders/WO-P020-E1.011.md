# WO-P020-E1.011 -- Automate Account Balance / Risk / Max Position in P_000 Account Parameters (Eliminate Hand-Maintained Monthly Update)

**Status:** IN_PROGRESS -- implementation complete 2026-08-09 (5 files deployed, hash-verified; config updated). Dry-run validated against real file content in sandbox (both no-op and threshold-triggered write cases passed clean). Live end-to-end run blocked -- P_020 Schwab token currently dead (same root cause under investigation in WO-P020-E1.010). Awaiting independent review + a live balance pull once token access is restored.
**Owner:** P_020
**Affects:** P_000 (file owner -- `P_000_Account_Parameters_Current.md`)
**Depends On:** WO-P020-E1.009 (built the write mechanism this extends --
  `domain/params_md_writer.py`, `infrastructure/p000_params_writer.py`,
  hooked into `run_balance_command()`) -- COMPLETE, unblocks this WO.
**Related:** Conversation 2026-05-30 ("P_020 Saturday, May 30, 2026
  Enhancement") -- Tony asked if a monthly process existed to update this
  file. Answer at the time: no formal process; recommended a flag-and-confirm
  step during monthly review (±10% threshold vs. $35,000 baseline, ask Tony
  to confirm before anything changes) rather than silent auto-write, given
  the numbers drive real position-sizing math. That got built into the
  Session Initialization Prompt as a conversational flag step -- Claude
  checks the balance during monthly review and asks. It was never built as
  file automation. This WO is that gap, revisited now that E1.009 has
  already built and live-tested the write mechanism.

---

## WHY

Tony currently hand-calculates and hand-writes three numbers into
`P_000_Account_Parameters_Current.md` monthly, per that file's own "Monthly
Review Checklist": pull current balance from broker, calculate Risk Capital
(balance x 0.015), calculate Max Position (balance x 0.05), then manually
update the file. E1.009 proved the write path works (Buying Power / Cash
Available now auto-populate on every balance pull). This WO extends the
same mechanism to Account Balance, Risk per Trade, and Max Position --
eliminating the manual monthly task.

**Historical note on "Account Balance":** Parameter History rows are
annotated "Net Liq per broker" -- this already matches `pull_balance()`'s
`total_value` field (`curr.get("liquidationValue") or curr.get("totalValue")`),
so no new Schwab data is needed, same as E1.009.

## SCOPE

Unlike E1.009 (2 independent reference rows), this touches 5 places in the
same file that must stay synchronized from one balance number:

1. **Active Parameters table** -- Account Balance, Risk per Trade, Max
   Position (5%) rows.
2. **Risk Mode Adjustments table** -- 5 rows (OFF/CORRECTION, HALF,
   STANDARD, FULL, HOT), each a percentage of the same base Risk/Max.
3. **Three-Gate Position Sizing code block** -- Gate 1 and Gate 3 dollar
   figures embedded in a fenced code block, not a markdown table.
4. **Growth Projections table** -- the "(current)" row only; the fixed
   $35K/$40K/$50K/$75K/$100K projection rows are static and untouched.
5. **Parameter History table** -- append one new row per update
   (Date | Balance | Risk | Max | Notes), never edit/delete prior rows.

A single domain function needs to derive all of the above from one balance
figure and rewrite all 5 locations atomically -- partial sync (e.g. Active
Parameters updated but Risk Mode Adjustments left stale) would be worse
than the current fully-manual process, since a stale table reads as
authoritative.

## DESIGN DECISIONS -- RESOLVED 2026-08-09 (Tony)

Three open design questions blocked implementation. All three decided this
session:

1. **Auto-write mode: B -- Threshold-gated.** Write only when balance moves
   ±X% from the last *written* baseline (not the last pull) -- day-to-day
   noise never touches the file, meaningful moves do, no confirmation
   dialogue needed. **X not explicitly specified by Tony this session --
   implementation defaults to ±10%, matching the threshold flag already
   live in `P_020_INIT.py` output and the SIP's existing conversational
   flag step (both keyed off the same $35K baseline concept). Flag this
   default to Tony at plan time; change is one constant if he wants a
   different X.**
2. **Trigger: every weekly balance pull.** Same hook as E1.009
   (`run_balance_command()`) -- no separate monthly-only code path. The
   threshold gate from decision 1 is what keeps this from writing on every
   pull; the pull frequency itself is not the gating mechanism.
3. **Next Review date field: auto-bump.** When a threshold-triggered write
   fires, the "Next Review" field updates alongside the other 4 locations
   (making this 5 synced locations plus the History append, six total
   touch points per triggering pull) rather than staying a manually-edited
   note.

## OUT OF SCOPE

- Any change to `RiskConfig.json` / P_010's risk_mode authority rule --
  that JSON field stays authoritative over the avg_posture ranges shown in
  the Risk Mode Adjustments table; this WO only keeps the table's *dollar
  values* in sync with balance, not the mode-selection logic itself.
- Changing the 1.5% / 5% base percentages themselves -- pulled from
  existing `P_020_Account_Params.json`, not hardcoded fresh here.
- P_400 or any other consumer's read-side logic -- `params_reader.py`
  changes are explicitly out of scope (same boundary E1.009 drew).

## ACCEPTANCE CRITERIA

- One balance pull produces a fully self-consistent file -- all 5 locations
  (plus Next Review date, plus History append = 6 touch points) reflect the
  same balance, no stale cross-references, whenever the ±10% (or
  Tony-adjusted X) threshold is crossed.
- A pull that does NOT cross the threshold writes nothing -- silent no-op,
  matches decision 1's intent (noise never touches the file).
- Parameter History gets exactly one new row per triggering update, never
  edits or removes prior rows.
- No regression to E1.009's Buying Power / Cash Available rows.
- Threshold percentage (X) is a named constant, not a magic number, so
  Tony can change it without a code review.

## IMPLEMENTATION -- 2026-08-09

Deployed (all hash-verified byte-identical, sandbox to disk):
- domain/account_params_calc.py (new, 113 lines)
- domain/params_md_writer.py (extended 65 -> 202 lines, original 2
  functions preserved verbatim)
- domain/params_history_writer.py (new, 100 lines -- split out to keep
  both domain files under the 250-line split threshold)
- infrastructure/p000_params_writer.py (extended 44 -> 140 lines,
  original function preserved verbatim)
- application/account_commands.py (+6 lines, one new hook call after the
  existing E1.009 call)
- config/P_020_Account_Params.json (added max_position_pct: 0.05 -- was
  missing; OUT OF SCOPE above assumed it already existed alongside
  default_risk_pct, only the latter was actually present)

Dry-run: built a fixture from the live file's actual content, ran the
full pipeline in the sandbox (not against the live file). Verified:
-2.7% pull -> correct no-op; +12.3% pull -> all 6 locations updated
correctly, nothing else in the file touched (diffed line-by-line). HOT
risk-mode row's non-percentage text format preserved correctly.

Not yet done: a live end-to-end run via the actual weekly balance pull
CLI command. Blocked on P_020's Schwab token being dead as of this
session (see WO-P020-E1.010) -- infrastructure-independent of this WO's
own code, which is deployed and ready to fire on the next successful
balance pull.

---

**Created:** 2026-07-26 (P_020 session, Tony's request)
**Design decided:** 2026-08-09 (Tony, via session elicitation)
**Implementation deployed:** 2026-08-09
**Logged by:** Claude


---

**2026-08-11 -- First live end-to-end run (no-op branch only).** Token access restored (WO-P020-E1.010 fix). Ran `P_020_Trade_Manager.py balance --account AJZ` live: pulled $30,390.67 vs. last-written baseline $31,348.39 (-3.06%) -- under the ±10% gate, correctly produced a silent no-op. Verified directly against `P_000_Account_Parameters_Current.md`: Account Balance/Risk/Max Position/Risk Mode Adjustments/Three-Gate block/Growth row/History all untouched, Next Review still September 2026 -- exactly the no-op acceptance criterion. E1.009's Buying Power ($35,300.78) and Cash Available auto-write ran with no regression. The threshold-*triggered* 6-location sync path is still unverified live -- no real pull has crossed ±10% yet; that branch remains sandbox-only (dry-run +12.3% case from 2026-08-09).
