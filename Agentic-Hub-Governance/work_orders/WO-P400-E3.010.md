# WO-P400-E3.010 -- spec Cache Miss on APPROVED_WITH_CAUTION / APPROVED_WITH_SEVERE_WARNING (Exact-String Gate Regression of E3.009)

**Status:** PENDING
**Owner:** P_400
**Affects:** none -- internal P_400 only
**Depends On:** WO-P400-E3.009 (spec cache mechanism this regresses)
**Acks:** N/A

---

## WHY

Found live 2026-07-24 running MRCY through STEP 4-6. `evaluate` returned
`APPROVED_WITH_SEVERE_WARNING` (RISK role POSITION_COUNT warning, book at
8/8 -- SEVERE_WARNING can never block per the 2026-07-20 Council change).
`spec` then failed: `[ERROR] No valid v2 packet found for MRCY` -- the
packet had already been archived by `evaluate`'s unconditional end-of-run
archive call, and no cache existed for `spec` to fall back on.

Root cause: `application/commands.py` `cmd_evaluate`, stock-only branch
(added by WO-P400-E3.009):

```python
if result.verdict == "APPROVED":
    ...
    cache_spec_text(result.symbol, spec_text, stock_fields)
else:
    write_eval_cache(result.symbol, stock_fields)
```

The gate is an exact string match on `"APPROVED"`. `APPROVED_WITH_CAUTION`
and `APPROVED_WITH_SEVERE_WARNING` both fall into the `else` branch --
`write_eval_cache()` only, no spec text cached -- even though both verdict
tiers are meant to proceed to STEP 6 (SIP: CAUTION "state each caution,
wait for confirm, STEP 6"; SEVERE_WARNING carries the same expectation,
RISK cannot block). Grep confirms no other branch handles either string;
this was a plain oversight in E3.009 -- SEVERE_WARNING as a tier postdates
that WO (added 2026-07-20), CAUTION was already broken before AND after,
just not hit until now (Tony had been confirming CAUTION cases with a
fresh symbol/day, not back-to-back same-session).

**Workaround used this session:** manual `ZipFile` restore of
`2026-07-24_MRCY_v2.0.json` from `2607_ProcessedJson.zip`, then `spec`
re-run -- worked, but produced a duplicate zip entry (same known,
non-harmful `UserWarning: Duplicate name` pattern from E3.009's Issue 2,
resolved via existing `Select-Object -Last 1` retrieval convention).

## Fix Direction (not yet built)

`cmd_evaluate`'s stock-only branch should cache spec text for any
verdict that reaches STEP 6 in the SIP -- `APPROVED`, `APPROVED_WITH_CAUTION`,
`APPROVED_WITH_SEVERE_WARNING` -- not just the literal `"APPROVED"` string.
Suggest a set/tuple constant (e.g. `SPEC_CACHEABLE_VERDICTS` in config.py)
rather than another inline string comparison, so a future verdict tier
addition doesn't reproduce this same gap a third time.

## Verify

One APPROVED_WITH_CAUTION and one APPROVED_WITH_SEVERE_WARNING symbol,
each run through `evaluate` then `spec` back-to-back, same session: zero
manual zip restores needed, zero duplicate zip entries, `spec` prints the
"[INFO] Served from today's evaluate cache" line on both.

---

## CHANGE LOG

- 2026-07-24 v1.0: Filed. Diagnosed live on MRCY (APPROVED_WITH_SEVERE_WARNING).
  Not yet built.

---

## FOURTH INSTANCE FOUND -- 2026-07-24 (same root cause, discovered during E4.003 PEH regression sweep)

`infrastructure\record_writer.py`'s `_resolve_lifecycle_status()`:
```python
if order_id and verdict in ("APPROVED", "APPROVED_WITH_CAUTION"):
    return "SUBMITTED"
```
Same gap, fourth spot. This one had already fired live and silently
corrupted data: MRCY's paper SUBMITTED record (order_id=5374526392,
verdict=APPROVED_WITH_SEVERE_WARNING) got `lifecycle_status=PAPER`
instead of `SUBMITTED` -- the function's own docstring says order_id
present means Tony executed the trade, should be SUBMITTED, per the
function's stated intent, not just a guess.

Fixed: `APPROVED_WITH_SEVERE_WARNING` added to the tuple. Corrective
action for the already-written MRCY record: re-run `cli.py record MRCY
--order-id 5374526392` through the now-fixed pipeline (overwrite-idempotent
per config.py `overwrite_records=true`) rather than hand-editing the
markdown file directly.

**Swept the rest of the codebase same session** -- grepped every file
mentioning `APPROVED_WITH_CAUTION` without also mentioning
`APPROVED_WITH_SEVERE_WARNING`. Two more hits, both confirmed false
positives (different, self-contained enums with no SEVERE_WARNING
equivalent at all): `domain\options_council.py` (PASS/BLOCK/CAUTION,
options viability only) and `domain\spread_council.py` (PASS/BLOCK only,
"no CAUTION tier, severity resolved 2026-07-04" per its own header
comment). Neither touches stock Council's verdict tiers. No further
action needed there.

Four instances of this exact root cause in one session (spec cache gate,
record_commands.py submit/decline, P_800 VERDICT_MAP, now this) is a
pattern worth naming explicitly: any future Council verdict tier addition
needs a full-codebase sweep for verdict-string allowlists, not just the
one or two call sites touched during that tier's own build.

**Corrective re-run confirmed 2026-07-24, 13:23:** MRCY record regenerated -- lifecycle_status=SUBMITTED, write_route=BUY, version 2 (clean overwrite, no duplicate). Both this bug and E3.011's fix verified together on the real record.
