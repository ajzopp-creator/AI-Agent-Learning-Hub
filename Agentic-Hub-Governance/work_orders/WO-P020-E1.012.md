# WO-P020-E1.012 -- Session INIT Command Block Is Non-Functional (`Start-Job` Does Not Survive an MCP Tool Call)

**Status:** CLOSED -- independent review completed 2026-08-09 (fresh session, did not implement
  the original fix). All 5 acceptance criteria verified live against disk. See INDEPENDENT
  REVIEW section below.
**Owner:** P_020
**Affects:** P_020 only (`docs\P_020_MASTER_SYSTEM_DOCUMENTATION_v1_0.md` Section 9.6, Section 8;
  `SESSION_INITIALIZATION_PROMPT_v2_9.md` Step 2); also `.claude\skills\p020-project-context\SKILL.md`
  (Code Delivery section) -- found stale during this review, fixed same session, see below.
**Depends On:** none
**Related:** SIP v3.1 changelog (2026-06-18) -- "Start-Process -NoNewWindow reference
  removed (banned per ERROR 002)". That change correctly banned `-NoNewWindow`, but the
  replacement it installed (`Start-Job + cmd /c`) was never verified end-to-end from a
  live MCP session. It has been the documented INIT path since 2026-06-18.

---

## WHY

Every P_020 session opens by running the INIT block. The block documented in Section 9.6
does not work and has not worked since it was written.

Observed 2026-08-03 at session start:

1. Ran the Section 9.6 block verbatim. `Start-Job` returned a job object, reported
   "job started", exited clean.
2. Separate MCP call with `Start-Sleep 20; Get-Content "C:\Temp\init_out.txt"` --
   relay stalled and burned the full 4-minute MCP timeout.
3. `Write-Output "ping"` at 15s confirmed the relay itself was healthy.
4. `Test-Path "C:\Temp\init_out.txt"` returned false. The file was never created.

**Root cause:** each Windows-MCP PowerShell call is an isolated process. The background
job is a child of that process and is torn down when the call returns -- before `cmd /c`
ever flushes output to disk. This is the same failure mode already recorded in the
`peh-handoff` skill ("Start-Job/Get-Job do not survive across separate MCP tool calls"),
but Section 9.6 was never reconciled with it. Two governance documents contradicted each
other, and the wrong one was the one INIT tells Claude to follow.

**Secondary defect:** putting `Start-Sleep` in the read-back call is what stalled the
relay. The sleep must not be inside an MCP call.

## WHAT WAS DONE

Working pattern, confirmed live this session -- INIT ran and returned the full block:

```
$ts = Get-Date -Format "HHmmss"
Start-Process -FilePath "<p140 python>" -ArgumentList '"<script>"' `
  -WindowStyle Hidden -RedirectStandardOutput $out -RedirectStandardError $err
```
then `Get-Content $out -Raw` in a **separate** call, no sleep.

`-WindowStyle Hidden` is not the banned `-NoNewWindow`. The ban stands.

Changes applied by `Agentic-Hub-Governance\verify\run_this.py`:

1. **Section 9.6** -- replaced the `Start-Job` block with the `Start-Process
   -WindowStyle Hidden` + redirect block. Rule line now bans `Start-Job` alongside
   `-NoNewWindow`, and states the no-`Start-Sleep`-in-MCP-call and unique-filename rules.
2. **Section 8 (Error Corrections Log)** -- new permanent entry.
3. **SIP Step 2** -- pointer text updated to name the correct pattern.
4. **SIP changelog** -- v3.2 entry.

Backups written alongside each file (disk shows `*_backup_2026-08-04.md` for both --
one day after the WHY section's 2026-08-03 observation date; not a discrepancy in the
fix itself, just in this WO's own prose, noted and left as-is per Independent Review
below).

## OUT OF SCOPE

- Any other project's INIT/SIP. P_010, P_115, P_300, P_400 may carry the same
  `Start-Job` pattern -- **not checked, not touched here.** If they do, that is a
  separate WO against each owning project.
- The `peh-handoff` skill file -- already correct, no change needed.
- `P_020_INIT.py` itself -- the script is fine; only the invocation wrapper was broken.

## ACCEPTANCE CRITERIA

- [x] Section 9.6 block, copied verbatim into a fresh MCP session, produces the INIT
      output block on the second call. -- Verified 2026-08-09: ran the literal Section
      9.6 code block in a fresh session, second call (`Get-Content -Raw`, no sleep)
      returned the full INIT output, no timeout.
- [x] Section 9.6 rule line bans `Start-Job` and `-NoNewWindow` both. -- Confirmed on
      disk: "NEVER `Start-Process -NoNewWindow`... NEVER `Start-Job`..."
- [x] Section 8 carries a permanent entry for this failure. -- Confirmed: "Error: INIT
      Command Block Used Start-Job -- Output File Never Written", Status: Resolved.
- [x] SIP Step 2 no longer names `Start-Job`. -- Confirmed: Step 2 now reads "NEVER use
      `Start-Process -NoNewWindow` or `Start-Job` (WO-P020-E1.012)" -- names it only to
      ban it, does not prescribe it. Intent satisfied.
- [x] Backups exist for both edited files. -- Confirmed:
      `P_020_MASTER_SYSTEM_DOCUMENTATION_v1_0_backup_2026-08-04.md` and
      `SESSION_INITIALIZATION_PROMPT_v2_9_backup_2026-08-04.md` both present.

## INDEPENDENT REVIEW -- 2026-08-09

Performed by a fresh session (this session did not write the original 2026-08-03 fix;
qualifies as independent per WO_COMPLETION_GATE's Independent Review Requirement).

**Method:** re-read each acceptance criterion against the actual files on disk, then
additionally ran the literal Section 9.6 block live (not a paraphrase of it) as the
strongest form of criterion 1. All 5 passed. No corrections needed to Section 9.6,
Section 8, or SIP Step 2.

**Gap found and fixed same session (Caller Propagation):** `.claude\skills\
p020-project-context\SKILL.md` Code Delivery section still read "Always `Start-Sleep 3`
before `Get-Content`" -- the exact anti-pattern this WO banned. The 2026-08-03 fix swept
the system doc and SIP but never swept this skill file, so a future session loading only
the skill (not the system doc) would have been told to do the banned thing. Same shape as
WO-P020-E1.010's P_400-skill gap. Fixed same session: skill bumped v2.4 -> v2.5, Code
Delivery corrected to the `Start-Process -WindowStyle Hidden` + separate-call pattern,
bug added to the skill's own "Bugs Fixed" registry so it can't silently regress again.
File hash-verified byte-identical on transfer (SHA256 match, sandbox vs. disk).

**Minor discrepancy noted, not corrected:** this WO's own WHY section says the failure
was observed 2026-08-03 and its "WHAT WAS DONE" section describes backups written
"alongside each file as `*_backup_2026-08-03.md`" -- but the actual backup files on disk
are dated 2026-08-04. The fix itself is correct and verified; only the WO's narrative
date is off by one day. Left as-is (matches the actual git history better than editing
history after the fact would).

**SIP version-header note:** `SESSION_INITIALIZATION_PROMPT_v2_9.md` top-of-file
`**Version:** 3.1` was not bumped to 3.2 even though the Changelog section already
carries a "### v3.2 -- 2026-08-03" entry describing this WO's fix. Cosmetic only (the
correct content is present and in effect), not gating this WO's closure. Worth a
one-line fix next time that file is touched for something else.

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[x] Any new or changed shared-resource location reflected in:
    - P_000_SYSTEM_DOCUMENTATION.md (Document Index section) -- N/A, this WO touches no
      shared/Hub-level resource, P_020-scoped only (plus the skill file caught above)
    - Affected project CLAUDE.md files -- P_020 has none referencing the old Start-Job
      pattern; not applicable
    - Affected project skill files (.claude\skills\*\SKILL.md) -- p020-project-context
      checked and fixed this session (see Independent Review above); no other project's
      skill file references P_020's INIT block
[x] CALLER PROPAGATION: named callers checked -- SIP Step 2 (already correct pre-review),
    p020-project-context skill Code Delivery section (found stale, fixed this session).
    No other caller of the INIT invocation pattern found in P_020.
[x] IMPERATIVE SWEEP: Section 9.6's rule line and the skill's Code Delivery section both
    now state the live rule (Start-Process -WindowStyle Hidden, separate-call read-back,
    no Start-Job, no -NoNewWindow, no Start-Sleep-in-MCP-call) as an imperative, not just
    a changelog line.
[x] Downstream projects in Affects: notified -- Affects is P_020-only; system doc, SIP,
    and skill are the notification, all three now consistent.
[x] No sys.path side-channels introduced (ref WO-P000-E2.003) -- N/A, no code changed,
    documentation/skill-text only.
[ ] If schema/signal contract changed: version bumped, consuming projects notified --
    N/A, no schema/signal contract touched.
[x] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\ -- none
    existed for this WO.
[x] One ledger entry per WO confirmed -- WO-P020-E1.012.md only (plus this session's
    dated backup, per standard convention, not a duplicate ledger entry).

---

**Created:** 2026-08-03 (P_020 session INIT, Tony's request)
**Logged by:** Claude
**Closed:** 2026-08-09 (independent review, fresh session, Tony's request)
