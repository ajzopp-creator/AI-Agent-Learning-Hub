# WO-P020-E1.018 -- Same-Day (0DTE) Option Expiration Not Recorded as an Exit

**Status:** OWNER_DONE -- design finalized and built same session (2026-09-15, Tony approved the design plan before any code, per system-doc-initializer APPROVAL GATE). Independent Review performed 2026-09-15 (separate session) -- BLOCKED, not CLOSED. See "Independent Review (2026-09-15)" section below.
**Owner:** P_020
**Affects:** P_020 (schwab_mapper.py / ingestion pipeline -- general gap, not
P_210-specific; any system trading same-day-expiration options hits this,
same framing as WO-P020-E1.017: P_210 is a `systems` table tag only, no
separate codebase)
**Depends On:** none
**Related:** manual stopgap for the 4 trades this surfaced on is
`close_p210_expired_worthless_20260913.py` (data correction, not the code
fix -- does not touch schwab_mapper.py, tracked separately from this WO's
scope, same pattern as E1.017's own Cleanup Required section)

---

## WHY

Discovered 2026-09-13 (P_020 session) reviewing the regenerated dashboard:
P_210 showed 4 open trades, $0.00 P&L, despite two spreads (NDXP puts opened
2026-09-09, NDXP calls opened 2026-09-11) that should have settled same day
-- Tony confirmed P_210 trades are same-day (0DTE) options.

Root cause: 0DTE options don't generate a closing BOT/SOLD transaction when
they expire -- there's nothing to trade, the position simply ceases to
exist at expiration. The ingestion pipeline only closes a trade when it
matches an explicit closing transaction, so these sit `open` indefinitely
with `exit_pnl` never recorded, even though the trade has definitively
settled and Schwab knows the outcome.

Confirmed via the raw pull JSON
(`data\api_pulls\ajz_strategies\P_020_raw_AJZ_Strategies_2026-09-05_to_2026-09-12_20260912_113950.json`):
no `EXPIRATION`-type transaction exists anywhere in the file (only
`TRADE`), but each option's `instrument` block carries its own
`expirationDate` and `closingPrice` fields independent of the transaction
list -- e.g. all 4 P_210 legs show `closingPrice: 0.0001` (Schwab's floor
value for expired-worthless) with `expirationDate` already past the pull
timestamp. That data already answers "what did this option settle at,"
just not in transaction-record shape.

## PROPOSED FIX (superseded by Acceptance Criteria + Files Built below -- kept as the original framing)

For any open trade where the option's `expirationDate` has passed and no
matching closing `TRADE` transaction exists in the pull: use the
instrument's `closingPrice` as a synthetic exit price and close the trade
via the normal `calculate_exit_pnl()` / `insert_exit()` /
`update_trade_status()` path -- same math the manual stopgap script uses,
just triggered automatically during ingestion instead of by hand.

This generalizes past "expired worthless" -- `closingPrice` also covers
ITM cash-settled expirations (a real nonzero settlement value), so the
same mechanism should handle both without a separate "worthless" special
case.

**Scope risk, flag before building:** this is only safe for cash-settled
instruments (NDXP and similar index options -- P_210's actual product).
American-style equity options can be exercised/assigned at expiration,
which creates a stock position rather than a cash close -- a naive
"closingPrice as exit" would misrepresent an assignment as a cash exit and
lose the resulting stock position entirely. Scope this WO to cash-settled
index options only; equity assignment/exercise handling is a different,
larger problem and should not be folded into this fix.

## Acceptance Criteria -- Status

- [x] Design doc or WO amendment: how to reliably detect "closing
      transaction never arrived because it doesn't exist" (expiration)
      vs. "just hasn't been pulled yet" (false positive risk) -- design
      presented to and approved by Tony before any code (2026-09-15).
      Detection rule: status='open' + asset_type in (call,put) +
      underlying_symbol in explicit CASH_SETTLED_OPTION_ROOTS allowlist +
      expiration_date at least EXPIRATION_CLOSE_BUFFER_DAYS (1) in the
      past. Buffer guards the false-positive case; allowlist guards the
      assignment-risk case (confirmed by scanning all 32 live pull files
      on disk -- Schwab's own data gives no field distinguishing
      cash-settled index options from assignable equity/ETF options).
- [x] Cash-settled-only guard -- `config.CASH_SETTLED_OPTION_ROOTS`,
      seeded with `{"NDXP"}` only. `domain.expiration_closer.
      is_eligible_for_expiration_close()` checks it before anything else
      about a trade. `test_equity_option_not_auto_closed` and
      `test_spy_not_auto_closed_even_though_index_tracking` lock this in
      -- the SPY case specifically because SPY looks identical to NDXP in
      Schwab's payload (assetType='OPTION', type='VANILLA') but is
      physically-settled and assignable.
- [x] New test: 0DTE spread with both legs at closingPrice ~0 closes
      correctly, P&L matches manual calculation --
      `test_0dte_worthless_expiry_closes_correctly`, matches the manual
      2026-09-13 stopgap's math.
- [x] New test: ITM cash-settled expiration (nonzero closingPrice) closes
      correctly -- `test_itm_cash_settled_nonzero_close`.
- [x] New test: equity option past expiration is NOT auto-closed by this
      path (regression guard against scope creep) --
      `test_equity_option_not_auto_closed` +
      `test_spy_not_auto_closed_even_though_index_tracking`.
- [x] Existing regression suite still passes -- 160/160 passed live
      2026-09-15 (`tests\` + `python\database\tests\` combined), 0
      failures. (4 tests failed mid-build when the trades table schema in
      `db_client.create_all_tables()` -- used by in-memory test fixtures
      -- hadn't been updated alongside the live-DB migration; fixed same
      session, full suite green afterward.)
- [x] Compile-clean under p140 `-W error::SyntaxWarning` -- all 14
      touched/new files, live 2026-09-15.
- [ ] Independent Review, separate session from implementation. --
      PERFORMED 2026-09-15, BLOCKED. See section below. Not checked
      because a real 300-line-cap violation was found and several claims
      (test pass count, compile-clean, live smoke test, DB migration)
      could not be independently re-executed this session.

## Files Built (2026-09-15)

| File | Change | Lines |
|---|---|---|
| `config.py` | +`CASH_SETTLED_OPTION_ROOTS`, `EXPIRATION_CLOSE_BUFFER_DAYS` | +18 |
| `schemas.py` | `Trade.expiration_date`, `Trade.settlement_price` | +14 |
| `infrastructure\migration_add_expiration_columns.py` | NEW, idempotent, run against live DB | 71 |
| `infrastructure\db_client.py` | `create_all_tables()` trades DDL -- same 2 columns, so fresh/test DBs match the live migrated schema | +2 |
| `domain\expiration_closer.py` | NEW -- eligibility + synthetic exit, pure logic | 150 |
| `infrastructure\schwab_instrument_parser.py` | NEW -- split out of schwab_mapper.py (was at 299/300, no room left) | 112 |
| `infrastructure\schwab_mapper.py` | uses the split-out helpers, captures expiration/settlement at parse time | 251 (net -48) |
| `domain\exit_allocator.py` | carries the 2 new fields through `_build_trade_dict()` | 181 |
| `application\ingest_pipeline.py` | `_build_trade()` passes them to `Trade(...)`; trimmed 2 other blocks to stay under 300 | 299 |
| `infrastructure\db_writer.py` | `insert_trade()` writes the 2 new columns | 269 |
| `infrastructure\expiration_reader.py` | NEW -- the one read query, kept separate since db_writer.py/db_reader.py were both already near the cap | 59 |
| `application\close_expired_options.py` | NEW -- dry-run-by-default orchestration, mirrors the manual stopgap | 123 |
| `P_020_Trade_Manager.py` | new `close-expired-options [--commit]` subcommand | 224 (net +16) |
| `tests\test_expiration_closer.py` | NEW -- 8 tests | new |

Two touch points found during build that weren't in the original plan
Tony approved (`domain/exit_allocator.py`, `application/ingest_pipeline.py`
-- both single-reason pass-through wiring, flagged to Tony as found, not
silently added) plus one design correction (`cli.py` doesn't handle
`import`/etc. at all -- `P_020_Trade_Manager.py` is the real entry point,
per this skill's own documented gotcha) and one file-size correction
(`db_writer.py` was wrongly assumed to have headroom -- it and
`db_reader.py` were both already near 300; the read function went to its
own file, `expiration_reader.py`, instead).

`db_client.py`'s `create_all_tables()` was not in the original plan at
all -- found mid-build when 4 pre-existing tests failed against an
in-memory test DB still on the old schema. The live migration only
covered the one real database file; fresh/test DBs are built from this
function's DDL independently and needed the same 2 columns added there
too.

---


## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[x] Any new or changed shared-resource location reflected in docs -- N/A,
    all changes are P_020-internal (`python\database\`), nothing in
    `shared_resources\`
[x] CALLER PROPAGATION -- the 2 new Trade fields' full call chain traced
    and updated in this WO: schwab_instrument_parser (extract) ->
    schwab_mapper (capture) -> exit_allocator (carry through trade dict)
    -> ingest_pipeline (build Trade object) -> db_writer (persist). Every
    link checked, not just the owning module.
[x] IMPERATIVE SWEEP -- N/A, this WO adds a new opt-in command, it does
    not change an existing rule/Must/Must-Not
[x] Downstream projects in Affects: notified -- Affects: states P_020
    only (schwab_mapper.py / ingestion pipeline), ledger entry is the
    notification
[x] No sys.path side-channels introduced -- N/A, no cross-project bridge
    imports in this WO
[x] If schema/signal contract changed: version bumped, consuming projects
    notified -- trades table gained 2 nullable columns (backward
    compatible, no existing reader breaks); migration run live 2026-09-15
    AND `create_all_tables()` DDL updated to match (found only after 4
    tests failed against the old in-memory schema -- see Files Built)
[x] DRAFT files for this WO deleted -- none exist (confirmed via
    directory listing, 2026-09-15)
[x] One ledger entry per WO confirmed
[x] No open VERIFY/Acceptance Criteria item deferred to Independent
    Review by label -- only the review gate itself is open, correctly
[x] PREMISE VERIFICATION -- this WO's WHY section is Tony's own live
    discovery (P_210 dashboard review, 2026-09-13), not inherited from a
    parent/planning WO's summary -- no premise to verify against a prior
    claim

## Independent Review (2026-09-15)

Performed in a separate session from implementation, per
WO_COMPLETION_GATE.md's Independent Review Requirement. Re-verified
against live code, not the WO's own prose. Result: **BLOCKED, not
CLOSED** -- one real discrepancy found (schemas.py over the project's
300-line hard cap), plus several claims that could not be independently
re-executed because this review session has no shell/command-execution
tool available (confirmed via tool search -- disabled for this session
and for any subagent spawned from it).

**What was re-verified directly against code (holds):**

1. Read the WO in full (WHY, PROPOSED FIX, Acceptance Criteria, Files
   Built, Completion Gate, Occurrence Log) -- consistent internally.
2. `domain\expiration_closer.py::is_eligible_for_expiration_close()`
   (lines 55-68) confirmed to require ALL FIVE of: `status == "open"`,
   `asset_type in ("call", "put")`, `underlying_symbol in
   cash_settled_roots`, `expiration_date` and `settlement_price` both
   not None, and `(as_of_date - expiration_date).days >= buffer_days`.
   Matches the WO's claimed detection rule exactly.
3. `config.py` (read live): `CASH_SETTLED_OPTION_ROOTS = frozenset({"NDXP"})`
   exactly (line 166) -- not silently broadened. `EXPIRATION_CLOSE_BUFFER_DAYS
   = 1` (line 172). Both match the WO's claims exactly.
4. Field propagation chain traced end-to-end, every link confirmed real
   (not just claimed):
   - `infrastructure\schwab_instrument_parser.py::extract_expiration_fields()`
     (lines 76-112) parses `instrument["expirationDate"]` and
     `instrument["closingPrice"]` into `expiration_date`/`settlement_price`.
   - `infrastructure\schwab_mapper.py::_parse_transaction()` calls it
     (line 102) and includes both fields in the returned fill dict
     (lines 146-147).
   - `domain\exit_allocator.py::_build_trade_dict()` carries
     `entry.get("expiration_date")` / `entry.get("settlement_price")`
     into the trade dict (lines 158-159).
   - `application\ingest_pipeline.py::_build_trade()` passes
     `raw.get("expiration_date")` / `raw.get("settlement_price")` into
     `Trade(...)` (lines 115-116).
   - `infrastructure\db_writer.py::insert_trade()` INSERT statement
     column list and values tuple both include `expiration_date`,
     `settlement_price` (lines 100-106, 127-128).
   All five links are real code, not documentation-only claims.
5. `infrastructure\db_client.py::_create_trades_table()` DDL (lines
   144-145) confirmed to include `expiration_date DATE` and
   `settlement_price REAL` -- the mid-build fix for in-memory/test DBs
   did land.
6. `tests\test_expiration_closer.py` read in full: contains exactly 8
   test functions as claimed (`test_0dte_worthless_expiry_closes_correctly`,
   `test_itm_cash_settled_nonzero_close`, `test_equity_option_not_auto_closed`,
   `test_spy_not_auto_closed_even_though_index_tracking`,
   `test_same_day_expiration_not_closed_yet`,
   `test_one_day_past_expiration_is_eligible`,
   `test_missing_settlement_price_not_eligible`,
   `test_find_expiration_closes_filters_mixed_batch`). Each test's
   assertions were hand-traced against `expiration_closer.py`'s actual
   logic and match what the test names claim (e.g. the worthless-expiry
   test's expected P&L of -604.99 matches `(0.0001 - 6.05) * 1 * 100`
   against a long entry at 6.05, consistent with `build_expiration_exit()`).
7. Line counts checked for all 14 files in the Files Built table by
   reading each file directly and confirming the last content line
   number:
   - `config.py`: 173 lines (no total claimed in WO; fine)
   - `infrastructure\migration_add_expiration_columns.py`: 71 lines -- matches
   - `infrastructure\db_client.py`: 249 lines (no total claimed; fine)
   - `domain\expiration_closer.py`: 150 lines -- matches
   - `infrastructure\schwab_instrument_parser.py`: 112 lines -- matches
   - `infrastructure\schwab_mapper.py`: 251 lines (incl. trailing blank) -- matches
   - `domain\exit_allocator.py`: 181 lines -- matches
   - `application\ingest_pipeline.py`: 299 lines -- matches exactly
   - `infrastructure\db_writer.py`: 269 lines -- matches
   - `infrastructure\expiration_reader.py`: 59 lines -- matches
   - `application\close_expired_options.py`: 123 lines -- matches
   - `P_020_Trade_Manager.py`: 224 lines -- matches
   - `tests\test_expiration_closer.py`: 148 lines (no total claimed; fine)

**DISCREPANCY FOUND -- schemas.py exceeds the 300-line hard cap:**

`python\database\schemas.py` was read in full. Its last content line is
line 303 (`put_call: Optional[Literal["CALL", "PUT"]] = None`), with line
304 being a trailing blank/EOF. **The file is 303 lines -- over the
project's documented hard 300-line-per-file cap** (confirmed live in
`.claude\skills\python-project-architecture\SKILL.md`: "File size | Hard
300 lines; split at 250"). This is the one row in the WO's own Files
Built table that gives only a delta (`+14`) with no total, unlike every
other touched/new file, all of which disclose a total specifically to
demonstrate cap compliance. The `+14` this WO added
(`Trade.expiration_date` / `Trade.settlement_price` with docstrings) was
not checked against the file's resulting total before OWNER_DONE was set.
(Root cause appears to be an unrelated pre-existing `Order` model, dated
2026-09-07 per its own docstring / WO-P400-E6.001, already present in
schemas.py before this WO touched it -- but current state is what the
300-line rule and this review gate against, regardless of which WO pushed
it over.)

**BLOCKED -- could not independently re-execute (no shell tool available
this session):**

This review session has no Bash/PowerShell/command-execution tool
available (confirmed via tool search; the restriction applies to any
subagent spawned from this session too). The following items from the
Independent Review instructions could therefore NOT be independently
re-run, and their WO-claimed results are UNCONFIRMED by this review:

- Full regression suite (`pytest tests python\database\tests -v`) --
  WO claims 160/160 passed. Not re-run.
- `tests\test_expiration_closer.py` run directly via pytest -- test
  content was read and hand-verified against the module's logic (see
  above), but not executed.
- Compile-clean check (`python -W error::SyntaxWarning -m py_compile`)
  for all 14 files -- not re-run.
- Live smoke test (`P_020_Trade_Manager.py close-expired-options`,
  no `--commit`) -- not re-run.
- Live DB schema check (`PRAGMA table_info(trades)` against
  `data\database\P_020_trades.db` to confirm `expiration_date` /
  `settlement_price` columns exist) -- not re-run. `db_client.py`'s DDL
  and the standalone migration script were both confirmed by reading
  the code (see above), but the live database file itself was not
  queried.

**Conclusion:** Status remains OWNER_DONE, NOT CLOSED. The Independent
Review Acceptance Criteria box above is left unchecked.

**BLOCKED ON:**
1. `schemas.py` must be brought back under the 300-line hard cap (split
   or trim) before this WO can close -- a real, confirmed violation of
   Hub-wide project policy, not a false positive.
2. A session with working shell/command execution must re-run: the full
   regression suite, the new test file directly, the py_compile check on
   all 14 files, the live smoke test, and the live DB `PRAGMA
   table_info(trades)` check -- and record actual output, before the
   Independent Review box can be checked and this WO moved to CLOSED.

## Occurrence Log

**2026-09-13** -- WO opened. Found while reviewing the regenerated P_020
dashboard after today's SYSTEM_ORDER/display_order fix exposed P_210 as
its own row for the first time (previously silently filtered out). Manual
stopgap (`close_p210_expired_worthless_20260913.py`) written same session
to close the 4 already-identified trades without waiting on the general
fix. Tony reviewed the dry-run and approved -- committed same session.
All 4 trades now `closed`, exits recorded, +$224.00 total confirmed
independently against the trades/exits tables (not just the script's own
printed output). `stats_export.py` and `generate_dashboard.py` re-run
after the commit -- dashboard now shows P_210 correctly: 4 closed, 50%
win rate, +$224.00, profit factor 1.23. General ingestion fix (this WO)
still not started.

**2026-09-15** -- Design presented to Tony (detection rule, file plan,
opt-in-vs-automatic decision) and approved before any code, per
system-doc-initializer APPROVAL GATE. Built same session -- see Files
Built above for the full file list and two design corrections found
mid-build. Full regression suite run live: 160/160 passed (4 initial
failures traced to `db_client.create_all_tables()` not carrying the new
columns for in-memory test DBs, fixed same session). All 14 touched/new
files compile clean under p140 `-W error::SyntaxWarning`. Live smoke test
of `P_020_Trade_Manager.py close-expired-options` (dry-run) against the
real DB: ran clean, "No open trades eligible" (correct -- the 4 known
P_210 NDXP trades are already closed via the 2026-09-13 manual stopgap).
Status PENDING -> OWNER_DONE. Independent Review still required in a
separate session before CLOSED.

**2026-09-15 (Independent Review, separate session)** -- Re-verified
Acceptance Criteria against live code. Eligibility logic, config
constants, and full field-propagation chain (parser -> mapper ->
allocator -> pipeline -> writer -> DDL) all confirmed real and correct.
Found `schemas.py` at 303 lines, over the project's 300-line hard cap --
not disclosed in the Files Built table (the only row with no total
given). Could not independently re-run the regression suite, the new
test file, the compile check, the live smoke test, or the live DB
`PRAGMA table_info` check -- this review session has no shell/command
execution tool available. Status left at OWNER_DONE, NOT moved to
CLOSED. See "Independent Review (2026-09-15)" section above for full
detail and BLOCKED ON list.

---

**Created:** 2026-09-13 (P_020 session)
**Author:** Claude
