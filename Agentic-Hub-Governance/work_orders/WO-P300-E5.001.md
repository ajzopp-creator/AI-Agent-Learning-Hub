# WO-P300-E5.001 -- Import-Linter Architectural Boundary Enforcement

**Status:** CLOSED (2026-09-14)
**Owner:** P_300
**Affects:** New tooling only -- no production files touched beyond Batch 1/2
  cleanup (PatternMetadata/MineCandidateRow relocated out of infrastructure/,
  utilities/ split into utilities/ + tools/) and the new `python\.importlinter`
  config.
**Depends On:** None.
**Acks:** N/A -- P_300-internal tooling only, no schema/signal contract changed.

---

## WHY

Filed 2026-07-17 (evening) during a live-batch-wait discussion of a KB
article ("10 Python Libraries That Made Me Stop Writing Messy Code").
The article's diagnosis on `import-linter`: Python's import system has
no enforcement mechanism, so a clean layer diagram (domain /
infrastructure / application, as documented in P_300's own
architecture doc and `p300-project-context` skill) is honor-system
only -- nothing stops a future session (mine or otherwise) from
importing across a layer boundary because it's the shortest path.

P_300 specifically has named, documented layers already (unlike most
of the other Hub projects, which weren't designed with this split) --
making it the only project where writing real `import-linter`
contracts is a natural fit rather than a forced retrofit. Previously
enforced only by session-by-session review and the skill file's
written rules, never by an automated check.

## SCOPE NOTE

- P_300 only. Not Hub-wide -- other projects (P_010/P_020/P_115/
  P_400/P_800) may not share this same formal layer split; bolting
  contracts onto them is a separate, bigger conversation, not part of
  this WO. (That conversation is now open: WO-P000-E30.001, filed
  2026-09-10, cites this WO directly.)
- No CI exists in this environment -- PEH is a manual, operator-
  triggered step. This is a PEH check run before closing a WO, not an
  automatic gate. Weaker enforcement than the article assumes, still
  real value over pure manual review.

## NEXT STEPS -- ALL FOUR COMPLETE (2026-09-10)

1. DONE -- prose layer contract defined, Tony sign-off obtained.
2. DONE -- `python\.importlinter` written (layer order bottom-to-top:
   domain -> utilities -> infrastructure -> (application | tools,
   independent siblings) -> cli_commands). `promote_gate.py`'s
   cli_commands->domain/infrastructure direct import needed no special
   exception -- a standard Layers contract already permits a higher
   layer to import any lower layer. First real `lint-imports` audit
   run via PEH (`run_this_P300_20260910_213000.py`): **PASS -- 91
   files analyzed, 159 dependencies, 0 violations.**
3. DONE -- nothing to triage; clean pass. One harmless parser skip
   noted, not a violation: `application/daily_evaluate_pipeline_
   backup_2026-07-25_WO-P800-E3.003.py`, a stale backup whose dotted
   filename confuses import-linter's module-name parsing. Minor
   housekeeping candidate, not acted on (out of scope).
4. DONE -- file-level plans presented before both Batch 1 (schema/
   infrastructure cleanup) and Batch 2 (utilities/->tools/ split)
   builds, ahead of writing the config in step 2.

## VERIFY / Acceptance Criteria -- fresh-session Independent Review (2026-09-14)

Reviewing session wrote none of the code under review (INIT-only
start, confirmed live disk state before any other action -- M-017/
M-054 discipline).

- [x] `python\.importlinter` exists on disk; content matches the layer
      order and exception logic described in tasks/todo.md's
      2026-09-10 (8th) entry. Read in full this session.
- [x] Directory tree confirms the claimed Batch 2 split: `python\
      utilities\` and `python\tools\` both exist as siblings (one dir
      before this WO).
- [x] `python\.import_linter_cache\` exists, created 2026-09-10
      21:26:19 -- matches the `run_this_P300_20260910_213000.py`
      21:30 run, corroborating a real `lint-imports` execution
      occurred, not just a claimed one (M-120).
- [x] tasks/todo.md's 2026-09-10 (10th) entry states the audit result
      in matching specific detail (PASS / 91 files / 159 deps / 0
      broken) -- consistent across the WO file, the P_300 boot
      summary, and this session's own read of the raw log entry.
- [~] Class-level relocation (PatternMetadata/MineCandidateRow out of
      infrastructure/) -- confirmed indirectly: no matching filename
      in `python\infrastructure\`, directory contents consistent with
      a completed split. Not verified via a full import-statement
      grep of every file. MONITORING item, not a blocker -- no
      contrary evidence found; re-confirm on next full architecture
      pass of P_300.
- [x] Doc-drift corrected this session: Status header and NEXT STEPS
      body were still "PENDING (not planned, not built)" / "not
      started" despite the work completing 2026-09-10 -- same shape as
      EC-008 (Status Header Sync). Fixed in this edit.

**Verdict: CLOSED.** Claimed work matches live-disk evidence on every
surface checked (config file, directory tree, cache timestamp, raw
todo.md log). No violations found, no blocker outstanding.

## Completion Gate (ref WO-P000-E3.001)

- [x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
- [x] Any new or changed shared-resource location reflected in:
    - P_000_SYSTEM_DOCUMENTATION.md (Document Index) -- N/A, no
      shared-resource path added; `.importlinter` is P_300-internal.
    - Affected project CLAUDE.md files -- N/A, same reason.
    - Affected project skill files -- p300-project-context skill's
      layer description already matches the shipped contract.
- [x] CALLER PROPAGATION: N/A -- no capability added/changed/removed
      that any caller invokes; `.importlinter` is a standalone
      PEH-run check, nothing imports it.
- [x] IMPERATIVE SWEEP: N/A -- no RULE changed; a manual verification
      tool was added.
- [x] Downstream projects in Affects: notified -- P_300-internal,
      doc-only in effect; WO-P000-E30.001 (Hub-wide rollout
      assessment) already references this WO directly.
- [x] No sys.path side-channels introduced.
- [x] Schema/signal contract changed: none -- N/A.
- [x] DRAFT files for this WO deleted from work_orders\ -- none were
      created.
- [x] One ledger entry per WO confirmed -- this file.
- [x] No open VERIFY/Acceptance Criteria item deferred to Independent
      Review by label -- the one `[~]` item above is an explicit
      MONITORING item, not deferred owner work.
- [x] PREMISE VERIFICATION: this WO's completion claim was inherited
      from tasks/todo.md session logs, not this WO's own original
      investigation -- checked against live disk this session (config
      file, directory tree, cache timestamp, raw todo.md entries).
      Named above.

---

Created: 2026-07-17 (evening), during the CNK/GURE/LCID/NNDM/TWST
BulkAddPattern batch's long-running "post" walk-forward wait. Deferred
until that batch resolved and normal session work resumed (M-015/M-017
discipline: live docs, not thread memory, are authoritative).

---

## Independent Review (2026-09-14)

Fresh session, INIT-only start, wrote none of the code under review
(confirmed live-disk state before any other action). Checked the
Completion Gate and VERIFY items above against real files:
`python\.importlinter`, the `python\utilities\` + `python\tools\`
split, `python\.import_linter_cache\` timestamp, and tasks/todo.md's
raw 2026-09-10 (7th)-(10th) entries. All consistent, no contradictions
found. Corrected the stale Status header and NEXT STEPS body in the
same edit (doc-drift flagged at this session's boot, EC-008 pattern --
Status Header Sync). **CLOSED.**
