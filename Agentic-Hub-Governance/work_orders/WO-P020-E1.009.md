# WO-P020-E1.009 -- Surface Buying Power / Cash Available for Trading in P_000 Account Parameters

**Status:** CLOSED -- Independent Review completed 2026-08-22, separate session from implementation (2026-07-26). See Independent Review section below.
**Owner:** P_020
**Affects:** P_000 (file owner), P_400 (reference consumer only, no code change)
**Depends On:** none -- `schwab_balance_pull.py` (P_020) already live, already pulls both fields
**Related:** WO-P400-E4.001/E4.002 (Schwab API live integration on the P_400 side --
  snapshot/chain fetch; this WO is the balance-side counterpart, P_020 lane)

---

## WHY

P_400's `evaluate`/`spec`/`compare` require a manual `--cash` flag on every run.
Tony's explicit call (2026-07-25, P_400 session): keep it manual -- cash is a
deliberate per-trade decision, not something P_400 should auto-read from the
broker (architecture doc Section 3.3, "cash is per-trade, not tracked").

Right now the only way to know the real numbers before typing `--cash` is to
check the Schwab app/TOS by hand. P_020's `database\infrastructure\schwab_balance_pull.py`
already pulls both `cashAvailableForTrading` and `buyingPower` from
`securitiesAccount.currentBalances` on every balance-pull run (`pull_balance()`
already returns both, `store_balance()` already writes both into the
`account_balances` SQLite table) -- but neither value is surfaced anywhere
Tony can glance at quickly. This WO doesn't add a new Schwab API call; it
surfaces data already being pulled.

## SCOPE

1. Extend the existing balance-pull invocation (`database\application\account_commands.py`,
   or wherever the weekly job calls it) so that, after `store_balance()` writes
   the SQLite snapshot, it also writes two new rows into
   `projects\P_000_PythonClaudeLocalLLM\config\P_000_Account_Parameters_Current.md`'s
   "Active Parameters" table: "Buying Power" and "Cash Available for Trading" --
   same `pull_balance()` return dict already in hand, no new API surface area.
2. `P_000_Account_Parameters_Current.md`: two new rows in Active Parameters,
   plus a one-line note under "Critical Rules > Cash Balance (Separate Concept)"
   clarifying these are broker-reported reference numbers only -- P_400's
   `--cash` stays a manual figure Tony types himself, never auto-read from
   these fields.
3. No P_400 code changes. `params_reader.py` does not need to parse these two
   new fields unless/until Tony later wants automatic `--cash` (explicitly
   out of scope this WO).

## OPEN DESIGN QUESTION (resolve with Tony when this WO is picked up)

Piggyback on the existing weekly balance-pull cadence only, or also add an
on-demand refresh command for mid-week checks? This is a P_020 job-runner
scope decision, not decided here (filed from a P_400 session).

## OUT OF SCOPE

- Auto-filling `--cash` on P_400's `evaluate`/`spec`/`compare` -- Tony's
  explicit call (2026-07-25) to leave it manual.
- Any change to P_400's `params_reader.py` or `config.py`.
- Any new Schwab API endpoint/call -- `pull_balance()` already fetches both
  fields today.

## ACCEPTANCE CRITERIA

- `P_000_Account_Parameters_Current.md` shows current Buying Power and Cash
  Available for Trading, refreshed on whatever cadence is decided above.
- Values match `pull_balance()`'s return for the AJZ account exactly.
- P_400's `--cash` behavior completely unchanged -- still required, still
  manual, still typed by Tony.

---

**Created:** 2026-07-25 (P_400 session, Tony's request)
**Logged by:** Claude


## Open Design Question -- Resolved

Piggyback-only vs. separate refresh command: resolved by the implementation itself, not a separate decision. `write_balance_to_p000_params()` is called from inside `run_balance_command()` (`application/account_commands.py`), which is the single entry point behind `balance --account`. Every invocation -- weekly scheduled job or Tony's manual run -- triggers the same write. No separate refresh command exists or is needed.

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[x] Any new or changed shared-resource location reflected in P_000_SYSTEM_DOCUMENTATION.md / CLAUDE.md / skill files -- N/A, no shared-resource location changed; write target is P_000's existing params file, already documented as canonical in WO_COMPLETION_GATE.md's Hub Canonical Path Standards
[x] CALLER PROPAGATION: single caller is `run_balance_command()` in `application/account_commands.py`, itself the only handler behind CLI `balance --account` -- confirmed by direct read, no other entry point needs wiring
[x] IMPERATIVE SWEEP: N/A -- no rule/Must/Must-Not changed, additive write only
[x] Downstream projects in Affects: notified -- P_000 (file owner) and P_400 (reference consumer) noted in Affects:; P_400's `--cash` flag confirmed untouched, no P_400 file referenced anywhere in this change
[x] No sys.path side-channels introduced (ref WO-P000-E2.003) -- confirmed, no sys.path edits in account_commands.py or schwab_balance_pull.py
[x] If schema/signal contract changed: version bumped, consuming projects notified -- N/A
[x] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\ -- none exist (confirmed via directory listing, 2026-08-22)
[x] One ledger entry per WO confirmed

## Independent Review -- 2026-08-22

Performed in a separate session from the one that implemented and tested the fix (implementation/testing: 2026-07-26; this review: 2026-08-22).

Verified independently against live code, live tests, and live disk state:
- `application/account_commands.py` read directly: `run_balance_command()` calls `write_balance_to_p000_params(balance['buying_power'], balance['cash_available'])` whenever either field is not None, immediately after `store_balance()` -- matches Scope item 1 exactly.
- `infrastructure/schwab_balance_pull.py` read directly: `pull_balance()` returns `cash_available` (prefers `cashAvailableForTrading`, falls back to `availableFunds` -- WO-P020-E1.016's fix, already live in this same file) and `buying_power` (`curr.get("buyingPower")`) -- field names match what `account_commands.py` passes through exactly, no naming drift.
- Compile-clean under p140 `-W error::SyntaxWarning`: PASS (`account_commands.py`, `schwab_balance_pull.py`), run live this session.
- `tests/test_schwab_balance_pull.py`: 4/4 passing, run live this session (`test_falls_back_to_available_funds_when_cash_available_absent`, `test_falls_back_to_available_funds_when_cash_available_is_none`, `test_prefers_cash_available_when_present_and_nonzero`, `test_both_absent_returns_none_not_fabricated_value`).
- `P_000_Account_Parameters_Current.md` read directly, live on disk, same day: Buying Power and Cash Available for Trading rows present and current (see WO-P020-E1.014's Independent Review, same session, same file); the required Scope item 2 note is present verbatim under "Cash Balance (Separate Concept)": *"Buying Power and Cash Available for Trading in the table above are broker-reported reference numbers only. P_400's `--cash` flag stays a manual figure Tony types himself -- never auto-read from these fields."*
- No P_400 file referenced, read, or modified anywhere in the implementation -- Acceptance Criterion 3 (P_400's `--cash` behavior completely unchanged) satisfied by absence of any P_400 touch, not just by claim.

All Acceptance Criteria independently confirmed met. Values match `pull_balance()`'s return dict exactly (same field names, no transformation beyond `write_balance_to_p000_params()`'s own formatting, independently verified in WO-P020-E1.014's review).

**Status note:** this WO sat at IN_PROGRESS rather than OWNER_DONE despite the 2026-07-26 note describing complete, tested work -- status was never bumped, so no Completion Gate checklist was ever attached (not an EC-005 violation, since OWNER_DONE was never set; simply a status-hygiene miss). Checklist added now, at the same edit that moves Status to CLOSED, consistent with EC-005's requirement.

Status -> CLOSED.

## CHANGE LOG

- 2026-08-22: Independent Review performed (separate session from 2026-07-26 implementation). Open Design Question resolved (piggyback via single entry point, confirmed in code). Completion Gate checklist added. Status IN_PROGRESS -> CLOSED.
