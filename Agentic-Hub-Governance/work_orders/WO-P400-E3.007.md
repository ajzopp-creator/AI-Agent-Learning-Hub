# WO-P400-E3.007 -- Move Tier-1 Screen Into INIT (STEP 0.7), Not a Separate Manual Invocation
**Status:** CLOSED
**Owner:** P_400
**Affects:** (none -- internal P_400 only; SIP sequencing change, no code touched)
**Depends On:** None
**Acks:** N/A

---

## WHY

Discussed 2026-07-05 in the context of applying loop-engineering concepts
(external article: "How To Use Loop Engineering To Build A Self-Improving
Quant Trading System") to P_400. screen-all is deterministic and
read-only (WO-P400-E2.018 already auto-disposes FAILs inside it) -- Tony's
call was to run it automatically as part of session INIT rather than as
a scheduled cron job or a separately-invoked manual step later in the
session. No new automation infrastructure needed; just move where in the
SIP sequence it fires.

## SCOPE

`docs\prompts\P_400_SESSION_INITIALIZATION_PROMPT_v2_0.md`:
1. New STEP 0.7 -- Auto Tier-1 Screen, runs immediately after STEP 0.6
   (Session Header), same command as before.
2. STEP 1 renamed Tier Selection -- reviews the PASS list already
   produced by 0.7; no longer runs screen-all itself.
3. Version bumped v2.2 -> v2.3, changelog entry added.
4. Backup of pre-edit file saved as `.bak_2026-07-05` before editing.

## OUT OF SCOPE

- Any change to screen.py, dispose_failed.py, or cli.py -- this is SIP
  sequencing only, zero code touched.
- True scheduled automation (Windows Task Scheduler) -- considered and
  explicitly rejected in favor of running at INIT, so a human is always
  present to read the output same session.

## ACCEPTANCE CRITERIA

- SIP STEP 0.7 exists and precedes STEP 1 in document order.
- STEP 1 no longer instructs running screen-all -- selection only.
- Next live session confirms the new sequence produces no surprises
  (screen-all output appears before Tony is asked to name a symbol).

---

## CHANGE LOG

- 2026-07-05 v1.0: Opened and CLOSED same session. SIP edited, backup
  saved, version bumped to v2.3. No code changes, no consumers to Ack.

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths
[x] N/A -- no shared-resource location changed, SIP is P_400-internal
[x] N/A -- Affects: none, internal P_400 only
[x] N/A -- no code touched, no sys.path relevance
[x] N/A -- no schema change
[x] Confirmed -- no DRAFT files for E3.007 in Agentic-Hub-Governance\work_orders\
[x] Confirmed -- one ledger entry (WO-P400-E3.007.md)