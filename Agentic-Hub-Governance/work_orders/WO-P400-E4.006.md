# WO-P400-E4.006 -- No Market-Holiday Calendar (market_open + sessions_since_earnings Both Approximate)

**Status:** OWNER_DONE (2026-07-27) -- PEH verify passed clean, see Execution below
**Owner:** P_400
**Affects:** none -- internal P_400 only
**Depends On:** none
**Related:** WO-P400-E4.005 (`is_market_open_now()`, flagged this limitation live 2026-07-26); WO-P400-E2.023 (`_sessions_since_earnings()`, same gap, independently flagged same class of limitation 2026-07-24)

---

## WHY

Two separate P_400 functions approximate market-calendar behavior using
wall-clock/weekday math only, with no actual US market holiday calendar
anywhere in the codebase:

1. `domain\market_hours.py`'s `is_market_open_now()` (WO-P400-E4.005) --
   Mon-Fri 9:30-16:00 ET, no holiday check. On a weekday holiday (July 4th,
   Thanksgiving, Christmas, Labor Day, etc.) this returns `market_open: true`
   when the market is actually closed -- TAPE's MARKET_CLOSED gate
   (architecture Section 2.5, exception #3) can't fire on those days, and
   Schwab quote/price-history data would also be stale/unavailable with
   nothing catching it.

2. `application\evaluate_signal.py`'s `_sessions_since_earnings()`
   (WO-P400-E2.023) -- weekday-count approximation, explicitly flagged in
   that WO's own Execution notes as `no holiday calendar`. Feeds the
   `POST_EARNINGS_STABILIZATION_SESSIONS` MACRO caution (default 3
   sessions) -- a holiday inside that window makes the count wrong by one
   session per holiday, silently shortening or lengthening the caution
   window.

Both were built and flagged independently, in different sessions, same
root gap, never connected until this review.

## Root Question -- Needs Tony's Call Before Build

**Approach A -- hardcoded holiday list.** A small constant/module (e.g.
`US_MARKET_HOLIDAYS` in config.py, or a new `market_holidays.py`), one
line per year, sourced from NYSE's published calendar. Zero new
dependencies. Needs a manual update each year (or listed several years
out at once). Simple, auditable, matches the Hub's existing bias toward
explicit values over hidden logic.

**Approach B -- calendar library.** e.g. `pandas_market_calendars` or
`exchange_calendars` (pip-installable in p140). Handles NYSE holidays,
early closes, and year-rollover automatically, no manual maintenance. New
third-party dependency in the p140 conda env -- import surface neither
current module has today.

Leaning Approach A given the Hub's existing bias against new dependencies
where a short static list does the job (same reasoning behind E4.005's
own wall-clock-only, don't-trust-the-API's-own-session-metadata choice) --
but this is explicitly Tony's call, not decided here.

## Scope (proposed, not yet built -- pending Root Question)

| File | Layer | Change |
|---|---|---|
| `domain\market_holidays.py` (NEW) | domain | Single source of truth: `is_market_holiday(date) -> bool`; holiday list/logic per Root Question decision |
| `domain\market_hours.py` | domain | `is_market_open_now()` -- add holiday check alongside existing wall-clock check |
| `application\evaluate_signal.py` | app | `_sessions_since_earnings()` -- skip holidays in the weekday walk |
| `test_market_hours.py` | tests | New holiday-date boundary tests (a Saturday-observed holiday like July 4 2026 doesn't prove the weekday-holiday path -- need a true weekday holiday fixture, e.g. Labor Day Sept 7 2026 or Thanksgiving Nov 26 2026) |
| `test_evaluate_signal.py` | tests | Boundary test: a real holiday inside the 3-session stabilization window doesn't miscount |

## Out of Scope

- Early-close days (day after Thanksgiving, Christmas Eve, etc.) -- a
  separate question neither originating WO raised. Noted here so it isn't
  forgotten, but not building it without its own decision.
- Retroactively re-verifying E2.023's own CLF live-test window against
  this fix -- that window is likely closed by the time this WO builds.

## Acceptance Criteria

- [ ] Root Question decided (Approach A vs B).
- [ ] Single holiday-check utility built, consumed by both
      `market_hours.py` and `evaluate_signal.py` -- not duplicated
      logic in each.
- [ ] Tests: at least one real 2026 weekday holiday correctly flags
      `market_open=False` and is correctly skipped in the
      sessions-since-earnings count.
- [ ] Full pytest suite green via PEH.
- [ ] Live verification not required (pure date logic, no live API call)
      but a real evaluate/fetch-snapshot run on or adjacent to a holiday
      is a nice-to-have if the calendar lines up with a live session.

---

**Created:** 2026-07-27 (P_400 session, filed from an enhancement-candidates
review this session -- not yet built, Root Question outstanding)
**Author:** Claude / Tony

---

## Root Question -- DECIDED 2026-07-27

Approach C, not A or B: pure rule-based computation in plain Python, zero
dependencies, zero per-year data to maintain. Every NYSE holiday reduces
to Nth-weekday-of-month, last-weekday-of-month, a fixed date with
weekend-shift, or Good Friday via the Anonymous Gregorian Easter
algorithm. All rules verified against NYSE's published 2026-2028 calendar
(web search + a standalone script) before any file was written -- every
date matched, including the 2027 Juneteenth weekend-shift and the 2028
New Year's Day non-observance exception. This beats Approach A (hardcoded
list) because a hardcoded list needs annual manual maintenance and fails
silently (not loudly) if forgotten -- exactly the class of bug this
session spent most of its time on for an unrelated reason (WO-P800-E3.003
skill drift). It beats Approach B (calendar library) because it needs no
new dependency in p140 for logic this small and this verifiable.

## Ownership -- DECIDED 2026-07-27

Stays in P_400 (``domain\market_holidays.py``), not promoted to
``shared_resources``. Checked first: P_020 does not currently have any
market-open/holiday logic (zero hits searching its codebase), so there
was no existing P_020 ownership to preserve or take over. Precedent
(``schwab_client.py``, ``atr.py``) is promotion-on-real-second-use, not
speculative generalization -- revisit if/when P_020 or another project
actually needs this.

## Execution -- 2026-07-27 (same session)

| File | Change |
|---|---|
| ``domain\market_holidays.py`` (NEW) | 125 lines. ``is_market_holiday(d) -> bool``, ``market_holidays(year) -> set[date]``, plus Nth-weekday/last-weekday/Easter/weekend-shift helpers. No I/O, no dependency. |
| ``domain\market_hours.py`` | 51 lines (was 47). ``is_market_open_now()`` now checks ``is_market_holiday()`` alongside the existing weekend/wall-clock check. Docstring's "known limitation" note removed. |
| ``application\evaluate_signal.py`` | 292 lines (was 290). ``_sessions_since_earnings()`` now skips holidays in its weekday walk, not just weekends. |
| ``test_market_holidays.py`` (NEW) | 80 lines, 13 tests -- all ten 2026 holidays, one ordinary weekday, the 2027 Juneteenth weekend-shift, the 2028 New Year's non-observance exception. |
| ``test_market_hours.py`` | 64 lines (was 57). +1 test: Labor Day 2026-09-07 correctly closed. |
| ``test_evaluate_signal.py`` | 296 lines (was 268). +1 test: holiday-aware session counting (fixed anchor 2026-06-01, spans Juneteenth + observed July 4th). |

**Two unrelated bugs found and fixed during PEH verification, not part of
this WO's original scope but blocking a clean run:**

1. ``test_schwab_market_data.py``'s ``_mock_client()`` helper only
   configured ``client.get_quote`` (singular). Production code was
   switched to ``client.get_quotes`` (plural) earlier this same session
   (BRK/B slash-symbol fix, undocumented WO -- ``get_quote()`` can't
   handle non-alphanumeric symbols per schwab-py's own docstring). Test
   mock was never updated to match, so it silently exercised an
   unconfigured MagicMock instead of the real mocked response. Fixed:
   mock now configures both.
2. ``test_sessions_since_earnings_weekday_count`` (originally patched by
   WO-P400-E4.005's addendum) was itself weekday-of-today-dependent --
   that patch was verified only against a Sunday and silently broke on
   the next weekday "today" (Monday 2026-07-27, this WO's own PEH run:
   got 6, expected 5). Root cause: the old backward-walk never accounted
   for ``_sessions_since_earnings()`` counting today itself when today is
   a weekday. Fixed with Tony's explicit go-ahead: new construction finds
   the N most recent qualifying days working back from today (today
   included if it qualifies) -- correct regardless of today's weekday.
   Assertion value unchanged (``target = 5``); only the date-construction
   method changed.

**Verification (Tony's terminal, p140, via PEH handoff):** first run
surfaced the three issues above (one holiday-unrelated NameError, mine --
missing inline ``datetime`` import in the new test -- fixed same pass).
Second run: **285 passed, 0 failed, 3 warnings** (all three warnings
pre-existing and unrelated: Pydantic v2 config-class deprecation in
``shared_resources``, a docstring escape-sequence warning in
``test_schwab_market_data.py``, and a ``websockets.legacy`` deprecation
from a dependency -- none touched this session, not fixed here).

## Acceptance Criteria (status)

- [x] Root Question decided -- Approach C (rule-based, zero dependencies).
- [x] Single holiday-check utility built, consumed by both
      ``market_hours.py`` and ``evaluate_signal.py``.
- [x] Tests: 2026-2028 real holidays confirmed correct (13 dedicated
      tests), plus both consumer sites' regression tests updated.
- [x] Full pytest suite green via PEH -- 285 passed, 0 failed.
- [ ] Live verification (nice-to-have, not required -- pure date logic,
      no live API call) -- not attempted this session.

**Status held at OWNER_DONE, not CLOSED** -- per the Independent Review
Requirement (WO_COMPLETION_GATE.md), this session implemented the WO and
cannot also be the closing review. A fresh session should re-verify
against actual code/output before CLOSED, same pattern as E4.001-E4.005
and E2.023 earlier this week.