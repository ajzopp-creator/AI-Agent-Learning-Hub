# WO-P400-E8.001 -- Cross-Project Bridge Import Collision (p020_order_writer.py) + Regression Test Coverage

**Status:** OWNER_DONE (fix applied + live-verified 2026-09-11; regression test file not yet written -- see ACCEPTANCE CRITERIA)
**Owner:** P_400
**Affects:** shared_resources\python_utils\p020_order_writer.py -- Hub-wide bridge, used by any project writing orders into P_020's database (P_400 confirmed via this incident; other consumers not yet audited)
**Depends On:** none
**Acks:**

---

## WHY

Live 2026-09-11, 12:06 ET: `record EXPD --order-id 15108638441 --paper` wrote the
P_400 vault record cleanly (SUBMITTED, vault_write=OK) but the P_020 order write
failed: `cannot import name 'Exit' from 'schemas'` pointing at P_400's own
`python\schemas.py`, not P_020's.

Root cause: `db_order_writer.py` (P_020's own file) does `from schemas import
Exit, Order, Trade` -- unqualified, meant to resolve against P_020's
`database\schemas.py` once `_load_p020()` inserts that folder at the front of
`sys.path`. But `sys.modules` caches by bare name process-wide; P_400's own
`schemas.py` was already loaded into `sys.modules['schemas']` earlier in the
same process (for P_400's own trade-spec models), so Python returned the
cached P_400 module instead of re-resolving via the path insert. P_400's
schemas.py has no Order/Trade/Exit, so the import failed on the first missing
name.

Same bug class as two prior live incidents in this same bridge file:
`infrastructure`/`domain` (WO-P400-E6.001, 2026-09-07) and `config`
(2026-09-09) -- each time, a P_020 module import collided with a same-named
P_400 module already cached, and each time the fix was adding the colliding
name to `_load_p020()`'s `_COLLIDING_PACKAGES` stash/restore tuple. `schemas`
was explicitly excluded from that tuple based on a comment claiming "no
caller in this Hub has a same-named top-level module" -- false as of this
incident.

## WHAT

1. Add `"schemas"` to `_COLLIDING_PACKAGES` in `p020_order_writer.py`'s
   `_load_p020()` -- same stash/restore mechanism already used for
   `infrastructure`/`domain`/`config`.
2. Correct the now-false docstring sentence claiming no caller has a
   same-named `schemas` module.
3. Update the module-level comment above `_COLLIDING_PACKAGES` with a dated
   note matching the existing `config`/`infrastructure`/`domain` entries, so
   the incident history stays in one place.
4. Per Regression Test Governance (python-project-architecture skill,
   Hub-wide non-negotiable): a permanent `tests/test_p020_order_writer.py`
   encoding this fix as an assertion -- not yet written.

## FIX APPLIED (2026-09-11)

Patched live via `windows-mcp:PowerShell`, same read -> backup -> normalize
CRLF -> match-count-guard -> replace -> UTF-8-no-BOM-write -> read-back-verify
pattern as all other Hub file edits. Backup:
`p020_order_writer.py.backup_2026-09-11`.

`_COLLIDING_PACKAGES` changed from `("infrastructure", "domain", "config")` to
`("infrastructure", "domain", "config", "schemas")`. Docstring's false
"schemas isn't stashed" sentence corrected to reflect the addition and
explain why (P_400 has its own top-level schemas.py, unrelated in content,
colliding only on the bare module name).

## LIVE VERIFICATION (2026-09-11, 12:18 ET)

Re-ran `record EXPD --order-id 15108638441 --paper` (safe re-run -- P_020-side
dedup keys on `schwab_order_id`, already-written for this order):
```
P_020 order write: OK (order_id=8)
```
Confirms the fix end to end through the real CLI path, not just a unit test.
P_400 vault note bumped to version 2 on the re-run rather than being skipped
-- vault-side dedup doesn't cover this case, noted for awareness, not in
scope for this WO.

## ACCEPTANCE CRITERIA

- [x] `record` on a symbol whose P_020 write previously failed with the
      `schemas` collision now succeeds (`P_020 order write: OK`).
      -- VERIFIED live 2026-09-11, 12:18 ET, EXPD order_id=15108638441,
      P_020 order_id=8 (see LIVE VERIFICATION above).
- [x] `_COLLIDING_PACKAGES` includes `"schemas"`; docstring's false claim
      corrected.
      -- VERIFIED live 2026-09-11 via PowerShell read-back after the edit.
- [ ] Permanent `tests/test_p020_order_writer.py` exists, encoding this fix
      as an assertion (e.g. a fixture where a caller's `sys.modules['schemas']`
      is pre-populated with an unrelated module lacking `Order`/`Trade`/`Exit`,
      asserting `_load_p020()` still resolves P_020's real schemas.py) -- per
      Regression Test Governance, non-negotiable, not yet satisfied by today's
      live-verification alone.
- [ ] Audit which other Hub projects call into `p020_order_writer.py` /
      `_load_p020()` (P_820 Order Signal Capture is a likely second consumer,
      per its own project context) -- not yet confirmed whether any of them
      have their own `schemas.py`/`config.py`/etc. that could still collide
      on a name not yet in the tuple.

---

*Filed 2026-09-11, same session as discovery and fix (P_400 `record` EXPD
paper trade). Fix applied and live-verified same session; WO opened
retroactively per Tony's explicit request to track the outstanding
regression-test and cross-consumer-audit items rather than let the live
patch go undocumented, per WO_COMPLETION_GATE Premise Verification standard.*
