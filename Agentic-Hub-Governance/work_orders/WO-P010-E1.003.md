# WO-P010-E1.003 -- Daily Posture Pipeline Must Fail Loud, Not Silent

**Status:** OWNER_DONE
**Owner:** P_010
**Affects:** P_400, P_115, P_300, P_020
**Depends On:** WO-P010-E1.002 (root cause this WO is reacting to)
**Related:** WO-P010-E1.002

---

## WHY

E1.002 sat broken 4 trading days (7/14-7/17) with zero surfaced warning.
`P_010_daily_posture_v5.py` threw `ERROR: File not found` to its own log
every morning, then the note writer and intraday script both proceeded
anyway on stale data as if nothing happened. Nobody saw a problem until a
mtime/internal-timestamp mismatch got noticed by accident during a P_400
session. That's the actual failure -- not the typo, the silence around it.

## PROBLEM

Current pipeline has no fail-loud path:
- `P_010_daily_posture_v5.py` errors to a log file only. Log files are
  never checked unless someone goes looking.
- Downstream steps (note writer, intraday check) don't check whether the
  morning run actually succeeded before consuming
  `P_010_RiskConfig.json` -- they just read whatever's there.
- `P_010_RiskConfig.json`'s `timestamp` field is the only real staleness
  signal, and nothing compares it against today's date automatically.
- Guardian bat (`P_010_Start_Guardian.bat`) watches for sync/API errors
  but does not appear to check for a failed morning run vs. a skipped one
  (SKIP_TODAY.flag) -- both currently look identical downstream.

## PROPOSED FIX (not yet built, needs Tony sign-off on approach)

1. `P_010_daily_posture_v5.py`: on any exception (file-not-found or
   otherwise), do not let the batch chain continue silently. Options,
   pick one:
   a. Non-zero exit code + `.bat` wrapper checks it and halts before
      calling the note writer / intraday step.
   b. Write an explicit `MORNING_RUN_FAILED.flag` file the intraday
      script and Guardian both check for before proceeding.
2. Guardian (or the daily batch itself) fires a `windows-mcp:Notification`
   toast on failure -- title "P_010 Morning Run FAILED", message = the
   exception line. Tool already available, no new dependency.
3. Note writer and intraday script both add a same-day `timestamp` check
   against `P_010_RiskConfig.json` before running -- if stale, note
   writer should either skip with a visible banner or mark the note
   `[STALE POSTURE -- MANUAL REVIEW]` instead of writing clean HALF/FULL/
   OFF as if it were fresh.
3. Downstream consumers (P_400 STEP 0, P_115/P_300 screens) already check
   `timestamp` per project-context skills -- keep that as a second layer,
   not the only layer. The point of this WO is source-level alerting, not
   pushing the check further downstream every time.

## ADDENDUM (2026-07-17, same-day manual re-run)

Confirmed a second, related noise problem while manually re-running the
pipeline to close out E1.002: `P_010_run_intraday_vp_check.bat` printed
both `[SUCCESS] Intraday VP validation completed.` AND, immediately after,
`[ERROR] Intraday check failed with exit code 0` -- despite the run being
genuinely clean (JSON written correctly, P_010_RiskConfig.json updated
correctly, morning_risk_mode/intraday_signal/final risk_mode all
consistent and correct: HALF -> DOWNGRADE -> OFF). Exit code 0 is a
success code; the wrapper's errorlevel check appears inverted or
otherwise wrong, so it prints ERROR on success.

This is the same category of problem this WO already exists to fix: a
false ERROR on a good run trains the operator to distrust/ignore
`[ERROR]` output, which defeats the point of the fail-loud alerting this
WO proposes. Any alerting/notification mechanism built under item 2 above
MUST NOT fire on this false positive -- fix the errorlevel check in
`P_010_run_intraday_vp_check.bat` (or wherever the exit code is set/read)
as part of this WO, not as an afterthought.

Added to VERIFY: re-run intraday check on a known-good pass, confirm no
`[ERROR]` line prints alongside `[SUCCESS]`.

## SCOPE NOTE

This is detection/alerting only. It does not change risk_mode calculation
logic, does not touch P_400/P_115/P_300 consumer code, and does not
require code changes outside P_010.

## VERIFY

- Deliberately break a grid filename, run the morning batch, confirm:
  - batch halts before note writer / intraday step, OR both clearly mark
    output as stale
  - a toast notification fires
  - restoring the correct filename and re-running clears the flag and
    produces a clean run

---

## Priority Update (2026-08-10, P_010 session)

WO-P010-E1.002 moved to OWNER_DONE same session -- filename fix confirmed
verified on disk, dependency this WO lists is now clear. Tony directed this
WO (E1.003, fail-loud alerting) to top of queue: the filename typo itself
is fixed, but nothing in the pipeline would catch a repeat of the same
failure mode automatically -- that gap is what this WO exists to close.
Status remains PENDING -- no implementation work has started yet, only
the dependency block lifting and priority reordering.

**Logged by:** Claude, P_400 session, 2026-07-17, per Tony's request

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths -- yes, all new files
    (toast_notify.py, staleness_check.py) live in P_010's own python\
    folder; no shared-resource path needed (this WO's scope note
    restricts it to P_010 source-level alerting only)
[x] Any new or changed shared-resource location reflected -- N/A, no
    shared-resource location touched
[x] CALLER PROPAGATION -- confirmed via live grep, 2026-08-26: the
    MORNING_RUN_FAILED.flag halt + staleness check is wired into all
    consumers that need it -- P_010_daily_posture_v5.py (writes flag on
    exception, imports toast_notify), P_010_intraday_vp_check_v4.py
    (imports and calls staleness_check.is_morning_data_stale),
    P_010_write_daily_note.py (same import/check pattern), and
    P_010_daily_posture.bat (checks "if exist MORNING_RUN_FAILED.flag"
    and halts before STEP 2). Four callers named and confirmed -- not
    just the owning module.
[x] IMPERATIVE SWEEP -- DONE 2026-08-26. p010-project-context SKILL.md
    updated: Critical Paths (flag row), Anti-Pattern 9, Must rule 7,
    Session-Start Checklist. SESSION_INITIALIZATION_PROMPT.md v2.10
    updated: Step 2, Fail-Fast Conditions, Quick Reference. Both also
    had a separate, pre-existing schema drift corrected in the same
    pass (stale v4 intraday_adjustment/MIN() table -> live V5.0
    intraday_signal/UPGRADE-CONFIRM-DOWNGRADE), spotted by Tony against
    the live JSON, not part of this WO's original scope but fixed
    alongside since both docs were already open. Windows disk canonical
    files updated; Tony still needs Customize -> Skills -> re-upload for
    the p010-project-context skill to sync the live version Claude loads.
[x] Downstream projects in Affects: notified -- via this WO entry (P_400,
    P_115, P_300, P_020 all read the ledger at INIT)
[x] No sys.path side-channels introduced -- confirmed via grep, imports
    are plain sibling-module imports (from toast_notify import
    send_toast / from staleness_check import is_morning_data_stale), no
    sys.path manipulation found
[x] If schema/signal contract changed -- N/A, P_010_RiskConfig.json
    schema unchanged; MORNING_RUN_FAILED.flag is a sibling file, not a
    JSON field
[x] DRAFT files for this WO deleted -- none found in ledger folder for
    this WO
[x] One ledger entry per WO confirmed -- yes, this file

## Verification (2026-08-26, P_010 session)

Confirmed via live grep against production files, not self-reported from
memory:
- python\P_010_daily_posture_v5.py: line 21 comment references this WO,
  line 23 confirms __main__ writes the flag, line 41 imports
  toast_notify, line 272 constructs flag_path
- python\P_010_intraday_vp_check_v4.py: imports and calls
  is_morning_data_stale, prints "Check MORNING_RUN_FAILED.flag..." on
  stale detection
- python\P_010_write_daily_note.py: same staleness import/check pattern
- P_010_daily_posture.bat: lines 42/44/46 check "if exist
  MORNING_RUN_FAILED.flag" and halt STEP 2 (note writer) with a visible
  [HALT] message
- MORNING_RUN_FAILED.flag itself does not currently exist on disk --
  expected, since no run has failed since this was built
- Failure-injection test (14 automated checks) referenced from the
  building session -- NOT independently re-run this session; the
  deliberate-break VERIFY step from this WO's own text (rename QQQ file,
  confirm halt+toast fires, restore, confirm clean re-run clears it)
  also not re-run live today. Wiring confirmed present and correct by
  static inspection; end-to-end failure-path behavior not re-exercised.

Status set to OWNER_DONE, not COMPLETE/CLOSED -- implementation and
wiring confirmed from disk, but IMPERATIVE SWEEP is open (skill docs not
updated) and the live failure-injection VERIFY step was not re-run this
session. A separate session must independently confirm both before this
moves to CLOSED, per WO_COMPLETION_GATE.md.

**Logged by:** Claude, P_010 session, 2026-08-26, per Tony's direction
this session

## Live Verification (2026-08-26, same session, after Completion Gate above)

Re-ran the failure-injection VERIFY step from this WO's original text --
the step the 08-10 session did not re-run live:

1. Renamed `History Grid (QQQ)_V3.xlsx` to break it, ran
   `P_010_daily_posture.bat`. Result: PASS.
   - `MORNING_RUN_FAILED.flag` written, content: exit-code-1 detail line,
     correct timestamp.
   - `P_010_Daily_20260826.log` shows the expected
     `ERROR: File not found` line.
   - STEP 2 (note writer) did not run -- `P_010_RiskConfig.json` and
     today's Obsidian note were untouched during the failure.
   - Toast notification: NOT independently confirmed -- no screen-watch
     capability this session, and ERROR 003 already established that
     exit-code/return-value alone cannot prove a toast rendered. Static
     code path (`_notify_failure` -> `send_toast`) is present and was
     exercised, but "fired" is unconfirmed.
2. Restored the correct filename, re-ran the batch. Result: PASS.
   - Flag cleared automatically at start of the new run, as designed.
   - STEP 1 recomputed identical spy/qqq/avg posture and risk_mode
     values from the same underlying grid file -- confirms the fix
     didn't require the specific bytes touched on 08-08, just the
     correct filename.

**Side effect disclosed:** the manual STEP-1 re-run at 17:45 overwrote
today's `P_010_RiskConfig.json` with a fresh `timestamp` (was
07:30:03, now 17:45:05) and dropped `morning_risk_mode` /
`intraday_signal` / `intraday_reason` -- STEP 1 always writes those
fields fresh and doesn't know about the 2 PM intraday run that already
happened today. Repaired immediately by re-running
`P_010_run_intraday_vp_check.bat`, which restored `morning_risk_mode:
OFF`, `intraday_signal: CONFIRM`, matching this morning's original
values exactly (only the timestamp is now 17:45 instead of 07:30 --
functionally equivalent, all computed values identical). A second
audit file (`intraday_vp_check_20260826_174628.json`) now sits
alongside the original 14:00 one in `outputs\` -- harmless, audit
files are meant to accumulate, just flagging the extra one exists.

**NOT RESOLVED -- addendum item still open:** the intraday restore run
reproduced the exact false-positive from this WO's 2026-07-17 addendum:
console printed `[SUCCESS] Intraday VP validation completed.`
immediately followed by `[ERROR] Intraday check failed with exit code
0`. The addendum explicitly scoped fixing this errorlevel check into
this WO ("MUST NOT fire... fix... as part of this WO, not as an
afterthought") and listed re-confirming a clean run with no `[ERROR]`
line as a VERIFY item. That VERIFY item fails today, live-reproduced,
16+ months after the addendum was written -- the errorlevel/exit-code
bug in `P_010_run_intraday_vp_check.bat` was never actually fixed.
Status is left at OWNER_DONE because the PROPOSED FIX's core
deliverable (flag + halt + toast, items 1-2) is genuinely done and
independently verified above -- but this WO is not fully closed-scope
per its own addendum, and this should weigh against moving to CLOSED
until the errorlevel check is separately fixed. Recommend either a
follow-up WO or reopening this one's scope -- Tony's call.

**Verified by:** Claude, P_010 session, 2026-08-26

## RESOLUTION -- Addendum Item Fixed (2026-08-26, same session, per Tony's
"reopen and fix" direction)

Root-caused and fixed the false-positive above -- logged as ERROR 004 in
docs\P_010_Error_Corrections_Log.md (full detail there; summary below).

**Root cause:** `P_010_run_intraday_vp_check.bat`'s
`if %errorlevel% equ 0 ( ... ) else ( ... )` block contained one echo line
with an unescaped, literal `(in outputs/)`. cmd.exe parses the whole
if/else as one parenthesized unit; the stray `)` closed the outer `if (`
early, so everything after it -- including the `) else (` and the ELSE
branch -- ran unconditionally instead of being gated by the actual exit
code. That's why `[SUCCESS]` and `[ERROR]` always printed together
regardless of the real result.

**Fix:** escaped to `^(in outputs/^)`. One line changed in
`P_010_run_intraday_vp_check.bat`.

**Live verification (2026-08-26, market closed, real grid data):**
re-ran the batch after the fix -- output now shows
`[SUCCESS] Intraday VP validation completed.` alone, no `[ERROR]` line.
`P_010_RiskConfig.json` unaffected (intraday-only run refreshes
intraday_signal/intraday_reason in place, does not touch morning fields).

**Also updated:** p010-project-context SKILL.md Anti-Pattern 7a (new)
references ERROR 004; Windows disk canonical file updated, needs
Customize -> Skills re-upload same as the earlier IMPERATIVE SWEEP items
in this WO.

This WO's full scope -- original PROPOSED FIX (items 1-2) AND the
2026-07-17 addendum's errorlevel fix -- is now genuinely complete and
independently verified from disk/live-run, all in this session. Status
remains OWNER_DONE, not CLOSED -- per WO_COMPLETION_GATE.md, Independent
Review must come from a session other than the one that implemented and
self-verified the fix, which this session is not.

**Fixed & verified by:** Claude, P_010 session, 2026-08-26




