# WO-P000-E4.001 -- INIT Execution Bypass + Session Header Standardization

**Status:** OWNER_DONE (P_300 pilot only -- Hub-wide rollout + SYSTEM_DOCUMENTATION entry remain open under P_000)
**Owner:** P_000
**Affects:** P_300 (pilot), P_115, P_116, P_117, P_118, P_400, P_800 (follow-on)
**Depends On:** None
**Acks:** P_300 (2026-06-18, pilot complete -- see below)

---

## WHY

windows-mcp's PowerShell/Python transport has a hard ~4-minute timeout ceiling
that is not tunable from the session level. peh-handoff already documents this
firing on roughly 9 of 10 Python subprocess calls. P_300 SIP v3.1 Steps 5b
(catalog-summary) and 5c (LM Studio readiness check) both invoke Python via
windows-mcp:PowerShell from inside the live chat session -- exactly the call
shape that trips the ceiling. Every INIT run carries a near-certain stall risk
on two steps that produce small, predictable outputs.

Separately, session header format is not consistent Hub-wide. P_300's SIP v3.1
already specifies `[Project_ID] [Day], [Month] [DD], [YYYY] -- [HH:MM] ET` as
its header. No other project has confirmed it follows the same line. This
WO adopts that format as the Hub standard rather than inventing a new one.

---

## WHAT P_000 OWNS GOING FORWARD

### 1. INIT Execution Bypass Pattern (Preflight-bat + Status JSON)

- Any INIT step that needs Python output is moved out of the chat-session
  execution path entirely, using the same operating model already proven for
  P300AddPattern.bat and P300DailyEvalv2.bat -- run outside Claude, on Tony's
  own schedule.
- Pilot file: `C:\Users\Trader\AI-Agent-Learning-Hub\projects\P_300_Vantage_Point_Pattern_Recognition\P300_Preflight.bat`
  Runs catalog-summary and the LM Studio readiness check, writes results plus
  a timestamp to:
  `C:\Users\Trader\AI-Agent-Learning-Hub\projects\P_300_Vantage_Point_Pattern_Recognition\P_300_preflight_status.json`
- P_300 SIP v3.1 Steps 5b and 5c are rewritten to read that JSON via
  windows-mcp:FileSystem (mode=read) instead of invoking python via
  windows-mcp:PowerShell. No Python subprocess call remains inside INIT.
- Staleness rule: if the JSON timestamp is older than the last session close,
  INIT displays WARNING and asks Tony to re-run the bat -- same HALT/reconcile
  posture as today, triggered by a stale timestamp instead of a subprocess
  timeout. Exact threshold to be fixed during the P_300 implementation session.
- Once proven on P_300, the identical pattern is the default for any Python-
  dependent INIT step in P_115, P_116, P_117, P_118, P_400, or P_800.

### 2. Session Header Standardization (REVISED 2026-06-18)

- Canonical format, Hub-wide, every project, every session:
  `[Project_ID] [Day], [Month] [DD], [YYYY] [HH:MM] ET [optional session-type label]`
  Example (no label): `P_300 Wednesday, June 17, 2026 14:32 ET`
  Example (with label): `P_115 Thursday, June 18, 2026 07:50 ET Market Analysis`
- Original 2026-06-17 draft mandated a `--` separator and no trailing label,
  copied as-is from P_300. Revised after auditing P_115's live SIP
  (`SESSION_INITIALIZATION_PROMPT.md` v3.2): P_115's header has carried a
  trailing session-type label since v2.7 (2026-02-11) -- predates this WO and
  is genuinely load-bearing for distinguishing parallel sessions across
  Tony's multi-window setup. Rather than strip a working, older pattern to
  match a newer one chosen somewhat arbitrarily, the standard widened: `--`
  is no longer required, and a trailing label is explicitly permitted.
- P_300 updated to drop its `--` for consistency with the revised standard
  (see P_300 SIP changelog).
- P_115 audited: COMPLIANT under the revised standard, no change required.
- P_000 continues auditing P_116/117/118/400/800 INIT/SIP docs against the
  revised line; patch directly (trivial mismatch) or log a follow-on WO
  (non-trivial mismatch).
- Canonical format recorded in P_000_SYSTEM_DOCUMENTATION.md (Document Index
  or Standards section) so future projects inherit it by default.

---

## OUT OF SCOPE

- Rewriting catalog-summary or LM Studio check logic itself -- only the
  execution path changes.
- Cowork or any other delegated-execution tooling -- separate decision, not
  part of this WO.
- Full INIT rewrites for P_115/116/117/118/400/800 -- each project converts
  its own Python-dependent steps in its own session once the pattern is
  proven on P_300.

---

## ACCEPTANCE CRITERIA

- P300_Preflight.bat exists at the path above, runs catalog-summary and the
  LM Studio check, writes P_300_preflight_status.json with a timestamp.
- P_300 SIP v3.1 Steps 5b/5c read that JSON via windows-mcp:FileSystem; zero
  windows-mcp:PowerShell Python subprocess calls remain in P_300 INIT.
- A full P_300 INIT completes end to end with no 4-minute stall.
- Staleness threshold defined and documented; tested against a deliberately
  stale JSON to confirm WARNING fires correctly.
- Canonical header format recorded in P_000_SYSTEM_DOCUMENTATION.md.
- P_115/116/117/118/400/800 INIT/SIP docs audited against the header
  standard; each logged as compliant or has a follow-on WO filed.

---

## Completion Gate (ref WO-P000-E3.001)

[ ] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[ ] Any new or changed shared-resource location reflected in:
    - P_000_SYSTEM_DOCUMENTATION.md (Document Index section)
    - Affected project CLAUDE.md files
[ ] Downstream projects in Affects: notified (WO comment or session note)
[ ] No sys.path side-channels introduced (ref WO-P000-E2.003)
[ ] If schema/signal contract changed: version bumped, consuming projects notified
[ ] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\
[ ] One ledger entry per WO confirmed

---

## CHANGE LOG

- 2026-06-17 v1.0: Registered. Scope: P_300 INIT execution bypass (pilot) +
  Hub-wide session header standardization.
- 2026-06-18 v1.1: Session header standard revised to permit an optional
  trailing session-type label and drop the mandatory `--` separator, after
  auditing P_115 (see section 2 above). P_300 SIP updated to match; P_115
  logged compliant as-is.

---

Registered WO-P000-E4.001 -- 2026-06-17. Owner: P_000.

================================================================================
OWNER_DONE (P_300 pilot) -- 2026-06-18 (P_300 session)
================================================================================
python/schemas_preflight.py (NEW v1.0): PreflightStatus Pydantic model --
  catalog fields (path, filename, pattern_count, symbol_count, hollow_count,
  overall, error) + LM Studio fields (running, model, mismatch, message,
  error) + generated_at timestamp.
python/utilities/preflight_status.py (NEW v1.0): gathers both via existing
  logic only -- db_utils.get_latest_catalog, verify_ingestion.
  _check_no_hollow_instances, integrations.lm_studio.infrastructure.
  lm_studio_api.get_wrapper_status. No catalog or LM Studio logic
  reimplemented. Writes P_300_preflight_status.json to the project root.
  HUB_ROOT bootstrap for the LM Studio import verified against a known
  Hub-level directory at runtime rather than trusted blind (M-036 history
  made this worth a guard).
P_300_Preflight.bat (NEW): operator-run, outside the chat session -- same
  model as P_300_AddPattern.bat / P_300_DailyEval_v2.bat. Invokes
  preflight_status.py via the p140 interpreter.
docs/P_300_System_Initialization_Prompt_v3_1.md: bumped 3.1 -> 3.2. Steps
  5b/5c rewritten to read P_300_preflight_status.json via
  windows-mcp:FileSystem instead of invoking python via
  windows-mcp:PowerShell. Zero subprocess calls remain in P_300 INIT.
  Fail-Fast table and Quick Reference updated. Pre-edit v3.1 backed up to
  P_300_System_Initialization_Prompt_v3_1_backup_2026-06-18.md.
.claude/skills/p300-project-context/SKILL.md: Critical Paths gained a
  Preflight status row; PowerShell-subprocess-timeout row rescoped to
  "any OTHER ad-hoc python need"; Layer Architecture tree gained
  schemas_preflight.py; Session-Start Checklist timeout line rewritten for
  the new mechanism; Pairs-With table row corrected (was pointing at a
  different, stale filename); changelog entry added.
tasks/lessons.md: M-055 added (INIT must never call python via
  windows-mcp:PowerShell; read a pre-generated status file instead).
tasks/todo.md: dated entry + Closed-in-Current-Session block added.
Session header format (`P_300 [Day, Month DD, YYYY -- HH:MM ET]`) confirmed
  already canonical -- P_300 is the source pattern this WO adopts Hub-wide,
  not a target of change. No P_300-side action needed on that half.
Verified: read-back of both edited tasks/*.md files confirmed the inserted
  sections landed correctly (one edit batch initially mis-anchored on the
  lessons.md insertion point, dropping the prior lesson's closing timestamp
  line -- caught and corrected in the same session per M-048 discipline).
  LIVE EXECUTION CONFIRMED 2026-06-18 (Tony ISE run): P_300_Preflight.bat ->
  PASS. P_300_preflight_status.json content verified directly -- catalog
  HEALTHY (286 patterns / 212 symbols / 061726catalog.db / 0 hollow),
  LM Studio running (deepseek-r1-distill-qwen-14b, model_mismatch=false),
  all 12 schema fields populated, valid JSON. Both the catalog SQL path
  and the LM Studio HUB_ROOT-bootstrap + async path execute clean end to
  end -- this closes the M-040 gap noted at OWNER_DONE time (code-reviewed
  only, not execution-tested). INIT Steps 5b/5c are now safe to rely on
  next live P_300 session.
Remaining open (P_000, not P_300): audit P_115/116/117/118/400/800
  INIT/SIP docs against the canonical header format; record the canonical
  preflight-bat-plus-status-JSON pattern and the header format in
  P_000_SYSTEM_DOCUMENTATION.md; roll the same INIT execution bypass
  pattern out to any other project with a Python-dependent INIT step.
