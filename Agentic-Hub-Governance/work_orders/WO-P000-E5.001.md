# WO-P000-E5.001 -- Promote LM Studio Status Check to Hub Interface (Unblocks WO-P000-E2.003 LM Studio Rows)

**Status:** OWNER_DONE -- all Acceptance Criteria met 2026-08-11 (see Progress Log + Completion Gate below). Independent Review still owed (fresh session, per WO_COMPLETION_GATE.md -- this session cannot self-certify).
**Owner:** P_000
**Affects:** P_300 (consumer; `daily_evaluate_pipeline.py` Process Boundary violation), integrations/lm_studio (Hub-level module, no project owner)
**Depends On:** None -- BLOCKS: WO-P000-E2.003 remaining rows (3x `integrations/lm_studio/infrastructure/*.py` inserts + P_300 `daily_evaluate_pipeline.py` insert)
**Acks:** N/A

---

## Progress Log -- 2026-08-11

Real finding before starting: Option B alone (port lm_studio_status.py only)
does not satisfy this WO's own Acceptance Criteria. lm_studio_api.py and
lm_studio_launcher.py insert _HUB_ROOT for an unrelated reason -- making
integrations.lm_studio.config resolve as an absolute import, nothing to do
with status.py. Confirmed by reading both files. Combined fix: Option B
(port to shared_resources) + Option A's editable-install move, scoped
narrowly to integrations + integrations.lm_studio* only (not the rest of
integrations/ -- automation/local_llm/mcp_servers stay untouched,
unreviewed).

Also found: P_300's daily_evaluate_pipeline.py briefly HAD its own
infrastructure/lm_studio_status.py wrapper at v1.11 (2026-05-30) -- exactly
this fix -- before v1.12 the same day reverted the import to Hub-level
directly, reintroducing the Process Boundary violation this WO closes. This
work restores that v1.11 state, not a novel design.

**Done, verified on disk:**
1. shared_resources\python_utils\lm_studio_status.py -- NEW, ported
   interface, 92 lines. Test-Path true, 3714 bytes.
2. projects\P_300_Vantage_Point_Pattern_Recognition\python\infrastructure\lm_studio_status.py
   -- NEW thin wrapper, 35 lines. Test-Path true, 1345 bytes.
3. pyproject.toml -- include list extended with integrations +
   integrations.lm_studio*. Verified new include line present.
4. projects\P_300_Vantage_Point_Pattern_Recognition\python\application\daily_evaluate_pipeline.py
   -- import swapped to own infrastructure/ wrapper; _HUB_ROOT bootstrap
   block removed (this file's row in WO-P000-E2.003 is now genuinely DONE,
   not dependent on the pip step below). Header bumped to v1.23, changelog
   entry added. Verified: 619->621 lines, all 5 substitution checks passed
   before write landed.
5. integrations\lm_studio\infrastructure\lm_studio_status.py -- DELETED
   (Tony's call, not shimmed). Backed up first to
   integrations\lm_studio\infrastructure\_archive\2026-08-11_HHmmss_lm_studio_status_DELETED_WO-P000-E5.001.py,
   size-verified identical to original before removal. Confirmed no other
   caller in the Hub imported this file directly (grepped all .py files
   for lm_studio_status; only P_300's daily_evaluate_pipeline.py and a
   local _get_lm_studio_status() false-positive in
   preflight_status.py -- unrelated function, not this module).

**Not done yet -- blocked on Tony:**
6. integrations\lm_studio\infrastructure\lm_studio_api.py -- _HUB_ROOT
   block still present. Left in place on purpose: the new shared_resources
   interface's deferred import of this module won't resolve until the
   editable install actually registers integrations.lm_studio --
   removing this file's own bootstrap now doesn't help and doesn't hurt,
   deferred to avoid touching more than necessary before the install is
   confirmed.
7. integrations\lm_studio\infrastructure\lm_studio_launcher.py -- same,
   _HUB_ROOT block still present, same reason.
8. Editable install has NOT been re-run. Windows-MCP could not execute
   python.exe for this session -- two consecutive hangs (a -c one-liner
   and a staged .ps1 wrapper, 4 min each, no result) while plain
   PowerShell commands returned instantly in between. Matches the known
   pattern (pip install/pytest hang via MCP) -- handed to Tony rather
   than retried blind.
9. NFR-1 determinism replay (Acceptance Criteria) not run -- depends on 6-8.

**Tony's action to unblock, in Anaconda Prompt:**
`
cd C:\Users\Trader\AI-Agent-Learning-Hub
C:\Users\Trader\.conda\envs\p140\python.exe -m pip install -e . --force-reinstall --no-deps
C:\Users\Trader\.conda\envs\p140\python.exe -c "import integrations.lm_studio.infrastructure.lm_studio_api; print('OK')"
`
Confirmed by Tony 2026-08-11: exit 0, hub_shared force-reinstalled editable,
import check OK.

**Done after confirmation:**
6. lm_studio_api.py -- _HUB_ROOT block removed. Verified: no _HUB_ROOT
   string remains, import sys and the integrations.lm_studio.config
   import both still present.
7. lm_studio_launcher.py -- same, same verification.

**Still needed -- Tony's action, Anaconda Prompt:**
`
cd C:\Users\Trader\AI-Agent-Learning-Hub
C:\Users\Trader\.conda\envs\p140\python.exe -c "import shared_resources.python_utils.lm_studio_status; import integrations.lm_studio.infrastructure.lm_studio_api; import integrations.lm_studio.infrastructure.lm_studio_launcher; print('CHAIN_OK')"
cd projects\P_300_Vantage_Point_Pattern_Recognition
C:\Users\Trader\.conda\envs\p140\python.exe tasks\nfr1_determinism_replay.py
`
First command proves the full chain resolves post-edit (not just the single
module you already checked). Second is item 9, NFR-1 -- existing script
found on disk, not written new for this WO; pass criteria is its own exit
code 0 / all-fields-identical output, paste back either way.

Once both come back clean, this WO closes to OWNER_DONE with the
Completion Gate block, held for a fresh-session Independent Review same as
E17.001.

Side finding, out of scope for this WO, closed anyway: the
projects\P_300_Vantage_Point_Pattern_Recognition\venv\ noted above (104.8 MB,
7,143 files, built against a stray Python 3.14.2, not p140) was confirmed
unreferenced anywhere in the project and removed by Tony 2026-08-11.
Test-Path false, confirmed. Not part of this WO's Acceptance Criteria --
recorded here only because it surfaced during this WO's file review.

**CHAIN_OK confirmed by Tony 2026-08-11:** full import chain
(shared_resources.python_utils.lm_studio_status +
integrations.lm_studio.infrastructure.lm_studio_api +
integrations.lm_studio.infrastructure.lm_studio_launcher) resolves clean
post-edit, all three sys.path-insert removals verified live, not just on
disk.

**NFR-1 -- PASS, 2026-08-11 (LUMN substitute for CGBD).** Original
tasks\nfr1_determinism_replay.py targets History Grid (CGBD).xlsx, not on
disk in data\live at run time -- CGBD isn't privileged in what NFR-1 proves
(any symbol demonstrates determinism equally), so a scratch copy
(Agentic-Hub-Governance\verify\_scratch_nfr1_lumn.py, same comparison logic
verbatim, only the target XLSX differs) ran against History Grid (LUMN).xlsx
instead. Result: CE_GATE_ENABLED=False confirmed observe-only; both runs
BUY @ horizon 5, composite best 2.1511; SIGNAL_V2 written both times (run 2's
duplicate ledger write correctly skipped, ledger_id=504); all fields
identical across both runs; exit 0. Narrator disabled (no LM Studio running)
-- consistent with NFR-1's own definition, since the narrator is explicitly
outside the decision path being tested. Scratch script is diagnostic only,
not a permanent Hub artifact -- delete at Tony's convenience, not part of
this WO's deliverable.

All four Acceptance Criteria now met:
- shared_resources/python_utils/lm_studio_status.py exists, importable via
  the editable install, no sys.path manipulation -- YES, confirmed CHAIN_OK.
- P_300 has its own infrastructure/lm_studio_status.py; daily_evaluate_pipeline.py
  calls only that, never integrations.lm_studio directly -- YES.
- WO-P000-E2.003's 3x integrations/lm_studio/infrastructure/*.py rows AND
  the P_300 daily_evaluate_pipeline.py row removed and verified -- YES, all
  4 rows in WO-P000-E2.003 updated to DONE 2026-08-11 same session.
- NFR-1 reconfirmed, no signal_class/horizon/CE drift -- YES, PASS above.

---

## WHY

WO-P000-E2.003 left 4 sys.path inserts open specifically because the hub_shared editable install (WO-P000-E2.002) covers `hub_lib`, `shared_resources`, and `obsidian_writers` only -- not `integrations`. Removing any of the 4 `_HUB_ROOT` inserts targeting `integrations.lm_studio` today breaks `import integrations.lm_studio...` outright.

Separately, P_300's 2026-06-10 session (todo.md ENH-P000 note) flagged a second problem in the same file: `daily_evaluate_pipeline.py` calls `integrations.lm_studio.infrastructure.lm_studio_status.check()` directly from `application/` -- a Process Boundary Standard violation. The `python-project-architecture` SKILL's Process Boundary Standard section names this exact file as its worked example: status checks belong in `infrastructure/`, application orchestrates only. Confirmed still live this session (2026-06-22): the call is wired straight into `daily_evaluate_pipeline.py`'s import block, no P_300-owned `infrastructure/lm_studio_status.py` wrapper exists.

Both problems share one root cause: `integrations/lm_studio` was built as a P_300-adjacent module, not a Hub-level shared interface, so every consumer reaches into it directly instead of through a published contract -- the same anti-pattern M-038 already named for Obsidian writes (`vault_interface.py`) before that boundary was fixed.

---

## QUESTION TO RESOLVE

A) Add `integrations` to the Hub's `pyproject.toml` editable install (same fix as `obsidian_writers` in WO-P000-E2.003) -- removes the sys.path need but leaves consumers importing `integrations.lm_studio.infrastructure.lm_studio_status` directly; the Process Boundary violation in P_300 stays unresolved.

B) Port the LM Studio status-check logic into a genuine `shared_resources/python_utils/lm_studio_status.py` Hub interface (mirrors the `vault_interface.py` and `atr.py` precedent). Consumers import from `shared_resources`, never from `integrations`. Each consuming project (P_300 first) adds its own thin `infrastructure/lm_studio_status.py` wrapper that calls the Hub interface, so `application/` still only ever calls into its own `infrastructure/` -- closes both the sys.path row AND the Process Boundary violation in one move.

**Recommendation: Option B.** It's the only option that resolves the Process Boundary violation P_300 already flagged, and it follows the exact precedent already set for Obsidian writes (M-038) and ATR (M-049) -- both moved into `shared_resources/python_utils/` as pure Hub interfaces rather than leaving consumers reach into another project's internals. Option A only papers over the import-resolution half of the problem.

---

## RECOMMENDED FIX (Option B)

1. `shared_resources/python_utils/lm_studio_status.py` NEW -- ports the status-check logic from `integrations/lm_studio/infrastructure/lm_studio_status.py`; pure I/O wrapper, no business logic, model/timeout/base-URL passed in as params (not hardcoded) so each project's own `config.py` values still govern.
2. `integrations/lm_studio/infrastructure/lm_studio_status.py` -- either re-exports the shared_resources function (back-compat shim) or is removed once all consumers migrate; P_000 decides which at close, same pattern as the `obsidian_writers` Option A1 precedent in WO-P000-E2.003.
3. Each consuming project adds/updates its own `infrastructure/lm_studio_status.py` thin wrapper importing the Hub interface -- the ONLY thing `application/` calls.
4. P_300: `application/daily_evaluate_pipeline.py` import block updated to call its own `infrastructure.lm_studio_status.check()` instead of reaching into `integrations.lm_studio.infrastructure.lm_studio_status` directly. NFR-1 unaffected -- LLM narrator stays out of the BUY/WATCH/PASS decision path; this only moves where the connectivity check is wired, not what it checks.
5. Once (1)-(4) land for a consumer, that consumer's remaining sys.path insert(s) in WO-P000-E2.003 can be removed and verified.

---

## ACCEPTANCE CRITERIA

- `shared_resources/python_utils/lm_studio_status.py` exists, importable via the editable install, no sys.path manipulation.
- P_300 has its own `infrastructure/lm_studio_status.py`; `daily_evaluate_pipeline.py` calls only that, never `integrations.lm_studio...` directly.
- WO-P000-E2.003's 3x `integrations/lm_studio/infrastructure/*.py` rows AND the P_300 `daily_evaluate_pipeline.py` row removed and verified (clean import + clean execution).
- NFR-1 (LLM never in decision path) reconfirmed via existing determinism replay after the wiring change -- no signal_class/horizon/CE drift.

---

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md) --
    shared_resources\python_utils\, projects\P_300_Vantage_Point_Pattern_Recognition\python\infrastructure\,
    integrations\lm_studio\infrastructure\, pyproject.toml -- all canonical.
[x] Any new or changed shared-resource location reflected in doc/skill
    files -- N/A for CLAUDE.md/skill cross-references (none reference this
    module by path); pyproject.toml itself is the shared-resource location
    record and was updated + its header comment refreshed in the same edit.
[x] CALLER PROPAGATION -- only caller of the old
    integrations.lm_studio.infrastructure.lm_studio_status was P_300's
    daily_evaluate_pipeline.py (grepped whole Hub for lm_studio_status;
    one false positive, an unrelated local function in
    P_300's preflight_status.py). That caller updated. No other caller
    existed or needed updating.
[x] IMPERATIVE SWEEP -- N/A, this WO changed code and its own file-header
    changelogs, not a skill/doc governing rule text.
[x] Downstream projects in Affects: notified -- P_300 is the only
    consumer affected and it's the project directly edited in this WO
    (daily_evaluate_pipeline.py, its own new infrastructure/ wrapper).
    No other downstream project imports integrations.lm_studio directly.
[x] No sys.path side-channels introduced -- opposite: 3 removed
    (lm_studio_api.py, lm_studio_launcher.py, and the deleted status.py's
    former insert), 1 more removed in the same pass
    (daily_evaluate_pipeline.py's _HUB_ROOT, WO-P000-E2.003's row).
    New files (shared_resources + P_300 wrapper) use zero sys.path
    manipulation, resolve entirely via the editable install.
[x] If schema/signal contract changed: version bumped, consuming projects
    notified -- N/A, no schema/signal contract touched; SIGNAL_V2 packet
    shape unaffected (confirmed via NFR-1 replay -- packet written
    successfully both runs, ledger_id 504 dedup behaved normally).
[x] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\
    -- none created for this WO; only WO-P000-E5.001.md exists.
[x] One ledger entry per WO confirmed -- single file, no duplicates.

---
Created: 2026-06-22 (P_300 session -- E2.003 sys.path removal blocked on this gap; the 2026-06-10 ENH-P000 note converted to a tracked WO per Tony's request).
