# WO-P400-E8.003 -- Packet-Free Option Sizing (`size-option`) + T1/T2 Scale-Out Spec

**Status:** OWNER_DONE (2026-09-26) -- Completion Gate block added; review findings resolved (Affects completed, evaluate/spec `--target-2` scoped out, skill doc-synced); awaiting a separate-session Independent Review to CLOSE
**Owner:** P_400
**Affects:** application\evaluate_options.py, application\commands.py, application\build_option_spec.py, application\build_option_spec_scaleout.py (new), application\size_option_standalone.py (new), cli.py, domain\options_sizer.py (`translate_target_premium` made public), infrastructure\record_writer.py (`target_2` param)
**Depends On:** none
**Acks:** N/A -- P_400-internal only, per Ack Scope. `record_writer.py` writes `target_2` into the vault note's frontmatter, but that field already existed in P_800's P400 schema (`obsidian_writers\domain\vault_schemas.py` line 186, confirmed 2026-09-26), so no contract changed.
**Related:** WO-P400-E3.001 (evaluate_options/build_option_spec original build), WO-P400-E8.002 (same session pattern: found live, filed, built same day)

## WHY

Found live 2026-09-15 (NFLX): Tony manually found a liquid option contract
after batch-2b had already routed NFLX to STOCK (auto-picked option failed
the liquidity gate, OI=32 < 150 min). The signal packet was archived by
then, so `evaluate --options` -- the only tool that runs the real
risk-budget sizer + options council -- could not be re-run. Tony sized 4
contracts by eye off the stock price instead. Retroactive check with the
real sizer: risk budget (OFF regime) = $220.94, actual risk per contract
$89.60, Gate 1 (RISK) = 2 contracts. He was 62% over budget on a loser.

Root cause: `evaluate_options()` requires a live `SignalV2` packet, but
only uses three scalar values off it (symbol, guideline_stop,
guideline_target) -- everything else (chain, snapshot, posture, params)
already comes from elsewhere. The packet dependency is incidental, not
structural, and it's what blocks any packet-free re-evaluation.

Separately, Tony also wants a T1/T2 scale-out order structure (sell half
at T1, ride the rest to T2, or T2 alone when that's the only target that
makes sense) -- not currently possible; Pattern A/B render one target
only. The vault schema already has an unused `target_2` field (seen null
on every P400 record written today), so this isn't a new schema need,
just unwired rendering + sizing.

## Approach Taken (built 2026-09-15)

1. `evaluate_options()`: drop `packet: SignalV2` param, take
   `symbol/stock_stop/stock_target` directly. Existing caller
   (`commands.py`'s `cmd_evaluate --options` path) passes
   `packet.symbol/guideline_stop/guideline_target` -- zero behavior change
   for the live-packet path.
2. New `application\size_option_standalone.py`: packet-free entry point.
   Reads snapshot + chain the same way `evaluate_options()` always has,
   computes `stock_rr` internally from the same entry/stop/target already
   required (no extra input needed), runs the real sizer -> options
   council -> macro earnings gate, renders the spec, builds its own
   eval_cache fields (no fake packet/stock_result -- a dedicated
   field-builder sourced from what it actually has).
3. `build_option_spec()` gains optional `target_2`; delegates to new
   `build_option_spec_scaleout.py` when target_2 is set or target_1 is
   omitted. Two independent OCO brackets sharing one stop (TOS can't do
   1-triggers-2-OCO-groups natively), contracts split floor/remainder-to-
   T2. Single-target callers (existing `evaluate --options` path) see no
   change in output.
4. `cli.py` gains `size-option` subcommand wiring all of the above.

## Acceptance Criteria (draft -- refine at build time)

- [x] `evaluate --options` (live-packet path) produces byte-identical
      spec output to before the refactor -- confirmed via
      `test_evaluate_options.py`'s 3 existing tests, updated only for the
      new call signature, assertions unchanged, all passing.
- [x] A packet-free `size-option` run against the same chain/risk-budget
      numbers as today's live NFLX case reproduces the same sizer output
      (2 contracts, not 4) -- test_reproduces_live_nflx_gate1_sizing,
      proves the standalone path uses the real Gate 1/2/3 math, not a
      simplified stand-in.
- [x] `build_option_spec()` with no `target_2` renders identically to
      pre-WO output -- test_no_target_2_unchanged_from_before_wo_e8_003
      plus all 10 pre-existing tests in that file, unchanged and passing.
- [x] `build_option_spec()` with only `target_2` (no `target_1`) renders
      a single bracket at T2 -- Tony's stated "sometimes only T2 makes
      sense" case, not forced into a split. Caught and fixed a real bug
      here during build: target-2-only was initially still triggering
      the scale-out path against itself (T1==T2, two identical
      brackets) -- test_target_2_only_uses_it_as_primary now guards it.
- [x] `build_option_spec()` with both targets splits contracts
      floor/remainder-to-T2 and renders two independent OCO brackets
      sharing one stop -- test_build_option_spec_scaleout.py, 10 tests
      (split math even/odd/single-contract edge case, shared stop,
      single shared entry leg, T2 delta-translation, T2 R:R).
- [x] Regression tests per Regression Test Governance: every new/edited
      domain/application file gets its own test coverage -- 4 test files
      (2 edited, 2 new), 45 tests, all passing. Full P_400 suite:
      408 passed, 0 failed, 4 pre-existing skips (unrelated).

---

*Filed 2026-09-15, same session as the NFLX discovery, per Tony's
explicit "OK" to the presented plan. Build starts this session,
immediately after filing -- WHY section is this session's own live
investigation (NFLX retroactive sizer check, evaluate_options.py/
build_option_spec.py read live from disk) -- Premise Verification N/A.*

---

**Built 2026-09-15 (same session as filing).** Regression gate run before
any edit (12 pre-existing tests across `test_evaluate_options.py` +
`test_build_option_spec.py`, clean). Beyond the originally-presented file
list, `domain\options_sizer.py` needed one small addition: `_translate_target`
promoted to public `translate_target_premium` so
`build_option_spec_scaleout.py` could reuse the exact delta-translation
formula for T2's premium rather than duplicating it -- flagged here since
it wasn't in the original plan Tony approved, though it's the same
"explicit over hidden" principle applied one layer deeper.

Two real bugs found and fixed during build, both caught by the tests
written for this WO, neither shipped:
1. Split-math test asserted the wrong tie-break direction at the 1-contract
   edge case -- the *code* was already correct per the design described to
   Tony (remainder rides to T2); the *test* was fixed to match.
2. `size_option_standalone.py`: target-2-only was passing `stock_target_2`
   straight through to `evaluate_options()`, triggering a degenerate
   scale-out against itself (T1 and T2 identical, two redundant brackets)
   instead of a clean single bracket. Fixed to only pass a real second
   target through when both are genuinely given.

Also found and fixed, unrelated to this WO's own changes: a pre-existing
invalid escape sequence in `test_options_sizer.py`'s own module docstring
(`domain\options_sizer.py` -- `\o` isn't a valid escape), never caught
before because plain pytest doesn't run warnings-as-errors. One-line fix
(backslash to forward slash in the docstring path).

Compile-checked under p140 warnings-as-errors (12 files), then full
P_400 suite: 408 passed, 0 failed, 4 pre-existing skips (unrelated).
Build-verified in this session only -- Independent Review not yet done,
per this Hub's OWNER_DONE-vs-CLOSED distinction (work-order-governance
skill); do not treat this note as a substitute for that.

Correction, same day: the line above (written at first close-out) claimed
target_2 was already in eval_cache -- checked again when Tony asked what
it would take to finish this, and it was NOT actually there; the fields
dict never had a target_2 key because write_p400_record() had no such
parameter to accept it. Overstated in the original close-out; corrected
here rather than left standing.

Follow-on fix, same day: `write_p400_record()` gained `target_2:
Optional[float] = None`, added to the frontmatter dict right next to
target_1. Not a P_800 schema change -- write_p400_record() builds that
dict directly (no separate module call), and the same pattern was already
used once for why_code (WO-P020-E1.007/WO-P400-E6.001, comment in the
same function): "schema already defines the field... one field add."
`size_option_standalone.py`'s fields dict now actually passes
target_2=effective_target_2 through to both write_p400_record() and
write_eval_cache(). One syntax bug caught and fixed before it shipped:
first attempt placed target_2 (a defaulted param) before signal_source/
trade_mode_value (non-defaulted) in the signature -- a SyntaxError,
caught by the compile-check step, not by inspection. 3 new tests
(test_record_writer.py x2, test_size_option_standalone.py x1). Full
suite re-run: 411 passed, 0 failed, same 4 pre-existing skips.

Genuinely still open: the CLI-level `evaluate`/`spec` commands have no
`--target-2` flag, so the live-packet path can't use scale-out yet --
only size-option can. Low priority; nothing today goes through that path.

---

## Independent Review -- 2026-09-23 (separate session, P_400 11:07 ET)

**Result: NOT CLOSABLE.**

- **Acceptance criteria 1-6 confirmed against live code:** `evaluate_options()` signature is packet-free (`symbol, stock_stop, stock_target, ...`); `cli.py` has the `size-option` subparser with `--target-2`; `record_writer.py` accepts and writes `target_2`. Test counts (411 passed) are the owner's recorded claim -- not re-run.
- **Gate failure:** no Completion Gate block exists in this file. Per Enforcement, not OWNER_DONE; Status line corrected in this same edit.
- **Skill sync gap:** p400-project-context SKILL.md has no Bugs row and no changelog entry for E8.003 (grep 2026-09-23 found E8.002 only).
- **Affects incomplete:** omits `domain\options_sizer.py` (`translate_target_premium` made public) and `record_writer.py` (`target_2` param) -- both changed by this WO.
- **CALLER PROPAGATION:** `evaluate`/`spec` CLI paths have no `--target-2`. Documented above as "genuinely still open" but never explicitly scoped out or moved to a follow-on WO -- owner must do one or the other.
- **Formatting:** file has no H1 title line (starts at Status).

---

## Owner Close-Out -- 2026-09-26 (P_400 owner session)

Each 2026-09-23 review finding resolved:
- **Affects incomplete:** added `domain\options_sizer.py` and
  `infrastructure\record_writer.py`. Both confirmed on disk:
  `translate_target_premium` at options_sizer.py line 85,
  `target_2: Optional[float] = None` at record_writer.py line 30.
- **CALLER PROPAGATION (`--target-2` on evaluate/spec):** SCOPED OUT, no
  follow-on WO (Tony's call, 2026-09-26). Nothing routes scale-out through
  the live-packet path today, and `size-option` covers the need. The
  capability this WO delivered (packet-free sizing + scale-out) is fully
  reachable from its entry point, `cli.py size-option`.
- **Skill sync gap:** p400-project-context SKILL.md now has a `size-option`
  paragraph in Live Data & Dossier Automation, including the `--target-2`
  scope-out, plus a 2026-09-26 changelog entry. No Bugs row -- this is an
  enhancement, not a bug fix. Backup: `SKILL.md.backup_2026-09-26`.
- **Formatting:** H1 title line added.

## Completion Gate (ref WO-P000-E3.001)

- [x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md) --
      all files are canonical P_400 `python\` locations, edited or created
      in place.
- [x] Any new or changed shared-resource location reflected in: N/A -- no
      shared_resources\ file touched. The new CLI command is documented in
      p400-project-context SKILL.md (2026-09-26).
- [x] CALLER PROPAGATION: `evaluate_options()` signature change (packet ->
      symbol/stop/target) propagated to its only caller, `commands.py`'s
      `cmd_evaluate --options` path (confirmed by review 2026-09-23).
      `size-option` is wired in `cli.py` (subparser at line 101,
      `--target-2` at line 108, dispatch at line 217). `target_2` flows
      size_option_standalone -> write_p400_record/write_eval_cache.
      `evaluate`/`spec` `--target-2` explicitly scoped out -- see Owner
      Close-Out above.
- [x] IMPERATIVE SWEEP: no rule changed; a new command was added. The
      command text that drives behavior (skill Live Data section) now
      describes it and when to use it instead of `evaluate --options`.
- [x] Downstream projects in Affects: N/A -- P_400-internal. `target_2` was
      already a defined field in P_800's P400 vault schema, so there is no
      schema change to notify.
- [x] No sys.path side-channels introduced -- no cross-project import added.
- [x] If schema/signal contract changed: N/A -- SignalV2 untouched, and the
      vault schema already had `target_2`.
- [x] DRAFT files for this WO deleted from Agentic-Hub-Governance\work_orders\
      -- none existed (only this WO's own `.backup_2026-09-26` copy).
- [x] One ledger entry per WO confirmed -- single WO-P400-E8.003.md.
- [x] No open VERIFY/Acceptance Criteria item is deferred to Independent
      Review by label -- all six acceptance items are checked with test
      evidence. The one "genuinely still open" item is now explicitly
      scoped out, not deferred.
- [x] PREMISE VERIFICATION: N/A -- no inherited premise. WHY is this WO's
      own live investigation (NFLX retroactive sizer check, evaluate_options.py/
      build_option_spec.py read from disk 2026-09-15), per the filing note.
      The one overstated claim (target_2 already in eval_cache) was caught
      and corrected by the owner session the same day.
