# WO-P810-E1.001 — New Project Scaffold: P_810 Email Tax Extractor

**Status:** CLOSED
**Verified:** 2026-08-20 (implementing session) / 2026-09-06 (Independent Review, separate session — see below)
**Owner:** Claude
**Depends On:** —
**Affects:** P_810, P_000

---

## Problem

Tony needs a P_805-pattern email extraction pipeline scoped to AJZ Strategies
tax emails (receipts, notifications, statements) — distinct sender list,
distinct schema (no ticker/direction/consensus), distinct output. Rather than
bolt a `--taxes` flag onto P_805 (which explicitly scopes out whole-mailbox
category triage — see P_805 doc Section 1.4), stand up a peer project and
share the reusable mbox/IMAP/header-decode layer via
`shared_resources\python_utils\` rather than duplicating it.

## Scope (this WO)

Documentation and folder scaffold only — no Python code. Code comes in a
separate WO once Tony supplies the real AJZ Strategies tax sender list
(no guessing senders, per Hub convention).

## Fix — Files Created

| File | Purpose |
|------|---------|
| `projects\P_810_Email_Tax_Extractor\docs\P_810_SYSTEM_DOCUMENTATION.md` | Project system doc, modeled on P_805's doc structure |
| `projects\P_810_Email_Tax_Extractor\README.md` | Project overview |
| `projects\P_810_Email_Tax_Extractor\data\sender_sheet.csv` | Whitelist scaffold, headers only — Tony to populate |
| `projects\P_810_Email_Tax_Extractor\tasks\todo.md` | Initial session-state entry |
| `projects\P_810_Email_Tax_Extractor\python\{domain,infrastructure,application,tests,logs}\` | Layer folders + `__init__.py` placeholders |
| `projects\P_810_Email_Tax_Extractor\data\{daily,monthly}\` | Output folders |
| `P_000_SYSTEM_DOCUMENTATION.md` Section 1.4 | P_810 added to Related Projects table |

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths (see WO_COMPLETION_GATE.md)
[x] Any new or changed shared-resource location reflected in:
    - P_000_SYSTEM_DOCUMENTATION.md (Document Index / Related Projects section) — done this WO
    - Affected project CLAUDE.md files — N/A, P_810 has no CLAUDE.md (audit-first, no history yet)
    - Affected project skill files — N/A, no p810-project-context skill built yet (deferred — see Session Notes)
[x] CALLER PROPAGATION: N/A — no code, no callers this WO
[x] IMPERATIVE SWEEP: N/A — no rule changed, new project only
[x] Downstream projects in Affects: notified — P_000 doc edited same WO; P_805 unaffected (peer, not modified)
[x] No sys.path side-channels introduced
[x] No schema/signal contract changed
[x] No DRAFT files created for this WO
[x] One ledger entry for this WO confirmed

## Session Notes

- Project ID churned P_810 → P_820 → P_810 within this single session. The
  P_820 move was a mistake: Tony had P_820 mentally earmarked for a separate,
  unrelated future project (Order Signal Capture) and didn't catch the
  collision until after the rename executed. Reverted back to P_810 before
  any code existed, so no downstream references needed fixing beyond this
  WO file, the system doc, README, todo.md, and the P_000 Related Projects
  row — all four were touched twice (810→820, then 820→810).
- Skill `p810-project-context` deliberately NOT built this WO — every
  existing project skill (P_010/P_115/P_300/P_400/P_800/P_020) was written
  from real error corrections and anti-patterns after the fact, not as a
  starting scaffold. Same logic as the CLAUDE.md audit-first rule (P_000
  doc Section 3.6). Revisit once Phase 1 has run and produced real content.
- No Python written this WO — deferred pending Tony's real AJZ Strategies
  tax sender list (data\sender_sheet.csv currently header-only).
- Independent Review complete 2026-09-06 (separate session from the
  2026-08-20 implementing session — see Independent Review section below).

## Independent Review (2026-09-06)

Live-verified every claim in this WO against disk, not against the WO's
own narrative:

- Folder scaffold matches the claimed file list exactly: `docs\`,
  `data\{daily,monthly}`, `python\{domain,infrastructure,application,
  tests,logs}` with `__init__.py` placeholders, `tasks\todo.md`,
  `README.md` — all present via `Get-ChildItem -Recurse`.
- `data\sender_sheet.csv` confirmed header-only:
  `email_address,sender_name,date_added,category,enabled`, zero data
  rows — no fabricated senders.
- Zero `.py` logic files anywhere under `python\` — placeholders only,
  matches the WO's "docs/scaffold only, no code" scope claim.
- `P_000_SYSTEM_DOCUMENTATION.md` Section 1.4 confirmed carries the
  P_810 row as described.
- No DRAFT files found in this project's tree.

**Anomaly found, not caused by this WO — now positively identified (2026-09-06, later same session):**
`docs\P805 IMAP OAuth.txt` in the P_810 folder contains exactly one
string: `20df0d61-7668-4b75-a778-b67d22fe841b`. Cross-checked against
P_805's own system doc Parameter Registry — this is `OUTLOOK_OAUTH_CLIENT_ID`,
the Azure AD app ID from P_805's Entry 013 OAuth2 build, documented there
as "not secret." Not a live credential, not a security exposure — just a
P_805 reference value saved as a note in the wrong project's folder,
dated 2026-08-23 (same week as the Entry 013/014 OAuth work). Still
misfiled; Tony's call whether to move or delete it.

**Result: PASS.** All Fix/Files-Created claims and the Completion Gate
checklist confirmed accurate against live disk state.
