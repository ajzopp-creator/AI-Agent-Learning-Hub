# WO-P300-E5.002 -- Fold Bulk-Merge Promote Through verify_and_promote()

**Status:** CLOSED -- 2026-08-04. Independent review complete (fresh session, wrote none of this code or the 2026-07-30 completion-gate test). See Independent Review + Completion Gate sections at the bottom of this file for the full evidence trail.

> **2026-07-28 -- this status is probably wrong. RESOLVED 2026-07-29 (independently re-confirmed same day, see CONFIRMED note below) -- what this note flagged as "probably wrong" was the ledger's then-current PENDING/DISPUTED status, and that turned out to be correct: it WAS wrong. Kept verbatim below as the audit trail that triggered the fix, not as live doubt about the current BUILT status.**
>
> `promote_staging_to_live()` in `application/catalog_merge_pipeline.py:220`
> now calls `verify_ingestion.verify_and_promote()` with a real
> `expected_delta` (derived from `populate_topk_for_promote()`) and raises
> RuntimeError if verification fails. Its docstring states directly:
> *"this function previously bypassed it, doing its own unchecked
> checkout/atomic_move/checkin with no expected_delta at all --
> WO-P300-E5.002's gap, closed here."*
>
> That is this WO's gap, described as closed, in the source.
>
> `check_topk_cache` is deliberately left False -- the docstring explains
> `_check_no_hollow_topk` cannot distinguish a legitimately degenerate
> corpus from a real population bug, and `expected_delta`'s exact
> count-based check already catches real failures. Reasoned exclusion,
> not an oversight. Confirm whether this WO required that check
> specifically before treating it as an outstanding item.
>
> **Why this matters:** on 2026-07-28 this stale PENDING status caused
> Tony to be warned three separate times that the promote path skipped
> verification, and advised to run those checks manually after two large
> promotes. The advice was wrong; the checks were running automatically.
> The ledger was trusted over the source. Code is ground truth (M-017).
>
> **Do NOT simply flip this status on the strength of this note** -- a
> docstring is a claim, not evidence (M-051/M-054). Read
> `verify_ingestion.verify_and_promote()` and confirm the implementation
> matches the claim, then close with that evidence recorded.
>
> Cross-referenced from WO-P300-E5.005's BUILD STATUS section.

**CONFIRMED 2026-07-29** (fresh session, did not write this code). Read
`application/catalog_merge_pipeline.py` directly, not the docstring alone.
`promote_staging_to_live()` (line ~217) computes `new_pids` by diffing
staging against live, derives `expected_delta` from
`populate_topk_for_promote()` when `new_pids` is non-empty, and passes
both to `infrastructure/verify_ingestion.verify_and_promote()`, raising
`RuntimeError` on `not result.passed`. That is exactly what this WO asked
for -- the docstring's claim matches the real implementation. `check_topk_cache`
is deliberately left at its default `False`, as documented (reasoned
exclusion, not an oversight). Status corrected above from PENDING to BUILT.

**Separately found:** `verify_ingestion.py`'s own v1.1 changelog wrongly
claims `promote_staging_to_live()` is the caller that passes
`check_topk_cache=True` ("decision #10"). It doesn't -- confirmed via
the same direct read above, and `catalog_merge_pipeline.py`'s own
docstring says so explicitly ("deliberately NOT passed"). Stale doc
reference, not a functional bug -- the actual behavior is intentional
and correct. Worth a one-line fix in `verify_ingestion.py`'s changelog
when someone's next in that file.

**Addendum, 2026-07-29 (independent re-verification, Tony flagged the
status-line self-contradiction above):** the same wrong claim is not
just in the changelog -- it is ALSO in `verify_ingestion.py`'s
module-level docstring and in `verify_and_promote()`'s own function
docstring, both stating `promote_staging_to_live()` "is the one caller
that passes True" for `check_topk_cache`. Confirmed via direct read of
`catalog_merge_pipeline.py`'s actual call site: `verify_and_promote(
staging_path, resolved_live_path, expected_delta, pre_counts,)` --
4 positional args, `check_topk_cache` not passed, uses the function's
`False` default. Three copies of the same stale claim in one file, none
functionally wrong (behavior is correct), all cosmetic doc drift.
One fix, three sites, next time someone's in `verify_ingestion.py`.

**Real production confirmation, 2026-07-29 batch** (`logs\
BulkAddPattern_20260729_104345.log`): `verify_and_promote()` ran for
real against a genuine staging->live promote (1222 new patterns),
clean PASS, `atomic_move()` executed, backup written, `post_counts`
logged exactly as `verify_ingestion.py`'s own code produces them. This
confirms the SUCCESS path works correctly in production. It does NOT
confirm the FAILURE path -- i.e. that a genuinely bad promote (wrong
expected_delta, hollow records) actually gets caught and blocked. That
still needs NEXT STEPS item 4's deliberately-broken-copy test; nothing
in production has exercised the FAIL branch of `verify_temp_db()` yet.

Not marked CLOSED -- no dedicated WO_COMPLETION_GATE closure pass
(a real or simulated failure case) has been run under this WO's own
name; this confirmation covers ledger accuracy and the success path
only.

**Completion-gate fail-path test -- DONE 2026-07-30.**
`python/tests/test_verify_ingestion.py` built this session (262 lines,
3 checks) and run for real by Tony via the p140 interpreter --
`ALL CHECKS PASSED (exit 0)`:
  1. Wrong `expected_delta` -> `passed=False`, master untouched.
  2. Correct deltas but a hollow `pattern_instance` (missing
     `forward_labels`) -> still `passed=False`, master untouched.
  3. Control -- clean data + correct deltas -> `passed=True`, master
     promoted, `.bak` backup preserved.

Pre-delivery, the test logic was also run against a verbatim sandbox
reconstruction of `verify_ingestion.py` with the gate deliberately
disabled, to confirm the test actually fails when the gate is broken
(it did, exit 1) -- not a vacuous pass. The 2026-07-30 run above is the
authoritative one: the real file, Tony's machine, real exit 0.

This closes the FAIL-branch gap noted above -- both the success path
(2026-07-29 production batch) and the failure path (this test) are now
real-run confirmed, not just code-inspected.

**Owner:** P_300
**Affects:** application/catalog_merge_pipeline.py (promote_staging_to_live),
  application/ingest_mined_pipeline.py (uses the same promote pattern for
  the BulkAddPattern path) -- likely 2 files at minimum. No domain/
  infrastructure changes expected; infrastructure/verify_ingestion.py's
  verify_and_promote() already exists and should be reused, not rewritten.
**Depends On:** None.
**Acks:** N/A

---

## WHY

Filed 2026-07-19, prompted by Tony directly questioning why the
single-pattern AddPattern promote path and the bulk-merge promote path
don't share the same verification logic. Checked, not assumed -- they
genuinely don't:

- `infrastructure/verify_ingestion.py`'s `verify_and_promote()` (used
  by every single-pattern write) runs `verify_temp_db()` first: a
  per-table expected-delta check AND a hollow-record scan (patterns
  missing pattern_bars or forward_labels -- the EC-027/EC-057
  tripwire). Only promotes on a clean pass; leaves temp in place and
  master untouched on any failure.
- `application/catalog_merge_pipeline.py`'s `promote_staging_to_live()`
  (the bulk-merge path, used by both the STRICT-tier merge and
  BulkAddPattern's ingest-mined flow) does NEITHER check. It calls
  `catalog_checkout()` / `catalog_checkin()` for informational
  before/after row-count logging only -- no `expected_delta` is ever
  passed to `catalog_checkin()`, so nothing is actually enforced. It
  reuses `atomic_move()` from verify_ingestion.py (the safe part --
  backup + atomic rename), but skips the verification wrapper around
  it entirely.

Nothing in the code or changelog history (catalog_merge_pipeline.py
v1.0 2026-07-10, v1.1 2026-07-14) suggests this split was a deliberate
design choice -- it reads as catalog_merge_pipeline.py being built
later, reusing only the low-level atomic_move() primitive, and
reimplementing a lighter pre/post snapshot instead of computing a real
`expected_delta` from the merge summary and calling the existing
`verify_and_promote()` wholesale. Same shape as M-082 (duplicate logic
diverging across rewrites), one layer up -- at the write-protocol
level instead of the DTW-math level.

**Closed for one specific promote already (2026-07-19, staging_
ingest_mined.db -> live, 1792 new patterns):** a one-off script
computed a real `expected_delta` from actual config constants (new
pattern count x BULK_WINDOW_LENGTH x len(FORWARD_HORIZONS)) and called
the real `verify_temp_db()` before calling `promote_staging_to_live()`
-- verified clean, promote succeeded. That was a manual, one-time
workaround for this WO's gap, not a fix to the gap itself. The next
bulk promote (or a future session that doesn't remember to do this
manual step) gets the unverified path again unless this WO is done.

## SCOPE NOTE -- explicitly NOT decided yet

- Whether `promote_staging_to_live()` should call `verify_and_promote()`
  directly (cleanest, but verify_and_promote()'s signature currently
  assumes a single temp_path/master_path pair with an expected_delta
  the CALLER must already know -- bulk merge's caller (ingest_mined_
  pipeline.py / catalog_merge_pipeline.py's own build_staging_merge())
  would need to compute that delta from its own MergeResult/
  IngestMinedResult summary (inserted_count, and per-pattern bar/label
  counts which are currently NOT tallied anywhere in those summaries --
  may need a small summary-field addition) -- or whether a shared
  lower-level verify helper gets extracted that both single-pattern and
  bulk paths call, with each caller supplying its own expected_delta.
  Not decided -- first step below is to actually look at both callers'
  data before picking an approach.
- Whether the hollow-record scan alone (assumption-free, no
  expected_delta needed) should be split out and made cheap+automatic
  in promote_staging_to_live() even before the fuller expected-delta
  fold is designed -- a smaller, faster partial fix. Worth considering
  as a first increment rather than waiting for the full fold.

## NEXT STEPS (all resolved -- see Independent Review below)

1. ~~Read `IngestMinedResult` and `MergeResult`...~~ Superseded -- the
   direct-reuse approach was taken (promote_staging_to_live() calls
   verify_and_promote() directly), not the shared-helper alternative.
2. ~~Decide the shared-helper-vs-direct-reuse question...~~ Resolved as
   above.
3. ~~Full file-level plan...~~ Done as part of the 2026-07-19/2026-07-20
   build (see catalog_merge_pipeline.py v1.2 changelog).
4. ~~Once built: re-verify against a real staging batch...~~ DONE --
   success path 2026-07-29 production batch (1222 patterns), fail path
   2026-07-30 test_verify_ingestion.py, both independently re-confirmed
   2026-08-04 (see below).

---

## Completion Gate (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths -- `application/catalog_merge_pipeline.py`,
    `infrastructure/verify_ingestion.py`, `python/tests/test_verify_ingestion.py`,
    all under the project's standard `python/` layer per `config.py` convention.
[N/A] No new or changed shared-resource location -- P_300-internal fix only;
    nothing added to `shared_resources/`.
[x] CALLER PROPAGATION: `promote_staging_to_live()` has exactly 2 real call
    sites, confirmed via `grep` across the whole `python/` tree --
    `cli_commands/bulk_promote.py`'s `_cmd_merge_research_catalog()` (line 64)
    and `_cmd_ingest_mined()` (line 116), i.e. `merge-research-catalog --promote`
    and `ingest-mined --promote`. Both route through the identical fixed
    function; neither has a second, unverified promote path.
    `cli_commands/promote_gate.py` (WO-P300-E5.005) confirmed decide-only via
    direct source read -- it never calls `promote_staging_to_live()` itself,
    only sets an exit code the `.ps1` wrapper branches on. No caller bypasses
    the fix.
[x] IMPERATIVE SWEEP: this WO brought code into compliance with an anti-pattern
    rule ("Direct write to master DB without temp-DB pattern" / "Lock +
    Temp-DB + Atomic Move only") already present in `p300-project-context`
    SKILL and `CLAUDE.md` -- no new rule was needed. One stale doc reference
    remains (3 copies in `verify_ingestion.py`'s own docstrings claiming
    `promote_staging_to_live()` passes `check_topk_cache=True` -- it doesn't,
    confirmed False on the real call site); logged above, cosmetic, not a rule.
[N/A] Downstream projects in Affects -- none; P_300-internal.
[x] No sys.path side-channels -- confirmed via source read, standard
    `_PYTHON_DIR` insert pattern only, unchanged from project convention.
[N/A] No schema/signal contract changed.
[x] No DRAFT files for this WO in the ledger folder.
[x] One ledger entry (`WO-P300-E5.002.md`) confirmed.

## Independent Review -- CLOSED 2026-08-04

Fresh session (INIT'd this chat; wrote none of this code or the
2026-07-30 completion-gate test). Re-read `catalog_merge_pipeline.py`'s
`promote_staging_to_live()` directly: computes `new_pids` by diffing
staging vs. live `pattern_instance_id` sets, derives `expected_delta`
via `populate_topk_for_promote()` when non-empty, calls
`verify_and_promote()` with real `pre_counts`, raises `RuntimeError` on
failure -- matches this WO's claim exactly, confirmed against source,
not the docstring. `check_topk_cache` confirmed NOT passed (stays
default `False`), consistent with the documented reasoned exclusion.

Re-ran `test_verify_ingestion.py` fresh via PEH (not trusting the
2026-07-30 transcript alone -- M-054): **ALL CHECKS PASSED, exit 0**,
same three guarantees (delta-mismatch block, hollow-record block,
clean-data promote) independently reproduced on this machine, this
session.

Caller propagation confirmed via `grep` across the whole `python/`
tree: `promote_staging_to_live()` has exactly 2 real call sites
(`cli_commands/bulk_promote.py`, `merge-research-catalog --promote` and
`ingest-mined --promote`), both routing through this one fixed
function. No second, unverified promote path exists anywhere.

**Status: CLOSED.**

---

Created 2026-07-19, during the same session that ran the first M-079
walk-forward promote-gate + live promote since the JIT (Phase 2a-2c)
and the EVAL_PARALLEL_ENABLED flip. Filed now so it surfaces at every
future INIT rather than being lost to chat history (M-015/M-017
discipline: live docs, not thread memory, are authoritative).
