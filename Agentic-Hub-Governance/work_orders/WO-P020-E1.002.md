# WO-P020-E1.002 -- Multi-Leg Spread Support (Paper-First)

**Status:** CLOSED -- Independent Review completed 2026-08-22, separate session from implementation (2026-07-26 core logic, 2026-08-22 wiring fix). See Independent Review section below.
**Owner:** Claude
**Affects:** P_020
**Depends On:** WO-P020-E1.001 (sequenced after -- live data-integrity bug takes
priority since it actively misstates real-money P&L every week; spreads are
not live yet, so no active corruption risk from sequencing second)

**Correction note (2026-07-26):** The original version of this WO misidentified
the affected code as `infrastructure/schwab_mapper.py` (the Schwab API JSON
`transferItems` path, live-account only). Investigation this session found
that's wrong -- paper trades never touch `schwab_mapper.py` at all. The
actual drop happens in the legacy TOS CSV parser (`python\parsers\`), a
completely separate pipeline and file format. Old version preserved at
`WO-P020-E1.002_backup_MISSCOPED_2026-07-26.md`, superseded in full by this
rewrite. Schema decision from the original design session (spread_legs
child table, confirmed with Tony 2026-07-26) carries forward unchanged.

---

## WHY

Tony is planning to go live with multi-leg spread trades soon (he already
trades them regularly on paper -- 4-leg iron condors on SPY, not simple
verticals). Current code cannot represent them correctly.

**Confirmed affected file:** `python\parsers\P_020_TOS_Parser_v2.4.py` --
this is the live/active parser (last touched 2026-07-05, matching the most
recent paper `AccountStatement.csv` pull). It produces the `_OPTIONS_IMPORT.csv`
file that `infrastructure/paper_csv_reader.py` reads. **Do not confuse with**
the several older/archived copies sitting in the same folder:
`old_P_020_TOS_Parser_v2.py`, `P_020_TOS_Parser_v2.2.py`,
`P_020_TOS_Parser_v2.3.py` -- none of these are live.

`infrastructure/schwab_mapper.py` (Schwab API JSON `transferItems`, the
live-account pipeline) has its own, separate single-leg-only limitation
(`_extract_instrument()` returns only the first non-CURRENCY leg) -- this
is real and will need its own fix before live spreads can be traded, but
it is untouched by paper validation and **out of scope for this WO**. See
OUT OF SCOPE.

**Evidence the drop is silent, not just untested:** A real 4-leg iron
condor trade exists in the raw paper `AccountStatement.csv` (two TOS
orders, REF #5281971043 open / REF #5284552756 close -- see ACCEPTANCE
CRITERIA for the full detail). It does not appear in:
- The audit log (`*_AUDIT_LOG.csv`) -- zero mention, not even a "skipped" note
- The processed `_OPTIONS_IMPORT.csv` -- zero rows
- The `trades` table -- zero rows (confirmed via direct query, PAPER
  account, SPY, all dates)

The trade simply never existed anywhere downstream of the raw CSV. TOS
Account Statement CSV represents a multi-leg order as one line:
`BOT +1 1/-1/1/-1 CUSTOM SPY 100 20 FEB 26/20 FEB 26/20 FEB 26/20 FEB 26
665/675/705/695 CALL/CALL/PUT/PUT @14.45` -- ratio-per-leg, strikes, and
put/call all packed into slash-delimited groups in a single description
string. The parser's line-matching logic (built for single-symbol
`BOT +N SYMBOL ... @price` lines) does not recognize this format at all
and silently skips it -- not a "first leg only" bug like `schwab_mapper.py`
has; here it's a "whole order invisible" bug.

The `trades` table schema already accepts `asset_type = 'spread'`
(CHECK constraint exists), but no code path currently produces it.

---

## SCOPE

1. **Design first, before code.** Schema decision already made (2026-07-26):
   leg detail lives in a new `spread_legs` child table, mirroring the
   `exits` pattern:
   ```sql
   CREATE TABLE spread_legs (
       leg_id           INTEGER PRIMARY KEY AUTOINCREMENT,
       trade_id         INTEGER NOT NULL REFERENCES trades(trade_id),
       leg_number       INTEGER NOT NULL,
       full_symbol      TEXT NOT NULL,
       put_call         TEXT,
       position_effect  TEXT NOT NULL,
       direction        TEXT NOT NULL,
       qty              REAL NOT NULL,
       price            REAL NOT NULL,
       created_at       DATETIME DEFAULT CURRENT_TIMESTAMP
   );
   ```
   Still open: exact regex/parsing strategy for the packed
   `ratio/ratio/.../CUSTOM SYMBOL .../.../strike/strike/.../CALL/CALL/...`
   description format -- needs a short session against real examples
   before coding (this WO now has two: a clean one and a messy one, see
   ACCEPTANCE CRITERIA).

2. Detect multi-leg CUSTOM/COMBO orders in `P_020_TOS_Parser_v2.4.py` --
   the description string's leg count (from the ratio group, e.g.
   `1/-1/1/-1` = 4 legs) instead of the current single-symbol line match
   that silently skips anything it doesn't recognize.

3. Compute net entry_price (debit) / exit_price (credit) directly from the
   `@price` already present in the TOS line (this is already the net
   figure for the whole combo -- no need to sum individual legs); set
   `asset_type = 'spread'`. Parse the slash-delimited groups into
   individual `spread_legs` rows (strike, put/call, ratio/direction each).

4. Validate exclusively against PAPER account pulls first. Hard gate in
   code: spread detection only activates for `account_id = 'PAPER'` until
   Tony has reviewed real output against what TOS shows for the same
   trades.

5. Remove the live gate only on Tony's explicit confirmation after paper
   validation -- not bundled into this WO's completion. Live-account
   support requires a *separate* fix to `schwab_mapper.py`'s
   `_extract_instrument()` (different file, different bug shape, different
   input format) -- track as a follow-on WO once this one closes, not
   folded in here.

---

## OUT OF SCOPE

- `infrastructure/schwab_mapper.py` / live-account (Schwab API JSON) spread
  support -- confirmed separate file, separate bug, separate data format.
  Follow-on WO once paper validation proves the design.
- ThinkLog tag vocabulary changes
- Tracker Dashboard changes
- Backfilling any spread trades already silently dropped from historical
  paper pulls (the 1/2-1/8 iron condor and the messier 1/30 cluster both
  predate this fix and are not in the DB at all -- separate cleanup once
  detection logic exists and can be pointed at the archived raw CSVs)

---

## ACCEPTANCE CRITERIA

- **Primary validation trade** (clean, single open/single close, no
  intraday legging -- use this one):
  ```
  Open:  1/2/26  11:53:05  REF #5281971043
         BOT +1 1/-1/1/-1 CUSTOM SPY 100 20 FEB 26/20 FEB 26/20 FEB 26/20 FEB 26
         665/675/705/695 CALL/CALL/PUT/PUT @14.45   (net debit, fees -0.10/-2.60)

  Close: 1/8/26  14:46:21  REF #5284552756
         SOLD -1 1/-1/1/-1 CUSTOM SPY 100 20 FEB 26/20 FEB 26/20 FEB 26/20 FEB 26
         665/675/705/695 CALL/CALL/PUT/PUT @13.83   (net credit, fees -0.12/-2.60)
  ```
  Found in `data\tos_exports\paper\D_020_2026-02-07-AJZ_Strategies_YTD_AccountStatement.csv`
  (same trade also present in `D_020_2026-04-13_Paper_AccountStatement.csv`).
  Round-trips through the pipeline producing **one** trade row: 4 rows in
  `spread_legs` (665 CALL long, 675 CALL short, 705 PUT long, 695 PUT
  short), `asset_type = 'spread'`, `entry_price = 14.45`, `exit_price = 13.83`.
- **Secondary/stress case (optional, don't block on this one):** the
  1/30/26 SPY iron condor with intraday leg adjustments (REF #5294246337 +
  cluster of REF #5294437xxx SINGLE fills same day) -- messier, useful for
  confirming the parser doesn't choke on it, not required to fully resolve
  for this WO to close.
- Live account (AJZ6348) spread orders are NOT processed by the new logic
  -- gate stays until explicitly removed in a later WO.
- No regression on existing single-leg call/put/stock parsing in
  `P_020_TOS_Parser_v2.4.py` (the two existing correctly-parsed single-leg
  SPY paper trades -- 1/30/26 692 CALL, 2/5/26 681 PUT -- must still parse
  identically).

## COMPLETION GATE (ref WO-P000-E3.001)

[x] All file paths use Hub canonical paths
[x] No sys.path side-channels introduced -- confirmed clean in paper_spread_import.py, paper_import.py, and the new wiring test file
[x] DRAFT files for this WO deleted from work_orders\ -- none exist; the two _MISSCOPED backup files are backups per WO_COMPLETION_GATE's Never Touch list, not DRAFTs
[x] One ledger entry per WO confirmed

---

## Independent Review -- 2026-08-22

Performed in a separate session from both implementation dates (2026-07-26 core logic, 2026-08-22 wiring fix -- the wiring fix itself was done and reviewed in the same session it was written, which is the normal same-session-build pattern; the *original* spread-detection logic being reviewed here is the 2026-07-26 work, genuinely a separate session).

**Primary validation trade re-confirmed independently.** Ran the actual pipeline against the real raw CSV this session (not re-reading the WO's own claim): `Found 1 spread position(s) in raw statement` from `D_020_2026-02-07-AJZ_Strategies_YTD_AccountStatement.csv`, matching the documented trade_id=3089 case exactly.

**Gap found and fixed this session, not just reviewed:** the spread pipeline (`spread_leg_parser.py`/`spread_matcher.py`/`paper_spread_import.py`) was built and validated standalone on 2026-07-26 but was never wired into `paper_import.py` -- the file Tony actually runs weekly (`python paper_import.py --options ... --stocks ... --thinklog ... --commit`). Options/stocks IMPORT CSVs never contain spread data at all (the old TOS parser drops CUSTOM/multi-leg lines before that stage), so this wasn't a redundant safety net -- it was the only path spread detection could reach production through, and it didn't exist.

**Checked for real damage first:** grepped every paper AccountStatement.csv exported since 2026-07-26 (`D_020_2026-07-27_YTD_AccountStatement.csv`, `2026-08-11-AccountStatement.csv`, `D_020_2026-08-11-AccountStatement.csv`, `D_020_2026-08-17_AccountStatement.csv`) for `CUSTOM` and `COMBO` -- zero matches across all four. No multi-leg spread has been traded on paper since the primary validation trade, so nothing was actually lost to the gap. Confirmed via data, not assumed.

*(Correction, same session, later: this "zero matches" check turned out to be incomplete -- see "Follow-up: VERTICAL/IRON CONDOR Extension" below. The literal CUSTOM/COMBO grep was accurate as far as it went, but real trading activity used TOS's named-strategy formats instead, which weren't covered by this check at the time.)*

**Fix applied (approved by Tony, 2026-08-22):**
- `application/paper_spread_import.py`: extracted the DB-write loop into a reusable `import_spreads(conn, csv_path, commit, verbose)` function. Standalone CLI (`python paper_spread_import.py <csv>`) unchanged and still works.
- `application/paper_import.py`: added `--raw-csv` argument. When given, calls `import_spreads()` in the same run, propagating the same `--commit`/dry-run flag -- one command now catches both single-leg and multi-leg trades. Omitting `--raw-csv` reproduces prior behavior exactly (confirmed by test).
- `tests/test_paper_import_spread_wiring.py` (new, 4 tests): confirms spread detection only fires when `--raw-csv` is given, `--commit` propagates correctly to the spread path, dry-run doesn't write, and missing-file returns zero stats without crashing.
- Backups of both modified files taken before edit (`paper_spread_import_backup_20260822.py`, `paper_import_backup_20260822.py`).

**Verification, all live this session:**
- Compile-clean under p140 `-W error::SyntaxWarning`: PASS (both modified files + new test file).
- `tests/test_paper_import_spread_wiring.py`: 4/4 passing.
- `tests/test_spread_matcher.py` (pre-existing): 2/2 passing, no regression.
- Live dry-run smoke test against `D_020_2026-07-27_YTD_AccountStatement.csv` (no spread trades) via the actual `paper_import.py` CLI: 57 single-leg trades parsed exactly as before (unchanged), plus `Found 0 spread position(s) in raw statement` -- correct, no false positive.
- Live dry-run smoke test against `D_020_2026-02-07-AJZ_Strategies_YTD_AccountStatement.csv` (the real primary validation trade) via `run_spread_import()`: `Found 1 spread position(s) in raw statement`, `{'found': 1, 'imported': 0}` in dry-run -- correctly detects the real spread and correctly withholds the write without `--commit`.

**Acceptance criteria status:**
- Primary validation trade: MET, re-confirmed independently.
- Secondary/stress case (1/30 messy cluster): still not attempted -- explicitly optional per the WO's own acceptance criteria ("don't block on this one").
- Live account (AJZ6348) gate: MET by construction -- `import_spreads()` hardcodes `account_id="PAPER"`, structurally cannot write to any other account, no separate gate flag to verify.
- No regression on existing single-leg parsing: MET, confirmed via the 07-27 smoke test (57 trades, identical output shape) and the pre-existing test suite.
- Independent review: this section.

**Assumption flagged, not hidden:** `paper_spread_import.py`'s own docstring said it was intentionally standalone "per the WO's hard gate until Tony has reviewed real output against TOS." The 2026-07-26 note describes the primary trade's output matching acceptance criteria exactly, which this session is treating as satisfying that review gate. If Tony's own visual check against the TOS platform hasn't specifically happened yet, that's worth a quick confirmation -- the wiring fix itself doesn't depend on it (it just connects an already-built, already-validated pipeline to the real entry point), but flagging per the same principle as WO-P020-E1.014's EC-005 note: don't bury an assumption a closeout rests on.

Status -> CLOSED.

## Follow-up: VERTICAL/IRON CONDOR Extension + Backfill -- 2026-08-22

Separate work session from the original close-out above, prompted by Tony catching a false "nothing traded" claim -- the original spread-detection scope only ever recognized the `CUSTOM` description format; real trading activity since late July used TOS's named-strategy formats (`VERTICAL`, `IRON CONDOR`) instead, invisible to the parser the whole time.

**Survey (every paper export checked):** `VERTICAL` and `IRON CONDOR` active and heavily traded since 2026-07-27 forward. `COMBO`: one real fill ever (2026-01-30), predates the 2026-06-13 account reset -- out of scope. `STRANGLE`: zero real fills anywhere, only match was a CANCELED order that never executed -- no implementation needed.

**Leg-direction rule, confirmed by Tony (2026-08-22):** for any multi-leg SOLD/BOT package, within each right (CALL or PUT), the first listed strike takes the container's action (short if SOLD, long if BOT), the second takes the opposite. Confirmed as "the correct, standard way to unpack these notations," consistent with how P_010/OIL journal already records them.

**Code changes:**
- `domain/spread_leg_parser.py`: added `_parse_named_spread()` alongside the existing `_parse_custom()` (renamed from the original inline logic), dispatched from `parse_multi_leg_description()`. Handles both VERTICAL (1 right, 2 strikes) and IRON CONDOR (2 rights, 4 strikes) with one generalized pairing rule -- strikes divide into pairs of 2 per right, first-in-pair = container direction, second = opposite. No special-casing per strategy name needed.
- `application/paper_spread_import.py`: `import_spreads()` gained an optional `min_open_date` parameter -- excludes any position opened before a given date (used here to enforce the 2026-06-13 reset cutoff so stale pre-reset artifacts still present in "YTD"-style raw exports don't contaminate current performance tracking).
- `tests/test_spread_leg_parser_named.py` (new, 10 tests): every example is a real line pulled from an actual export, not synthesized -- covers VERTICAL SOLD/BOT, IRON CONDOR SOLD/BOT (all 4 legs individually asserted against the confirmed rule), no-leading-zero price format (`.69`), CUSTOM regression (unaffected), COMBO still gracefully skipped, single-leg lines still return None.

**Verification:**
- Compile-clean under p140 `-W error::SyntaxWarning`: PASS.
- Full suite: 70/70 passing, zero regressions.
- Live dry-run against `D_020_2026-08-11-AccountStatement.csv`: went from 0 detected (before this extension) to 4 real spread positions found -- confirmed previously-invisible activity is now visible.
- One self-caught bug during this work: a PowerShell edit silently dropped the `min_open_date` parameter from the function signature (docstring/body got it, signature didn't, due to a `Set-Content` nested inside an unrelated failed conditional in an earlier edit). Caught immediately via a `TypeError` on the very next live run, fixed, recompiled, retested (70/70) before proceeding.

**Backfill executed, 2026-08-22, Tony approved live commit after reviewing dry-run totals:**
Processed `D_020_2026-07-27_YTD_AccountStatement.csv`, both `2026-08-11-AccountStatement.csv` pulls, and `D_020_2026-08-17_AccountStatement.csv` against one shared DB connection (cross-file dedup via `schwab_transaction_id`, already-tested logic, no special handling needed). 4 pre-reset positions correctly excluded by the `min_open_date` filter. Result: **4 unique new spread trades** (trade_id 3141-3144), zero duplicates despite heavy file overlap:

- 3141: SPY IRON CONDOR, opened 2026-07-27, still open, 4 legs (745C long/750C short/735P long/730P short)
- 3142: SPY VERTICAL, 2026-07-28 -> 2026-07-29, closed, P&L=$17.00 (BOT @1.18 -> SOLD @1.35)
- 3143: SPY VERTICAL, opened 2026-07-29, still open (same 735/730 PUT strikes as 3142 but a distinct REF#, correctly kept separate)
- 3144: SPY VERTICAL, opened 2026-07-31, still open

DB trade count 622 -> 626, spread-asset-type count 1 -> 5 (the original E1.002 validation trade plus these 4). `system`/`reason` left at `TOS_Import`/blank on all 4 at time of import -- no ThinkLog join ran during this raw backfill, same as the original validation trade. (Since re-tagged -- see next section.)

**Not yet done:** extending live-account (AJZ6348, Schwab API JSON via `schwab_mapper.py`) multi-leg support, still explicitly out of scope per the original WO's Design Decision -- separate file, separate bug, separate data format, follow-on work once paper validation (now including named strategies) has proven the design.

## Follow-up: ThinkLog/System Tagging -- 2026-08-22

Trades 3141-3144 tagged `system='P_010'`, `reason='P_010'` per Tony's direction (2026-08-22). `P_010` had never been seeded into the `systems` table at all (unlike `INV`, which was seeded in code but not applied) -- added to both `infrastructure/db_seeder.py`'s `_SYSTEMS` list and the live DB directly, satisfying the FK constraint before the trade update.

## RECOVERY NOTE -- 2026-08-22

This file was accidentally overwritten in full (used `mode="write"` instead of `"append"` on the ThinkLog-tagging update above) -- destroyed the original WO text, Completion Gate, Independent Review, and both follow-up sections. Recovered by pulling the original pre-session text from git (`git show HEAD:...`, last commit predates this whole session, from 2026-08-04) and reassembling every edit made this session from verbatim tool-call history. Flagged to Tony immediately on discovery. Content below this note (and above it, except the accidental-overwrite text itself) is a reconstruction, not the untouched original file -- noted here for the audit trail rather than silently presenting it as if nothing happened.

---

## CHANGE LOG

- 2026-06-21 v1.0: Opened. Tony confirmed plans to go live with spreads soon;
  decided to address now rather than defer to backlog, with paper-first
  validation required before live activation.
- 2026-07-26 v2.0: Corrected affected file (`P_020_TOS_Parser_v2.4.py`, not
  `schwab_mapper.py`) after locating real dropped trades in raw paper CSVs
  via audit-log/IMPORT-CSV/DB cross-check (all three showed zero rows for
  the known multi-leg orders). Schema decision (spread_legs table)
  confirmed with Tony. Added concrete primary/secondary validation
  examples. Live-account (schwab_mapper.py) fix split out as an explicit
  future follow-on WO, not bundled here.
- 2026-08-22: Independent Review performed (separate session). Found and fixed a real Caller Propagation gap -- spread detection was built and validated but never reachable from the command Tony actually runs. Checked all paper exports since 2026-07-26 for missed spread trades: none found by the CUSTOM/COMBO check available at the time (later found incomplete -- see follow-up). Wired `paper_import.py` to `paper_spread_import.py` via a new `--raw-csv` option and extracted `import_spreads()` reusable function. 4 new tests, all passing; no regression on existing tests. Live-verified against real files (both a no-spread file and the real primary-validation-trade file). Completion Gate checklist filled in. Status IN_PROGRESS -> CLOSED.
- 2026-08-22 (same day, follow-up session): extended parser for VERTICAL/IRON CONDOR (Tony confirmed leg-direction rule), added `min_open_date` filter, backfilled 4 real trades (3141-3144) from post-reset (2026-06-13+) paper exports, Tony approved live commit. Tagged all 4 to system=P_010 per Tony's direction; seeded P_010 into systems table (never existed before). File accidentally overwritten and reconstructed from git + session history -- see RECOVERY NOTE above.
