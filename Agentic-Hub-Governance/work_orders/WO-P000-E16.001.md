# WO-P000-E16.001 -- Hub-Wide Startup Utility (Chrome + claude --chrome launcher)
**Status:** OWNER_DONE -- built and verified live 2026-08-28. See RESOLUTION section below.
**Status:** PENDING -- all four Open Decisions resolved 2026-08-10; ready to build on Tony's go-ahead
**Owner:** P_000
**Affects:** All projects (shared infrastructure, no project-specific logic)
**Verified:** 2026-08-28, live end-to-end test on Tony's machine -- see RESOLUTION.
**Verified:**
**Depends On:**
**Acks:** (none required at file creation -- shared infra, no downstream project files touched)

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------
Offer to build this originated in a P_300 session. Correctly reclassified as
P_000-owned: the Chrome extension is the Hub-wide transport layer every Claude
Desktop session depends on for internet access -- the same governance category
as claude_desktop_config.json, which is already P_000-owned. A launcher for it
is Hub infrastructure, not P_300 domain logic, per the standing rule "code
belongs under the project that creates it" (ref: WO-P800-E3.001 precedent,
2026-06-05) -- this code is not created BY P_300, it happens to have been
proposed FROM a P_300 session.

--------------------------------------------------------------------------------
DESIGN REQUIREMENT -- MUST PARAMETERIZE, NOT HARDCODE
--------------------------------------------------------------------------------
As offered, the script hardcodes the P_300 folder as the launch target. This
defeats the premise of a "general utility" -- a Hub-wide launcher that only
ever opens one project isn't general, it's a P_300 launcher with a generic
name. Fix: target project is a parameter (command-line arg, or a small config
value read at launch), defaulting to Hub root (P_000's own context) if no
argument given -- see OPEN DECISIONS item 3 for the reasoning. Landing
directly in P_010, P_115, P_400, etc. must be a one-line parameter change,
not a rewrite.

--------------------------------------------------------------------------------
LOCATION
--------------------------------------------------------------------------------
RESOLVED 2026-08-10: reuse the existing utils\ folder --
  C:\Users\Trader\AI-Agent-Learning-Hub\Agentic-Hub-Governance\utils\
A new utilities\ folder was considered and rejected -- Agentic-Hub-Governance
already contains a utils\ folder; a second, near-identically-named folder
would create exactly the kind of drift Hub governance exists to prevent.

--------------------------------------------------------------------------------
FUNCTIONAL SPEC (as offered by P_300 session, pending Tony confirmation)
--------------------------------------------------------------------------------
1. Runs at Windows sign-in via a shell:startup shortcut (RESOLVED -- see
   OPEN DECISIONS item 2).
2. Launches Chrome.
3. Drops into claude --chrome scoped to the target project folder
   (parameterized per above; default is Hub root, NOT P_300 -- see OPEN
   DECISIONS item 3).
4. /login and /chrome are also automated by this launcher -- NOTHING manual
   at morning startup (Tony decision, 2026-08-10). Script must programmatically
   send /login then /chrome into the claude --chrome session after launch, not
   just open the window and wait for keystrokes. Needs investigation: whether
   claude --chrome accepts piped/scripted stdin input for slash commands, or
   whether this requires SendKeys-style keystroke injection via PowerShell/
   AutoHotkey after a launch-detection delay. Flag as the primary technical
   risk of this WO -- if slash commands cannot be reliably scripted, escalate
   back to Tony with findings before falling back to a partial-manual design.

--------------------------------------------------------------------------------
OPEN DECISIONS FOR TONY
--------------------------------------------------------------------------------
1. RESOLVED 2026-08-10: existing Agentic-Hub-Governance\utils\ folder. No new
   folder created -- see LOCATION section above.
2. RESOLVED 2026-08-10: shell:startup shortcut. Tony wants Chrome itself
   launching at Windows sign-in, not just at some scheduled/triggered time --
   Startup folder matches that intent directly. Task Scheduler dropped.
3. RESOLVED 2026-08-10: no project default -- Hub root
   (C:\Users\Trader\AI-Agent-Learning-Hub), not P_300. Tony's reasoning: since
   P_000 owns this as Hub-wide infrastructure, defaulting into any one
   downstream project (P_300 or otherwise) reintroduces the same bias the
   DESIGN REQUIREMENT above already ruled out for hardcoding. P_000's own
   context IS the neutral default. A specific project remains available as an
   explicit parameter for anyone who wants to land directly in P_300, P_115,
   etc.
4. RESOLVED 2026-08-10: nothing manual. /login and /chrome must be automated
   too, not just the Chrome launch. See DESIGN REQUIREMENT and item 4 above for
   the technical risk this introduces.

--------------------------------------------------------------------------------
NEXT STEP
--------------------------------------------------------------------------------
On Tony's go-ahead and answers to the Open Decisions above, build in a P_000
session per Hub governance (python-project-architecture skill loaded first
if any .py wrapper is involved; peh-handoff skill applies if this produces
files via Windows-MCP). Independent Review not required for shared-infra
scripts under ~50 lines per existing precedent, but Completion Gate block
still required in the same edit that sets OWNER_DONE.

--------------------------------------------------------------------------------
RESOLUTION (2026-08-28)
--------------------------------------------------------------------------------
Built: Agentic-Hub-Governance\utils\hub_chrome_claude_startup.ps1 (102 lines).
Params: TargetProject (default Hub root), ClaudeStartupDelaySeconds (default 12),
CommandDelaySeconds (default 3), InstallStartupShortcut (switch).

Chrome check-before-launch added per Tony's request during build (Get-Process
guard, only launches if not already running -- Chrome itself is single-instance
safe regardless, this just avoids an unnecessary extra window).

PRIMARY RISK RESOLVED: slash commands are interactive-mode only in Claude Code
(confirmed against Anthropic's own docs/issue tracker) -- no scripted-stdin path
exists. Built via WScript.Shell SendKeys against the launched window instead.

ITERATION DURING LIVE TEST: first live run failed -- AppActivate(processId)
could not find a window, because Windows Terminal (not the launched
powershell.exe child process) owns the top-level window. Fixed by switching
to AppActivate title-matching on "Claude Code" instead of matching by process
ID, which is stable regardless of which process actually owns the window.
Re-test succeeded -- both /login and /chrome fired without the activation
warning.

SECOND FINDING (accepted, not fixed): /login opens an interactive OAuth
sub-prompt ("Paste code here if prompted") when a fresh login is actually
needed, rather than returning immediately to the normal prompt. On those
days, /chrome sent on the fixed delay can land as literal text in that box
instead of firing as a command. Confirmed live: /chrome had to be re-sent
manually after Tony completed sign-in. Decided with Tony not to add
state-detection complexity for this -- most days the session is already
authenticated (confirmed in the same test round: /login was a clean no-op
when already signed in), and the failure mode is harmless (stray text in a
code box, cleared with Esc) rather than damaging.

Also confirmed: /login opens the OAuth URL in the OS default browser (Edge
on this machine), not the Chrome window this script launches -- expected,
not a bug. Actual credential entry stays manual either way, which is
correct and not something this script should automate.

FULL VERIFICATION, in order, same live session:
1. -InstallStartupShortcut: shell:startup .lnk created, target/args/workdir
   confirmed correct via WScript.Shell inspection.
2. Chrome-not-running -> full run: Chrome launched, claude --chrome launched
   scoped to Hub root, title-match activation succeeded, /login fired and
   returned "Login successful" (session was already authenticated from a
   prior run at this point).
3. Second full run after Tony force-closed Chrome (fresh-login scenario):
   AppActivate + /login fired correctly, opened OAuth sub-prompt as
   described above; Tony completed sign-in manually; /chrome then sent
   manually to confirm the connection path -- returned "Status: Enabled,
   Extension: Installed, Enabled by default: Yes".

Independent Review note: this script is 102 lines, over the ~50-line
precedent this WO's own NEXT STEP section cites for skipping Independent
Review. Not skipping it on that basis -- flagging explicitly rather than
silently claiming the exemption still applies.

--------------------------------------------------------------------------------
## Completion Gate (ref WO-P000-E3.001)
--------------------------------------------------------------------------------
[x] All file paths use Hub canonical paths -- Agentic-Hub-Governance\utils\
    (existing folder, per this WO's own RESOLVED Location decision)
[x] New/changed shared-resource location reflected: this WO is the record;
    no P_000_SYSTEM_DOCUMENTATION.md Document Index entry needed (utils\ is
    already a known canonical path, not a new one); no project CLAUDE.md or
    skill file references this script yet, so none to update
[x] CALLER PROPAGATION: N/A -- this is a new standalone entry point (the
    shell:startup shortcut), not a change to an existing shared function
    with other callers to check
[x] IMPERATIVE SWEEP: N/A -- no existing rule/Must/anti-pattern changed
[x] Downstream projects in Affects notified -- Affects is Hub-wide/all
    projects but shared infra with no project-specific logic touched;
    documented here, Tony present for the entire live test
[x] No sys.path side-channels introduced -- N/A, PowerShell not Python
[x] No schema/signal contract changed -- N/A
[x] DRAFT files cleanup / one ledger entry confirmed -- no DRAFT files for
    this WO
[ ] Independent Review -- NOT YET DONE. Flagged above: script exceeds the
    ~50-line no-review precedent this WO cited, so Independent Review in a
    separate session is recommended before treating this as fully closed,
    even though OWNER_DONE is appropriate now that build + live
    verification are complete.
--------------------------------------------------------------------------------
Created 2026-08-10 by P_000 (ledger admin) per Tony, reclassifying an offer
made in a P_300 session as Hub-wide shared infrastructure.

--------------------------------------------------------------------------------
UNRELATED FINDING -- flagged for P_000, not part of this WO's scope
--------------------------------------------------------------------------------
Added 2026-08-10 by a P_400 session (WO-P400-E5.005), at Tony's direction, to
the last P_000 WO by mtime -- not filed as its own WO since that call belongs
to a P_000 session, not this one. Purely a landing spot for a finding; not
scoped, sized, or owned as work here.

FINDING: no verification step exists, anywhere, confirming a per-action-
triggered skill (a skill meant to fire before every occurrence of some action,
not once at session start) is actually current in a Claude session's live
practice versus what's on disk.

Concrete case: peh-handoff v1.7 (WO-P000-E15.001, 2026-08-09) moved the
default file-write remedy from PowerShell-command-string embedding to a
direct-write tool, specifically because command-string embedding was the
confirmed root cause of three corrupted files in one session (2026-08-05).
A P_400 session on 2026-08-10 (WO-P400-E5.005) used the old, superseded
method for an entire session of file writes -- including reintroducing the
exact escape-sequence-corruption failure class the v1.7 fix exists to
prevent (a skill-file docstring corrupted via backtick+letter misread as a
PowerShell escape). Caught only because Tony independently ran the test
suite and got a SyntaxError; not caught by anything in the session itself.

WHY: p400-project-context (session-start-triggered: "load at the start of
ANY session involving P_400 work") gets read every session because INIT is
one concrete moment every session hits. peh-handoff (per-action-triggered:
"before any Windows-MCP call expected to produce a file") has no equivalent
enforcement point -- it depends entirely on the session correctly
self-invoking it before the first of potentially dozens of file-writing
calls, with no gate, checklist, or INIT step confirming that happened, and
no check for whether the invoking session's own memory/recall of the skill's
content is current versus stale.

NOT PROPOSING A SPECIFIC FIX -- P_000's call, not this session's. Two shapes
worth considering, not a recommendation:
1. INIT-time fresh-read of any skill whose own trigger text is per-action
   rather than session-level (peh-handoff is the known instance; worth an
   audit for others written the same way) -- answers "is this current"
   once, visibly, before work starts, rather than never.
2. Some structural gate on Windows-MCP file-writing tool calls themselves,
   analogous to how the computer_use skill-check reflex is wired for
   Linux-sandbox file creation (docx/pptx/etc.) but is not currently wired
   for Windows-MCP calls at all -- this may be the deeper gap: session-start
   skill loading and the "check skill before writing a file" reflex are two
   separate mechanisms, and only the first one reliably fires here.

Cost: Tony's own words -- "this cost me tokens and my time which I find
valuable." A real syntax break, caught externally, on a Hub-governance file,
by a rule that had already been written down once specifically to prevent
this class of failure.
