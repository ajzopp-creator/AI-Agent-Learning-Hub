PEH RUN -- 2026-08-04 -- Archive the 20 mined XLSX left in data\bulk\mine\

CONTEXT:
Today's 12:03 BulkAddPattern batch promoted cleanly to live
(080426catalog.db confirmed on disk, 175.3 MB, .bak matches yesterday's
live size exactly -- catalog is fine) but the archive-mined step failed
on all 20 files: E: drive was completely full (0 GB free), zip write hit
[Errno 28] No space left on device. You've since freed space on E:
(682.82 GB free now) and the corrupt 994-byte zip stub is already gone.

WHAT THIS SCRIPT DOES:
Calls the same archive_mined_file.run_archive() function archive-mined
--xlsx normally calls, once per file, in the original batch's order.
For each file: appends to Aug26BULKPattern.zip in E:\AI-Agent-Learning-
Hub_BackupFiles\, verifies the entry actually landed in the zip, and
only then deletes the source XLSX from data\bulk\mine\. Does NOT touch
the catalog, does NOT re-run mine-patterns or ingest-mined -- promote
already happened, this is archive-only.

SUCCESS CRITERIA:
"Archived 20 / 20 files." at the end, exit 0. data\bulk\mine\ should be
empty of these 20 files afterward (each deleted only after its zip entry
verified).

IF IT FAILS:
- Any single file failing (still E: space issue, permissions, etc.) --
  paste the full output, do not summarize. That file's source XLSX is
  guaranteed to still be sitting in data\bulk\mine\ (the function never
  deletes on failure) -- nothing is lost, just re-run this script once
  fixed.
- If it stops partway, safe to just re-run -- already-archived files are
  skipped ("not found" -- they were deleted from data\bulk\mine\ on
  their successful archive) and the zip's own "already in zip" check
  prevents duplicate entries either way.
