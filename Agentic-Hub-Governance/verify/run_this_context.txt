PEH VERIFY -- 2026-08-04 -- Independent review re-run for WO-P300-E5.002 / WO-P300-E5.005 closure

WHAT THIS SCRIPT TESTS:
Re-runs 5 existing test files as subprocesses (p140 interpreter), fresh,
in a session that wrote none of them -- the WO_COMPLETION_GATE Independent
Review Requirement. Does not modify any test or production code.

  1. test_verify_ingestion.py -- WO-P300-E5.002/E5.005 shared completion-
     gate test (verify_and_promote() fail-branch + pass-branch). Already
     run once for real 2026-07-30 (ALL CHECKS PASSED); this is an
     independent re-confirmation, not a repeat.
  2. test_walkforward_report_io.py -- WO-P300-E5.005 gate report parser
  3. test_promote_gate.py -- WO-P300-E5.005 domain decision logic
  4. test_promote_marker_io.py -- WO-P300-E5.005 stop-marker read/write
  5. test_cli_registry_inventory.py -- confirms 18 commands / 6 modules
     still registered in cli_commands/main.py (no drift since 2026-07-29)

SUCCESS CRITERIA:
All 5 subprocess calls exit 0. Script prints "ALL 5 TEST FILES PASSED"
and itself exits 0.

IF IT FAILS:
- Any single file's non-zero exit -- paste the full output for that file
  back, do not summarize. This blocks closing WO-P300-E5.002 and/or
  WO-P300-E5.005 until root-caused.
- Import errors -- likely a source file changed since 2026-07-29/30;
  report which import failed, do not guess a fix.

Do not change test assertions in any of the 5 files. Read-only against
the real project -- no catalog writes, no file writes outside tempfile.TemporaryDirectory().
