WO-P400-E2.023 -- Backward-Looking Post-Earnings Proximity Check (MACRO)
Date: 2026-07-24

WHAT THIS TESTS:
New CAUTION-only (never BLOCK) check in domain\council.py macro_vote() --
flags candidates within POST_EARNINGS_STABILIZATION_SESSIONS (config.py,
default 3) of their last earnings report. New helper
_sessions_since_earnings() in application\evaluate_signal.py (weekday-
count approximation, no holiday calendar). New SnapshotDict field
last_earnings_date (schemas.py). New CLI flag --last-earnings-date on
fetch-snapshot (cli.py, application\fetch_snapshot.py). New reason code
RC_POST_EARNINGS_STABILIZATION (domain\council_codes.py). 8 new tests
across test_council.py (4) and test_evaluate_signal.py (4).

SUCCESS CRITERIA:
1. Import smoke check passes -- confirms no circular import / syntax
   error introduced across the 6 touched files.
2. Full pytest suite green -- was 210 passed / 4 skipped before this WO
   (per architecture doc's last recorded baseline); expect 218 passed
   (210 + 8 new) / 4 skipped, zero regressions elsewhere.

IF IT FAILS:
- Import error -- most likely a missed import in council.py or
  evaluate_signal.py (POST_EARNINGS_STABILIZATION_SESSIONS,
  RC_POST_EARNINGS_STABILIZATION). Check the import blocks at the top of
  both files first.
- test_council.py failures -- check macro_vote()'s new
  sessions_since_earnings branch sits AFTER the existing
  earnings_in_window/binary_events branch (forward-looking check must
  still take priority; this WO doesn't change that).
- test_evaluate_signal.py failures on the new _sessions_since_earnings
  tests -- these are computed relative to date.today() by design (walk
  backward N real weekdays), not hardcoded dates, so they should not rot.
  If one fails, the weekday-walk logic itself likely has an off-by-one.
- Any failure in a PRE-EXISTING test (not one of the 8 new ones) means a
  real regression -- do not change existing test assertions to make them
  pass; report back instead.
- Do not change test assertions to make them pass. Fix the production
  code or report back if something genuinely doesn't match expectations.

Paste the full output back, including the import smoke check section.