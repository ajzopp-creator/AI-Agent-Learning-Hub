WO-P400-E6.001 Scope item 2 -- File 9 FIX (shared_resources\python_utils\
p020_order_writer.py's sys.modules collision) -- 2026-09-07

Background: Claude Code's own PEH run of the file-10 handoff surfaced a
real live bug in file 9 (written earlier the same day): P_400's own
top-level "infrastructure" and "domain" packages (both real, both
already loaded whenever P_400 code runs) get resolved by Python's import
cache in place of P_020's same-named packages, because
_load_p020()'s "from infrastructure.db_client import ..." looks up
sys.modules['infrastructure'] first -- the sys.path insert never gets
consulted once a module of that name is already cached. Confirmed live:
"No module named 'infrastructure.db_client'" when called from a P_400
process. Fix: sys.modules stash/restore around the P_020 import,
scoped to just "infrastructure"/"domain" (the two colliding names) --
Tony approved this specific fix ("yes").

File written directly via windows-mcp FileSystem (mode=write, full
rewrite -- only _load_p020() and its docstring/module comment changed,
submit_order() itself is byte-for-byte unchanged). Durable-signal
Test-Path/Length check succeeded directly via PowerShell (6,514 bytes).
Skipped the compile subprocess call via windows-mcp:PowerShell -- stalled
4+ min on every file this session so far. Handing straight to Claude
Code.

CRITICAL -- this script deliberately RECREATES the exact collision that
caused the live bug, rather than just re-running file 9's original
(collision-free) tests, which would NOT have caught this. It imports
P_400's real infrastructure.eval_cache FIRST (establishing "infrastructure"
in sys.modules as P_400's package, exactly like any real P_400 process),
THEN calls submit_order() and confirms it still reaches P_020's
db_client/db_order_writer correctly. infrastructure.db_client.
get_connection is monkeypatched to a scratch in-memory DB before the
real call -- no live P_020 database is touched.

What it does:
  1. Confirms the file exists with a plausible line count, compiles
     under warnings-as-errors.
  2. Recreates the collision: imports P_400's real
     infrastructure.eval_cache, confirms "infrastructure" is now in
     sys.modules pointing at P_400's package.
  3. Re-runs the file-9 lazy-import regression check (bare import of the
     bridge module must not touch P_020's sys.path).
  4. Briefly reaches into P_020's infrastructure.db_client to monkeypatch
     get_connection -- immediately restores P_400's stash afterward, so
     this setup step itself doesn't leave the collision "pre-resolved"
     before the real test.
  5. Calls submit_order() THROUGH the still-standing collision (P_400's
     infrastructure.eval_cache is still loaded) -- this is exactly the
     call that raised ModuleNotFoundError before the fix. Confirms it
     returns a real order_id and the row lands correctly in the scratch
     orders table.
  6. Confirms P_400's own infrastructure package -- SAME OBJECT IDENTITY
     (id() check, not just "still importable") -- and its
     read_eval_cache function are both intact immediately after
     submit_order() returns.
  7. Calls submit_order() a SECOND time, confirms it still works and
     P_400's infrastructure identity is still unchanged -- proves the
     stash/restore is repeatable per-call, not a one-shot fix.

Success criteria: script prints "PASS: p020_order_writer.py's
sys.modules collision fix validated (P_400<->P_020 both intact)." and a
run_this_P020_20260907_214809.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs.
- Collision fix still failing (submit_order returns None or raises):
  read _load_p020()'s stash/restore block directly -- likely
  _colliding_names() isn't catching a submodule name correctly (check
  the startswith(pkg + ".") logic), or the stash pop/restore is
  happening in the wrong order relative to the import statements.
- P_400 infrastructure identity changed after the call: the `finally`
  block's restore isn't running, or is restoring the wrong dict --
  check the try/finally actually wraps the three import statements, not
  just some of them.
- Second call failing: confirms the fix isn't idempotent -- likely
  P_020's infrastructure got left in sys.modules after the first call
  (restore incomplete), so the second call's sys.path insert is skipped
  (already in sys.path) but the stale P_020 infrastructure object
  doesn't get cleared before... trace _colliding_names() being called
  fresh each time inside _load_p020(), not cached from the first call.

Do not change test assertions. This script never opens the real
P_020_trades.db.

Sequencing note: file 9 fix. This closes out the file-10 handoff's
open item. Next in the P_020 sequence per the WO's build-order
narrative: P_800\regenerate_p400_notes.py (vault becomes a real
generated view for live orders), then paper_order_history_parser.py
(largest single lift, deferred until now on purpose), then paper wired
into order_reconciler.py/reconcile_command.py last.
