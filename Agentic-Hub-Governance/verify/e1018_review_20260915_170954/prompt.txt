Perform an Independent Review of WO-P020-E1.018 at:
C:\Users\Trader\AI-Agent-Learning-Hub\Agentic-Hub-Governance\work_orders\WO-P020-E1.018.md

You are a SEPARATE session from the one that implemented this WO, per
WO_COMPLETION_GATE.md's Independent Review Requirement. Your job is to
re-verify each Acceptance Criteria item against live code/tests/DB -- not
to trust the WO's own prose. Read WO_COMPLETION_GATE.md first (same
folder) for what Independent Review is and is not.

Project root: C:\Users\Trader\AI-Agent-Learning-Hub\projects\P_020_AJZStrategies_PerformanceAnalysisSystem

Re-verify, independently, each of these:
1. Read the WO in full -- WHY, PROPOSED FIX, Acceptance Criteria -- Status,
   Files Built, Completion Gate, Occurrence Log.
2. Re-run the full regression suite live:
   python -m pytest tests python\database\tests -v
   (run from the project root, using
   C:\Users\Trader\.conda\envs\p140\python.exe)
   Confirm the actual pass count matches what the WO claims -- do not
   trust the number in the WO text.
3. Re-run just the new test file and confirm all tests pass and actually
   test what their names claim:
   python -m pytest tests\test_expiration_closer.py -v
4. Open domain\expiration_closer.py and confirm the eligibility logic
   really does require ALL of: status='open', asset_type in (call,put),
   underlying_symbol in the CASH_SETTLED_OPTION_ROOTS allowlist, and
   expiration_date at least EXPIRATION_CLOSE_BUFFER_DAYS in the past.
5. Open config.py and confirm CASH_SETTLED_OPTION_ROOTS is exactly
   {"NDXP"} (not silently broader) and EXPIRATION_CLOSE_BUFFER_DAYS is 1.
6. Trace the expiration_date/settlement_price field all the way through:
   infrastructure\schwab_instrument_parser.py (extraction) ->
   infrastructure\schwab_mapper.py (capture) ->
   domain\exit_allocator.py (carried through the trade dict) ->
   application\ingest_pipeline.py (_build_trade passes it to Trade(...)) ->
   infrastructure\db_writer.py (insert_trade writes it).
   Confirm every link is real, not just claimed.
7. Confirm infrastructure\db_client.py's create_all_tables() trades DDL
   includes expiration_date and settlement_price columns (this was a
   fix made mid-build -- confirm it actually landed).
8. Compile every file listed in the WO's "Files Built" table under:
   C:\Users\Trader\.conda\envs\p140\python.exe -W error::SyntaxWarning -m py_compile <file>
   Confirm every one is compile-clean AND check each file's line count
   against the project's 300-line hard limit (this project uses a hard
   300-line-per-file cap) -- flag anything at or over 300.
9. Re-run the live smoke test yourself:
   cd python\database
   C:\Users\Trader\.conda\envs\p140\python.exe P_020_Trade_Manager.py close-expired-options
   Confirm it runs without error and does not write anything (no --commit
   was passed).
10. Confirm the migration actually ran against the live DB -- query
    data\database\P_020_trades.db and confirm the trades table has
    expiration_date and settlement_price columns (PRAGMA table_info).

If everything above holds: edit ONLY the WO file
(WO-P020-E1.018.md) to add an "## Independent Review (2026-09-15)"
section documenting exactly what you re-verified and how (be specific --
actual pass counts, actual line counts, actual query results, not vague
claims), check the "Independent Review, separate session from
implementation" Acceptance Criteria box, and change the Status line at
the top from OWNER_DONE to CLOSED with a one-line note.

If ANYTHING does not hold -- a test fails, a claim doesn't match what you
find, a file is over 300 lines, a field doesn't actually propagate --
do NOT mark it CLOSED. Instead add the same "## Independent Review
(2026-09-15)" section documenting the specific discrepancy found, leave
Status as OWNER_DONE, and add a clear "BLOCKED ON:" line describing
exactly what needs fixing.

Do not modify, edit, or create any file other than WO-P020-E1.018.md. Do
not touch any .py file, even to fix something you find wrong -- your job
is to report, not to fix. Running pytest and python (read-only against
the code, but they do write __pycache__ / .pytest_cache as a side
effect, which is fine) is expected and required for this review; writing
to any other tracked file is not.
