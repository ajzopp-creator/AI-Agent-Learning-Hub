WO-P400-E6.001 Scope item 2 -- File 11 of 18, part 1 of 3
(p400_regenerate\infrastructure\vault_note_scanner.py, new) -- 2026-09-07

First file this session inside P_800's own project folder
(P_800_Automation_Note_Taking\python\p400_regenerate\ -- a new
sub-project mirroring the existing p115_backfill\ precedent in the same
folder: namespaced domain/infrastructure/application, imports as
"p400_regenerate.infrastructure.x", not bare "infrastructure.x" --
sidesteps the exact sys.modules collision class found and fixed in file
9 earlier today, by construction). __init__.py files for p400_regenerate\
and p400_regenerate\infrastructure\ were also created (332 bytes and 0
bytes respectively) as part of this same write.

Checked against p800-project-context SKILL before writing anything:
P400Record's vault schema already has lifecycle_status/entry_date/
close_date/realized_pnl fields (no schema change needed), and P_020's
own infrastructure\vault_system_reader.py already reads P_400 vault
notes directly from disk (frontmatter line-scanner, not full YAML) for
system attribution -- this file's _parse_frontmatter() mirrors that
proven approach. Reading vault content is not restricted to P_800 the
way writing is.

Durable-signal Test-Path/Length check succeeded directly via PowerShell
(3,076 bytes). Skipped the compile subprocess call via
windows-mcp:PowerShell -- stalled 4+ min on every file this session so
far. Handing straight to Claude Code.

IMPORTANT -- the real trading_journal\ vault is NEVER touched by this
script. obsidian_writers.config.VAULT_ROOT and VAULT_FOLDER_MAP are
monkeypatched to point at a temporary directory (tempfile.mkdtemp) with
4 synthetic note files, before find_note_by_order_id() is ever called.
The temp directory is cleaned up (shutil.rmtree) at the end regardless
of pass/fail.

What it does:
  1. Confirms the file exists with a plausible line count, compiles
     under warnings-as-errors.
  2. Builds a synthetic vault: 2 notes in TradeOrderManagement/P400/
     (order_id 7001 = target, order_id 7002 = decoy), 1 malformed note
     with no frontmatter at all in the same folder (must not crash the
     scan), 1 note in TradeOrderManagement/P400/paper/ (order_id 8001).
  3. Confirms find_note_by_order_id("7001") returns the correct note's
     full field dict (ticker=AAPL, signal_date=2026-09-01), with
     _schema='P400' and _note_path ending in the right filename.
  4. Confirms find_note_by_order_id("8001") finds the PAPER note and
     tags it _schema='P400_PAPER' -- proves both folders get searched,
     not just P400.
  5. Confirms a non-existent order_id returns None cleanly.
  6. Confirms the malformed note didn't crash tests 1-3 (implicit --
     it sits in the same folder as the successfully-found notes).

Success criteria: script prints "PASS: vault_note_scanner.py validated
against a synthetic vault (real vault untouched)." and a
run_this_P800_20260907_221648.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs this
  session (check the module docstring for unescaped backslashes --
  this file's docstring header includes Windows-style path fragments).
- Note not found / wrong note matched: read find_note_by_order_id()'s
  glob + _parse_frontmatter() call directly against the synthetic
  NOTE_TEMPLATE this script writes -- likely a strip()/quote-handling
  bug in _parse_frontmatter() on the order_id line specifically.
- P400_PAPER note not found: confirms the for-loop over ("P400",
  "P400_PAPER") isn't actually iterating both, or VAULT_FOLDER_MAP
  lookup for "P400_PAPER" is failing silently (check the `if not rel:
  continue` branch isn't swallowing a real entry).
- Crash on the malformed note: _parse_frontmatter() must handle a file
  with no "---" opening delimiter (returns {} immediately) -- confirm
  that early-return branch is still there.

Do not change test assertions. This script builds and tears down its
own temp vault every run -- confirm any fix keeps the real
trading_journal\ vault untouched.

Sequencing note: file 11 of 18, part 1 of 3. Next in this file:
p400_regenerate\infrastructure\p020_reader.py (lazy cross-project read
of P_020's recently-closed, not-yet-synced orders), then
p400_regenerate\application\regenerate_command.py (orchestrates: find
note by order_id -> merge reconciled fields -> write_to_vault()).
