Phase/WO: P_115 STEP 2 signal emission (PATH, P_910-sourced, Fund-verified BUY)
Date: 2026-09-11

What this tests: Emits the P_115->P_400 SIGNAL_V2 JSON packet for PATH using
the same emit_signal() call cli.py normally runs via command line. Staged
proactively this time (not after a timeout) since the identical cli.py call
for EXPD earlier this session took over 4 minutes via direct Windows-MCP
PowerShell. Launched via Start-Process -WindowStyle Hidden (detached) rather
than a blocking synchronous call, per peh-handoff v1.10.

Success criteria: Script prints "Signal written: <path>" followed by "PASS",
and writes run_this_P115_20260911_112040.py.done next to itself. The path
must exist on disk (json file under
trading_journal\TradeOrderManagement\signals\).

If it fails: Read the exception message. Most likely causes are an import
error (config.py / application.emit_signal.py path), a schema validation
error inside emit_signal, or a permissions issue writing to the signals
folder. Do not change test assertions -- there are none here, this is a
production call, not a unit test. Fix the underlying cause and re-run.
Paste full output back to Tony.
