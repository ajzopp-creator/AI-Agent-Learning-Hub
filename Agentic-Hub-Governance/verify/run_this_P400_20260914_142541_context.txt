P_400 Trade Order Management -- PEH handoff
Date: 2026-09-14
Phase/WO: none open (post-evaluate/spec, record step)

Context: ADP cleared the full pipeline -- Tier-1 screen (RR=10.42) ->
compare (vehicle=STOCK, option leg BLOCKED on OI/spread) -> evaluate
(APPROVED, all five council roles PASS, 2 shares, $22.89 risk, R:R 8.36,
gate=CONCENTRATION) -> spec (PATTERN A -- Stock OCO Bracket: entry
$275.70 limit / T1 $372.74 / stop $264.26 stop-limit $264.21 limit).

Tony just filled it. Fill confirmation (his TOS paste, verbatim):
  #15109450962 PAPERMONEY BOT +2 ADP @275.88 MARK=275.8812 IMPL VOL=28.90%, ACCOUNT D-******25
"PAPERMONEY" = TOS paper account -- this is a PAPER fill, not real. Order
ID: 15109450962. Fill price $275.88 vs spec's $275.70 planned entry --
normal minor fill variance, not a re-evaluate trigger.

What the script does: records this fill via the documented syntax
(p400-project-context skill, "Record trade_mode" section):
```
cli.py record ADP --order-id 15109450962 --paper
```
`record` accepts `--paper` independently of what evaluate/spec cached
(WO-P400-E5.001) -- do not omit `--paper` here, this fill is paper, not
real, regardless of what evaluate's "Mode: REAL" default label showed.

Success criteria: script prints PASS, .done marker shows status=PASS, and
the captured stdout confirms a P400 vault write for ADP with trade_mode
PAPER (schema `P400_PAPER` per the paper book dir:
VAULT_ROOT\TradeOrderManagement\P400\paper\, VAULT_ROOT =
C:\Users\Trader\AI-Agent-Learning-Hub\trading_journal\) and the order ID
15109450962 present in the record.

If it fails: read STDOUT/STDERR. If it's a CLI-flag/usage mismatch (same
class as the compare/evaluate handoffs earlier today), correct THIS
script's invocation freely and note the corrected syntax in the pasted
output so it can be added to the skill file. If it's a real error inside
record_writer.py/obsidian_writers (a genuine production bug -- e.g. wrong
vault path, wrong schema, order ID not captured), do NOT patch production
code -- report the exact error and propose a fix for Tony to review.

Do not change test assertions (record must complete, must show PAPER
mode, must show order ID 15109450962). Do not modify any production file
under projects\P_400_TradeOrderManagement\ -- this script only shells out
to the existing CLI and reads/verifies its output and the resulting vault
file.
