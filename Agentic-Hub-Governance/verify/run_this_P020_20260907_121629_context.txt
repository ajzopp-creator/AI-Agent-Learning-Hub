WO-P400-E6.001 Scope item 2 -- File 1 of 18 (migration_add_orders_table.py) -- 2026-09-07

What this tests: The Claude session just wrote
projects\P_020_AJZStrategies_PerformanceAnalysisSystem\python\database\
infrastructure\migration_add_orders_table.py directly via windows-mcp
FileSystem (mode=write). The follow-up validation call (Test-Path +
Get-Item Length/line-count + py_compile under -W error::SyntaxWarning)
stalled 4+ minutes with "No result received." Pinged the relay
separately afterward with a trivial Write-Output -- it responded in
under a second, so the relay itself is healthy; the stall was specific
to that call. Per peh-handoff protocol: zero retries of the actual
work/validation call, hand off to Claude Code instead.

This script validates the file WITHOUT touching the real P_020
production database (P_020_trades.db) -- it never calls migrate() or
opens DB_PATH. It only:
  1. Confirms the file exists on disk with a plausible line count
     (rules out a chunk-boundary merge or truncated write).
  2. Compiles it under warnings-as-errors (rules out invalid escape
     sequences in the docstring, self-terminating docstrings, etc. --
     known peh-handoff failure modes).
  3. Executes ONLY the ORDERS_TABLE_SQL constant against a throwaway
     :memory: SQLite connection to confirm the SQL itself is valid.
  4. Checks the resulting column set matches the WO's BUILD PLAN spec
     exactly (20 columns, see script for the full list).
  5. Confirms the DB_PATH constant in the file points at the real
     P_020_trades.db path, without connecting to it.

Success criteria: script prints "PASS: migration_add_orders_table.py
validated, production DB untouched." at the end, and a
run_this_P020_20260907_121629.py.done file appears next to this one
with status: PASS.

What Claude Code should fix on failure:
- Line count out of [100, 140] range: read the file directly, look for
  a chunk-boundary merge (two lines joined with no newline between --
  most likely spot is right after the module docstring's closing
  triple-quote) or a truncated write. Re-report the actual line count
  and the suspect region; do not silently rewrite the file.
- Compile error: read the exact SyntaxError/SyntaxWarning text. If it's
  an escape-sequence warning inside the docstring (e.g. `\t`, `\d`),
  the fix is switching the docstring to a raw string (r"""...""") or
  escaping the backslash -- check the docstring doesn't already use a
  raw prefix before assuming this.
- SQL invalid against :memory:: read the sqlite3.Error text directly --
  likely a typo in a column type or a trailing comma issue in
  ORDERS_TABLE_SQL. Fix in the production file, do not patch around it
  in this test script.
- Missing/extra columns: compare against the 20-column list in this
  script's step 4 -- that list is copied verbatim from WO-P400-E6.001's
  BUILD PLAN table entry for this file. If the production file's
  columns are wrong, fix the production file's ORDERS_TABLE_SQL, not
  this script's expected set.
- DB_PATH mismatch: confirm against
  data\database\P_020_trades.db (the same path
  migration_add_tag_columns.py in the same folder already uses) --
  fix the production file if it drifted.

Do not change test assertions. Do not call migrate() or connect to the
real P_020_trades.db from this script or any fix -- that is a
separate, explicitly-approved step, not part of this validation.

Note for whoever picks this up: there is ALSO an older unconsumed
handoff sitting in this same verify\ folder --
run_this_P020_20260905_105257.py (2026-09-05, P_020 Weekly Update
Steps 2-5 resume, no .done file). That one is unrelated to this WO
(different task, older, real production Schwab pull) and was flagged
to Tony separately. If picking up both in one sitting, run that one
first -- it is the older, real production task; this one is a fresh
build's validation step.
