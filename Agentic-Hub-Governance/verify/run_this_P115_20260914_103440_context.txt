Task: P_115 STEP 2 signal emission -- ADP, SOURCE:P_116 (Options Income Launchpad,
bounce/premium), 2026-09-14.

What this tests: runs cli.py to emit a SIGNAL_V2 packet for ADP (entry 273.76, stop
264.26 PA-Stop/structure, target 372.74 = T2, horizon "10-15 trading days") and
confirms the expected output file exists on disk with a plausible size -- fixed
verification approach (Test-Path the known convention path directly), per the
2026-09-14 lesson that stdout-text parsing produced a false FAIL on NFLX's run
even though that emission actually succeeded.

Diagnostics (P_115_BuytheDipChart_V16, chart timestamp 9/14/26): Fund 4 (200-MA
14.9% NORMAL, no penalty), Anal 3, Candle 2, Setup 3, STR 0, HybridTier 7 (meets
BUY>=6, comfortably above floor unlike NFLX's exact-6). P_116 excluded from Fund
Verification. No earnings flag: last report 7/29/26, next expected ~early Nov 2026
(fiscal Q1, not confirmed/imminent).

FLAGGED ASSUMPTIONS -- confirm or correct with Tony, don't let this pass silently:
- --confidence HIGH: no locked HybridTier->confidence formula exists; chose HIGH
  because HybridTier=7 clears the BUY floor with room (vs NFLX's exact-6, logged
  MEDIUM) and Fund status is NORMAL (no pullback penalty) -- reasoned, not ruled.
- --volume 49008: chart shows RVOL 0.17 and VolumeAvg(20) 288,285; this is
  0.17 x 288,285, a derived estimate, not a value read directly off the chart.
- --strategy bounce_income: same free-text convention used on NFLX, no locked
  P_116-specific label exists in signal_schemas.py.
- --source-link "TradeManagement/P115/2026-09-14_ADP.md": placeholder path, no
  P_800 vault note written for this signal yet.
- Chart shows T1 Exit and T2 Exit as the SAME value (372.74) -- flagged as a
  possible chart-indicator oddity, not silently resolved. Used 372.74 as --target
  since it's the only value present either way.

If it fails: read the actual error, and specifically check whether the failure is
a real emit_signal problem or another false-negative in this run_this script's own
verification logic (Test-Path the exact path first before assuming emit_signal
broke). Do NOT change --source -- must stay P_115 (packet signal_source is always
P_115 regardless of scan origin; tracker's SignalSource column is the P_116 one).
Do not silently substitute a different confidence/volume/strategy value to make
it pass -- flag back to Tony.

Do not change test assertions.
