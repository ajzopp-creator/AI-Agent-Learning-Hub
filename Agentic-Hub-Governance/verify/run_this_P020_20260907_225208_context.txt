WO-P400-E6.001 Scope item 2 -- File 13 of 18, FINAL FILE of this WO scope
item (infrastructure\db_reader.py +get_open_paper_orders,
application\reconcile_command.py paper wiring + run_reconcile_command()
refactor, cli.py --paper/--statement flags) -- 2026-09-07

Completes the entire WO-P400-E6.001 Scope item 2 build: 13 files across
P_020/P_400/shared_resources/P_800, live path fully wired end-to-end
(files 1-8), vault sync (files 9-11), and now paper wired into the exact
same reconciliation code as live (files 12-13) -- per Tony's explicit
requirement, same logic for both, not a parallel pipeline.

All 3 files written directly via windows-mcp FileSystem (db_reader.py
via append=True, the other two via mode=write full rewrite). Durable-
signal Test-Path/Length checks succeeded directly via PowerShell
(9,085 / 10,570 / 3,829 bytes). Skipped the compile subprocess call via
windows-mcp:PowerShell -- stalled 4+ min on every file this session so
far. Handing straight to Claude Code.

IMPORTANT -- run_reconcile_command()'s signature changed: start/end
moved from required to Optional[str]=None (paper mode legitimately omits
both), and --start/--end moved from argparse's required=True to an
internal check inside run_reconcile_command() itself (since a single
argparse parser can't conditionally require args based on --paper).
This script explicitly re-tests the "missing --start/--end" case to
confirm the new internal check still produces the same SystemExit(1)
behavior the file-8 handoff proved via argparse alone.

IMPORTANT -- the paper-path test does NOT mock
paper_order_history_parser.py -- it runs for real against a synthetic
statement file (same verbatim-real-row MRCY bracket content as the
file-12 handoff), since that module has no external dependencies
(no network, no DB) to mock. infrastructure.db_client.get_connection
IS mocked (scratch in-memory DB), so no real P_020 database is touched.

What it does:
  1. Compiles all 3 files under warnings-as-errors.
  2. get_open_paper_orders(): inserts 3 orders (PAPER+has-id [should
     match], REAL+has-id [wrong mode], PAPER+no-id [excluded]), confirms
     exactly the right one is returned. Also confirms get_all_trades/
     get_open_real_orders are still present (regression).
  3. reconcile_paper_orders() end-to-end: real parser output flows
     through the SAME _index_pulled_orders()/_reconcile_one() as live,
     confirms the MRCY paper order gets closed with realized_pnl=None
     (MKT exit, unrecoverable price -- same behavior proven in file 12,
     now confirmed through the full orchestration path too). Then
     confirms a second call with no open paper orders left returns all
     zeros cleanly.
  4. run_reconcile_command() LIVE-PATH REGRESSION: mocks
     reconcile_live_orders and schwab_positions.get_account_hash,
     confirms the live path still works after the refactor (same
     pattern as the file-8 handoff).
  5. run_reconcile_command() missing-args checks: confirms SystemExit(1)
     when --start/--end are missing on the live path (now an internal
     check, not argparse) AND when --paper is set without --statement.
  6. cli.py --paper/--statement dispatch: confirms cli.main() correctly
     parses and passes through paper=True, statement=<path>, and that
     start/end come through as None (not accidentally defaulted to
     empty strings or similar) when omitted.

Success criteria: script prints "PASS: db_reader.py,
reconcile_command.py, and cli.py all validated -- WO-P400-E6.001 Scope
item 2 build complete." and a run_this_P020_20260907_225208.py.done file
appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs this
  session.
- get_open_paper_orders() wrong result: read its SQL directly, same
  class of bug as get_open_real_orders()'s original filter (file-7
  handoff).
- reconcile_paper_orders() failures: read reconcile_paper_orders()'s
  call sequence directly against reconcile_live_orders() -- they should
  be structurally identical except the order source
  (get_orders_for_account vs parse_order_history) and the reader
  function (get_open_real_orders vs get_open_paper_orders). If
  _index_pulled_orders()/_reconcile_one() themselves needed a change to
  make this work, STOP and flag to Tony -- those must stay identical
  for both paths, per this whole file's reason for existing.
- Live-path regression failure: read run_reconcile_command()'s
  refactored branching directly -- likely the get_connection()/finally
  structure moved something out of the try block that needs to stay
  inside it.
- Missing-args checks failing: confirm both `if not (start and end):`
  (live path) and `if not statement:` (paper path) checks are still
  present and reachable given the new argument structure.
- cli.py dispatch failing: read the updated p_reconcile argparse block --
  --start/--end must now default=None (not required=True), --paper must
  be action="store_true", --statement must default=None.

Do not change test assertions. This closes out WO-P400-E6.001 Scope
item 2 in full -- all 13 files across 4 project areas now validated
PASS this session.
