WO-P400-E6.001 Scope item 2 -- File 7 of 18 (application\reconcile_command.py
new) + infrastructure\db_reader.py extension (+get_open_real_orders) --
2026-09-07

Both files written directly via windows-mcp FileSystem (db_reader.py via
append=True, reconcile_command.py via mode=write, new file). Durable-
signal Test-Path/Length checks succeeded directly via PowerShell
(db_reader.py: 7,667 bytes; reconcile_command.py: 5,928 bytes). Skipped
the compile subprocess call via windows-mcp:PowerShell -- stalled 4+ min
on every file this session so far. Handing straight to Claude Code.

IMPORTANT -- no live Schwab API call. This script monkeypatches
infrastructure.schwab_order_puller.get_orders_for_account with a fake
function returning canned fixture data before calling
reconcile_live_orders(). reconcile_command.py's local
"from infrastructure.schwab_order_puller import get_orders_for_account"
re-resolves that name from the module's current attribute at call time,
so the patch takes effect without needing to patch inside
reconcile_command's own namespace. No network access anywhere in this
script.

IMPORTANT -- read-only real-DB access only, same pattern as the prior
handoff: P_020_trades.db opened once via sqlite3 URI mode=ro solely to
read trades/exits/systems CREATE TABLE SQL. Every actual test runs
against a fresh :memory: scratch DB. No row in the real database is
ever read or written.

What it does:
  1. Compiles both files under warnings-as-errors.
  2. Builds the scratch DB (real trades/exits/systems schema + orders
     table), this time with row_factory=Row set to match
     infrastructure.db_client.get_connection()'s real behavior (found
     while writing this handoff -- get_connection() sets row_factory,
     the prior file-6 handoff's scratch DB didn't, though it happened
     not to matter there since nothing relied on Row's dict-style
     access; reconcile_command.py's _reconcile_one() does rely on it,
     e.g. order_row["schwab_order_id"], so this script sets it to match
     production).
  3. db_reader.py REGRESSION: confirms all 7 pre-existing functions
     (get_all_trades, get_trades_by_date_range, get_open_trades,
     get_open_trade_for_symbol, get_trade_by_symbol_and_date,
     get_exits_for_trade, get_trade_summary) are still present after
     the append.
  4. Inserts 6 orders covering every branch of get_open_real_orders()'s
     filter: order_x/y/z are REAL, non-terminal, with a schwab_order_id
     (should be selected); order_w has no schwab_order_id, order_v is
     PAPER, order_u is already closed (all three should be excluded).
     Confirms the candidate set is exactly {order_x, order_y, order_z}.
  5. Mocks the Schwab pull to return the SAME TIER1_ORDER/
     TIER2_ENTRY_ORDER/TIER2_EXIT_ORDER fixtures as the file-5 handoff
     (schwab_order_ids 2001/3001/3002 -- order_x/y's schwab_order_id
     values were chosen to match these on purpose). Runs the real
     reconcile_live_orders() end to end.
  6. Confirms the returned counts are exactly
     {"reconciled": 2, "still_open": 0, "not_in_pull": 1} -- order_x via
     Tier 1 (chain linkage), order_y via Tier 2 (FIFO fallback,
     unrelated standalone order_id 3002 as the candidate exit), order_z
     not in the mocked pull at all (schwab_order_id=9999).
  7. Confirms order_x and order_y are now status='closed' with the
     exact same hand-verifiable P&L as the file-5/file-6 handoffs
     (525.00 and 500.00), order_z is untouched, and exactly 2 rows now
     exist in the trades table (both promoted correctly).

Success criteria: script prints "PASS: db_reader.py extension and
reconcile_command.py validated end-to-end." and a
run_this_P020_20260907_204856.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs.
- get_open_real_orders() wrong candidate set: read its SQL directly --
  check the trade_mode='REAL' / schwab_order_id IS NOT NULL / status
  NOT IN (...) conditions are ANDed correctly, not OR'd.
- reconcile_live_orders() counts wrong: read _index_pulled_orders() and
  _reconcile_one() directly. A common failure mode here is an int/str
  mismatch on schwab_order_id -- the orders table stores it as TEXT,
  Schwab's raw orderId is an int; _reconcile_one() converts via
  int(order_row["schwab_order_id"]) before the chains_by_id lookup --
  confirm that conversion is still there if the lookup silently misses.
- order_x/y not updated correctly: read _reconcile_one()'s call to
  update_order_status() -- likely a field passed under the wrong
  keyword, or a str()/None conversion issue on entry_date/close_date.
- Wrong trades_count: check promote_order_to_trade() is actually being
  called (not silently skipped) for both order_x and order_y -- read
  the logger.warning branch in _reconcile_one() for a clue if one of
  them silently failed there.

Do not change test assertions. The P&L numbers (525.00, 500.00) are the
same hand-verifiable ones from the file-5/file-6 handoffs -- don't "fix"
a failure by adjusting them.

Sequencing note: file 7 of 18. Files 1-6 already validated PASS earlier
today. Next in the P_020 sequence per the WO's build-order narrative:
cli.py (extend, +~15 lines -- wires reconcile_live_orders() to a CLI
command), then the shared p020_order_writer.py + P_400's
infrastructure\order_submit_writer.py (live orders start flowing
end-to-end, submit side). Paper support
(infrastructure\paper_order_history_parser.py) stays deferred to near
the end of this build.
