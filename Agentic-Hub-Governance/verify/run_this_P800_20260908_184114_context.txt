WO-P800-E6.001 -- P_805 Consensus Dashboard, RE-verify after regression fix
Date: 2026-09-08

Why this exists (follow-on to run_this_P800_20260908_182141):
That run PASSED but exposed a real bug: "Today" was 0 because
ranked_csv_reader.py's rows use first_seen dates that lag a day behind
the file reporting them (2026-09-08's file held first_seen=2026-09-07
rows). The runner was filtering "today" by first_seen instead of by
which file the row came from.

Fix applied this session:
- domain/consensus_join.py: removed candidates_in_window() entirely
  (wrong semantics for this use case -- dead code now).
- application/consensus_dashboard_runner.py: today/mtd/ytd candidates
  are now three independent read_candidates() calls windowed by file
  date, matching how MTD/YTD already worked. No first_seen filtering
  anywhere in the runner now.

What this script tests:
Same as the prior run_this_P800_20260908_182141.py (compile check +
read-only pipeline run against live data), updated for the new
runner shape. Still does NOT call write_section() -- does not touch
Dashboard.md.

Success criteria:
PASS, and this time "Today" should be > 0 if today's ranked.csv file
has any rows (it does: CHWY, MLM per the last run). MTD/YTD numbers
should be >= what the prior run showed (164 YTD / 15 MTD), since the
windowing logic changed but the underlying data didn't shrink.

What Claude Code should fix on failure:
- Import error mentioning candidates_in_window: some other file still
  references it -- grep for it and remove the reference, don't re-add
  the function.
- Anything else: same guidance as the prior context.txt (don't touch
  JOIN_WINDOW_TRADING_DAYS without flagging it).

Paste full output back for review before authorizing the Dashboard.md write.