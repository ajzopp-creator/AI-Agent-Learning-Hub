WO: WO-P400-E6.001, Remaining Gate 1 -- migration_add_orders_table.py has
never been executed outside :memory:.

Purpose: run the migration for real against the live P_020_trades.db, then
verify the orders table and full column set exist.

Risk: additive only -- CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT
EXISTS, no destructive statements, no writes to any other table. DB backed
up first at P_020_trades.db.backup_2026-09-08.

On success: report the full "Migration complete." output plus the printed
column list back to Tony/Claude Desktop.
On failure: report the exact traceback, do not retry, do not touch the DB
further -- flag back to Tony.