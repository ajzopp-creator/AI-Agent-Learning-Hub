WO-P400-E6.001 Scope item 2 -- File 2 of 18 (schemas.py Order model) -- 2026-09-07

What this tests: Claude appended a new `Order` Pydantic model to the end of
projects\P_020_AJZStrategies_PerformanceAnalysisSystem\python\database\
schemas.py via windows-mcp FileSystem (mode=write, append=True) -- existing
content untouched, nothing above the append point was rewritten. A direct
Test-Path/Length/line-count check succeeded fine (229 lines, 11,478 bytes).
The follow-up py_compile subprocess call via windows-mcp:PowerShell then
stalled 4+ minutes with "No result received." Pinged the relay separately
right after -- responded in under a second, so the relay is healthy; the
stall is specific to invoking python.exe as a subprocess through this
relay (matches a known peh-handoff pattern: inline python calls stalling
even when the relay itself is fine). Per protocol: zero retries of the
actual work call, hand off to Claude Code.

This script:
  1. Confirms the file exists with a plausible line count (already know
     it's 229 from the earlier direct PowerShell check -- this re-confirms
     under Python's own read, catching anything the append call itself
     might have disturbed).
  2. Compiles under warnings-as-errors.
  3. Imports the module and confirms all 9 pre-existing classes (Account,
     TradingSystem, Trade, Exit, SpreadLeg, TradeParams, TrackerEntry,
     TrackerLookup, LastRunFile) are still present -- regression check
     that the append didn't corrupt anything above it.
  4. Instantiates Order with minimal valid data, checks the four fields
     with defaults (trade_mode=REAL, status=pending,
     confidence_tier=CONFIRMED, source_project=P400) come back correct.
  5. Confirms Order rejects an invalid side="sideways" value -- proves
     Pydantic validation is actually wired on the Literal fields, not
     just documentation.

Success criteria: script prints "PASS: schemas.py Order model validated,
pre-existing classes intact." and a
run_this_P020_20260907_122849.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Line count out of [210, 245]: read the file directly around the append
  boundary (end of the pre-existing LastRunFile class, start of the new
  "Order model" section) for a chunk-boundary merge.
- Compile error: same known failure modes as the last handoff (invalid
  escape sequences, self-terminating docstrings) -- read the exact error.
- Missing pre-existing class: the append may have overwritten instead of
  appending -- compare against schemas.py's content before this session
  (git or provided context) if unsure, do not guess.
- Order instantiation failure: read the Pydantic ValidationError directly
  -- likely a Literal/type mismatch in the new class definition. Fix in
  schemas.py, not this test script.
- Wrong defaults: check the `= "REAL"` / `= "pending"` / `= "CONFIRMED"` /
  `= "P400"` default values in the Order class match exactly (case-
  sensitive).
- Validation not rejecting side="sideways": confirms `side` isn't actually
  typed as `Literal["long", "short"]` -- check for a stray `str` fallback.

Do not change test assertions. This script only imports and instantiates
in-memory Pydantic models -- it never touches any file, database, or
network resource beyond reading schemas.py itself.

Sequencing note: this is file 2 of 18 in WO-P400-E6.001's BUILD PLAN.
File 1 (migration_add_orders_table.py) already validated PASS earlier
today (run_this_P020_20260907_121629.py.done). Next in the P_020
sequence after this: domain\order_linkage.py.
