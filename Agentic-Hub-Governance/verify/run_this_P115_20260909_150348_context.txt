P_115 STEP 2 emission -- QMCO -- 2026-09-09

What this tests: Calls application.emit_signal() directly with QMCO's SIGNAL_V2
fields (P_118-sourced ticker, evaluated through the mandatory P_115 recheck
engine per V111; --source stays "P_115" per architecture v1.3 rule -- do not
change to P_118). Confirms the emitter writes a signal file to the
TradeOrderManagement\signals\ inbox without raising.

Success criteria: prints "Signal written: <path>" then "PASS", and writes
run_this_P115_20260909_150348.py.done with status=PASS.

Context: submitted Fund=3 could not be independently confirmed via
stockanalysis.com this session (site unreachable via web_fetch); alternate
web sources suggested a materially weaker fundamental picture. Tony reviewed
a live Chaikin Power Gauge screenshot showing QMCO Very Bullish with
Financials/Technicals/Experts all strongly rated, and issued an explicit
"EMIT QMCO" override. This script proceeds on that override -- do not treat
the earlier hold flag as still active.

If it fails: read the exception in the .done file / printed FAIL line,
check emit_signal's required-field list against application/emit_signal.py
and domain/signal_builder.py for a signature mismatch, propose a fix, do not
change confidence_level, guideline_entry/stop/target, or signal_source away
from the values above without asking -- those are Tony-approved trade
parameters, not test assertions to relax.
