WO-P400-E6.001 Scope item 2 -- Files 1/2/3/5 (revised) + File 6 of 18
(db_order_writer.py new) -- 2026-09-07

Background: two rounds of gap-finding happened before this handoff, both
already flagged to and approved by Tony:
  1. LinkedOrder (file 3) was extended with quantity/asset_type/
     avg_fill_price/entered_time/close_time (already validated PASS
     earlier today).
  2. While sizing db_order_writer.py's promote_order_to_trade(), found
     Trade.entry_price needs the ACTUAL fill price (orders only had
     planned_entry_price) and Trade.asset_type needs a real
     EQUITY/OPTION + putCall pair (schwab_mapper.py already has a
     _map_asset_type() helper for this, reused not duplicated). Fixed
     by extending, in one bundled pass: migration_add_orders_table.py
     (+3 columns: entry_fill_price/asset_type/put_call -- edited in
     place, never run against the real DB yet so no ALTER needed),
     schemas.py's Order model (+ same 3 fields), order_linkage.py's
     LinkedOrder (+put_call), order_reconciler.py's ReconciledOrder
     (+entry_fill_price/asset_type/put_call, threaded through both
     tiers). Tony approved all of it together ("yes").

All 5 files written directly via windows-mcp FileSystem (mode=write).
Durable-signal Test-Path/Length checks succeeded directly via PowerShell
for all 5 (sizes in this script's FILES dict / scratch-DB setup). Skipped
the compile subprocess call via windows-mcp:PowerShell -- it has stalled
4+ min on every file this session so far, relay confirmed healthy each
time. Handing straight to Claude Code.

IMPORTANT -- read-only real-DB access only. This script opens
P_020_trades.db exactly once, via sqlite3 URI mode=ro (true read-only at
the SQLite level, not just "don't call .commit()"), solely to read the
CREATE TABLE SQL for trades/exits/systems from sqlite_master. It then
builds those same tables plus the new orders table in a fresh :memory:
database and runs every actual test (insert_order, update_order_status,
promote_order_to_trade) against that scratch DB. No row in the real
database is ever read or written by this script.

What it does:
  1. Compiles all 5 files under warnings-as-errors.
  2. Builds the scratch DB as described above.
  3. schemas.py: confirms Order accepts entry_fill_price/asset_type/
     put_call.
  4. order_linkage.py: confirms put_call extracts correctly from a
     synthetic OPTION/CALL order.
  5. order_reconciler.py: confirms reconcile_order() returns None (not a
     crash) for a standalone SINGLE order with no filled exit yet.
  6. db_order_writer.py, equity round trip: insert_order() on a fully-
     reconciled AAPL order (entry_fill_price=150.25, realized_pnl=525.00,
     qty=100), confirms dedup on a second insert_order() call with the
     same schwab_order_id, then promote_order_to_trade() and checks the
     resulting trades row (asset_type='stock', entry_price=150.25,
     system='P_115' from why_code, reason='BTD_SIGNAL' from sig_code)
     AND the resulting exits row (back-calculated exit_price=155.50,
     exit_pnl=525.00 -- hand-verifiable: 150.25 + 525.00/100 = 155.50).
  7. db_order_writer.py, COALESCE update path: insert a minimal PENDING
     TSLA option order, confirms a status-only update_order_status()
     call leaves close_date/realized_pnl untouched, then a full
     reconciliation update (OPTION/CALL, entry_fill_price=5.00,
     realized_pnl=2.50), then promotes it and confirms asset_type maps
     to 'call' (not 'stock' or 'put').
  8. db_order_writer.py, unreconciled order: promote_order_to_trade() on
     an order with no close_date/realized_pnl returns None, doesn't
     crash or insert a garbage trade row.

Success criteria: script prints "PASS: files 1/2/3/5 (revised) and file
6 all validated end-to-end." and a run_this_P020_20260907_135335.py.done
file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs.
- Scratch DB build failure ("real DB has no 'trades' table" or similar):
  confirm P_020_trades.db actually has trades/exits/systems tables (it
  should -- this is the live production DB) -- if the read-only open
  itself fails, check file permissions, not the script logic.
- asset_type mapping wrong: read infrastructure/schwab_mapper.py's
  _map_asset_type() directly (unchanged, imported not modified) against
  db_order_writer.py's _build_trade()'s call to it -- likely an argument
  order swap (schwab_asset_type, put_call) vs (put_call,
  schwab_asset_type).
- exit_price back-calculation wrong: read db_order_writer.py's
  _build_exit() directly -- the sign convention must match
  order_reconciler.py's _compute_pnl() exactly (long: exit > entry when
  pnl positive; short: exit < entry when pnl positive). Don't "fix" the
  test's expected 155.50/7.50-style numbers -- they're hand-verifiable
  from the fixture inputs; the code is wrong if they don't come out
  right.
- COALESCE check failing: read update_order_status()'s SQL directly --
  every reconciliation-field column must use COALESCE(?, column), not a
  bare '= ?', or a status-only call would null out existing data.
- Dedup not working: confirm insert_order() checks schwab_order_id
  before inserting, same pattern as db_writer.py's transaction_exists().

Do not change test assertions. All dollar amounts and dates in the
fixtures were chosen for easy hand-verification -- see item 6/7 above.

Sequencing note: files 1/2/3/5 (revised) + file 6 of 18. File 4
(schwab_order_puller.py) unaffected by any of this session's changes.
Next in the P_020 sequence per the WO's build-order narrative:
application\reconcile_command.py, wired to the live path only --
orchestrates schwab_order_puller -> order_linkage -> order_reconciler ->
db_order_writer end to end for real Schwab orders. Paper support stays
deferred to near the end of this build.
