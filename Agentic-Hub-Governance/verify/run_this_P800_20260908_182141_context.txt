WO-P800-E6.001 -- P_805 Consensus Dashboard, first live-verified run
Date: 2026-09-08

What this tests:
Confirms the 13 new p805_consensus files (config/schemas/domain/
infrastructure/application/cli) compile cleanly under
-W error::SyntaxWarning, then runs the real read+join pipeline against
live data (P_805's ranked.csv history + the Tracker Log workbook) and
prints the computed MTD/YTD consensus table per email source.

This is READ-ONLY. It does not call dashboard_section_writer.write_section()
and does not touch Dashboard.md. Writing the vault section is a separate,
explicit next step once Tony has reviewed these printed numbers.

Success criteria:
Script prints PASS and a per-email-source table with plausible-looking
row counts (Today/MTD/YTD candidate counts should be small positive
integers; BUY/ASYM counts should be <= their matching candidate counts).

What Claude Code should fix on failure:
- Compile errors: fix the specific file named in the COMPILE ERROR line.
- read_candidates failure: check P_805's ranked.csv folder/schema hasn't
  changed shape.
- read_tracker_rows failure: check the Tracker workbook is closed (Excel
  file locks can block pandas.read_excel) and the OneDrive env var
  resolves on this machine.
- Do not change test assertions or the join-window constant
  (JOIN_WINDOW_TRADING_DAYS in config.py) without flagging it to Tony --
  that number is a reasoned default pending his confirmation, not a bug.

Paste full output back for review before authorizing the Dashboard.md write.