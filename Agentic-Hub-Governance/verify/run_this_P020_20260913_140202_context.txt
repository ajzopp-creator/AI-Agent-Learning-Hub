Task: P_020 session, 2026-09-13, ad-hoc diagnostic (no WO number)
No prior WO -- Tony asked whether there's one canonical file/source for
valid Project IDs / trading systems, after generate_dashboard.py's
SYSTEM_ORDER list was found missing P_210.

What this script tests:
Dumps the `systems` table in P_020_trades.db (system_id, system_name,
active) and the distinct `system` values actually present in `trades`,
then diffs those against the three hardcoded system-code lists already
found via grep in this session:
  1. generate_dashboard.py SYSTEM_ORDER (7 entries, confirmed missing P_210)
  2. resolve_tos_imports.py VALID_SYSTEMS (confirmed has P_210)
  3. tracker_reader.py _VALID_SYSTEMS (confirmed has P_210)

Why: two inline `python -c` calls through the Windows-MCP PowerShell relay
timed out at the 4-min ceiling querying this same table (matches the known
anti-pattern in peh-handoff SKILL.md) -- handing off per that skill instead
of a third live attempt.

Success criteria: script prints PASS and the three comparison blocks
(missing-from-list / extra-in-list) for each hardcoded list. Read-only,
touches no production file.

What Claude Code should fix on failure: if the DB path is wrong or the
`systems`/`trades` schema doesn't match (e.g. column renamed), report the
actual error and schema back -- do not guess a fix or change what the
script asserts. Do not change test assertions.
