WO-P400-E6.001 Scope item 2 -- File 3 revision + File 5 of 18
(domain\order_linkage.py extended, domain\order_reconciler.py new) -- 2026-09-07

Background: while sizing order_reconciler.py, found LinkedOrder (file 3,
already validated PASS earlier today) was under-scoped -- it only carried
identity/linkage fields, not the quantity/price/timestamp/asset_type data
the reconciler actually needs to compute entry_date/close_date/
realized_pnl. Extended LinkedOrder with 5 new fields (quantity,
asset_type, avg_fill_price, entered_time, close_time) rather than
re-walking the tree a second time downstream. Flagged to Tony before
building the reconciler; he approved both changes together ("yes").

A second gap was found and fixed in the same sitting, before this
handoff: order_reconciler.py's Tier-2 fill-dict conversion was missing
the "asset_type" key that exit_allocator.py's _build_trade_dict()
requires (would have KeyError'd on first real use). Added asset_type to
LinkedOrder (see above) and threaded it through
order_reconciler._to_fill_dict(). No further approval sought for this
one -- it was a bug in not-yet-validated code, not a scope change.

Both files written directly via windows-mcp FileSystem (mode=write).
Durable-signal Test-Path/Length checks on both succeeded directly via
PowerShell (order_linkage.py: 6,261 bytes; order_reconciler.py: 7,636
bytes). Skipped the compile subprocess call via windows-mcp:PowerShell --
it has stalled 4+ min on every file this session (4 for 4 so far), relay
confirmed healthy each time. Handing straight to Claude Code.

This script validates BOTH files together since order_reconciler.py
directly depends on order_linkage.py's new fields -- splitting them into
two handoffs would just mean re-reading the same context twice.

What it does:
  1. Compiles both files under warnings-as-errors, checks plausible line
     counts.
  2. order_linkage.py REGRESSION: re-runs file 3's original assertions
     (root/descendant shape, find(), all_order_ids) against the exact
     same sample used in that earlier PASS -- confirms the field
     additions didn't disturb existing behavior. Also confirms
     avg_fill_price correctly comes back None (not a crash) when neither
     orderActivityCollection nor a top-level price field is present.
  3. order_linkage.py NEW FIELDS: parses a new TIER1_ORDER sample (same
     TRIGGER->OCO->2xSINGLE shape, now with real execution/quantity/
     asset-type data) and checks quantity, asset_type, avg_fill_price
     (weighted from executionLegs), entered_time, close_time all extract
     correctly on both the root and a nested child.
  4. order_reconciler.py TIER 1: feeds the TIER1_ORDER chain into
     reconcile_order() and checks matched_tier=1, entry_date=2026-09-01,
     close_date=2026-09-03, realized_pnl=(155.50-150.25)*100=525.00.
  5. order_reconciler.py TIER 2: builds two SEPARATE standalone
     (childless) order chains -- a naked MSFT entry (2026-08-20,
     $300.00) and an unrelated closing order (2026-08-25, $310.00) --
     and confirms reconcile_order() falls through to Tier 2, calls the
     real exit_allocator.allocate_exits() underneath, and returns
     matched_tier=2, correct dates, and
     realized_pnl=(310.00-300.00)*50=500.00.
  6. Edge cases: an entry still WORKING (not filled) returns None, not a
     crash; a naked entry with NO candidate_exit_chains argument at all
     returns None without attempting Tier 2.

Success criteria: script prints "PASS: order_linkage.py (extended) and
order_reconciler.py both validated." and a
run_this_P020_20260907_134314.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs this
  session (invalid escape sequences, self-terminating docstrings).
- Regression failures: something in the new fields broke the original
  parsing logic -- diff order_linkage.py's _parse_node()/_walk_children()
  against what file 3 originally had (both essentially unchanged except
  the new field extractions added alongside the old ones).
- New-fields failures: check _extract_avg_fill_price()'s
  orderActivityCollection walk and _parse_node()'s instrument.get(...)
  calls directly against this script's TIER1_ORDER dict shape.
- Tier 1 failures: read _reconcile_tier1() directly -- likely a
  close_time vs entered_time mix-up, or the "most recent filled
  descendant" sort picking the wrong node.
- Tier 2 failures: read _reconcile_tier2() and _to_fill_dict() directly
  against exit_allocator.allocate_exits()'s actual behavior (domain\
  exit_allocator.py, unchanged, imported not modified) -- likely a
  missing/wrong key in the fill-dict conversion, same class of bug as
  the asset_type gap already found and fixed this session.
- ImportError on "domain.exit_allocator" or "domain.order_linkage": the
  sys.path insert must add the python\database directory (two levels up
  from the domain\ file), not domain\ itself -- check DATABASE_DIR in
  this script if the production import style seems right but still
  fails.

Do not change test assertions. All dollar amounts, dates, and quantities
in the test fixtures were chosen to make hand-verification trivial
((155.50-150.25)*100=525.00, (310.00-300.00)*50=500.00) -- don't "fix" a
failure by adjusting these numbers to match wrong code; the code is
wrong if the numbers don't come out right.

Sequencing note: files 3 (revised) and 5 of 18. File 4
(schwab_order_puller.py) already validated PASS earlier today; it was
built before this revision and is unaffected (it never touches
order_linkage.py's LinkedOrder fields, just returns raw dicts). Next in
the P_020 sequence per the WO's build-order narrative (not the file
table's grouping order): infrastructure\db_order_writer.py
(insert_order/update_order_status/promote_order_to_trade), then
application\reconcile_command.py wired to the live path only -- paper
support (infrastructure\paper_order_history_parser.py) stays deferred to
near the end of this build per the WO's own sequencing note.
