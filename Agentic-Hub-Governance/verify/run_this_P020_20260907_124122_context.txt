WO-P400-E6.001 Scope item 2 -- File 3 of 18 (domain\order_linkage.py) -- 2026-09-07

What this tests: Claude wrote
projects\P_020_AJZStrategies_PerformanceAnalysisSystem\python\database\
domain\order_linkage.py directly via windows-mcp FileSystem (mode=write) --
new file, not an edit. A direct Test-Path/Length check succeeded fine
(4,048 bytes). Skipped attempting the compile subprocess call via
windows-mcp:PowerShell this time -- the same call stalled 4+ minutes on
both of the last two files this session (migration_add_orders_table.py,
schemas.py), both times with the relay itself confirmed healthy via a
fast ping right after. Pattern looks specific to invoking python.exe as a
subprocess through this relay, not a one-off. Handing straight to Claude
Code rather than spending another 4 minutes confirming the same failure a
third time.

This script:
  1. Confirms the file exists with a plausible line count.
  2. Compiles under warnings-as-errors.
  3. Builds a synthetic order tree shaped exactly like the WO's confirmed
     live example (2026-08-21 diagnostic): TRIGGER entry -> OCO bracket ->
     two SINGLEs (one FILLED, one CANCELED), GE 260918C410-style symbol.
     Feeds it to build_order_chain() and checks:
       - root node fields (order_id, order_strategy_type, no parent,
         symbol extracted from orderLegCollection)
       - exactly 3 descendants found (OCO + 2 SINGLEs)
       - parent_order_id linkage correct at each level
       - depth correct (TRIGGER=0, OCO=1, SINGLEs=2)
       - status carried through correctly (FILLED vs CANCELED)
  4. Checks the OrderChain.all_order_ids and .find() helper methods.
  5. Confirms build_order_chain() raises ValueError on an order dict with
     no orderId (domain layer should raise, not swallow -- per Hub
     Python style rules).

Success criteria: script prints "PASS: order_linkage.py validated against
synthetic TRIGGER->OCO->2xSINGLE tree." and a
run_this_P020_20260907_124122.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Line count out of [90, 150]: read the file directly for a
  chunk-boundary merge or truncation.
- Compile error: same known failure modes as prior handoffs this session.
- Root/descendant field mismatches: read order_linkage.py's
  _parse_node()/_walk_children()/build_order_chain() directly against
  this script's SAMPLE_ORDER dict -- the recursion or field extraction
  logic likely has an off-by-one on depth, or is reading the wrong dict
  key (e.g. "orderLegCollection" vs a typo).
- ValueError not raised for missing orderId: check _parse_node() is
  actually checking `order_id is None` and raising, not defaulting to
  some sentinel.

Do not change test assertions -- the synthetic tree shape (TRIGGER -> OCO
-> 2 SINGLEs) is copied verbatim from WO-P400-E6.001's own confirmed-live
diagnostic finding, not invented for this test. This script never touches
the network, a file other than order_linkage.py, or any database.

Sequencing note: file 3 of 18. Files 1 (migration) and 2 (schemas.py
Order model) already validated PASS earlier today. Next in the P_020
sequence: infrastructure\schwab_order_puller.py (I/O wrapper that will
actually call the Schwab API and hand its raw response to this file's
build_order_chain()).
