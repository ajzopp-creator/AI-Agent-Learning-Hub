WO-P400-E6.001 Scope item 2 -- File 11 of 18, part 2+3 of 3
(p400_regenerate\infrastructure\p020_reader.py,
p400_regenerate\domain\note_merger.py,
p400_regenerate\application\regenerate_command.py, all new) -- 2026-09-07

Completes file 11 (part 1, vault_note_scanner.py, already validated PASS
earlier today). All 3 files + their __init__.py files written directly
via windows-mcp FileSystem (mode=write). Durable-signal Test-Path/Length
checks succeeded directly via PowerShell (2,405 / 2,554 / 2,437 bytes).
Skipped the compile subprocess call via windows-mcp:PowerShell -- stalled
4+ min on every file this session so far. Handing straight to Claude
Code.

IMPORTANT: regenerate_command.py imports get_closed_p400_orders,
find_note_by_order_id, and write_to_vault at MODULE TOP LEVEL --
learned the hard way in the file-10 handoff (P_400's record_commands.py)
that patching the ORIGIN modules for a top-level import silently doesn't
reach the caller, since the name is already bound in the importing
module's own namespace at import time. This script patches all three
directly on p400_regenerate.application.regenerate_command, not on
p400_regenerate.infrastructure.p020_reader /
p400_regenerate.infrastructure.vault_note_scanner /
shared_resources.python_utils.vault_interface.

IMPORTANT -- no real P_020 database, no real vault write anywhere:
  - infrastructure.db_client.get_connection (P_020's own module) is
    monkeypatched to a scratch in-memory DB before p020_reader.py's
    get_closed_p400_orders() is ever called for real.
  - write_to_vault is monkeypatched throughout regenerate_command.py's
    tests -- the real trading_journal\ vault is never opened.

What it does:
  1. Compiles all 3 files under warnings-as-errors.
  2. p020_reader.py: inserts 4 orders into the scratch DB covering every
     filter branch -- (a) closed + P400 + has schwab_order_id [the only
     one that should match], (b) closed but source_project='P020' [wrong
     source], (c) status='working' [not closed yet], (d) closed + P400
     but schwab_order_id IS NULL [never actually submitted to Schwab].
     Confirms get_closed_p400_orders() returns exactly 1 row, the right
     one.
  3. note_merger.py: builds a synthetic existing-note dict that
     deliberately INCLUDES P_800-injected bookkeeping keys (note_version,
     write_route, write_route_history, source) and the scanner's own
     _note_path/_schema keys, feeds it plus a fake reconciled order dict
     into merge_reconciled_order(), and confirms: lifecycle_status
     becomes 'CLOSED', entry_date/close_date/realized_pnl come from the
     order (not the note), ticker/signal_date are PRESERVED from the
     original note, and all 6 bookkeeping/scanner keys are stripped from
     the output (re-submitting P_800's own injected values back to it
     would be wrong, and _note_path/_schema aren't real frontmatter
     fields at all).
  4. regenerate_command.py, 4 scenarios, all through run_regenerate():
     A) normal sync -- write_to_vault fires once with schema='P400',
        counts={synced:1, already_synced:0, no_note_found:0}.
     B) note's OWN current lifecycle_status is already 'CLOSED' --
        write_to_vault must NOT be called at all, counts reflect
        already_synced:1.
     C) no matching note found -- counts reflect no_note_found:1, no
        crash.
     D) write_to_vault raises -- run_regenerate() must not propagate the
        exception, and synced must stay 0 (the increment sits after the
        call inside the try block, so a raised exception should skip it).

Success criteria: script prints "PASS: p020_reader.py, note_merger.py,
and regenerate_command.py all validated (no real DB/vault touched)." and
a run_this_P800_20260907_222439.py.done file appears with status: PASS.

What Claude Code should fix on failure:
- Compile errors: same known failure modes as prior handoffs this
  session.
- p020_reader.py wrong row count/content: read its SQL WHERE clause
  directly against this script's 4 fixture rows -- likely one of the
  four conditions (status/source_project/schwab_order_id IS NOT NULL) is
  ANDed/ORed incorrectly.
- note_merger.py: a stripped key leaking through means _STRIP_KEYS or
  _SCANNER_KEYS is missing an entry, or the dict comprehension's
  condition is inverted. A preserved field going missing means the
  comprehension is filtering too aggressively.
- Scenario A/B/C/D failures: read run_regenerate()'s loop directly --
  most likely a keyword/attribute-name mismatch between what
  find_note_by_order_id() returns (existing.get(...)) and what
  merge_reconciled_order() or the lifecycle_status check expects.
- Scenario D exception propagating: confirm the write_to_vault() call is
  actually inside the try block, not accidentally placed before it or
  after an early counts["synced"] += 1.

Do not change test assertions. This script never opens the real
P_020_trades.db or the real trading_journal\ vault.

Sequencing note: file 11 of 18 complete (all 3 parts validated). Next in
the P_020 sequence per the WO's build-order narrative:
infrastructure\paper_order_history_parser.py (~190 lines, the largest
single lift in this build -- deferred until now on purpose, new parser
for the raw TOS Account Statement CSV's Order History section, which
nothing today reads), then domain\order_reconciler.py's/
application\reconcile_command.py's paper wiring last.
